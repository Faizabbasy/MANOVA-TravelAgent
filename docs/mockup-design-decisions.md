# Mockup Design Decisions — MANOVA

Status dokumen: log keputusan kumulatif lintas tahap, **direstrukturisasi di Prompt 4** ke format decision-record standar (Prompt 4 bagian E). Seluruh isi dari versi sebelumnya (Prompt 2 & 3) dipertahankan — hanya format penyajian yang dirapikan, tidak ada informasi yang dihapus. ID keputusan (`D-001` dst.) mengikuti nomor urut versi sebelumnya secara 1:1 (mis. `D-018` = entri `#18` versi lama) agar referensi silang dari dokumen lain (`mockup-information-architecture.md`, `route-and-role-matrix.md`, `mockup-open-questions.md`) tetap valid.

**Format setiap entri:** Decision ID · Title · Status · Date · Affected files/modules, diikuti Context / Decision / Reason / Consequence.
**Status yang dipakai:** `LOCKED` · `PROPOSED` · `NEEDS_VALIDATION` · `DEFERRED` · `SUPERSEDED`.

---

## Kelompok A — Diwarisi dari Prompt 0 (Konteks Bisnis)

### D-001 — Party Tunggal untuk Prospect dan Client
**Status:** LOCKED · **Date:** 2026-07-29 · **Affected:** model data `Party`, seluruh modul CRM
**Context:** Template lama tidak punya konsep CRM sama sekali; MANOVA butuh cara merepresentasikan Prospect dan Client tanpa duplikasi data pihak yang sama.
**Decision:** Client dan Prospect memakai satu master data pihak/customer (`Party`/Customer Account) dengan field lifecycle/status yang membedakan keduanya, bukan dua entitas terpisah.
**Reason:** Keputusan eksplisit user di Prompt 0.
**Consequence:** Model UI dan detail teknis dirinci di D-024 (Prompt 3).

### D-002 — Opportunity Won Otomatis Membuat Project
**Status:** LOCKED · **Date:** 2026-07-29 · **Affected:** modul CRM, Project, alur Opportunity-to-Project
**Context:** Baseline alur bisnis MANOVA dari Lead sampai Project selesai.
**Decision:** Opportunity yang berubah menjadi Won otomatis membuat Project; tidak ada jalur "buat project manual" sebagai default.
**Reason:** Keputusan eksplisit user di Prompt 0 bagian C.
**Consequence:** Wizard create-project manual lama harus direpurpose, bukan dipertahankan sebagai entry point independen — lihat D-018.

### D-003 — Seluruh Role Dipakai Saat Demo
**Status:** LOCKED · **Date:** 2026-07-29 · **Affected:** RBAC, Role & Access Matrix, Dashboard
**Context:** MANOVA harus didemokan dengan seluruh peran organisasi terwakili.
**Decision:** Seluruh 11 role minimum Prompt 0-E digunakan saat demo, tidak ada yang di-skip.
**Reason:** Eksplisit dari user.
**Consequence:** Role & Access Matrix (D-030) harus mencakup ke-11 role tanpa kecuali.

### D-004 — Hak Ubah Opportunity ke Won Berbasis Role/Permission Terdokumentasi
**Status:** LOCKED · **Date:** 2026-07-29 · **Affected:** modul CRM, permission model
**Context:** Aksi Won men-trigger efek besar (pembuatan Project) sehingga tidak boleh sembarangan.
**Decision:** Hak mengubah Opportunity menjadi Won mengikuti role/permission yang direkomendasikan berdasarkan audit dan kebutuhan operasional — tidak boleh hardcode tanpa didokumentasikan.
**Reason:** User meminta rekomendasi berbasis analisis, bukan asumsi sepihak.
**Consequence:** Model role final dirinci di D-025.

### D-005 — Sistem adalah Frontend Mockup Murni
**Status:** LOCKED · **Date:** 2026-07-29 · **Affected:** seluruh codebase
**Context:** Perlu kejelasan batas scope teknis sebelum implementasi.
**Decision:** Sistem saat ini adalah frontend mockup dummy data; tidak ada backend nyata yang perlu dipertahankan.
**Reason:** Dikonfirmasi ulang oleh audit Prompt 1 — tidak ditemukan integrasi API/backend nyata di codebase existing.
**Consequence:** Seluruh data "tersimpan"/"terkirim" tetap simulasi client-side.

### D-006 — Larangan Fabrikasi Integrasi Nyata
**Status:** LOCKED · **Date:** 2026-07-29 · **Affected:** seluruh modul, khususnya Vendor/Operations/Finance
**Context:** Risiko miskomunikasi ke stakeholder bila mockup terlihat seperti sistem produksi penuh.
**Decision:** Tidak boleh mengarang integrasi airline/hotel/payment gateway/WhatsApp API/vendor API nyata, dan tidak boleh mengklaim fitur mockup sebagai sudah terintegrasi.
**Reason:** Eksplisit dari user (Prompt 0-C).
**Consequence:** Sidebar item `/integrations` dieksklusi (lihat D-021); dummy vendor memakai nama fiktif, bukan brand nyata.

---

## Kelompok B — Fondasi Stack & Reuse (dikonfirmasi ulang di Prompt 4 untuk kelengkapan traceability)

### D-034 — Nuxt.js Dipertahankan sebagai Stack
**Status:** LOCKED · **Date:** 2026-07-29 · **Affected:** seluruh codebase
**Context:** Prompt 0 mensyaratkan reuse stack existing, bukan migrasi framework.
**Decision:** Stack Nuxt 4 + Vue 3 + TypeScript + Tailwind + shadcn-nuxt/Reka UI dipertahankan sepenuhnya; tidak ada migrasi ke framework lain.
**Reason:** Audit Prompt 1 mengonfirmasi stack ini solid dan layak dipakai ulang; migrasi framework dilarang eksplisit (Prompt 0-H: "jangan mengubah stack Nuxt.js menjadi framework lain").
**Consequence:** Seluruh keputusan implementasi berikutnya harus kompatibel dengan konvensi Nuxt 4 (file-based routing, auto-import, dst.).

### D-035 — Design System Existing Direuse, Tidak Dibangun Baru
**Status:** LOCKED · **Date:** 2026-07-29 · **Affected:** `tailwind.config.ts`, `assets/css/tailwind.css`, `app/components/ui/*`
**Context:** Template sudah punya design token (warna/radius/breakpoint via CSS variable) dan 15 primitive shadcn-nuxt/Reka UI yang bebas domain logic.
**Decision:** Design token dan seluruh primitive `ui/*` direuse apa adanya; komponen baru mengikuti pola visual yang sama (token HSL, CVA variant), bukan sistem desain paralel.
**Reason:** Audit Prompt 1 mengonfirmasi primitive ini generic dan sehat; Prompt 0-H eksplisit melarang "membuat design system baru bila template sudah memilikinya".
**Consequence:** Primitive baru yang dibutuhkan (`ui/tabs`, dll. — lihat D-009) harus mengikuti pola CVA + token yang sama dengan primitive existing.

### D-036 — Kebijakan Penambahan Package
**Status:** LOCKED · **Date:** 2026-07-29 · **Affected:** `package.json`
**Context:** Prompt 0-H melarang instalasi package baru sebelum memastikan yang existing tidak cukup.
**Decision:** Tidak ada package baru dipasang sepanjang Prompt 0–4 (murni dokumentasi); setiap kebutuhan fungsional dicek dulu terhadap dependency existing (`vee-validate`, `zod`, `date-fns`, `chart.js`, `vue-draggable-plus`, dll.) sebelum mengusulkan package baru pada tahap implementasi.
**Reason:** Kebijakan eksplisit Prompt 0-H; dikonfirmasi lewat audit Prompt 1 bahwa banyak dependency yang sudah terpasang namun belum dipakai (`vee-validate`+`zod` 0% dipakai, `date-fns` 0% dipakai).
**Consequence:** Keputusan adopsi `vee-validate`/`zod` untuk form baru tetap terbuka sebagai Q7 (bukan larangan, tapi harus melalui audit dulu, bukan otomatis instal alternatif).

---

## Kelompok C — Keputusan dari Gap Analysis (Prompt 2)

### D-007 — Strategi Reuse per Route (Kategori Awal)
**Status:** LOCKED (sebagian di-supersede — lihat catatan) · **Date:** 2026-07-29 · **Affected:** seluruh route existing, `docs/template-reuse-mapping.md`
**Context:** 9 route existing perlu kategori reuse eksplisit sebelum IA final disusun.
**Decision:** Kategori reuse per route mengikuti `docs/template-reuse-mapping.md` bagian C (mis. `/projects/:id` → `REUSE_LAYOUT_REPLACE_CONTENT`, `/login` → `REUSE_COMPONENTS`).
**Reason:** Berdasarkan tingkat kematangan pola interaksi vs seberapa domain-specific isi komponennya.
**Consequence:** Sub-mapping untuk `/projects/create` dan `/tasks` di tabel asli **SUPERSEDED oleh D-018 dan D-019** (Prompt 3) — keduanya tidak lagi jadi route/menu mandiri. Baris lain (`/`, `/login`, `/expenses`, `/projects`, `/projects/:id`, `/projects/:id/edit`, `/[...slug]`) tetap berlaku sebagai starting point implementasi.

### D-008 — Arsitektur Tab Project Workspace Dipertahankan sebagai Kerangka
**Status:** LOCKED · **Date:** 2026-07-29 · **Affected:** `pages/projects/[id]/index.vue`
**Context:** Ini aset reuse struktural paling bernilai di codebase (drag-drop kanban, chart, file list, activity log).
**Decision:** Arsitektur tab existing dipertahankan strukturnya sebagai kerangka Project Workspace MANOVA; isi diganti total per domain.
**Reason:** Kompleksitas dan kematangan interaksi tertinggi di seluruh codebase.
**Consequence:** Detail final 8-tab dan keputusan single-route dirinci di D-026/D-027 (Prompt 3) — memperkuat, bukan mengubah, keputusan ini.

### D-009 — Ekstraksi Primitive `ui/tabs`
**Status:** PROPOSED · **Date:** 2026-07-29 · **Affected:** `app/components/ui/tabs/*` (baru), `pages/projects/[id]/index.vue`
**Context:** Reka-ui menyediakan primitive Tabs, tapi belum dibungkus jadi komponen `ui/` (temuan Prompt 1); tab project workspace saat ini ad-hoc.
**Decision:** Implementasi tab existing diekstrak menjadi primitive `ui/tabs` berbasis reka-ui, dipakai ulang di Party Detail, Vendor Detail, dan Project Detail.
**Reason:** Menghindari duplikasi logic tab di banyak halaman detail baru.
**Consequence:** Ekstraksi harus hati-hati agar tidak merusak Project Workspace yang sudah kompleks — tetap PROPOSED (belum diputuskan teknis detailnya), bukan LOCKED, karena ini keputusan implementasi bukan IA.

### D-010 — Status-Badge Terpusat (lihat juga D-038)
**Status:** LOCKED · **Date:** 2026-07-29 · **Affected:** komponen badge lintas modul
**Context:** `ProjectsTable`, `TasksOverview`, `StatsCard` existing masing-masing mendefinisikan ulang peta warna status sendiri (drift risk, temuan Prompt 1 bagian 8).
**Decision:** Status-badge (project status, task status/priority, invoice/payment status, dll.) memakai satu wrapper/mapping warna terpusat.
**Reason:** Mencegah drift taksonomi status yang sudah terjadi di template existing.
**Consequence:** Digeneralisasi lebih lanjut sebagai kebijakan status-constant menyeluruh di D-038.

### D-011 — `BudgetChart.vue` sebagai Basis "Budget vs Actual"
**Status:** PROPOSED · **Date:** 2026-07-29 · **Affected:** `app/components/dashboard/BudgetChart.vue`
**Context:** Satu-satunya komponen chart existing yang konsepnya 1:1 cocok dengan kebutuhan MANOVA (Prompt 0-F).
**Decision:** `BudgetChart.vue` menjadi basis komponen "Budget vs Actual", direfactor jadi props-driven per-project.
**Reason:** Reuse dan hindari membangun chart baru dari nol.
**Consequence:** Bug `console.log` dan non-reaktifnya warna tema harus diperbaiki sekalian saat refactor.

### D-012 — Pola `expenses.vue` sebagai Basis Invoice/Traveler Table
**Status:** PROPOSED · **Date:** 2026-07-29 · **Affected:** `pages/expenses.vue`, komponen Invoice/Traveler table baru
**Context:** Pola table+filter+search+modal+toast paling matang di codebase.
**Decision:** Dijadikan acuan/basis untuk komponen Invoice table dan Traveler table baru.
**Reason:** Kematangan interaksi tertinggi di antara pola table existing.
**Consequence:** Bug `handleDelete` (`ReferenceError`) wajib diperbaiki dulu sebelum pola ini direplikasi ke modul lain, agar bug tidak ikut ter-copy.

### D-013 — Data Dummy Dipusatkan Sebelum Modul Baru Dibangun
**Status:** PROPOSED · **Date:** 2026-07-29 · **Affected:** seluruh dashboard widget & page existing
**Context:** Project/Task/Expense existing punya 2–3 shape tidak sinkron (temuan Prompt 1).
**Decision:** Data dummy lintas dashboard-widget dan lintas-page dipindahkan ke satu modul data terpusat sebelum modul MANOVA baru dibangun di atasnya.
**Reason:** Mengonfirmasi peringatan Prompt 0-G soal data yang harus "berasal dari satu source yang terpusat".
**Consequence:** Lokasi modul (`app/data/` vs `app/constants/`) tetap DEFERRED — lihat D-015.

### D-014 — Skema ID Bergaya Bisnis
**Status:** PROPOSED (detail DEFERRED) · **Date:** 2026-07-29 · **Affected:** seluruh entitas MANOVA
**Context:** `Date.now()` dipakai di 2 tempat existing untuk ID runtime, rawan collision; tipe `id` campuran number/string.
**Decision:** ID entitas MANOVA memakai skema string bergaya bisnis (mengikuti pola `PRJ-001` yang sudah sebagian ada).
**Reason:** Konsistensi dan menghindari collision.
**Consequence:** Format detail per entitas — lihat D-016.

## Kelompok D — Keputusan yang Sengaja Ditunda dari Prompt 2 (DEFERRED)

### D-015 — Lokasi Modul Data Terpusat
**Status:** DEFERRED · **Date:** 2026-07-29 · **Affected:** struktur folder `app/`
**Context:** Prompt 0 aturan teknis #5–6 mensyaratkan struktur data terpusat, tapi lokasi persis (`app/data/` vs `app/constants/`) belum diputuskan.
**Decision:** Ditunda ke tahap implementasi/desain type definitions.
**Reason:** Lebih tepat diputuskan bersamaan dengan desain type, bukan di tahap dokumentasi murni.
**Consequence:** Belum ada folder yang dibuat sampai tahap ini.

### D-016 — Format ID Final per Entitas
**Status:** DEFERRED · **Date:** 2026-07-29 · **Affected:** seluruh entitas baru
**Context:** Prinsip umum sudah dikunci di D-014, detail prefix per entitas (mis. `TRV-`, `VND-`) belum.
**Decision:** Ditunda ke tahap implementasi.
**Reason:** Baru relevan saat type definitions benar-benar dirancang.
**Consequence:** Skenario data demo di `docs/mockup-data-scenarios.md` tetap memakai contoh ID konkret sebagai preview, namun format resmi final tetap ditentukan saat implementasi.

### D-017 — Nasib Teknis Duplikasi Kode (`cn()`, Dialog)
**Status:** DEFERRED · **Date:** 2026-07-29 · **Affected:** `app/lib/utils.ts`, `app/utils/cn.ts`, `app/components/ui/dialog/*`
**Context:** Dua file `cn()` identik; dua komponen Dialog ~90% identik.
**Decision:** Ditunda ke fase implementasi.
**Reason:** `cn()` dipanggil hampir di semua komponen — perubahan berisiko lebih aman dilakukan bersamaan mulainya implementasi, bukan berdiri sendiri sebagai keputusan dokumentasi.
**Consequence:** Tidak ada tindakan sampai tahap ini.

---

## Kelompok E — Keputusan Finalisasi IA/Route/Role/Workflow (Prompt 3)

Seluruh entri berikut berstatus LOCKED karena Prompt 3 secara eksplisit meminta "finalisasikan rancangan".

### D-018 — Wizard `/projects/create` Direpurpose, Bukan Entry Point Mandiri
**Status:** LOCKED · **Date:** 2026-07-29 · **Affected:** `pages/projects/create.vue`, alur Opportunity-to-Project
**Context:** Resolusi open question Q1 — wizard manual berpotensi bertentangan dengan D-002.
**Decision:** Wizard 3-langkah existing tidak lagi jadi entry point mandiri "buat project manual"; pola stepper-nya direpurpose sebagai tampilan konfirmasi/setup awal Project yang muncul otomatis setelah Opportunity Won disetujui.
**Reason:** Entry point manual independen berlawanan langsung dengan D-002; me-repurpose pola tetap memenuhi prinsip reuse Prompt 0.
**Consequence:** Detail lengkap: `docs/route-and-role-matrix.md` bagian 2.

### D-019 — `Tasks` Tidak Jadi Menu Top-Level
**Status:** LOCKED · **Date:** 2026-07-29 · **Affected:** `pages/tasks.vue`, tab "Tasks" Project Detail
**Context:** Resolusi Q2 — duplikasi konsep task antara `/tasks` dan Kanban tab.
**Decision:** Task management terjadi di dalam tab "Tasks" pada Project Detail; visibilitas lintas-project lewat widget dashboard, bukan menu/route tersendiri.
**Reason:** Mencegah duplikasi konsep task dengan shape data berbeda.
**Consequence:** Route `/tasks` dieksklusi dari IA final.

### D-020 — Operations & Travelers Bukan Menu Top-Level, Vendor Tetap Top-Level
**Status:** LOCKED · **Date:** 2026-07-29 · **Affected:** IA, sidebar, Project Detail
**Context:** Resolusi Q3 — menghindari menu global kosong/tipis.
**Decision:** Operations melebur jadi tab "Itinerary & Services", Travelers melebur jadi tab "Travelers" — keduanya di Project Detail. Vendor tetap menu top-level (`/vendors`).
**Reason:** Operations/Traveler inheren single-project-scoped tanpa kebutuhan direktori lintas-project eksplisit di Prompt 0; Vendor melayani banyak project sekaligus.
**Consequence:** Detail lengkap: `docs/mockup-information-architecture.md` bagian 3.4–3.6. Opsi menu top-level tetap dicatat sebagai kemungkinan evolusi (D-033).

### D-021 — Resolusi 9 Dead Link Sidebar
**Status:** LOCKED · **Date:** 2026-07-29 · **Affected:** `AppSidebar.vue` (rencana), route baru
**Context:** Resolusi Q4.
**Decision:** `/clients`→`/crm/clients`, `/invoices`→`/finance/invoices`, `/reports`→`/reports`, `/files`→tab Documents Project Detail, `/team`→`/admin/users`+info tim Overview, `/time-tracking`/`/templates`/`/integrations`→excluded.
**Reason:** Per item, lihat `docs/route-and-role-matrix.md` bagian 1.8.
**Consequence:** Tidak ada eksekusi (rename/hapus) pada tahap dokumentasi ini.

### D-022 — Settings Minimal, Bukan Item Sidebar Utama
**Status:** LOCKED · **Date:** 2026-07-29 · **Affected:** popover profil `AppSidebar.vue`, `pages/settings.vue` (rencana)
**Context:** Resolusi Q5.
**Decision:** Settings dipertahankan skema minimal (profil/akun pribadi), diakses lewat popover profil user, bukan item sidebar utama.
**Reason:** Isi saat ini tidak cukup substansial untuk jadi menu top-level tersendiri.
**Consequence:** Tombol "Profile Settings" existing punya tujuan jelas untuk implementasi berikutnya.

### D-023 — `AIAssistant.vue` Tidak Dilanjutkan
**Status:** LOCKED · **Date:** 2026-07-29 · **Affected:** `app/components/dashboard/AIAssistant.vue`
**Context:** Resolusi Q6 — tidak ada mapping domain MANOVA.
**Decision:** Tidak dilanjutkan sebagai bagian desain dashboard MANOVA.
**Reason:** Tidak ada konsep AI assistant di Prompt 0 manapun.
**Consequence:** Penghapusan fisik file tetap menunggu tahap cleanup (Prompt 5) — bukan dieksekusi sekarang.

### D-024 — Model UI Party/Prospect/Client
**Status:** LOCKED · **Date:** 2026-07-29 · **Affected:** type `Party`, modul CRM
**Context:** Merinci D-001 menjadi model UI konkret.
**Decision:** Satu record `Party` dengan field `lifecycleStatus` (`Prospect`|`Client`); transisi `Prospect`→`Client` otomatis saat opportunity milik party tsb menjadi Won; history (Activity/Contact/Opportunity) tidak hilang setelah transisi.
**Reason:** Mencegah pola "banyak shape untuk satu konsep" seperti Project/Task/Expense di template lama.
**Consequence:** Detail lengkap: `docs/mockup-information-architecture.md` bagian 5.

### D-025 — Model Role Approval Won Dua-Langkah
**Status:** LOCKED · **Date:** 2026-07-29 · **Affected:** modul CRM, Opportunity Detail
**Context:** Merinci D-004 — daftar role Prompt 3-H tidak menyediakan "Sales Manager" terpisah.
**Decision:** Sales mengajukan ("Submit as Won" / stage internal `WonRequested`); Management atau Super Admin menyetujui (aksi yang benar-benar mengeksekusi pembuatan Project).
**Reason:** Memakai role yang sudah tersedia tanpa mengarang role baru, tetap merepresentasikan pemisahan tanggung jawab sesuai Prompt 3-E.
**Consequence:** Model approval berjenjang berdasarkan nilai/kompleksitas dipertimbangkan tapi tidak dipilih untuk versi pertama — dicatat di D-032. Detail lengkap: `docs/route-and-role-matrix.md` bagian 2.3.

### D-026 — Konsolidasi Project Detail Jadi 8 Tab
**Status:** LOCKED · **Date:** 2026-07-29 · **Affected:** `pages/projects/[id]/index.vue`
**Context:** 16 kandidat area Prompt 3-C perlu dikonsolidasi agar tidak berlebihan.
**Decision:** Overview, Itinerary & Services (sub-section kondisional sesuai `project.serviceScope`), Travelers, Vendors, Finance, Tasks, Documents, Activity & Changes.
**Reason:** Menggabungkan area yang seharusnya satu konteks (mis. Flight/Hotel/Transportation/MICE/Timeline → satu tab).
**Consequence:** Ini juga jadi keputusan eksplisit untuk "conditional modules berdasarkan service" (Prompt 4-E) — lihat D-039. Detail lengkap: `docs/mockup-information-architecture.md` bagian 4.

### D-027 — Tab Project Detail Tetap Satu Route
**Status:** LOCKED · **Date:** 2026-07-29 · **Affected:** routing `/projects/[id]`
**Context:** Prompt 3-B mencontohkan opsi nested route (`/projects/[id]/itinerary`, dst.) tapi tidak mewajibkan.
**Decision:** Tab tetap satu route `/projects/[id]` dengan state client-side (deep-link via query param), bukan nested route per tab.
**Reason:** Mempertahankan pola coding existing yang sehat dan menghindari overengineering (Prompt 0 aturan teknis #3, #7).
**Consequence:** Route inventory `docs/route-and-role-matrix.md` tidak mencantumkan route terpisah per tab.

### D-028 — Project Status Final (8 status)
**Status:** LOCKED · **Date:** 2026-07-29 · **Affected:** enum `ProjectStatus`
**Context:** Prompt 3-F meminta status cukup untuk demo tanpa berlebihan.
**Decision:** Draft, Planning, Confirmed, In Progress, Ongoing Trip, Completed, On Hold, Cancelled — transisi linear maju + cabang On Hold/Cancelled dari status non-terminal manapun.
**Reason:** Cukup mewakili siklus hidup project travel tanpa kompleksitas berlebih.
**Consequence:** Detail lengkap termasuk badge & attention condition: `docs/route-and-role-matrix.md` bagian 3.

### D-029 — Service Status Generik
**Status:** LOCKED · **Date:** 2026-07-29 · **Affected:** enum `ServiceStatus` (Flight/Hotel/Transportation/MICE)
**Context:** Prompt 3-G meminta menghindari status berbeda total per jenis layanan.
**Decision:** Not Started → Sourcing → Quoted → Pending Confirmation → Confirmed → (Changed ⇄ Confirmed) → Completed, dengan Cancelled sebagai cabang terminal. Perbedaan antar jenis layanan lewat field tambahan, bukan status set berbeda.
**Reason:** Konsistensi lintas 4 jenis layanan operasional.
**Consequence:** Detail lengkap: `docs/route-and-role-matrix.md` bagian 4.

### D-030 — Role & Access Matrix (5 Level, Granularity Modul)
**Status:** LOCKED · **Date:** 2026-07-29 · **Affected:** seluruh modul
**Context:** Prompt 3-H meminta matrix untuk 11 role x modul, tanpa granularity field.
**Decision:** `NONE`/`VIEW`/`MANAGE`/`APPROVE`/`ADMIN` pada granularity modul (CRM/Project/Vendor/Finance/Reports/Administration); pengecualian granularity untuk role sub-domain (Ticketing/Accommodation/Transportation/MICE) yang scoped ke satu sub-section layanan (sesuai definisi role di Prompt 0-E).
**Reason:** Memenuhi instruksi eksplisit untuk tidak membuat permission field-level.
**Consequence:** Detail lengkap: `docs/route-and-role-matrix.md` bagian 5.

### D-031 — Dashboard Satu Komponen, Widget Kondisional per Role
**Status:** LOCKED · **Date:** 2026-07-29 · **Affected:** `pages/index.vue`
**Context:** Prompt 3-I melarang dashboard terpisah sepenuhnya per role.
**Decision:** Satu komponen dashboard, widget difilter berdasarkan role user login.
**Reason:** Instruksi eksplisit Prompt 3-I.
**Consequence:** Detail lengkap: `docs/route-and-role-matrix.md` bagian 6.

## Kelompok F — Alternatif yang Dipertimbangkan, Belum Dipilih (DEFERRED)

### D-032 — Approval Won Berjenjang Berdasarkan Nilai/Kompleksitas
**Status:** DEFERRED · **Date:** 2026-07-29 · **Affected:** model role Opportunity
**Context:** Alternatif terhadap D-025.
**Decision:** Tidak dipilih untuk versi pertama.
**Reason:** Menambah kompleksitas tanpa dasar threshold nyata dari bisnis di Prompt 0; melanggar instruksi "pilih model paling sederhana yang tetap realistis".
**Consequence:** Bisa dievaluasi lagi setelah ada data threshold nyata — lihat juga Q9 (`docs/mockup-open-questions.md`).

### D-033 — Direktori Operations/Travelers Lintas-Project sebagai Menu Top-Level
**Status:** DEFERRED · **Date:** 2026-07-29 · **Affected:** IA, sidebar
**Context:** Alternatif terhadap D-020.
**Decision:** Tidak dipilih untuk fase mockup awal.
**Reason:** Tidak ada kebutuhan agregasi lintas-project yang tervalidasi di Prompt 0 saat ini.
**Consequence:** Dipertimbangkan lagi bila kebutuhan agregasi lintas-project (mis. "traveler yang dokumennya belum lengkap di semua project") benar-benar tervalidasi di fase setelah mockup awal.

---

## Kelompok G — Pelengkap Traceability Prompt 4 (status constants & formatting)

### D-037 — Format Rupiah dan Tanggal Konsisten
**Status:** LOCKED · **Date:** 2026-07-29 · **Affected:** formatter currency/date (baru, lokasi menyusul di implementasi)
**Context:** Audit Prompt 1 menemukan nol pemakaian format IDR dan format tanggal yang tidak konsisten (`'2025-01-15'` vs `'Today'` vs `'Feb 15, 2025'`) di seluruh codebase existing; `date-fns` terpasang tapi 0% dipakai.
**Decision:** Seluruh nilai currency bertipe IDR wajib memakai format Rupiah Indonesia (`Intl.NumberFormat('id-ID', {style:'currency', currency:'IDR'})` atau setara); seluruh tanggal demo memakai satu formatter konsisten berbasis `date-fns` (dependency existing, tidak perlu package baru).
**Reason:** Wajib sesuai Prompt 0 aturan teknis #11–12; `date-fns` sudah terpasang sehingga tidak melanggar kebijakan penambahan package (D-036).
**Consequence:** Seluruh skenario data di `docs/mockup-data-scenarios.md` memakai nilai Rupiah dan tanggal yang konsisten formatnya sebagai contoh.

### D-038 — Sentralisasi Status Constants (generalisasi dari D-010)
**Status:** LOCKED · **Date:** 2026-07-29 · **Affected:** seluruh enum status (Opportunity stage, Project status, Service status, Invoice/Payment status, Task status/priority)
**Context:** D-010 awalnya hanya membahas status-badge warna; Prompt 0 aturan teknis #10 mensyaratkan seluruh status memakai constant/enum konsisten, bukan hanya soal warna badge.
**Decision:** Seluruh status di seluruh modul MANOVA (bukan hanya Project) didefinisikan sebagai satu set constant/enum terpusat per domain, dipakai bersama oleh logic maupun badge — tidak didefinisikan ulang per komponen seperti pola lama.
**Reason:** Mencegah drift taksonomi status seperti temuan Prompt 1 (kategori expense berbeda di 3 tempat, status project berbeda di 3 tempat).
**Consequence:** Lokasi modul constant mengikuti keputusan D-015 (masih DEFERRED lokasinya, tapi prinsip sentralisasi sudah LOCKED).

### D-039 — Conditional Modules Berdasarkan Service (restatement eksplisit)
**Status:** LOCKED · **Date:** 2026-07-29 · **Affected:** tab "Itinerary & Services" Project Detail
**Context:** Prompt 4-C secara eksplisit meminta dokumentasi "conditional service sections" sebagai bagian IA.
**Decision:** Sub-section dalam tab "Itinerary & Services" (Flight/Hotel/Transportation/MICE) tampil kondisional sesuai `project.serviceScope`, sesuai 4 kombinasi tipe project Prompt 0-B (flight-only, +hotel, +transport, +MICE).
**Reason:** Menghindari UI menampilkan section kosong untuk layanan yang tidak dipakai project tsb.
**Consequence:** Ini adalah restatement eksplisit dari bagian D-026 untuk traceability langsung terhadap requirement Prompt 4-C — tidak mengubah keputusan, hanya menegaskan.

### D-040 — Nilai Ambang Batas Default untuk Attention Condition
**Status:** DEFERRED (asumsi aman dipakai untuk mockup, bukan angka final bisnis) · **Date:** 2026-07-29 · **Affected:** widget dashboard "Attention list", `docs/mockup-data-scenarios.md`
**Context:** Resolusi Q9 (`docs/mockup-open-questions.md`) — Prompt 4-F melarang menjadikan hal kecil sebagai blocker bila ada asumsi aman untuk mockup.
**Decision:** Attention dipicu bila salah satu: `Actual Cost > 100% Budget`; keberangkatan ≤ 30 hari dari tanggal berjalan; invoice/task melewati tanggal jatuh tempo (tanpa masa tenggang).
**Reason:** Nilai default yang cukup realistis untuk mockup tanpa perlu menunggu data threshold bisnis nyata.
**Consequence:** Dipakai konsisten di `docs/mockup-data-scenarios.md`; dapat disesuaikan bila ada masukan bisnis nyata di kemudian hari (karena itu tetap `DEFERRED`, bukan `LOCKED`).

---

## Kelompok I — Change Request Prompt 19 (Customer Journey, Account Executive, Supplier, Commercial Approval)

Prompt 19 adalah **change request** di atas mockup 18-section yang sudah COMPLETED (Section 00–18), bukan lanjutan phasing baku. Keputusan berikut LOCKED untuk perubahan ini kecuali dinyatakan lain.

### D-046 — 2 Role Baru: Account Executive dan Supplier (Total 13 Role)
**Status:** LOCKED · **Date:** 2026-07-30 · **Affected:** `app/types/user.ts`, `app/constants/roles.ts`, `app/data/users.ts`
**Context:** Prompt 19 secara eksplisit meminta pemisahan tanggung jawab Sales (Lead) vs Account Executive (Opportunity/Quotation/Won), plus area External Partners dengan akun supplier ter-isolasi.
**Decision:** `RoleId` diperluas dari 11 menjadi 13 nilai — `account-executive` disisipkan setelah `management`, `supplier` ditambahkan di akhir. Satu demo user per role baru (`USR-014` Account Executive, `USR-015`/`USR-016` Supplier PT ABC/PT EFG).
**Reason:** D-003 (seluruh role dipakai demo) tetap berlaku — role baru mengikuti pola yang sama, bukan pengecualian.
**Consequence:** `ROLE_MODULE_ACCESS` bertambah kolom `supplier-portal` (module key baru, lihat D-048); seluruh matrix existing (11 role x 6 modul lama) **tidak diubah nilainya**, murni ekstensi baris/kolom.

### D-047 — Sales Dipersempit ke Lead, Account Executive Mengambil Alih Opportunity/Quotation
**Status:** LOCKED · **Date:** 2026-07-30 · **Affected:** `app/pages/crm/opportunities/[id]/index.vue`, `app/data/opportunities.ts`
**Context:** Prompt 19-2 eksplisit merinci pemisahan tanggung jawab: Sales = screening/qualification Lead; AE = create Opportunity s/d Won, tetap Account Owner setelah project berjalan.
**Decision:** Narrow role exception `canManageOpportunity` (Opportunity Detail, pola sama seperti `canManageParty` Section 07) dipindah dari `['sales', 'super-admin']` menjadi `['account-executive', 'super-admin']`. `Opportunity.ownerId` pada 7 record existing (OPP-001–007) di-reassign dari `USR-001` (Sales) ke `USR-014` (AE) — perubahan **nilai field**, bukan struktur/hapus data.
**Reason:** Di bawah model role baru, mempertahankan `ownerId` lama akan membuat "Account Owner" menunjuk ke role yang menurut definisi baru tidak lagi mengelola Opportunity — inkonsistensi yang perlu diperbaiki untuk integrasi (protokol bagian C, "diperlukan untuk integrasi").
**Consequence:** `canManageParty` (Party/Prospect, `crm/parties/[id]/index.vue` dan `crm/prospects.vue`) diperluas menambahkan `account-executive` (AE "mengelola relationship dengan prospect/client", literal Prompt 19-2) — Sales **tetap dipertahankan** di daftar ini (tidak ada larangan eksplisit Party-level). Dashboard (Section 06) widget Sales (Opportunity Pipeline read-only, Follow-up Mendatang) **sengaja tidak diubah** — read-only, tidak melanggar "Sales tidak boleh Mark as Won" (gate ada di halaman aksi, bukan dashboard).

### D-048 — Modul `supplier-portal` Terpisah dari `vendor`, Isolasi via `User.vendorId`
**Status:** LOCKED · **Date:** 2026-07-30 · **Affected:** `app/types/user.ts`, `app/constants/roles.ts`, `app/composables/usePermissions.ts`
**Context:** Prompt 19-1/7 eksplisit: supplier hanya boleh melihat company sendiri, tidak boleh melihat vendor lain, tidak boleh melihat Customer Journey/leads/opportunity internal/margin/activity internal.
**Decision:** `ModuleKey` baru `supplier-portal` (terpisah dari `vendor` yang tetap berarti "direktori vendor internal, dilihat seluruh role internal"). `User.vendorId?` (opsional, hanya terisi untuk role `supplier`) menyimpan satu-satunya vendor company yang boleh dilihat user tsb. `usePermissions()` menyediakan `vendorScopeId` (computed, `undefined` untuk role selain `supplier`) — seluruh halaman `/supplier/*` WAJIB memfilter data lewat `vendorScopeId`, tidak pernah membaca `VENDORS`/`PROJECT_SERVICES`/`VENDOR_QUOTATIONS` penuh.
**Reason:** Field-level filtering (satu vendor per user) tidak bisa direpresentasikan oleh granularity modul biasa (D-030) — ini bukan pengecualian terhadap D-030 (yang membahas permission action-level per modul), melainkan mekanisme **data-scoping** terpisah, pola baru yang eksplisit dibutuhkan Prompt 19 (vendor isolation).
**Consequence:** `ROLE_MODULE_ACCESS['supplier-portal']`: `supplier` = `MANAGE`, `super-admin` = `ADMIN` (oversight — melihat halaman `/supplier/*` tapi `vendor.value` selalu `undefined` karena Super Admin tidak punya `vendorId`, sehingga tetap tidak bocor data vendor manapun), seluruh role internal lain = `NONE`.

### D-049 — Commercial Approval Terpisah dari Won Approval (Quotation-Level, Bukan Merestrukturisasi Opportunity Stage)
**Status:** LOCKED · **Date:** 2026-07-30 · **Affected:** `app/types/opportunity.ts` (`QuotationApprovalStatus`), `app/data/index.ts` (`submitQuotationForApproval`/`approveQuotation`/`rejectQuotation`), `app/pages/crm/opportunities/[id]/index.vue`
**Context:** Prompt 19-3 meminta workflow "Draft Quotation→Submitted for Approval→Approved by Management→Negotiation/Final Confirmation→Opportunity Won", dengan AE tidak dapat Mark as Won sebelum approval Management. `Opportunity.stage` (D-028/state machine existing, LOCKED sejak Prompt 3, dipakai luas — Section 08/09/14) sudah punya gerbang approval sendiri di transisi `won-requested → won` (D-025, Management/Super Admin).
**Decision:** **Tidak merestrukturisasi** `OpportunityStage` (union 9 nilai existing dipertahankan utuh). Field baru `Quotation.approvalStatus` (`draft|submitted|approved|rejected`, opsional) + 3 mutator baru (`submitQuotationForApproval`, `approveQuotation`, `rejectQuotation`) mengimplementasikan Commercial Approval sebagai lapisan **tambahan, ortogonal** terhadap stage: tombol "Ajukan sebagai Won" (transisi `negotiation → won-requested`) di-gate tambahan `quotation?.approvalStatus === 'approved'`. "Negotiation/Final Confirmation" pada diagram Prompt 19 dipetakan ke stage `negotiation` existing (setelah quotation approved); "Opportunity Won" final tetap memakai gerbang `won-requested → won` (D-025) yang sudah ada — bukan langkah baru, melainkan "Final Confirmation" itu sendiri.
**Reason:** Merestrukturisasi state machine Opportunity yang sudah dipakai luas oleh Section 08/09/14 (dan divalidasi lintas banyak section) berisiko tinggi/besar untuk perubahan yang sebenarnya bisa dipenuhi secara aditif; protokol bagian C eksplisit meminta "perubahan paling minimal" saat menyentuh hasil section lama.
**Consequence:** 2 opportunity existing di-restage untuk mendemokan kedua skenario literal Prompt 19-9 ("satu quotation menunggu approval", "satu quotation approved") — **OPP-005 dikembalikan** dari `won-requested` (Section 09 CI-012) ke `negotiation` (`QUO-005.approvalStatus = 'submitted'`); **OPP-006 tetap `negotiation`** dengan `QUO-006.approvalStatus = 'approved'`, siap didemokan "Ajukan sebagai Won" → Approve Won (Management). Dicatat di `docs/mockup-change-impact-log.md`.

### D-050 — "Company" dan "Project Order" Adalah Party dan Project yang Sama (Bukan Entitas Paralel)
**Status:** LOCKED · **Date:** 2026-07-30 · **Affected:** `app/pages/customer-journey/customers/*`, `app/pages/customer-journey/project-orders/*`, `app/types/party.ts`
**Context:** Prompt 19-5B/5C meminta halaman "Customers/Companies" dan "Project Orders" dengan struktur tab/field yang terlihat seperti entitas baru; hard rule "Jangan membuat route duplikat bila route existing dapat diadaptasi" dan "gunakan fixture yang sama, bukan dataset terpisah".
**Decision:** "Company" = `Party` (D-001/D-024, tidak diubah), diperluas aditif dengan `size?`/`city?`/`phone?`/`accountOwnerId?`. "Project Order" = `Project` (tidak ada field baru — Account Executive derivasi via `Project.opportunityId → Opportunity.ownerId`, bukan field baru di `Project`). Halaman `/customer-journey/customers`/`/customer-journey/project-orders` adalah **route baru** (AE-centric lens, field/filter berbeda dari `/crm/prospects`/`/crm/clients`/`/projects`) tapi 100% membaca array `PARTIES`/`PROJECTS` yang sama — bukan alias/redirect, bukan dataset paralel.
**Reason:** IA literal Prompt 19-8 merekomendasikan route baru untuk kelompok menu "Customer Journey" yang terpisah dari sidebar "CRM"/"Projects" existing (kebutuhan navigasi/UX berbeda: AE ingin melihat "Customers"+"Project Orders" dalam satu konteks Customer Journey), namun hard rule melarang duplikasi dataset — kombinasi keduanya adalah route presentasi baru di atas data existing, bukan model data baru.
**Consequence:** `/crm/prospects`, `/crm/clients`, `/crm/parties/[id]`, `/projects`, `/projects/[id]` **tidak disentuh sama sekali** (selain ekstensi type `Party` yang backward-compatible). Tab "Documents" Customer Detail = union `getDocumentsByProject` lintas seluruh Project Order milik company (selektor baru `getDocumentsByParty`, bukan entitas `PartyDocument` baru).

### D-051 — Lead sebagai Entitas Baru (Pre-Party), Bukan Perluasan Party
**Status:** LOCKED · **Date:** 2026-07-30 · **Affected:** `app/types/lead.ts` (baru), `app/data/leads.ts` (baru)
**Context:** Prompt 19-5A meminta Lead dengan lifecycle sendiri (New/Contacted/Qualified/Unqualified, terpisah dari `Party.lifecycleStatus` Prospect/Client) dan aksi "Qualify & Create Opportunity" (bukan "Convert to Customer").
**Decision:** `Lead`/`LeadActivity` adalah entitas baru (bukan perluasan `Party`) — Lead merepresentasikan pra-kualifikasi sebelum ada relasi CRM formal; begitu di-qualify, `Party` (baru atau existing yang cocok namanya — mencegah duplicate company, literal Prompt 19-4) dan `Opportunity` dibuat, `Lead.partyId`/`opportunityId` diisi sebagai referensi (bukan Lead "berubah menjadi" Party). `LeadActivity` reuse `PartyActivityType` (union sama persis) — satu entitas untuk "Activities" dan "Follow-ups" drawer (follow-up = activity dengan `dueAt` terisi), pola identik `PartyActivity` (Section 07).
**Reason:** Lead dan Party mewakili konsep bisnis berbeda (pra-kualifikasi vs relasi CRM formal); memaksakan Lead sebagai varian `Party` akan mencampur dua lifecycle independen dalam satu field (anti-pola yang sama yang dihindari D-001/D-024 dengan memisahkan sekaligus tidak mengizinkan duplikasi shape).
**Consequence:** `getUpcomingFollowUps`(Party, Section 07)/`getLeadFollowUps` (Lead, Prompt 19) tetap 2 selektor terpisah (entitas sumber beda), tapi keduanya reuse fungsi generik `isFollowUpUpcoming` yang sama (tipe parameter dipersempit ke `{ dueAt? }` di `app/utils/attention.ts` — perubahan backward-compatible, bukan logic baru).

### D-052 — Activity Center: Narrow Role Override di Level Navigasi (Bukan Hanya Level Halaman)
**Status:** LOCKED · **Date:** 2026-07-30 · **Affected:** `app/constants/navigation.ts` (`NavItem.roles`), `app/components/layout/AppSidebar.vue`
**Context:** Prompt 19-6 eksplisit "Activity Center" hanya untuk Super Admin — lebih sempit dari modul `administration` existing (`ROLE_MODULE_ACCESS.administration` juga memberi Management/Viewer `VIEW`, dipakai `/admin/audit-trail`).
**Decision:** `NavItem` diperluas dengan field opsional `roles?: RoleId[]` — bila diisi, MENGGANTIKAN (bukan menambah) cek `moduleKey` untuk visibilitas item nav tsb. `AppSidebar.vue` diupdate untuk mengecek `roles` dulu sebelum fallback ke `moduleKey`. Item "Activity Center" dan 3 child "Customer Journey" (Customers/Project Orders/Lead Source Recap, dibatasi Sales — lihat Prompt 19-10) memakai field ini; seluruh nav item lain **tidak berubah perilakunya** (tidak mengisi `roles`, tetap murni `moduleKey`).
**Reason:** Ini adalah pola "narrow role exception" (sudah mapan di level halaman sejak Section 07/08 — `canManageParty`/`canManageOpportunity`) diterapkan di level nav — dibanding membuat modul baru granular (`activity-center`, `customer-journey`) yang akan menambah kolom permanen ke `ROLE_MODULE_ACCESS` untuk kasus yang genuinely sempit/khusus.
**Consequence:** Halaman `/activity-center` sendiri tetap melakukan pengecekan independen (`currentRole === 'super-admin'`) — `roles` di nav HANYA mengontrol visibilitas link, bukan pengganti page-level guard (defense in depth, konsisten pola `RoleAccessState` di seluruh halaman lain).

---

## Kelompok J — Change Request Prompt 20 (Sales Qualification to Account Executive Opportunity Flow)

Prompt 20 adalah **change request** di atas Prompt 19 (COMPLETED), memperbaiki alur Customer Journey agar pembagian tanggung jawab Sales (Lead Qualification) dan Account Executive (Opportunity/Requirement/Quotation/Won) benar-benar lengkap secara data dan gate — Prompt 19 sudah memisahkan role-nya, tapi Lead belum punya form Qualification terstruktur dan Opportunity belum punya Requirement Detail/gate nyata sebelum Quotation. Keputusan berikut LOCKED untuk perubahan ini kecuali dinyatakan lain.

### D-053 — AE "Mark as Won" Langsung Setelah Commercial Approval (Supersede Sebagian D-025 Khusus Won)
**Status:** LOCKED · **Date:** 2026-07-31 · **Affected:** `app/pages/crm/opportunities/[id]/index.vue`, `app/data/index.ts`
**Context:** D-025 (Prompt 3) mengunci model approval Won dua-langkah: Sales/AE "Ajukan sebagai Won" (`negotiation → won-requested`), lalu Management/Super Admin approve terpisah (`won-requested → won`). Prompt 20-1/13 eksplisit menggambarkan flow linear "...AE Submit ke Management→ Management Approve/Reject→ AE Mark as Won→ Active Client..." — satu approval Management (pada Quotation/Commercial Approval), diikuti AE langsung Mark as Won tanpa approval kedua terpisah untuk Won itu sendiri.
**Decision:** Tombol AE berubah dari "Ajukan sebagai Won" (submit, menunggu Management) menjadi **"Mark as Won"** (eksekusi langsung), gated: `quotation.approvalStatus === 'approved'` DAN `getOpportunityMissingRequirements` kosong DAN opportunity belum pernah dikonversi (`projectId` kosong). Implementasi **reuse 100% mutator existing** (`advanceOpportunityStage(id,'won-requested')` diikuti `approveOpportunityWon(id, quotation.approvedBy)` dipanggil berurutan secara sinkron dalam satu klik) — TIDAK ada mutator baru, `OpportunityStage` (union 9 nilai) **tidak diubah/dihapus nilainya** (`won-requested` tetap ada di union, hanya menjadi transisi sesaat yang tidak pernah terlihat persisten oleh user). `wonApprovedBy` diisi `quotation.approvedBy` (Management yang benar-benar approve komersial), bukan AE, agar field tetap akurat secara semantik.
**Reason:** Merestrukturisasi/menghapus `won-requested` dari union akan berisiko tinggi (dipakai luas Section 08/09/14, lihat D-049); reuse mutator existing dengan urutan pemanggilan berbeda adalah perubahan **paling minimal** yang memenuhi instruksi eksplisit Prompt 20, konsisten dengan protokol bagian C ("perubahan section lama hanya bila benar-benar dibutuhkan... diminta secara eksplisit oleh user").
**Consequence:** UI Opportunity Detail tidak lagi menampilkan section Approve Won/Reject Won terpisah untuk Management pada stage `won-requested` (section tsb secara fungsional sudah tidak pernah dicapai lewat alur normal — dipertahankan sebagai fallback informatif saja, defense-in-depth). `rejectOpportunityWon` (`app/data/index.ts`) tetap ada (tidak dihapus, backward-compatible, tidak ada consumer lain yang bergantung padanya saat ini) tapi tidak lagi dipanggil dari UI manapun. Dicatat di `docs/mockup-change-impact-log.md` CI-027.

### D-054 — Qualification Sebagai Field Aditif pada `Lead` (Bukan Entitas Terpisah)
**Status:** LOCKED · **Date:** 2026-07-31 · **Affected:** `app/types/lead.ts`, `app/data/index.ts`
**Context:** Prompt 20-4 meminta form Qualification dengan ~14 field (7 wajib, 7 opsional) sebelum Lead dapat di-qualify — pertanyaannya apakah ini entitas `LeadQualification` terpisah (1:1 ke `Lead`) atau field langsung pada `Lead`.
**Decision:** Seluruh field Qualification ditambahkan sebagai field opsional langsung pada `Lead` (bukan entitas terpisah), diisi progresif lewat `updateLeadQualification` ("Simpan Draft", tanpa gate) dan digerbangi lewat `getLeadMissingQualification` sebelum `qualifyLeadAndCreateOpportunity` (gate DI DALAM mutator, bukan hanya UI). "Hasil qualification" (Prompt 20-4) sengaja **tidak** disimpan sebagai field terpisah — direpresentasikan oleh `Lead.stage` (`qualified`/`unqualified`) itu sendiri, hasil dari aksi yang ditekan.
**Reason:** Field-field ini murni properti dari satu Lead yang sama (bukan konsep bisnis independen dengan lifecycle sendiri, berbeda dari alasan `Lead` dipisah dari `Party` di D-051) — entitas terpisah untuk relasi 1:1 murni akan menambah kompleksitas join tanpa manfaat, bertentangan dengan prinsip "pilih model paling sederhana yang tetap realistis".
**Consequence:** `Lead.handedOverTo` (sudah ada sejak Prompt 19) kini diisi Sales secara eksplisit lewat form Qualification ("Account Executive yang menerima lead") — bukan lagi ditentukan otomatis oleh sistem saat tombol "Qualify & Create Opportunity" diklik (perilaku lama `leads/index.vue` sebelum Prompt 20).

### D-055 — Requirement Gate Sebelum Quotation Terpisah dari Requirement Gate Sebelum Won
**Status:** LOCKED · **Date:** 2026-07-31 · **Affected:** `app/data/index.ts` (`getOpportunityRequirementGate`, baru — TERPISAH dari `getOpportunityMissingRequirements`, Section 09)
**Context:** Prompt 20-10 meminta Requirement Gate sebelum AE dapat membuat/submit Quotation. `getOpportunityMissingRequirements` (Section 09) sudah ada tapi justru **mensyaratkan Quotation sudah ada** (dipakai gerbang sebelum Won) — dipakai ulang langsung untuk gate pre-Quotation akan menjadi sirkular (Quotation tidak mungkin "sudah ada" sebelum dibuat).
**Decision:** Fungsi baru `getOpportunityRequirementGate` mengecek subset field literal Prompt 20-10 (destination, travel period, estimated traveler, service scope, requirement summary, contact person, estimated value) — TIDAK mengecek keberadaan Quotation. Dipakai untuk men-disable tombol "Buat Quotation" (dulu "Lanjut ke Proposal") pada stage `requirement-gathering`. Payment terms dan margin/cost summary **sengaja tidak digerbangi** (Prompt 20-10 menandainya "bila diwajibkan"/"bila dipakai pada approval" — kondisional tanpa mekanisme konfigurasi eksplisit lain di codebase, diperlakukan sebagai field opsional pada `Quotation`, bukan blocking gate) — asumsi aman untuk mockup, konsisten pola D-040.
**Reason:** Dua gerbang mewakili dua titik keputusan bisnis berbeda (siap dibuatkan Quotation vs siap di-Won-kan) — memaksakan satu fungsi untuk keduanya akan membuat salah satu gerbang salah logika.
**Consequence:** `getOpportunityMissingRequirements` (Section 09) **tidak diubah sama sekali** — tetap dipakai apa adanya sebagai gerbang final sebelum Won (dicek juga di tombol "Mark as Won", D-053).

### D-056 — `OpportunityWorkflowStatus` Dirivasi (Bukan Field Tersimpan), Menggantikan Label "Won (Menunggu Approval)"
**Status:** LOCKED · **Date:** 2026-07-31 · **Affected:** `app/types/opportunity.ts`, `app/data/index.ts` (`getOpportunityWorkflowStatus`), `app/constants/status.ts` (`OPPORTUNITY_WORKFLOW_STATUSES`)
**Context:** Prompt 20-10/14 meminta vocabulary status baru (Pending Requirement→Ready for Quotation→Quotation Draft→Pending Management Approval→Approved→Won→Lost) dan eksplisit "Jangan gunakan status Won (Menunggu Approval) karena membingungkan". Vocabulary baru ini tidak 1:1 dengan `OpportunityStage` (9 nilai, D-028/D-049 — TIDAK direstrukturisasi) maupun `QuotationApprovalStatus` (4 nilai) sendirian, melainkan kombinasi keduanya.
**Decision:** Status baru ini **dirivasi** lewat `getOpportunityWorkflowStatus(opportunityId)` (pure function dari `stage` + `quotation?.approvalStatus` + `getOpportunityRequirementGate`), BUKAN field tersimpan baru — mengikuti pola aditif/ortogonal D-049. Dipakai sebagai badge status utama di `PageHeader` Opportunity Detail (mengganti badge `OPPORTUNITY_STAGES` lama di posisi tsb). **Terpisah tapi tidak eksklusif** dari perbaikan literal label "Won (Menunggu Approval)" pada `OPPORTUNITY_STAGES` (stage `won-requested`, dipakai stepper) yang **juga** direname menjadi "Pending Management Approval" — value/key stage tidak berubah, murni label, sehingga acceptance criterion "Status tidak lagi menggunakan Won (Menunggu Approval)" terpenuhi di kedua tempat status tsb pernah tampil ke user.
**Reason:** Merestrukturisasi `OpportunityStage` untuk memuat vocabulary baru akan melanggar D-049 (LOCKED, dipakai luas); derivasi murni menghindari data ganda/tidak sinkron antara field tersimpan dan kondisi aktual `stage`+`quotation`.
**Consequence:** Stepper stage internal (`MAIN_STAGES`, dipakai kontrol alur AE memajukan stage) **tetap dipertahankan apa adanya** di bawah badge workflow-status baru — dua representasi untuk audiens berbeda (badge = ringkasan status bisnis untuk siapa pun, stepper = kontrol operasional untuk AE).

## Kelompok K — Section 00, Roadmap Section 00–24 (2026-08-01)

### D-057 — Roadmap Section 00–24 Diperlakukan sebagai Lapisan Baru di Atas Prompt 0–20 (Bukan Menggantikan Histori)
**Status:** LOCKED · **Date:** 2026-08-01 · **Affected:** `prompts/01-PROTOKOL-WAJIB.md` (versi baru "FRONTEND-ONLY CONTINUATION"), seluruh `prompts/SECTION_NN_*.md` dan `prompts/Section 0N — *.md`, `docs/frontend-*.md` (baru)
**Context:** User memperkenalkan `prompts/01-PROTOKOL-WAJIB.md` versi baru dan rangkaian file `Section 00`–`Section 24` yang menggambarkan roadmap frontend jauh lebih luas (role baru Client/Product Planner/Procurement, modul baru Client Portal/Product Planning/Ticketing-Accommodation-Transportation-MICE terpisah/Booking Service Orders/Changes-Cancellation-Refund-Incident/dst.) dibanding 18 section + 2 change request (Prompt 0–20) yang sudah COMPLETED.
**Decision:** Roadmap Section 00–24 diperlakukan sebagai **fase lanjutan baru** di atas hasil Prompt 0–20 (COMPLETED, tidak diulang/dihapus) — bukan restart dari nol. Section 00 (reconciliation) memetakan hasil Prompt 0–20 ke dalam kerangka Section 00–24: banyak scope Section 01–09/17/22 sudah punya fondasi dari Prompt 5–20 (COMPLETED/PARTIAL), sisanya (Section 03/08/10/13–16/18/19/21, role Client/Product Planner/Procurement) `NOT_STARTED`. Dokumentasi baru (`docs/frontend-module-map.md`, `docs/frontend-workflow-map.md`, `docs/frontend-implementation-roadmap.md`, `docs/frontend-known-issues.md`) dibuat sebagai lapisan pemetaan tambahan; dokumen `docs/mockup-*.md` existing **tetap dipertahankan apa adanya** sebagai source of truth historis Prompt 0–20 (tidak ditulis ulang/dihapus).
**Reason:** Protokol baru eksplisit meminta "Jangan mengulang atau merusak pekerjaan yang sudah selesai" dan "Identifikasi apa yang sudah selesai agar tidak dikerjakan ulang" — histori Prompt 0–20 merepresentasikan pekerjaan nyata yang sudah divalidasi (build sukses, smoke test lulus), menghapus/menulis ulang dokumentasinya akan menghilangkan jejak keputusan (LOCKED, D-001–D-056) yang sebagian besar masih relevan dan dipakai ulang oleh Section 00–24 (mis. Party/Prospect/Client model D-001/D-024, Opportunity stage machine D-028/D-049, Commercial Approval D-049).
**Consequence:** Penomoran section pada laporan (`docs/mockup-section-reports/section-NN-*.md`) sekarang punya DUA skema berbeda yang hidup berdampingan: `section-05-foundation.md` dst. (skema Prompt 0–20 lama) dan `section-00-current-progress-reconciliation.md` dst. (skema Section 00–24 baru, dimulai dari file ini). Tidak ada tabrakan nama karena numbering Section 00–24 baru memakai slug deskriptif berbeda (mis. `section-06-management-approval-won-client-activation.md` vs Prompt lama tidak punya "Section 06" — Section 6 lama adalah "Dashboard"). Pembaca laporan berikutnya harus memeriksa tanggal dan judul, bukan hanya nomor, untuk membedakan skema.

### D-058 — `app/data/index.ts` Diperlakukan sebagai Repository/Service Layer yang Sudah Ada (Tidak Dibangun Lapisan Paralel Baru)
**Status:** LOCKED · **Date:** 2026-08-01 · **Affected:** `app/data/index.ts`, `app/utils/mock-reset.ts` (baru), `app/plugins/mock-reset.client.ts` (baru), `app/pages/settings.vue`
**Context:** Section 01 (Wajib) meminta "Mock repository/service layer agar pages tidak membaca fixture langsung". Audit Section 00 (`docs/frontend-known-issues.md` bagian 1 sub Foundation) menemukan bahwa SELURUH halaman aktual (30+ halaman) sudah 100% mengimpor data lewat satu barrel module (`~/data` = `app/data/index.ts`) — tidak ada halaman yang mengimpor langsung dari `app/data/leads.ts`/`app/data/opportunities.ts`/dst., dan tidak ada halaman yang punya salinan fixture sendiri (kriteria acceptance literal Section 01: "Tidak ada fixture terduplikasi per halaman" — sudah terpenuhi). Yang secara literal berbeda dari "repository pattern" murni: beberapa halaman list membaca array reactive yang di-re-export (`LEADS`, `OPPORTUNITIES`, dst.) dan memfilter/mencari langsung di computed, bukan selalu lewat fungsi selector bernama (`getXByY`).
**Decision:** `app/data/index.ts` **diformalkan sebagai** repository/service layer yang sudah memenuhi maksud fungsional item Wajib ini (single source, tidak ada duplikasi, seluruh mutasi lewat mutator terdokumentasi) — **TIDAK dibangun lapisan repository paralel baru** (mis. `app/repositories/*.ts` yang hanya membungkus ulang fungsi yang sama dengan nama berbeda) karena akan menjadi abstraksi tanpa consumer (dead code sejak dibuat) dan berisiko memicu refactor besar-besaran ke 30+ halaman yang sudah berfungsi baik (melanggar instruksi eksplisit "Jangan mengganti foundation existing yang sudah sehat" dan prinsip "Perubahan lintas section harus minimal, dicatat, dan regression-tested"). Sebagai gantinya, gap yang benar-benar konkret dan belum terpenuhi dari daftar Wajib Section 01 — **"State reset, seed scenario"** — diimplementasikan aditif: `app/utils/mock-reset.ts` (snapshot + restore), `app/plugins/mock-reset.client.ts` (capture snapshot seawal mungkin saat app dimuat), tombol "Reset Demo Data" di `app/pages/settings.vue`.
**Reason:** Instruksi eksplisit Section 01 sendiri: "Jangan mengganti foundation existing yang sudah sehat." Membangun abstraksi yang tidak dipakai justru bertentangan dengan prinsip "no half-finished implementations"/"don't introduce abstractions beyond what the task requires" — nilai literal dari requirement (mencegah duplikasi fixture per halaman, satu source of truth) sudah tercapai lewat pola barrel module existing sejak Foundation (Prompt 5).
**Consequence:** `ErrorState.vue`/`LoadingState.vue` (shared component existing sejak Foundation, `app/components/shared/`) dikonfirmasi TERSEDIA dan REUSABLE untuk section berikutnya yang butuh simulasi error/loading — `ErrorState.vue` saat ini belum dipakai halaman manapun (bukan bug, karena mock data dibaca sinkron sehingga jarang ada momen "loading"/"error" nyata di alur existing; tersedia untuk dipakai section baru yang butuh simulasi async/error eksplisit). Komponen shared "Filter" dan "Timeline" generik TIDAK diekstrak pada Section 01 ini (pola ad-hoc per halaman sudah konsisten secara visual, ekstraksi tanpa kebutuhan konkret kedua akan jadi premature abstraction) — dicatat sebagai opsi masa depan di `docs/frontend-known-issues.md`, bukan gap blocking.

### D-059 — 3 Role Baru (Product Planner, Procurement, Client), Total 16 Role; Client Portal Shell Minimal di Section 02, Fitur Penuh Tetap Milik Section 08
**Status:** LOCKED · **Date:** 2026-08-01 · **Affected:** `app/types/user.ts` (`RoleId`+3, `ModuleKey`+`client-portal`, `User.clientPartyId`), `app/constants/roles.ts`, `app/composables/usePermissions.ts` (`clientScopeId`), `app/data/users.ts`, `app/constants/navigation.ts`, `app/pages/client/index.vue` (baru), `app/pages/admin/roles.vue`
**Context:** Section 02 (Wajib) meminta role final roadmap baru sepenuhnya tersedia (Super Admin s/d Client/Supplier, 16 role total) dan acceptance eksplisit "Client A tidak melihat Client B pada mock scope" — sementara Client Portal LENGKAP (dashboard, quotation confirm, dokumen, traveler submission, dst.) adalah scope Section 08 tersendiri.
**Decision:** `RoleId` diperluas 13→16 (+`product-planner`, +`procurement`, +`client`, mengikuti pola D-046 Prompt 19). `ModuleKey`+`client-portal` (pola identik `supplier-portal`, D-048) — `User.clientPartyId?: ID` menunjuk `Party` (Client = `Party`, konsisten D-050, BUKAN entitas baru). `usePermissions().clientScopeId` pola identik `vendorScopeId`. Route `/client` (shell MINIMAL: profil company + list Opportunity + list Project Order milik company sendiri, TANPA nilai komersial apa pun) dibangun di Section 02 — cukup untuk membuktikan mekanisme isolasi bekerja dan acceptance criteria dapat diuji, TIDAK mengimplementasikan fitur bisnis Client Portal penuh (quotation confirm/document/traveler submission/communication — tetap Section 08). 2 demo user Client (`USR-019`→`PTY-001`, `USR-020`→`PTY-002`, kebetulan cocok dengan `ContactPerson` existing `CP-001`/`CP-002` di company yang sama — dipertahankan sebagai konsistensi narasi, bukan direkayasa). Procurement diberi `vendor: MANAGE` (elevasi dari `VIEW` role lain) — menjadikannya pemilik fungsional direktori Vendor (`/vendors`, aksi "Tambah Vendor" yang sebelumnya hanya bisa Super Admin). Product Planner diberi VIEW read-only ke `crm`/`project`/`vendor`/`reports` (referensi costing) karena Section 10 (modul dedicated-nya) belum dikerjakan.
**Reason:** Mengikuti pola aditif yang sudah mapan (D-046/D-048/D-050) — role/module baru ditambahkan tanpa mengubah nilai role/module existing manapun. Shell minimal untuk Client Portal (bukan implementasi penuh) konsisten dengan D-058 (Section 01): selesaikan hanya gap yang benar-benar dalam scope section berjalan, hindari mengerjakan scope section lain secara prematur/setengah jadi.
**Consequence:** `ROLE_MODULE_ACCESS` bertambah 3 baris + 1 kolom (`client-portal`) untuk seluruh 16 role — matrix existing (13 role x 7 modul lama) TIDAK diubah nilainya, murni ekstensi. `app/pages/admin/roles.vue` (Matrix View, Wajib Section 02 — dikonfirmasi SUDAH ADA sejak Section 17 lama, bukan dibangun baru) diperluas 6→8 kolom modul + `ROLE_NOTES` untuk role yang sebelumnya belum tercatat (`account-executive`, `supplier`, ditambah 3 role baru) — gap dokumentasi pre-existing sejak Prompt 19 yang baru ditemukan dan ditutup di sini. Ditemukan juga bahwa Dashboard (`app/pages/index.vue`) tidak memiliki satu widget pun untuk `product-planner`/`procurement`/`client` (seluruh `visibleTo(...)` existing tidak menyertakan role baru) — akan menghasilkan Dashboard kosong total, dicatat sebagai bug regresi UX bila dibiarkan (bukan hanya "gap Section 08/10/17"). Ditutup dengan pola identik widget "Supplier Portal" (Prompt 19): 3 `SectionCard` welcome/redirect bersyarat baru (`showClientWelcome`/`showProcurementWelcome`/`showProductPlannerWelcome`) — Client diarahkan ke `/client`, Procurement ke `/vendors` (module yang sudah mereka `MANAGE`), Product Planner ke `/crm/opportunities` (referensi, sampai Section 10 ada).

### D-060 — Public Lead Intake: Route Tunggal `/lead-intake` (Bukan 4 Route Terpisah), Owner Default `USR-001`, Reuse `createLead`+`updateLeadQualification`
**Status:** LOCKED · **Date:** 2026-08-01 · **Affected:** `app/pages/lead-intake/index.vue` (baru), `app/pages/login.vue` (+link discoverability)
**Context:** Section 03 (Wajib) meminta "Public forms Corporate Travel, Group Travel, Individual, dan MICE" — empat kategori. Halaman ini juga harus di luar dashboard internal (tanpa login), menulis ke `LEADS` centralized state yang sama, dan menampilkan UTM/source/referrer preview.
**Decision:** Diimplementasikan sebagai **satu route** `/lead-intake` (`layout: false`, tanpa `middleware: 'auth'`) dengan selector 4 kategori (`LeadServiceCategory`, reuse type existing Prompt 20 — TIDAK ada type/entitas baru) yang mengubah copy/label form secara dinamis, bukan 4 file/route terpisah yang isinya 90% identik. Kategori dapat di-preset lewat query string (`?type=mice-event`) sehingga tetap punya 4 URL berbeda yang bisa dipakai terpisah di kampanye pemasaran mock. Submission memanggil `createLead()` (field dasar) lalu `updateLeadQualification()` (field tambahan opsional: `serviceCategory`/`destination`/`travelerEstimate`/`requirementSummary`/`qualificationNotes` berisi ringkasan UTM/referrer) — **reuse penuh 2 mutator existing** (Prompt 20), TIDAK ada mutator/field baru di `app/data/index.ts`/`app/types/lead.ts`. Owner default lead publik: `USR-001` (satu-satunya user demo role Sales, sudah jadi owner seluruh Lead fixture existing) — belum ada mekanisme distribusi/assignment otomatis lintas-Sales (di luar scope Section 03, dicatat sebagai simplifikasi eksplisit, bukan gap tersembunyi).
**Reason:** Empat file/route yang secara struktural identik (field sama, hanya label/copy berbeda) akan melanggar prinsip DRY dan instruksi "Gunakan komponen existing... jangan menduplikasi". Reuse mutator existing (bukan menulis mutator publik baru) menghindari dua jalur penulisan Lead yang bisa divergen dari waktu ke waktu.
**Consequence:** `document.referrer`/UTM query params ditampilkan sebagai preview transparan ke pengguna DAN disimpan ringkas ke `Lead.qualificationNotes` (bukan field terstruktur baru) — cukup untuk ditelusuri Sales/AE saat membuka Lead, tanpa mengubah shape `Lead`. Duplicate suggestion (cek `phone`/`email` terhadap `LEADS` existing) bersifat non-blocking (tidak mencegah submit) — konsisten dengan keputusan Section 04 bahwa "merge suggestion" penuh didefer, Section 03 hanya menampilkan sinyal awalnya. Link discoverability satu baris ditambahkan di `app/pages/login.vue` (halaman publik lain yang sudah ada) agar halaman ini benar-benar reachable tanpa menambahkannya ke navigasi internal manapun (tetap memenuhi "Public tidak mendapat dashboard internal").

### D-061 — Merge Suggestion: Archive-dengan-Referensi (Bukan Penggabungan Field), Duplicate Selector Dipusatkan
**Status:** LOCKED · **Date:** 2026-08-01 · **Affected:** `app/data/index.ts` (`getLeadDuplicateCandidates`, `mergeLeadAsDuplicate`, `reopenLead`, `updateLeadContact`, baru), `app/pages/customer-journey/leads/index.vue`, `app/pages/lead-intake/index.vue` (refactor), `app/data/leads.ts` (+`LED-011`)
**Context:** Section 04 (Wajib) meminta "Create/edit/archive/reopen/merge suggestion" pada Lead. "Merge" sesungguhnya (menggabungkan field dari 2 record jadi 1, memutuskan field mana yang menang per konflik) adalah fitur kompleks yang berisiko tinggi untuk mockup — Section 03 sudah lebih dulu membangun deteksi duplikat non-blocking (preview) di form publik.
**Decision:** "Merge suggestion" diimplementasikan sebagai **archive-dengan-referensi**: Sales meninjau kandidat duplikat (dideteksi otomatis via `getLeadDuplicateCandidates`, cocok phone/email, mengecualikan lead yang sudah archived sebagai target canonical), memilih salah satu untuk di-archive dengan `mergeLeadAsDuplicate` — mencatat 2 `LeadActivity` (di lead duplikat DAN lead canonical) sebagai jejak, TIDAK menghapus/menggabungkan field apa pun (kedua record tetap ada utuh sebagai histori, hard rule "jangan menghapus data"). Selector deteksi duplikat (`getLeadDuplicateCandidates`) **dipusatkan** di `app/data/index.ts` dan dipakai ULANG oleh `/lead-intake` (Section 03, direfactor dari logic lokal) DAN `/customer-journey/leads` (New Lead dialog + drawer Overview) — satu logic, bukan 2 implementasi paralel yang bisa divergen. `reopenLead` (kebalikan `archiveLead`) dan `updateLeadContact` (edit field kontak dasar, sebelumnya hanya bisa diisi sekali saat create) ditambahkan sebagai mutator baru bergaya sama dengan mutator Lead existing (Prompt 20). Fixture `LED-011` ditambahkan (bukan backfill LED existing) dengan email sengaja sama dengan `LED-007`, agar fitur ini benar-benar demonstrable tanpa mengubah satu pun nilai field Lead lama.
**Reason:** Field-merging penuh (pilih nilai mana yang menang per field, gabung activity history dari 2 lead jadi 1 timeline) adalah scope creep signifikan untuk item "Wajib" yang hanya menyebut "merge suggestion" (saran, bukan mesin merge otomatis) — pola archive-dengan-referensi tetap memberi Sales alat kerja nyata (menutup entri yang duplikat, menjaga daftar bersih) tanpa risiko kehilangan data.
**Consequence:** Tidak ada "Merged Lead" gabungan tunggal — Sales/AE yang membuka salah satu dari 2 lead yang pernah ditandai duplikat tetap melihat keduanya secara terpisah (satu archived dengan catatan, satu aktif) di riwayat activity masing-masing. Bila di kemudian hari dibutuhkan true field-merge, ini dicatat sebagai evolusi lanjutan, bukan gap tersembunyi.

### D-062 — Quotation Compare Terbatas pada Nilai Total (Bukan Histori Breakdown Penuh); Client Confirmation sebagai Gerbang Terpisah dari Commercial Approval
**Status:** LOCKED · **Date:** 2026-07-31 · **Affected:** `app/types/opportunity.ts` (`Quotation.taxIdr`/`markupIdr`/`currency`/`validUntil`/`termsAndConditions`/`inclusions`/`exclusions`/`sentToClientAt`, `Opportunity.clientConfirmedAt`/`clientConfirmationNote`), `app/data/index.ts` (`duplicateQuotationVersion`, `sendQuotationToClient`, `withdrawQuotationSubmission`, `recordClientConfirmation`), `app/pages/crm/opportunities/[id]/index.vue`, `app/pages/crm/opportunities/[id]/quotation-preview.vue` (baru)
**Context:** Section 05 (Wajib) meminta Quotation "create/edit/version/duplicate/compare/send mock/revise/withdraw", line item "tax/fee/markup terpisah dari discount", "PDF/print preview", dan gerbang "AE belum dapat Mark as Won sebelum approved + client confirmation". `Quotation.version`/`supersededAmountIdr` (Section 08) sudah memakai model "satu object dimutasi in-place, hanya nilai total versi sebelumnya yang disimpan" — bukan array snapshot penuh per versi.
**Decision:** (1) **Duplicate** (`duplicateQuotationVersion`) menyalin seluruh field quotation saat ini sebagai versi baru (`approvalStatus` direset ke draft) — berbeda dari **Create New Version**/`reviseQuotation` (Section 08, mengosongkan nilai untuk diisi ulang manual). (2) **Compare** mengikuti keterbatasan model existing: hanya `amountIdr` saat ini vs `supersededAmountIdr` yang dibandingkan (bukan breakdown/discount/tax/markup penuh per versi, karena versi lama tidak disimpan sebagai snapshot terpisah) — ditampilkan eksplisit dengan disclaimer di UI, bukan gap tersembunyi. (3) **Send to Client**/**Withdraw** murni timestamp/status mock (`sentToClientAt`, revert `submitted`→`draft`), tidak ada integrasi email/WA nyata (D-006). (4) **PDF/Print Preview** adalah halaman terpisah `layout: false` yang di-print lewat `window.print()` browser — BUKAN generator PDF (dilarang backend/library produksi). (5) **Client Confirmation** (`recordClientConfirmation`) adalah gerbang TAMBAHAN, terpisah dari `Quotation.approvalStatus` (Commercial Approval, Management/Prompt 19) — "Mark as Won" kini mensyaratkan KEDUANYA (`approvalStatus === 'approved'` DAN `Opportunity.clientConfirmedAt` terisi), bukan menggantikan gerbang `approvalStatus` yang sudah ada.
**Reason:** Menyimpan snapshot penuh per versi Quotation (array of Quotation, bukan satu object) adalah restrukturisasi data model besar yang tidak diminta literal Section 05 dan berisiko terhadap seluruh consumer `getQuotationByOpportunity` (Dashboard/Reports/Section 06 dependency) — pola "duplicate = salinan sebagai versi baru" dan "compare = current vs satu nilai sebelumnya" tetap memenuhi acceptance tanpa breaking change. Client Confirmation dipisah dari Commercial Approval karena secara bisnis keduanya adalah pihak berbeda (Management menyetujui secara internal; client mengonfirmasi secara eksternal) — literal Wajib eksplisit menyebut keduanya sebagai syarat terpisah ("approved + client confirmation").
**Consequence:** Quotation yang di-duplicate/direvisi berkali-kali tidak punya audit trail bernilai penuh per versi (hanya versi saat ini + satu nilai sebelumnya) — bila dibutuhkan histori lengkap, ini evolusi lanjutan (perlu migrasi `Quotation` tunggal → array versi, di luar scope Section 05). **OPP-006** (fixture existing, sebelumnya didemokan Prompt 20 sebagai "Quotation approved, Mark as Won aktif") kini menampilkan tombol Mark as Won **disabled** sampai Client Confirmation dicatat — perubahan perilaku yang disengaja (bukan regresi), didokumentasikan di `docs/mockup-change-impact-log.md` CI-034.

### D-063 — Management Approval Queue Agregat di `/crm/quotations`; "Reject" Berfungsi Sekaligus sebagai "Return for Revision"; Gerbang Won Dipindah ke Level Data
**Status:** LOCKED · **Date:** 2026-07-31 · **Affected:** `app/pages/crm/quotations.vue` (ditulis ulang total dari `ModulePlaceholder`), `app/constants/navigation.ts` (`comingSoon` dihapus), `app/data/index.ts` (`+getQuotationsPendingApproval`, `+getOpportunitiesPendingClientConfirmation`, `submitQuotationForApproval`/`approveQuotation`/`rejectQuotation` +`createPartyActivity` logging, `approveOpportunityWon` +guard `approvalStatus`/`clientConfirmedAt` +`party.accountOwnerId`)
**Context:** Section 06 (Wajib): "Management approval queue", "Detail review quotation, margin, discount, terms, complexity, risk", "Approve, reject, return for revision dengan notes/history", "AE Mark as Won hanya setelah approved dan confirmed [dapat diuji lewat role switcher]". Sebelum section ini, Commercial Approval hanya dapat diakses per-Opportunity (`/crm/opportunities/[id]`) — tidak ada agregat lintas Opportunity (Q14); gerbang "approved + confirmed" sebelum Won hanya dicek di UI (Section 05), bukan di mutator; tidak ada histori/audit trail approve/reject (hanya nilai `approvalNote`/`approvedBy` tunggal, tertimpa tiap keputusan).
**Decision:** (1) `/crm/quotations` (placeholder `comingSoon` sejak Foundation) ditulis ulang menjadi Management Approval Queue: 3 tab — "Menunggu Approval" (`getQuotationsPendingApproval`, quotation `submitted`, dengan dialog review lengkap margin/discount/tax/markup/terms/complexity/risk dan aksi Approve/Reject langsung dari queue), "Menunggu Client Confirmation" (`getOpportunitiesPendingClientConfirmation`, visibilitas Management SAJA — aksi tetap milik AE di Opportunity Detail, Section 05), "Semua Quotation" (seluruh `QUOTATIONS`, read-only browse). Tab state via query param `?tab=`, pola sama Project Detail (testable tanpa JS). (2) "Complexity" DIRIVASI dari `serviceScope.length`+`travelerEstimate` (fungsi lokal halaman, bukan field tersimpan/selector global — presentational-only, hindari premature abstraction). (3) "Reject" TIDAK mendapat status paralel baru "Returned for Revision" — `QuotationApprovalStatus` (`draft`/`submitted`/`approved`/`rejected`) tidak diubah; "reject" SECARA FUNGSIONAL selalu berarti "kembalikan untuk direvisi" (quotation `rejected` tetap dapat direvisi AE lewat "Create New Version"/`reviseQuotation`, Section 08, yang mereset `approvalStatus` ke draft). Tombol di UI diberi label eksplisit "Reject (Return for Revision)" untuk menegaskan makna ganda ini tanpa restrukturisasi data model. (4) `submitQuotationForApproval`/`approveQuotation`/`rejectQuotation` masing-masing kini mencatat `PartyActivity` (reuse tab "Activity / Follow-up" Opportunity Detail existing, TIDAK ada komponen histori baru) — memberi jejak "notes/history" yang literal diminta. (5) Guard `quotation.approvalStatus === 'approved' && opportunity.clientConfirmedAt` DIPINDAHKAN ke `approveOpportunityWon` (sebelumnya hanya dicek di tombol UI `submitMarkAsWon`, Section 05) — forbidden flow ("Mark as Won" tanpa approval/confirmation) kini diblokir di level data, bukan hanya disembunyikan lewat tombol disabled, sesuai literal "dapat diuji melalui role switcher". (6) `approveOpportunityWon` menegaskan ulang `party.accountOwnerId = opportunity.ownerId` saat Won (checklist "Account Owner AE") — aditif, tidak mengubah alur `qualifyLeadAndCreateOpportunity` yang sudah mengisi field ini sejak awal.
**Reason:** Menambah status `QuotationApprovalStatus` paralel ("returned-for-revision" terpisah dari "rejected") adalah restrukturisasi type yang dipakai luas (Dashboard, Reports, Opportunity Detail, seluruh fixture existing) untuk perbedaan yang murni semantik — "rejected" yang sudah ada SUDAH membawa makna "revisi diperlukan" secara fungsional (UI existing sejak Prompt 19 sudah bertuliskan "Ditolak — revisi quotation lalu submit ulang"). Guard di level data (bukan hanya UI) dipilih karena literal Wajib eksplisit menyebut "forbidden flow" harus "dapat diuji" — sebuah tombol disabled saja tidak benar-benar mencegah forbidden flow bila dipanggil dari jalur lain.
**Consequence:** Tidak ada breaking change pada consumer `QuotationApprovalStatus` existing (Dashboard/Reports/Section 08 tidak disentuh). Opportunity yang SUDAH `won` sebelum guard ini ditambahkan (`OPP-001`/`002`/`003`/`008`) tidak terpengaruh — guard hanya mengeksekusi pada transisi `won-requested → won` yang baru, bukan pada data historis. `party.accountOwnerId` sekarang eksplisit ditegaskan di 2 titik (create Party dari Lead, DAN Won) — konsisten, tidak ada kemungkinan Party kehilangan `accountOwnerId` di antara kedua titik tsb.

### D-064 — Customer Journey Funnel Dihitung Snapshot Independen per Tahap (Bukan Cohort Historis); AE Portfolio Scoping via Toggle Default-On
**Status:** LOCKED · **Date:** 2026-07-31 · **Affected:** `app/pages/customer-journey/index.vue` (rewrite), `app/pages/customer-journey/leads/index.vue` (+seed `stageFilter` dari query), `app/pages/customer-journey/customers/index.vue` (+owner filter, +toggle portfolio, +seed `statusFilter` dari query), `app/pages/customer-journey/project-orders/index.vue` (+date filter, +toggle portfolio), `app/pages/crm/opportunities/index.vue` (+seed `stageFilter` dari query), `app/pages/crm/quotations.vue` (+filter `status` pada tab "Semua"), `app/data/index.ts` (`+getPartiesByAccountOwner`)
**Context:** Section 07 (Wajib): "Overview funnel Lead→Qualified→Opportunity→Approved→Won→Client→Project Order", "Drill-down dari metrics ke records", "Filters by source, owner, stage, client, date, project type", "AE data scope ke portfolio miliknya; Super Admin seluruh data". Audit Section 00 (`docs/frontend-known-issues.md` bagian 5) menemukan funnel/drill-down belum ada (`NEEDS_VALIDATION`); Leads/Customers/Project Orders sub-page sudah COMPLETED (Prompt 19) tapi TIDAK ter-scope AE (bug ditemukan: dashboard utama sudah scoping Opportunity tapi TIDAK Lead maupun Party).
**Decision:** (1) Funnel 7-tahap dihitung independen per tahap dari kondisi data TERKINI (snapshot), bukan cohort historis per-lead (codebase tidak menyimpan event-log transisi) — pola sama `/customer-journey/lead-sources` yang sudah ada sejak Prompt 19. Setiap tahap = `NuxtLink` yang drill-down ke halaman list terkait dengan query filter yang sesuai (`?stage=`/`?status=`/`?tab=`), diverifikasi via SSR (select terpilih otomatis dari `route.query`). (2) "Complexity" data mismatch ("Opportunity" bisa melebihi "Qualified") didokumentasikan eksplisit di UI sebagai karakteristik data demo (sebagian Opportunity fixture pre-dating entitas Lead), bukan bug. (3) AE portfolio scoping: bug diperbaiki di dashboard utama (`scopedLeads` untuk AE sebelumnya TIDAK ter-scope sama sekali, kini `handedOverTo === currentUser.id`); selector baru `getPartiesByAccountOwner` untuk scoping Party/Client (sebelumnya "Active Clients" selalu global). Pada sub-list (`Customers`/`Project Orders`), scoping diimplementasikan sebagai **toggle default-ON untuk AE** ("Hanya Portfolio Saya", pola sama `assignedToMeOnly` di Leads, Section 04) — bukan hard filter tanpa override — agar AE tetap bisa browse seluruh data bila perlu (mis. riset company lain), sekaligus default view tetap portfolio sesuai literal Wajib.
**Reason:** Cohort funnel historis (melacak setiap Lead individual sampai ke Project Order) membutuhkan event-log per transisi yang tidak ada di codebase — restrukturisasi besar di luar scope literal "Overview funnel" (yang tidak secara eksplisit meminta cohort tracking). Toggle default-on (bukan hard filter) dipilih agar konsisten dengan pola `assignedToMeOnly` yang sudah divalidasi user sejak Section 04, dan tidak mengunci AE dari kemampuan browse data lain yang secara bisnis wajar (co-selling, riset kompetitor internal, dst.).
**Consequence:** Angka funnel "Opportunity" dapat > "Qualified" pada data demo saat ini (10 vs 3, per smoke test) — perilaku yang benar dan disengaja, bukan bug kalkulasi. `/crm/opportunities` dan `/crm/quotations` (Section 08/06, di luar folder `/customer-journey/*`) TIDAK mendapat AE portfolio scoping (di luar scope literal "Customer Journey Dashboard") — drill-down funnel ke sana tetap menampilkan seluruh data lintas AE, hanya ter-filter oleh stage/status yang diklik, bukan oleh portfolio AE yang sedang login.

### D-065 — Client Portal: Field Sanitization Eksplisit, Accept/Reject Self-Service Reuse `recordClientConfirmation`, Reject Bersifat Komunikasi (Bukan Mutasi Status), Dokumen Read-Only
**Status:** LOCKED · **Date:** 2026-07-31 · **Affected:** `app/pages/client/index.vue` (rewrite total dari shell Section 02), `app/pages/client/opportunities/[id]/index.vue` (baru), `app/pages/client/project-orders/[id]/index.vue` (baru), `app/types/lead.ts` (`LeadSource` +`client-portal`), `app/constants/status.ts` (`LEAD_SOURCES` +1 entri)
**Context:** Section 08 (Wajib): dashboard, company profile+contacts, travel request creation, quotation view+revision request+accept/reject confirmation mock, Project Orders, shared itinerary, traveler submission, shared documents, invoice/payment status sell-side, change request, support/contact AE-PM, notifications/action center — dengan larangan eksplisit "Client tidak melihat internal margin, supplier cost, internal notes, atau client lain". Shell minimal Section 02 (`/client`, `clientScopeId` isolation) sudah ada; section ini melengkapi seluruh fitur bisnis di atasnya.
**Decision:** (1) **Sanitization eksplisit** — Quotation client-facing menampilkan `amountIdr`/`discountIdr`/`taxIdr`/`currency`/`validUntil`/`paymentTerms`/`termsAndConditions`/`inclusions`/`exclusions`/`serviceBreakdown` (harga jual, bukan cost) TAPI TIDAK PERNAH `estimatedCostIdr`/`estimatedMarginIdr`/`markupIdr` (komponen cost/margin internal) maupun `approvedBy`/`approvalNote` (komentar internal Management). Project client-facing menampilkan `quotationAmountIdr` (total paket, = harga jual) TAPI TIDAK `budgetIdr`/`actualCostIdr` (biaya internal agency). Diverifikasi eksplisit lewat grep audit ketiga file baru/rewrite — nol kecocokan field terlarang. (2) **Accept/Reject** — "Accept" REUSE `recordClientConfirmation` (Section 05) apa adanya, kini dipanggil self-service oleh Client (bukan hanya dicatat manual oleh AE) — menutup gap yang dicatat eksplisit di known-issues Section 05 ("AE mencatat client confirmation secara manual, bukan self-service client"). "Reject" TIDAK memutasi `Quotation.approvalStatus` atau field apa pun (Client bukan Management/AE, tidak berwenang mengubah status komersial internal) — murni mencatat `PartyActivity` sebagai komunikasi keberatan yang harus ditindaklanjuti manusia (AE), pola sama "Request Revision". (3) **Change Request** REUSE `createChangeEntry` (Section 14) apa adanya, kategori dibatasi ke `traveler`/`itinerary`/`service`/`other` di dropdown (menyembunyikan `vendor`/`budget` yang murni internal-operational, tidak relevan/tidak pantas dipilih Client). (4) **Shared Documents** diimplementasikan READ-ONLY (bukan two-way upload) — `DOCUMENTS` (`app/data/activity.ts`) bukan `reactive()` dan tidak ada mutator create; upload mock akan membutuhkan migrasi `reactive()` tambahan yang tidak esensial untuk literal "Shared documents" (dibaca sebagai dokumen yang DIBAGIKAN OLEH agency, bukan wajib two-way). (5) **Travel Request** REUSE `createLead`+`updateLeadQualification` (Prompt 20/D-060, pola sama `/lead-intake`) — `LeadSource` +`'client-portal'` (aditif) agar Lead Source Recap dapat membedakan channel repeat-request dari Client existing vs public/manual lead baru; `ownerId`/`handedOverTo` diisi `party.accountOwnerId` (AE yang sudah menangani company tsb) alih-alih default generik `USR-001`, karena konteksnya company YANG SUDAH DIKENAL (bukan lead publik anonim).
**Reason:** Sanitization eksplisit (bukan implisit lewat "lupa menyertakan field") dipilih agar audit-able — setiap field yang di-exclude punya alasan tertulis, bukan kebetulan. Reject-sebagai-komunikasi (bukan mutasi status) menghormati batas kewenangan role (Client tidak pernah menjadi approver komersial, konsisten "Hanya Management yang mengapprove" dari Section 06/protokol). Documents read-only dipilih untuk membatasi blast radius perubahan data-layer (tidak menambah `reactive()` migration yang tidak esensial) sambil tetap memenuhi literal "Shared documents" secara wajar.
**Consequence:** Client TIDAK dapat meng-upload dokumen sendiri (mis. scan paspor) lewat halaman Documents — evolusi lanjutan bila dibutuhkan (akan memerlukan `DOCUMENTS` jadi `reactive()` + mutator `createProjectDocument` baru). "Reject Quotation" oleh Client tidak mengubah `OpportunityWorkflowStatus`/badge apa pun secara otomatis — AE harus membaca Activity/Follow-up dan bertindak manual (create new version, dst.), konsisten pola komunikasi mock existing (D-006, bukan integrasi/automasi nyata).

### D-066 — `ProjectOrderStatus` 10-Nilai Dirivasi dari `ProjectStatus` (LOCKED) + Field Handover/Ready/Closure Baru; Risk sebagai Entitas Baru; Task Diperluas untuk Milestone/Dependency (Bukan Entitas Paralel)
**Status:** LOCKED · **Date:** 2026-07-31 · **Affected:** `app/types/project.ts` (`+ProjectOrderStatus`, `+ProjectClosureChecklist`, `Project` +7 field aditif), `app/types/activity.ts` (`ProjectTask` +3 field aditif, `+ProjectRisk`/`ProjectRiskSeverity`/`ProjectRiskStatus`), `app/constants/status.ts` (`+PROJECT_ORDER_STATUSES`, `+RISK_SEVERITIES`, `+RISK_STATUSES`), `app/data/activity.ts` (`TASKS` jadi `reactive()`, `+PROJECT_RISKS`), `app/data/projects.ts` (`Project` +backfill handover), `app/data/index.ts` (+13 mutator/selector baru), `app/pages/projects/[id]/index.vue` (Overview +4 SectionCard baru, Tasks tab ditulis ulang)
**Context:** Section 09 Wajib: "Project Order list/detail. Source Opportunity dan approved Quotation. AE Account Owner, PM Operational Owner. AE-to-PM handover checklist. PM Accept/Return Handover dengan reason. Team assignment dan role responsibilities. Milestones, tasks, dependencies, documents, risks, activity. Status: Created, Handover Pending, Planning, Confirmed, Ready, In Progress, Completed, Closed, On Hold, Cancelled. Transition guards dan visible reason. Closure checklist shell." Q16 (blocking sebelum section ini) merekomendasikan derivasi aditif mengikuti pola D-053/D-056, BUKAN restrukturisasi `ProjectStatus` (8 nilai, LOCKED D-028, dipakai luas Section 08–18/Prompt 19). Audit langsung kode menemukan 2 gap tersembunyi: (a) `project.status` TIDAK PERNAH punya mutator — hanya di-set sekali saat `approveOpportunityWon` dan tidak pernah bertransisi lagi meski `PROJECT_STATUSES` dipakai luas untuk tampilan; (b) link "Project ini berasal dari opportunity" mengarah ke `/crm/opportunities` (list), bukan opportunity spesifiknya — bug kecil, diperbaiki bersamaan.
**Decision:** (1) `ProjectOrderStatus` (10 nilai) DIRIVASI oleh `getProjectOrderStatus(project)` — kombinasi `project.status` (tidak diubah) + `handoverAcceptedAt` (draft→Handover Pending/Planning), `readyAt` (confirmed→Ready), `closedAt` (completed→Closed). `'created'` tidak pernah persisten (project selalu lahir sebagai `draft`, pola sama `OpportunityStage.won-requested`). (2) **Handover**: `acceptProjectHandover`/`returnProjectHandover` (baru) — Return TIDAK mengubah `project.status` (tetap Handover Pending), murni mencatat alasan sebagai `ActivityEntry` visible. Guard: hanya dari `status === 'draft'` dan belum pernah accepted. (3) **Status transition**: peta `PROJECT_STATUS_TRANSITIONS` eksplisit (forward-only + on-hold/cancelled dari hampir semua state, on-hold dapat resume ke 3 state manapun) — `updateProjectStatus` mewajibkan `reason` untuk on-hold/cancelled (Wajib "visible reason"), dicatat sebagai `ActivityEntry` (bukan `isChange`, reuse tab Activity & Changes existing, TIDAK ada komponen histori baru). (4) **Team assignment**: `addProjectTeamMember`/`removeProjectTeamMember` (baru) — `teamUserIds` sudah ada sejak Foundation tapi sebelumnya tanpa mutator sama sekali. (5) **Milestones/Dependencies**: `ProjectTask` +`isMilestone?`/`dependsOnTaskId?`/`assignedTo?` (aditif) — BUKAN entitas paralel, "milestone" adalah task yang ditandai, dependency adalah single-link sederhana (bukan DAG penuh). `TASKS` (sebelumnya read-only murni, tidak ada create/edit sama sekali) diupgrade `reactive()` + `createProjectTask`/`updateProjectTask` baru. (6) **Risks**: `ProjectRisk` entitas BARU (bukan diperluas dari `ActivityEntry`/`Opportunity.requirementDetail.riskNotes` yang scoped berbeda — risk butuh severity/status/lifecycle sendiri yang tidak cocok dipaksakan ke model existing manapun). (7) **Closure checklist**: `ProjectClosureChecklist` SHELL — 4 item toggle mock, TIDAK menggerbangi transisi status apa pun (pemenuhan penuh sengaja diserahkan ke Section 24 sesuai instruksi literal "shell untuk dipenuhi section akhir"). (8) Fixture: `PRJ-101`/`102`/`103` di-backfill `handoverAcceptedAt` (sudah lama berjalan, historically masuk akal sudah di-accept); `PRJ-104` (satu-satunya `draft`) SENGAJA dibiarkan tanpa handover — demonstrasi hidup "Handover Pending" tanpa fixture baru. 2 `ProjectRisk` diseed pada `PRJ-103` (complex/MICE, paling wajar untuk risk tracking nyata).
**Reason:** Restrukturisasi `ProjectStatus` existing (LOCKED, dipakai Dashboard/Reports/Customer Journey/Client Portal/9+ halaman) untuk mengakomodasi 10 nilai baru berisiko sangat tinggi (breaking change lintas-section masif) untuk kebutuhan yang sepenuhnya terpenuhi lewat derivasi aditif — pola yang SAMA PERSIS sudah terbukti berhasil untuk `OpportunityWorkflowStatus` (D-053/D-056). Risk sebagai entitas baru (bukan reuse) dipilih karena tidak ada model existing yang punya semantik lifecycle risk (severity naik-turun, status open→mitigated→closed) — memaksakannya ke `ActivityEntry` akan mencampur dua konsep berbeda (log peristiwa vs item yang di-track statusnya).
**Consequence:** `Project.status` kini benar-benar dapat bertransisi (sebelumnya statis selamanya sejak fixture) — regresi terhadap 4 project existing tidak terjadi karena mutator baru murni ADITIF (tidak ada yang memanggilnya otomatis, PM harus mengklik tombol transisi secara eksplisit). `getProjectOrderStatus` menjadi satu-satunya sumber kebenaran tampilan status Project Order — badge `PageHeader` diganti dari `PROJECT_STATUSES` mentah ke hasil derivasi ini; `PROJECT_STATUSES` mentah tetap ditampilkan sebagai "Status Internal" di metadata (traceability, tidak dihilangkan). Closure checklist TIDAK memblokir/mengizinkan apa pun sampai Section 24 — PM dapat mencentang seluruh item tanpa efek pada status Closed (transparan, didokumentasikan, bukan tombol yang menipu).

### D-067 — Product Planning dan Costing: Dua Entitas Baru (`ProductTemplate`/`CostSheet`) Terpisah dari `Quotation`; Snapshot via `applyCostSheetToQuotation`; Modul `product-planning` Baru
**Status:** LOCKED · **Date:** 2026-07-31 · **Affected:** `app/types/product.ts` (baru), `app/types/user.ts` (`ModuleKey` +`product-planning`), `app/types/opportunity.ts` (`Quotation` +`costSheetId?`), `app/constants/roles.ts` (`ROLE_MODULE_ACCESS` +kolom `product-planning` seluruh role), `app/constants/navigation.ts` (+menu "Product Planning"), `app/data/products.ts` (baru), `app/data/index.ts` (+selector/mutator Product Template/Cost Sheet), `app/pages/product-planning/**` (baru), `app/pages/crm/opportunities/[id]/index.vue` (+SectionCard ringkasan Cost Sheet), `app/pages/admin/roles.vue` (+kolom matrix), `app/pages/index.vue` (widget Product Planner diperbarui)
**Context:** Section 10 (Wajib): "Product/package templates. Itinerary concept. Service alternatives. Cost sheet, markup, tax/fee, contingency, currency. Traveler-based costing. Scenario/version comparison. Inclusions, exclusions, assumptions, validity. Collaboration dengan AE, Operations, Finance. Snapshot konsep ketika dipakai pada quotation/project. Internal costing tidak terlihat Client." Audit Section 00 mengonfirmasi modul ini `NOT_STARTED` sepenuhnya — role `product-planner` sudah ada sejak Section 02 (`ROLE_MODULE_ACCESS['product-planner']` sebelumnya hanya `VIEW` ke `crm`/`project`/`vendor`/`reports`, tanpa modul dedicated), `Quotation` (Section 05/08, D-062) sudah punya field komersial (`estimatedCostIdr`/`estimatedMarginIdr`/`serviceBreakdown`) tapi diisi manual oleh AE — belum ada alat costing terstruktur milik Product Planner.
**Decision:** (1) Dua entitas baru, BUKAN perluasan `Quotation` — `ProductTemplate` (katalog reusable: itinerary concept, `serviceAlternatives[]` embedded pola sama `QuotationServiceItem`, inclusions/exclusions/assumptions/validity, `basePaxCount`, status draft→active→archived) dan `CostSheet` (perhitungan konkret: `lineItems[]` traveler-based, `markupPercent`/`taxPercent`/`contingencyPercent`/`currency`, opsional terhubung `productId` dan/atau `opportunityId`). Dipisah dari `Quotation` karena `Quotation` adalah dokumen komersial client-facing (setelah sanitasi) sedangkan `CostSheet` adalah kerja internal murni Product Planner yang tidak boleh bocor ke Client — mencampurnya ke satu type akan mempersulit audit "field mana yang aman ditampilkan ke Client" (sudah rapi di D-065). (2) **Kalkulasi** (`getCostSheetBreakdown`) DIRIVASI, bukan field tersimpan — pola sama `getProjectOrderStatus`/`getOpportunityWorkflowStatus`: base cost → +contingency → +markup → +tax = total sell, margin = total sell − (base+contingency). (3) **Scenario/version comparison**: Cost Sheet dengan `opportunityId` sama = skenario berbeda untuk dibandingkan (mis. CS-003/CS-004 "Economy"/"Premium" pada OPP-009); `duplicateCostSheetVersion` menaikkan `version`+menyimpan `supersededTotalSellIdr` — pola identik `duplicateQuotationVersion` (D-062). (4) **Snapshot**: `applyCostSheetToQuotation` menyalin hasil kalkulasi (`estimatedCostIdr`/`estimatedMarginIdr`/`serviceBreakdown`/`currency`) ke Quotation (dibuat baru bila belum ada, atau timpa yang masih draft — guard sama `updateQuotationDetails`), lalu MENGUNCI Cost Sheet (`status: 'final'`, `updateCostSheet` menolak edit lebih lanjut) — revisi lanjutan wajib "Duplicate as New Version". `Quotation.costSheetId` (field baru, aditif) menyimpan traceability tanpa mengubah alur Quotation existing. (5) **Internal costing tidak terlihat Client**: TIDAK ADA perubahan pada Client Portal (Section 08, D-065) — `CostSheet`/`ProductTemplate` sama sekali tidak diimpor di `app/pages/client/**`, dan field yang disalin ke Quotation (`estimatedCostIdr`/`estimatedMarginIdr`) SUDAH masuk daftar field yang disaring Client Portal sejak D-065 (grep audit ulang mengonfirmasi nol kecocokan). (6) **Modul baru** `product-planning` (`ModuleKey`) — `product-planner` `MANAGE` (satu-satunya pemilik penuh), `account-executive`/`operations`/`finance`/`project-manager`/`management`/`viewer` `VIEW` (kolaborasi literal Wajib "AE, Operations, Finance" + akses baca wajar untuk PM/Management/Viewer mengikuti pola modul lain), role lain (`sales`/`ticketing`/`accommodation`/`transportation`/`mice`/`procurement`/`client`/`supplier`) `NONE`.
**Reason:** Memaksakan Cost Sheet sebagai field tambahan pada `Quotation` akan mencampur dua siklus hidup berbeda (Cost Sheet bisa berdiri sendiri sebelum ada Opportunity/Quotation sama sekali — lihat CS-005 baseline reference) dan mempersulit "scenario comparison" (butuh banyak Cost Sheet per satu Opportunity, sementara `Quotation` per Opportunity secara desain hanya satu record aktif yang dimutasi in-place). Derivasi kalkulasi (bukan field tersimpan) menghindari field stale bila `travelerCount`/`markupPercent`/dst. diubah setelah dibuat. Snapshot-lalu-kunci (bukan live-reference) memastikan Quotation yang sudah dipakai AE tidak berubah diam-diam bila Product Planner merevisi Cost Sheet sumbernya di kemudian hari — konsisten prinsip "quotation harus stabil begitu dipakai" yang sudah diterapkan pada `approvalStatus` locking (D-049/D-062).
**Consequence:** Total sell hasil Cost Sheet (CS-001/CS-002, historis terhadap OPP-001/OPP-002 yang sudah Won) SENGAJA tidak dipaksa sama persis dengan `Quotation.amountIdr` — variance kecil merefleksikan negosiasi AE, bukan bug data (didokumentasikan di komentar `app/data/products.ts`). `applyCostSheetToQuotation` pada Opportunity yang Quotation-nya sudah `submitted`/`approved` akan gagal (return `undefined`) — Cost Sheet baru harus dibuat sebagai versi/skenario terpisah, tidak menimpa Quotation yang sudah dalam proses approval. Tidak ada perubahan pada `ProjectStatus`/`ProjectOrderStatus`/tab Project Detail manapun — "Snapshot ketika dipakai pada project" terpenuhi transitif lewat rantai existing (`Project.quotationAmountIdr`/`budgetIdr` sudah disalin dari `Quotation.amountIdr` sejak `approveOpportunityWon`, Section 09) tanpa field baru pada `Project`.

### D-068 — Traveler Document Fields Aditif pada Model Existing; Visa Dievaluasi Kondisional; Masking berbasis Role; Import Preview Menggantikan Import Langsung
**Status:** LOCKED · **Date:** 2026-07-31 · **Affected:** `app/types/project.ts` (`Traveler` +7 field), `app/utils/attention.ts` (`isTravelerDocumentMissing` diperluas), `app/utils/format.ts` (`+maskDocumentNumber`), `app/data/index.ts` (+`toggleTravelerVerification`, `+getTravelerReadiness`, `+previewTravelerImportMock`, `+commitTravelerImport`, `CreateTravelerInput` +6 field, `importTravelersMock` DIHAPUS), `app/pages/projects/[id]/index.vue` (tab Travelers diperkaya), `app/pages/projects/[id]/manifest-preview.vue` (baru), `app/pages/client/project-orders/[id]/index.vue` (form traveler diperkaya)
**Context:** Section 11 (roadmap Section 00–24 baru, Wajib): "Group, rooming, companion. Passport/ID/visa metadata. Expiry and missing document warnings. Emergency contact, dietary, accessibility, special request. Client self-submission. Bulk import preview dan error report mock. Internal verification. Sensitive values masked sesuai role. Manifest/rooming list export preview. Readiness indicator." Fondasi traveler/participant/group/rooming sudah COMPLETED sejak Section 11 lama (Prompt 11) — hanya `passportNumber`/`passportExpiryDate`/kontak darurat/`specialRequest` yang ada; "Client self-submission" sudah RESOLVED sejak Section 08 (reuse `createTraveler`/`updateTraveler` langsung). Gap konkret: ID/visa metadata, dietary/accessibility terpisah, companion, internal verification, masking, bulk import preview+error report (`importTravelersMock` lama langsung membuat baris tanpa tahap preview), manifest/rooming export preview, readiness indicator.
**Decision:** (1) Seluruh field baru (`idNumber`/`visaNumber`/`visaExpiryDate`/`dietaryRestrictions`/`accessibilityNeeds`/`companionOfTravelerId`/`documentsVerifiedAt`/`documentsVerifiedBy`) ditambahkan ADITIF ke `Traveler` existing (TIDAK ada entitas paralel) — konsisten prinsip "jangan membuat dataset paralel". (2) `isTravelerDocumentMissing` diperluas mengevaluasi visa HANYA bila `visaNumber` sudah diisi (visa opsional — tidak seluruh destinasi mewajibkannya, tidak ada model "requires-visa per destinasi" di codebase): traveler tanpa `visaNumber` sama sekali berperilaku identik seperti sebelum Section 11 baru (regression-safe). (3) "Internal verification" (`toggleTravelerVerification`) TERPISAH dari `isTravelerDocumentMissing` — verification adalah tindakan manusia (staf mengonfirmasi sudah diperiksa), bukan hasil derivasi otomatis kelengkapan field; keduanya bisa berbeda (traveler dengan dokumen lengkap tapi belum diverifikasi staf, atau sebaliknya). (4) "Sensitive values masked sesuai role" — HANYA `canManageTravelers` (Project Manager/Super Admin, narrow-role-exception existing Section 11 lama) yang melihat nomor paspor/ID/visa penuh; seluruh role lain (termasuk Management, Finance, Sales, AE, sub-domain Ticketing/Accommodation/Transportation/MICE, Viewer) melihat `maskDocumentNumber` (4 karakter terakhir saja) — berlaku di tabel dashboard MAUPUN manifest print preview (satu fungsi masking dipakai di kedua tempat, bukan implementasi ganda). Client Portal TIDAK di-masking (traveler adalah data milik company sendiri, bukan data pihak lain yang dilihat role internal). (5) "Bulk import preview dan error report mock" — `importTravelersMock` (Section 11 lama, langsung membuat baris) DIHAPUS, diganti `previewTravelerImportMock` (murni baca, menghasilkan baris dengan `errors[]` — sebagian sengaja error: nama kosong, paspor duplikat) + `commitTravelerImport` (hanya membuat baris valid) — dua tahap terpisah agar "preview" dan "error report" benar-benar ada sebagai konsep UI, bukan sekadar nama fungsi. (6) "Manifest/rooming list export preview" — halaman print-friendly terpisah (`manifest-preview.vue`, `layout: false`, `window.print()`), pola IDENTIK `quotation-preview.vue` (Section 05, D-062) — bukan generator dokumen produksi (D-006). (7) "Readiness indicator" — `getTravelerReadiness` derivasi murni (pola sama `getCostSheetBreakdown`), SCOPED ke kesiapan data traveler (dokumen lengkap/terverifikasi/rooming ditugaskan) — sengaja TIDAK tumpang tindih dengan readiness checklist/matrix agregat lintas-domain yang menjadi tanggung jawab Section 12 (`docs/frontend-known-issues.md` bagian 10, KNOWN_GAP terpisah).
**Reason:** Field aditif (bukan entitas/type paralel) menghindari duplikasi model traveler yang sudah dipakai luas (Itinerary Section 12, Vendor tab, Client Portal, manifest baru) — satu source of truth tetap `Traveler`. Visa kondisional (bukan wajib universal) mencegah SELURUH 17 traveler fixture existing (yang tidak punya data visa) tiba-tiba menjadi "Dokumen Belum Lengkap" tanpa alasan — perubahan status massal yang tidak diminta literal dan akan terlihat seperti regresi. Masking berbasis role mengikuti prinsip least-privilege yang sudah dipakai untuk `FULL_FINANCIAL_VISIBILITY_ROLES` (data sensitif hanya untuk role yang benar-benar butuh). Preview+commit dua-fungsi (bukan satu fungsi yang "sekalian preview") membuat UI dapat menampilkan tabel preview dan menahan aksi commit sampai user menekan tombol eksplisit — sesuai literal "preview" (bukan auto-commit).
**Consequence:** `TRV-1022` (PRJ-102, expiring passport <180 hari) dan `TRV-1025` (PRJ-102, visa TANPA tanggal kedaluwarsa — kondisi BARU akibat aturan visa Section 11) SENGAJA berubah dari "Dokumen Lengkap" menjadi "Dokumen Belum Lengkap" dibanding sebelum section ini — perubahan status yang disengaja untuk mendemokan aturan baru, didokumentasikan eksplisit di komentar `app/data/projects.ts`, BUKAN regresi tersembunyi. Fungsi `importTravelersMock` yang sebelumnya dipakai langsung di UI kini tidak ada lagi — satu-satunya pemanggil (`app/pages/projects/[id]/index.vue`) sudah diperbarui mengikuti alur preview baru (lihat `docs/mockup-change-impact-log.md` CI-041). Tiket dan asuransi TETAP tidak ada field/UI — di luar Wajib literal Section 11 (tidak disebutkan), tetap `KNOWN_GAP` eksplisit di `docs/frontend-known-issues.md`, bukan gap tersembunyi.

### D-069 — Operational Command Center: Readiness Gates Bersifat Advisory (Bukan Hard Gate); Shift Notes Entitas Baru Terpisah dari Activity Log; Itinerary Internal-Only via Field Aditif
**Status:** LOCKED · **Date:** 2026-08-01 · **Affected:** `app/types/project.ts` (`ItineraryItem` +`timezone`/`visibleToClient`), `app/types/activity.ts` (`ProjectTask` +`isBlocked`/`blockedReason`, `+ShiftNote`/`ShiftPeriod`), `app/data/index.ts` (+`updateItineraryItem`, `+getClientVisibleItineraryItems`, `+toggleTaskBlocked`, `+getServiceReadinessMatrix`, `+getDepartureReadiness`, `+getProjectAttentionQueue`, `+getShiftNotes`/`createShiftNote`), `app/data/activity.ts` (`+SHIFT_NOTES`), `app/data/projects.ts` (backfill `timezone`/`visibleToClient` + 3 item internal-only baru), `app/pages/projects/[id]/index.vue` (tab Itinerary & Services dan Tasks diperkaya), `app/pages/projects/[id]/run-sheet-preview.vue` (baru), `app/pages/client/project-orders/[id]/index.vue` (filter itinerary)
**Context:** Section 12 (Wajib): "Day-by-day itinerary. Timezone-aware schedule. Internal vs client-shared itinerary. Tasks, checklist, owner, deadline, blocker, dependency. Service readiness matrix. Departure readiness gates. Run sheet, contacts, emergency info. Attention/exception queue. On-trip updates dan shift notes mock. Calendar/timeline views." Acceptance: "PM dan Operations dapat membawa project dari Planning sampai On Trip melalui frontend." Audit Section 00 menemukan day-by-day itinerary dan Tasks (owner/deadline/dependency) sudah COMPLETED sejak Section 12 lama/Section 09 — gap konkret: timezone, internal/client-shared, blocker, readiness matrix/gate, run sheet, attention queue, shift notes, timeline view.
**Decision:** (1) **Timezone-aware schedule**: `ItineraryItem.timezone` (string label IANA, mis. "Asia/Manila") — murni teks ditampilkan berdampingan `time`, BUKAN konversi timezone otomatis (tidak ada library kalender/date-fns-tz baru — D-006, package addition policy D-036). (2) **Internal vs client-shared**: `ItineraryItem.visibleToClient` (default `true` bila kosong — regression-safe untuk 17 item existing) — `getClientVisibleItineraryItems` (selector baru) SATU-SATUNYA titik filter yang boleh dipakai halaman Client Portal; toggle "Jadikan Internal"/"Tampilkan ke Client" di halaman internal, `canManageOperations` (PM/Operations/Super Admin, pola sama `SERVICE_TYPE_ROLE_MAP`). (3) **Blocker**: `ProjectTask.isBlocked`/`blockedReason` — field ADITIF terpisah dari `status` (LOCKED), bukan status baru; toggle dengan alasan wajib saat memblokir (mengikuti pola `updateProjectStatus` yang mewajibkan reason untuk transisi berdampak besar, D-066). (4) **Service readiness matrix** dan **Departure readiness gates**: `getServiceReadinessMatrix`/`getDepartureReadiness` — DERIVASI murni (pola sama `getCostSheetBreakdown`/`getProjectOrderStatus`), mengagregasi selector existing lintas-section (Traveler Section 11, Service Section 12 lama, Task/Risk Section 09, Invoice Section 15) TANPA menyalin logic. Bersifat **ADVISORY** — TIDAK memblokir `updateProjectStatus`/transisi status manapun, pola sama Closure Checklist (D-066, "shell tanpa gating keras") — demo tetap bisa mencoba transisi status kapan pun tanpa harus memenuhi seluruh syarat readiness dulu. (5) **Attention/exception queue**: `getProjectAttentionQueue` — agregator lintas tab (task blocked/overdue, service changed, risk open, traveler dokumen kurang, invoice overdue), setiap item membawa `tab` tujuan untuk navigasi klik-langsung (`activeTab.value = item.tab`). (6) **Run sheet, contacts, emergency info**: halaman print-friendly terpisah `run-sheet-preview.vue` — pola IDENTIK `quotation-preview.vue` (D-062)/`manifest-preview.vue` (D-068), BUKAN generator dokumen produksi. Berisi jadwal harian + team contacts (`teamUserIds`→`USERS`) + vendor contacts (`getVendorContacts`) + traveler emergency info (masking sensitive value tetap berlaku, D-068). (7) **On-trip updates dan shift notes**: `ShiftNote` entitas BARU (`app/types/activity.ts`) — TERPISAH dari `ActivityEntry` (tab "Activity & Changes", berorientasi governance/approval perubahan) karena semantiknya murni catatan serah-terima operasional tanpa status approval; create-only (append log, tidak ada edit/hapus). (8) **Calendar/timeline views**: toggle List/Timeline pada Daily Itinerary — data sama persis, tampilan Timeline murni CSS (garis vertikal + dot marker per item), BUKAN komponen calendar/library baru.
**Reason:** Readiness gate advisory (bukan hard-block) dipilih agar tidak mengunci alur demo yang sudah divalidasi sejak Section 09 (transisi status project bebas dicoba PM kapan pun) — memaksa seluruh syarat readiness terpenuhi dulu akan bertentangan dengan fleksibilitas mockup dan berisiko membuat skenario demo existing (mis. PRJ-102 yang sengaja punya task blocked) tidak bisa diuji transisi statusnya. `ShiftNote` sebagai entitas baru (bukan reuse `ActivityEntry`) dipilih karena memaksakannya ke model yang sudah punya flag `isChange`/`approvalStatus` akan mencampur dua konsep berbeda (log perubahan yang butuh approval vs catatan operasional harian yang tidak). Timezone sebagai label teks (bukan konversi nyata) konsisten larangan fabrikasi integrasi (D-006) — tidak ada kebutuhan bisnis riil untuk kalkulasi lintas-zona di mockup ini.
**Consequence:** `PRJ-102` (`TSK-1021`) SENGAJA di-backfill `isBlocked: true` — Departure Readiness Gate project ini akan menampilkan "Belum Siap" dibanding sebelumnya tidak ada indikator sama sekali; ini demo hidup fitur baru, bukan regresi. 3 `ItineraryItem` baru (`ITIN-1015`/`1026`/`1037`, `visibleToClient: false`) ditambahkan sebagai catatan internal — Client Portal `/client/project-orders/[id]` (tab Itinerary) TIDAK LAGI menampilkan ketiganya (diverifikasi smoke test), sementara `/projects/[id]` (internal) tetap menampilkan seluruhnya dengan badge "Internal Only". `getDepartureReadiness`/`getServiceReadinessMatrix` TIDAK menyentuh field tersimpan apa pun pada `Project`/`ProjectService` — murni presentasi baru di atas data existing (konsisten pola Section 10 Overview yang murni presentasi).

### D-070 — Ticketing: `FlightBooking` Entitas Baru Terpisah dari `ProjectService`; Modul Top-Level `/ticketing` Mendampingi (Bukan Menggantikan) Tab Project Detail; Net Cost Disanitasi dari Dokumen Client-Facing
**Status:** LOCKED · **Date:** 2026-08-01 · **Affected:** `app/types/ticketing.ts` (baru), `app/types/user.ts` (`ModuleKey` +`ticketing`), `app/constants/roles.ts` (`ROLE_MODULE_ACCESS` +kolom `ticketing` seluruh role), `app/constants/navigation.ts` (+menu "Ticketing"), `app/constants/status.ts` (+`FLIGHT_BOOKING_STATUSES`/`CABIN_CLASSES`), `app/data/ticketing.ts` (baru), `app/data/index.ts` (+selector/mutator Flight Booking), `app/pages/ticketing/**` (baru), `app/pages/projects/[id]/index.vue` (+ringkasan Flight Booking di sub-section flight), `app/pages/admin/roles.vue` (+kolom matrix)
**Context:** Section 13 (Wajib): "Flight request/options. Fare/cabin/baggage/ancillary display. PNR/order reference mock. Segments dan traveler assignment. Ticketing deadline. Hold, Confirm, Issue, Reissue, Cancel, Refund state simulation. Group booking/name list. Schedule change/disruption. Fare rules and financial impact. Internal net cost vs client sell price. Documents/ticket preview." Acceptance: "Ticketing role dapat mengelola flight dari request sampai completed/refunded pada mock workflow." Audit Section 00 mencatat modul ini `PARTIAL (role-only)` — role `ticketing` sudah ada sejak Foundation, hanya menggerbangi sub-section flight generik di tab "Itinerary & Services" (`ProjectService`: label/status/vendor/bookingReference) — TIDAK ADA model lifecycle detail (options/PNR/segments/deadline/state machine granular/fare rules/net cost vs sell price). `docs/frontend-known-issues.md` bagian 11 mencatat potensi ketegangan dengan D-020 (LOCKED: Operations/Traveler melebur ke tab Project Detail, bukan menu top-level) sebagai sesuatu yang "perlu ditinjau ulang secara eksplisit" saat section ini dikerjakan.
**Decision:** (1) `FlightBooking` (baru, `app/types/ticketing.ts`) — entitas TERPISAH dari `ProjectService`, `serviceId` OPSIONAL menautkan balik ke baris `ProjectService` (tipe `flight`) yang sudah ada (pola sama `CostSheet.opportunityId` opsional, D-067) — `ProjectService` tetap ringkasan generik lintas SELURUH tipe layanan (tidak diubah), `FlightBooking` adalah model detail KHUSUS flight yang tidak bisa dipaksakan ke model generik itu. (2) **Resolusi ketegangan D-020**: dibuat modul TOP-LEVEL BARU (`/ticketing`, `ModuleKey` sendiri) YANG MENDAMPINGI (bukan menggantikan/memindahkan) ringkasan sub-flight di tab Project Detail — D-020 TETAP LOCKED (Operations/Traveler tetap tab, bukan menu), keputusan ini HANYA menambah satu modul baru untuk domain yang literal Wajib-nya secara eksplisit meminta lifecycle detail lintas-project (PNR/deadline/state machine) yang secara alami adalah workspace Ticketing sendiri, bukan sesuatu yang pas hidup di dalam satu Project Detail. Preseden ini eksplisit didokumentasikan untuk diikuti Section 14–16 (Accommodation/Transportation/MICE). (3) **Options/fare/cabin/baggage/ancillary**: `FlightOption[]` embedded array (pola sama `QuotationServiceItem`/`ProductServiceAlternative`), `selectFlightOption` toggle single-select untuk menandai opsi yang dipilih setelah perbandingan. (4) **Segments/traveler assignment/group booking**: `FlightSegment[]` + `travelerIds[]` embedded — `travelerIds` SEKALIGUS berfungsi sebagai "group booking/name list" (bukan field terpisah) saat lebih dari satu traveler. (5) **Lifecycle**: `FlightBookingStatus` 7 nilai literal (`requested`/`hold`/`confirmed`/`issued`/`reissued`/`cancelled`/`refunded`), peta transisi eksplisit (`FLIGHT_BOOKING_TRANSITIONS`, pola sama `PROJECT_STATUS_TRANSITIONS` D-066) via `updateFlightBookingStatus` — alasan WAJIB untuk `cancelled`/`refunded` (dampak besar), dicatat sebagai `ActivityEntry` pada project terkait (jejak historis, reuse tab Activity & Changes existing, TIDAK ada komponen histori baru). (6) **Internal net cost vs client sell price**: `netCostIdr`/`sellPriceIdr` opsional terpisah (belum terisi selagi `requested`/`hold`, harga belum final) — visibilitas `netCostIdr` digerbangi `canManageTicketing || canViewFinancials` (role Ticketing DAN role finansial penuh D-030, KARENA Ticketing-lah yang mengelola langsung net cost vs sell price sesuai literal acceptance); `eticket-preview.vue` (dokumen client-facing) HANYA menampilkan `sellPriceIdr`, TIDAK PERNAH `netCostIdr` (pola sanitasi sama D-065/D-067). (7) **Documents/ticket preview**: `eticket-preview.vue` — halaman print-friendly terpisah, pola IDENTIK `quotation-preview.vue`/`manifest-preview.vue`/`run-sheet-preview.vue` (D-062/D-068/D-069), BUKAN generator dokumen produksi. (8) Fixture SENGAJA ditautkan ke data existing yang relevan (bukan dataset paralel): `FLT-1011`/`1021`/`1031`/`1032` menautkan `serviceId` ke `SVC-1011`/`1021`/`1031`/`1032` (`bookingReference` existing menjadi `pnr`), segmen mengikuti tanggal `ItineraryItem` terkait, `fareIdr` opsi mengikuti `costPerPaxIdr` Cost Sheet Section 10 (`CS-001`/`CS-002`/`CS-003`) untuk destinasi yang sama, dan `FLT-1021` (`reissued`) menautkan narasi `CHG-1021` (perubahan tanggal, Section 14 lama) sebagai alasan reissue.
**Reason:** Memaksakan field lifecycle detail (options/PNR/segments/deadline/fare rules/net cost) ke `ProjectService` akan mencemari model generik yang dipakai SELURUH tipe layanan (hotel/transportation/mice) dengan field yang hanya relevan untuk flight — melanggar prinsip "jangan membuat dataset paralel" dari arah sebaliknya (memaksakan satu model generik menjadi terlalu spesifik). Modul top-level baru (bukan memindahkan tab existing) dipilih karena literal Wajib Section 13 secara implisit meminta workspace LINTAS-PROJECT untuk role Ticketing (acceptance: "Ticketing role dapat mengelola flight dari request sampai completed/refunded") — sebuah tab yang terkubur di dalam satu Project Detail tidak cocok untuk kebutuhan ini, sementara D-020 (kenapa Operations/Traveler tidak jadi menu top-level) spesifik tentang operasional PER-PROJECT, bukan tentang melarang SELURUH modul top-level baru untuk sub-domain manapun.
**Consequence:** `docs/frontend-known-issues.md` bagian 11 diperbarui — Ticketing RESOLVED, Accommodation/Transportation/MICE (Section 14–16) tetap `KNOWN_GAP` dengan preseden arsitektur ini sebagai acuan. `ProjectService` (Foundation) TIDAK diubah shape-nya — nol risiko regresi terhadap consumer existing (Dashboard/Reports/Client Portal/tab lain). `FlightBooking` yang `refunded` bersifat terminal (`updateFlightBooking` menolak edit lebih lanjut, pola sama `CostSheet.status === 'final'`) — TIDAK ada mekanisme "duplicate as new version" seperti `CostSheet`/`Quotation` (refund adalah akhir siklus hidup satu booking, bukan titik revisi); booking pengganti (mis. re-book setelah refund) dibuat sebagai `FlightBooking` baru terpisah.

### D-071 — Accommodation: `HotelBooking` Entitas Baru Terpisah dari `ProjectService`; Modul Top-Level `/accommodation` Mendampingi Tab Project Detail; Room Block/Rooming List dan Traveler Special Request Direuse dari Section 11 (Preseden D-070 Diikuti Persis)
**Status:** LOCKED · **Date:** 2026-08-01 · **Affected:** `app/types/accommodation.ts` (baru), `app/types/user.ts` (`ModuleKey` +`accommodation`), `app/constants/roles.ts` (`ROLE_MODULE_ACCESS` +kolom `accommodation` seluruh role), `app/constants/navigation.ts` (+menu "Accommodation"), `app/constants/status.ts` (+`HOTEL_BOOKING_STATUSES`/`MEAL_PLANS`), `app/data/accommodation.ts` (baru), `app/data/index.ts` (+selector/mutator Hotel Booking, +`getHotelRoomingList`), `app/pages/accommodation/**` (baru), `app/pages/projects/[id]/index.vue` (+ringkasan Hotel Booking di sub-section hotel), `app/pages/admin/roles.vue` (+kolom matrix)
**Context:** Section 14 (Wajib): "Hotel sourcing/options. Property, room type, rate plan, meal, policies. Room block, occupancy, rooming list. Check-in/out, early/late request. Quote, booking, confirmation, voucher. Amendment, cancellation, no-show, deadline, penalty. Traveler special requests. Internal cost isolation." Acceptance: "Accommodation role dapat menangani individual maupun group secara penuh." Section 13 (Ticketing) mendokumentasikan D-070 secara eksplisit sebagai preseden arsitektur untuk section ini — audit langsung kode mengonfirmasi kondisi Accommodation sebelum section ini identik dengan kondisi Ticketing sebelum Section 13 (`PARTIAL, role-only`: role `accommodation` sudah ada sejak Foundation, hanya menggerbangi sub-section hotel generik `ProjectService` di tab "Itinerary & Services" — tidak ada model lifecycle detail). Tambahan penting yang TIDAK ada di Section 13: rooming list/room block dan traveler special request SUDAH punya model dedicated sejak Section 11 (`TravelerGroup`/`RoomAssignment`/`Traveler.specialRequest` dkk.) — harus direuse, bukan diduplikasi.
**Decision:** (1) `HotelBooking` (baru, `app/types/accommodation.ts`) — entitas TERPISAH dari `ProjectService`, `serviceId` OPSIONAL menautkan balik ke baris `ProjectService` (tipe `hotel`) yang sudah ada, pola IDENTIK D-070 poin 1 — `ProjectService` tetap ringkasan generik lintas SELURUH tipe layanan (tidak diubah). (2) **Modul top-level baru** `/accommodation` (`ModuleKey` sendiri) MENDAMPINGI ringkasan sub-hotel di tab Project Detail, pola IDENTIK D-070 poin 2 — TIDAK meninjau ulang D-020 lagi (sudah diresolusi tuntas oleh D-070, preseden berlaku otomatis untuk seluruh sub-domain operasional lintas-project: Ticketing/Accommodation/Transportation/MICE). (3) **Room block, occupancy, rooming list** — SENGAJA TIDAK diberi field/entitas baru: `HotelBooking.groupId` (opsional) menautkan ke `TravelerGroup` (Section 11) yang sudah ada, `getHotelRoomingList(projectId, groupId)` (selector baru, murni filter) membaca `RoomAssignment` existing apa adanya — rooming list tab Travelers (Section 11) dan Hotel Booking Detail (Section 14) menampilkan BARIS YANG SAMA, bukan dua sumber data. `roomsBlocked` (angka) tetap field baru di `HotelBooking` karena merepresentasikan konsep berbeda (kuantitas kamar yang di-*block*/dipesan, bisa lebih besar dari jumlah `RoomAssignment` aktual yang sudah di-assign nama). (4) **Traveler special requests** — SENGAJA TIDAK diberi field baru: `HotelBooking.travelerIds` (pola sama `FlightBooking`) menunjuk ke `Traveler` existing, halaman detail membaca `specialRequest`/`dietaryRestrictions`/`accessibilityNeeds` (Section 11) langsung dari traveler yang ditunjuk. (5) **Hotel sourcing/options/property/room type/rate plan/meal/policies**: `HotelOption[]` embedded array (pola sama `FlightOption`), `selectHotelOption` toggle single-select. (6) **Individual maupun group** (acceptance): `travelerIds` 1 entri = individual (contoh `HTL-1035`, VIP suite aksesibilitas 2 traveler), banyak entri = group (contoh `HTL-1022`/`1033`/`1034`, Room Block 18/10/25 pax) — TIDAK ada flag/type terpisah "individual vs group", cukup derivasi dari panjang array, pola sama semantik `travelerIds` pada `FlightBooking`. (7) **Lifecycle**: `HotelBookingStatus` 7 nilai literal (`requested`/`quoted`/`confirmed`/`amended`/`completed`/`cancelled`/`no-show`), peta transisi eksplisit (`HOTEL_BOOKING_TRANSITIONS`, pola sama `FLIGHT_BOOKING_TRANSITIONS`) via `updateHotelBookingStatus` — alasan WAJIB untuk `cancelled`/`no-show` (dampak besar, sejalan literal "penalty"), dicatat sebagai `ActivityEntry` pada project terkait. `amended` BUKAN status terminal (berbeda dari `FlightBooking.reissued` yang hanya bisa lanjut ke `cancelled`) — booking hotel yang diamandemen (mis. upgrade tipe kamar) tetap harus bisa lanjut ke `completed`/`cancelled`/`no-show`, merefleksikan bahwa amandemen adalah peristiwa tengah siklus, bukan akhir siklus. (8) **Check-in/out, early/late request**: `checkInDate`/`checkOutDate`/`earlyCheckInRequested`/`lateCheckOutRequested` — field literal langsung, ditampilkan sebagai badge di header detail. (9) **Quote, booking, confirmation, voucher**: `confirmationNumber` (pola sama `pnr`), `voucherIssuedAt` (timestamp milestone), halaman print-friendly terpisah `voucher-preview.vue` (pola IDENTIK `eticket-preview.vue`). (10) **Deadline, penalty**: `cancellationDeadline`, `cancellationPenaltyIdr`/`noShowPenaltyIdr` (opsional, diedit manual lewat Edit dialog — TIDAK ada kalkulasi otomatis persentase, konsisten "jangan mengarang integrasi/mesin bisnis nyata"; fixture `HTL-1023` mendemokan penalti 15% dari estimasi biaya SEBAGAI DATA, bukan formula). (11) **Internal cost isolation**: `netCostIdr`/`sellPriceIdr` terpisah, visibilitas digerbangi `canManageAccommodation || canViewFinancials` (pola IDENTIK `canViewFlightFinancials`), `voucher-preview.vue` HANYA menampilkan `sellPriceIdr`. (12) Fixture ditautkan ke `SVC-1022`/`1023`/`1033` (`confirmationNumber` mengikuti `bookingReference` existing), `groupId` ke `GRP-001`/`002`/`003` (rooming list otomatis menampilkan `ROOM-001`/`002`/`003`), `ratePerNightIdr` mengikuti `costPerPaxIdr` Cost Sheet Section 10 (`CS-002`/`CS-003`/`CS-004`) dibagi 4 malam.
**Reason:** Preseden D-070 dinyatakan eksplisit oleh Section 13 sendiri sebagai acuan untuk Section 14–16 — mengulang analisis dari nol akan bertentangan dengan protokol "jangan mengulang pekerjaan yang sudah diputuskan". Reuse `TravelerGroup`/`RoomAssignment`/`Traveler` (bukan entitas paralel) WAJIB karena keduanya sudah menjadi source of truth rooming/special-request sejak Section 11 — membuat model rooming kedua di `HotelBooking` akan menciptakan dua kebenaran yang bisa berbeda (mis. rooming list di tab Travelers menunjukkan 5 kamar sementara Hotel Booking Detail menunjukkan angka lain), melanggar hard rule "semua flow harus memakai satu source of truth". `amended` sebagai status non-terminal (berbeda dari pola `reissued` pada Flight) dipilih karena realita bisnis hotel: amandemen (ganti tipe kamar, tambah malam, dst.) adalah kejadian yang bisa terjadi berkali-kali di tengah siklus booking yang sama, bukan penanda mendekati akhir siklus seperti reissue tiket.
**Consequence:** `docs/frontend-known-issues.md` bagian 11 diperbarui — Accommodation RESOLVED, Transportation/MICE (Section 15–16) tetap `KNOWN_GAP` dengan preseden D-070/D-071 sebagai acuan. `ProjectService`/`TravelerGroup`/`RoomAssignment`/`Traveler` (Foundation/Section 11) TIDAK diubah shape-nya — nol risiko regresi. `HotelBooking` yang `completed`/`cancelled`/`no-show` bersifat terminal (`updateHotelBooking` menolak edit lebih lanjut) — booking pengganti dibuat sebagai `HotelBooking` baru terpisah, pola sama `FlightBooking.refunded`.

### D-072 — Transportation: `TransportBooking` Entitas Baru Terpisah dari `ProjectService`; Modul Top-Level `/transportation` Mendampingi Tab Project Detail; Manifest/Group Allocation Direuse dari Section 11; Dua Dokumen Terpisah (Service Order Client-Facing vs Driver Sheet Internal Tanpa Harga)
**Status:** LOCKED · **Date:** 2026-08-01 · **Affected:** `app/types/transportation.ts` (baru), `app/types/user.ts` (`ModuleKey` +`transportation`), `app/constants/roles.ts` (`ROLE_MODULE_ACCESS` +kolom `transportation` seluruh role), `app/constants/navigation.ts` (+menu "Transportation"), `app/constants/status.ts` (+`TRANSPORT_BOOKING_STATUSES`/`VEHICLE_TYPES`), `app/data/transportation.ts` (baru), `app/data/index.ts` (+selector/mutator Transport Booking), `app/pages/transportation/**` (baru), `app/pages/projects/[id]/index.vue` (+ringkasan Transport Booking di sub-section transportation), `app/pages/admin/roles.vue` (+kolom matrix)
**Context:** Section 15 (Wajib): "Transfer/service request. Pickup/drop-off, route, schedule. Vehicle type, capacity, luggage, accessibility. Supplier, vehicle, driver, contact. Manifest/group allocation. Multi-leg dispatch. Standby/overtime/toll. Quote, assignment, confirmation, service order, driver sheet. Change, cancellation, incident, no-show." Acceptance: "Transportation role dapat merencanakan dan menutup seluruh service." Section 13/14 mendokumentasikan D-070/D-071 secara eksplisit sebagai preseden arsitektur untuk section ini — audit langsung kode mengonfirmasi kondisi Transportation sebelum section ini identik dengan kondisi Accommodation sebelum Section 14 (`PARTIAL, role-only`: role `transportation` sudah ada sejak Foundation, hanya menggerbangi sub-section transportation generik `ProjectService` di tab "Itinerary & Services"). Berbeda dari Ticketing/Accommodation, Wajib literal Section 15 TIDAK eksplisit menyebut "internal cost isolation" sebagai bullet terpisah — namun hard rule protokol ("Jangan menampilkan internal cost/margin kepada Client") tetap berlaku lintas-section tanpa perlu disebut ulang per section.
**Decision:** (1) `TransportBooking` (baru, `app/types/transportation.ts`) — entitas TERPISAH dari `ProjectService`, `serviceId` OPSIONAL menautkan balik ke baris `ProjectService` (tipe `transportation`) yang sudah ada, pola IDENTIK D-070/D-071 poin 1. (2) **Modul top-level baru** `/transportation` (`ModuleKey` sendiri) MENDAMPINGI ringkasan sub-transportation di tab Project Detail, pola IDENTIK D-070/D-071 poin 2. (3) **Manifest/group allocation** — SENGAJA TIDAK diberi entitas baru: `TransportBooking.groupId` (opsional) menautkan ke `TravelerGroup` (Section 11) existing, `travelerIds` (pola sama `HotelBooking`/`FlightBooking`) menunjuk ke `Traveler` existing — SEKALIGUS berfungsi sebagai indikator "individual vs group" dari acceptance literal (contoh: `TRN-1037` 1 traveler = individual, `TRN-1034`/`1035` banyak traveler = group), TIDAK ada flag/type terpisah. (4) **Vehicle type, capacity, luggage, accessibility**: `TransportOption[]` embedded array (pola sama `HotelOption`/`FlightOption`), `selectTransportOption` toggle single-select. (5) **Pickup/drop-off, route, schedule** + **Multi-leg dispatch**: `TransportLeg[]` embedded array — satu booking dapat memiliki banyak leg (contoh `TRN-1034`, 3 leg: airport-hotel, hotel-venue, hotel-airport). (6) **Supplier, vehicle, driver, contact**: Supplier DIREUSE dari `ProjectService.vendorId`/`Vendor` existing (Section 13 lama) — TIDAK ada entitas Supplier paralel; `assignedVehiclePlateNumber`/`driverName`/`driverPhone` field literal baru, terisi begitu status mencapai `assigned` ke atas (mock, D-006). (7) **Standby/overtime/toll**: `standbyHours`/`overtimeHours`/`tollFeeIdr` field eksplisit terpisah dari rate dasar opsi kendaraan, ditampilkan sebagai badge ringkasan di header detail. (8) **Lifecycle** ("Quote, assignment, confirmation, service order, driver sheet"): `TransportBookingStatus` 7 nilai literal mengikuti urutan tahap literal (`requested`/`quoted`/`assigned`/`confirmed`/`completed`/`cancelled`/`no-show`), peta transisi eksplisit (`TRANSPORT_BOOKING_TRANSITIONS`, pola sama D-070/D-071) via `updateTransportBookingStatus` — alasan WAJIB untuk `cancelled`/`no-show`, dicatat sebagai `ActivityEntry`. (9) **Change, incident**: `hasChange`/`changeNote` (pola sama `FlightBooking.hasScheduleChange`) dan `hasIncident`/`incidentNote` (field BARU, terpisah dari "Change" karena bersifat tidak terduga/di luar rencana — mis. kendaraan mogok) — keduanya flag informasional, BUKAN status lifecycle tambahan. TIDAK ada field "penalty" untuk Transportation (berbeda dari Accommodation D-071) — literal Wajib Section 15 tidak menyebutkannya, sengaja tidak ditambahkan agar tidak mengarang requirement baru. (10) **Service order, driver sheet**: DUA halaman print-friendly terpisah (berbeda dari Ticketing/Accommodation yang hanya satu) — `service-order-preview.vue` (client-facing, SANITIZED, hanya `sellPriceIdr`, pola IDENTIK `eticket-preview.vue`/`voucher-preview.vue`) dan `driver-sheet-preview.vue` (dokumen INTERNAL untuk driver lapangan, TIDAK menampilkan `netCostIdr` MAUPUN `sellPriceIdr` sama sekali — driver tidak butuh informasi harga, hanya route/vehicle/manifest/kontak darurat, pola print-friendly mengikuti `run-sheet-preview.vue` D-069). (11) Fixture ditautkan ke `SVC-1034` (`serviceId`, "Ground Transportation" PRJ-103) dan `groupId` ke `GRP-001`/`002`/`003` — rate kendaraan berada dalam skala yang sama dengan `VendorQuotation` VQ-009/VQ-010 (Rp45–52 juta untuk seluruh armada) TANPA memaksakan kesamaan angka persis (granularitas berbeda, keputusan vendor tetap milik Procurement/Section 13 lama, TIDAK diubah oleh section ini).
**Reason:** Preseden D-070/D-071 dinyatakan eksplisit oleh Section 13/14 sendiri sebagai acuan untuk Section 15–16 — mengulang analisis dari nol akan bertentangan dengan protokol "jangan mengulang pekerjaan yang sudah diputuskan". Dua dokumen terpisah (Service Order vs Driver Sheet) dipilih karena literal Wajib eksplisit menyebut KEDUANYA sebagai item berbeda ("service order, driver sheet") — memaksakan satu dokumen untuk dua audiens berbeda (client vs driver lapangan) akan mengaburkan kebutuhan sanitasi yang berbeda pula (client tetap melihat sell price sebagai konfirmasi harga, driver tidak perlu melihat harga sama sekali). "Incident" sebagai flag terpisah dari "Change" (bukan digabung jadi satu field) mengikuti pembedaan literal eksplisit dalam Wajib ("Change, cancellation, incident, no-show" — empat kata benda berbeda), dan realita operasional: perubahan rencana (change) bersifat terjadwal/diketahui sebelumnya, insiden (incident) bersifat tidak terduga.
**Consequence:** `docs/frontend-known-issues.md` bagian 11 diperbarui — Ticketing/Accommodation/Transportation RESOLVED, MICE (Section 16) tetap `KNOWN_GAP` dengan preseden D-070/D-071/D-072 sebagai acuan. `ProjectService`/`TravelerGroup`/`Vendor` (Foundation/Section 11/13 lama) TIDAK diubah shape-nya — nol risiko regresi. `VendorQuotation` VQ-009/VQ-010 (SVC-1034) TIDAK diubah status-nya (tetap `submitted`, keputusan vendor tetap wewenang Procurement) — `TransportBooking` beroperasi di granularitas berbeda (booking operasional per-group/leg) tanpa mengklaim menyelesaikan keputusan vendor level-project. `TransportBooking` yang `completed`/`cancelled`/`no-show` bersifat terminal (`updateTransportBooking` menolak edit lebih lanjut), pola sama `HotelBooking`/`FlightBooking`.

### D-073 — MICE: `MiceEvent` Entitas Baru Terpisah dari `ProjectService`, Kaya Struktur Internal (Bukan Banyak Baris Fixture); Client Approval Terpisah dari Event Status; Capacity/Schedule Conflict sebagai Advisory Derivation; Dua Dokumen Client-Facing (Rundown dan BOQ, Keduanya Tersanitasi)
**Status:** LOCKED · **Date:** 2026-08-01 · **Affected:** `app/types/mice.ts` (baru), `app/types/user.ts` (`ModuleKey` +`mice`), `app/constants/roles.ts` (`ROLE_MODULE_ACCESS` +kolom `mice` seluruh role), `app/constants/navigation.ts` (+menu "MICE"), `app/constants/status.ts` (+`MICE_EVENT_STATUSES`/`MICE_APPROVAL_STATUSES`/`MICE_BOQ_CATEGORIES`/`MICE_CHECKLIST_TASKS`), `app/data/mice.ts` (baru), `app/data/index.ts` (+selector/mutator MICE Event), `app/pages/mice/**` (baru), `app/pages/projects/[id]/index.vue` (+ringkasan MICE Event di sub-section mice), `app/pages/admin/roles.vue` (+kolom matrix)
**Context:** Section 16 (Wajib): "Brief, venue, rooms/sessions, agenda/rundown. Participant categories. Catering, AV, staging, equipment, booths. Vendor packages dan BOQ. Staffing/PIC. Setup/teardown/rehearsal/permit checklist. Capacity and schedule conflicts. Client approval states. Change order, incident, attendance, deliverables." Acceptance: "MICE role dapat mengelola event dari planning sampai post-event completion." Section 13-15 mendokumentasikan D-070/D-071/D-072 secara eksplisit sebagai preseden arsitektur — audit langsung kode mengonfirmasi kondisi MICE sebelum section ini identik dengan kondisi Transportation sebelum Section 15 (`PARTIAL, role-only`). Berbeda dari Section 13-15 (banyak booking terpisah per traveler/leg), MICE Wajib mendeskripsikan SATU struktur event yang kaya (brief/venue/sessions/BOQ/staffing/checklist/deliverables sekaligus) — satu project MICE realistis hanya punya SATU event utama, bukan banyak booking.
**Decision:** (1) `MiceEvent` (baru, `app/types/mice.ts`) — entitas TERPISAH dari `ProjectService`, `serviceId` OPSIONAL menautkan balik, pola IDENTIK D-070/D-071/D-072 poin 1. Berbeda dari FlightBooking/HotelBooking/TransportBooking, `MiceEvent` TIDAK didesain untuk banyak baris per traveler — strukturnya sendiri (6 array embedded: sessions/participantCategories/boqItems/staffAssignments/checklist/deliverables) yang merepresentasikan skala acara, konsisten realita satu event MICE per project. (2) **Modul top-level baru** `/mice` MENDAMPINGI ringkasan sub-mice di tab Project Detail, pola IDENTIK D-070/D-071/D-072 poin 2. (3) **Rooms/sessions, agenda/rundown**: `MiceSession[]` embedded — `isConfirmed` (field baru per sesi, BUKAN di section 13-15) membedakan sesi yang sudah final vs tentatif, dipakai juga sebagai input "Capacity and schedule conflicts". (4) **Participant categories + Attendance**: `MiceParticipantCategory[]` — `expectedCount`/`actualCount` dalam SATU struct yang sama (bukan dua array terpisah) karena "Attendance" secara literal adalah realisasi dari "Participant categories" yang sama, bukan konsep berdiri sendiri. (5) **Catering, AV, staging, equipment, booths + Vendor packages dan BOQ**: `MiceBoqItem[]` embedded (kategori: catering/av/staging/equipment/booth/other) — `vendorId` opsional DIREUSE dari `Vendor` existing (Section 13 lama, TIDAK ada entitas Supplier paralel), `netCostIdr`/`sellPriceIdr` per baris. Total BOQ (`getMiceBoqTotals`) DERIVASI murni (pola sama `getCostSheetBreakdown`) — BUKAN field tersimpan yang bisa stale. (6) **Staffing/PIC**: `MiceStaffAssignment[]` — `userId` DIREUSE dari `User` existing (Section 02), TIDAK ada entitas staff internal paralel. (7) **Setup/teardown/rehearsal/permit checklist** + **Deliverables**: `MiceChecklistItem[]`/`MiceDeliverable[]` embedded, masing-masing dengan toggle langsung (`toggleMiceChecklistItem`/`toggleMiceDeliverable`) tanpa perlu dialog Edit penuh — checklist/deliverable adalah interaksi tick-list yang wajar dilakukan cepat, berbeda dari array lain (sessions/BOQ/staffing) yang perlu form terstruktur. (8) **Capacity and schedule conflicts**: `getMiceScheduleConflicts` — DERIVASI murni, ADVISORY (TIDAK memblokir transisi status apa pun, pola sama Departure Readiness Gate D-069) — mendeteksi (a) sesi tumpang tindih waktu di room yang sama, (b) kapasitas sesi lebih kecil dari total peserta yang diharapkan. Fixture `MICE-1035` mendemokan kondisi (b) secara nyata (Hari 2 venue alternatif kapasitas 100 < 125 peserta diharapkan) — BUKAN data rekayasa lepas konteks, menautkan ke `RSK-1031`/`TSK-1032` existing (Section 09/12 lama) yang SUDAH mencatat ketidakpastian venue Hari 2 sebelum section ini. (9) **Client approval states**: `MiceApprovalStatus` (`draft`/`submitted`/`approved`/`rejected`) TERPISAH dari `MiceEventStatus` (`planning`/`confirmed`/`in-progress`/`completed`/`cancelled`), pola sama `QuotationApprovalStatus` terpisah dari `OpportunityStage` (D-049) — `rejected` dapat direvisi-dan-diajukan-ulang (`submitted`), `approved` bersifat stabil (perubahan lanjutan lewat "Change order", BUKAN re-approval/`submitted` ulang). Approval direkam oleh role `mice`/Management internal (representasi keputusan client), TIDAK menyentuh Client Portal (`app/pages/client/**`) — di luar scope literal section ini, menjaga perubahan lintas-section minimal. (10) **Change order, incident**: `hasChangeOrder`/`changeOrderNote` dan `hasIncident`/`incidentNote` — pola IDENTIK `TransportBooking.hasChange`/`hasIncident` (D-072), dua flag informasional terpisah (BUKAN status lifecycle tambahan), sesuai pembedaan literal eksplisit "Change order, incident, attendance, deliverables" (empat konsep berbeda). (11) **Dua dokumen client-facing**: `rundown-preview.vue` (agenda/sessions/venue/brief, untuk review client) dan `boq-preview.vue` (breakdown BOQ per baris + total, untuk budget sign-off client) — BERBEDA dari pola Transportation (1 client-facing + 1 internal): MICE TIDAK punya dokumen internal-only setara "driver sheet" karena literal Wajib Section 16 tidak menyebut audiens internal terpisah untuk dokumen manapun; KEDUANYA disanitasi (HANYA `sellPriceIdr`, `netCostIdr` TIDAK PERNAH tampil di keduanya — bahkan `rundown-preview.vue` sengaja tidak menampilkan angka apa pun, bukan hanya net cost, karena rundown secara literal adalah dokumen agenda bukan dokumen finansial).
**Reason:** Preseden D-070/D-071/D-072 dinyatakan eksplisit sebagai acuan untuk Section 16 — mengulang analisis dari nol akan bertentangan dengan protokol "jangan mengulang pekerjaan yang sudah diputuskan". Satu event kaya struktur (bukan banyak booking) dipilih karena realita bisnis MICE: satu project biasanya punya satu acara utama dengan banyak dimensi (sessions/BOQ/staffing/checklist), bukan banyak "booking" independen seperti flight/hotel/transport — memaksakan pola "banyak baris" akan menciptakan struktur yang tidak natural untuk domain ini. `expectedCount`/`actualCount` digabung dalam satu struct (bukan dua array) karena Attendance secara harfiah adalah pengisian ulang field yang sama pasca-sesi, bukan entitas terpisah — memisahkannya akan menciptakan risiko dua sumber kebenaran untuk kategori peserta yang sama. Capacity conflict ditautkan ke risk/task existing (bukan data baru lepas konteks) mengikuti prinsip "jangan membuat skenario demo yang tidak berhubungan dengan data yang sudah ada".
**Consequence:** `docs/frontend-known-issues.md` bagian 11 diperbarui — Ticketing/Accommodation/Transportation/MICE seluruhnya RESOLVED, TIDAK ADA lagi sub-domain operasional (Section 13-16) yang `KNOWN_GAP` role-only. `ProjectService`/`TravelerGroup`/`Vendor`/`User` (Foundation/Section 02/11/13 lama) TIDAK diubah shape-nya — nol risiko regresi. `RSK-1031`/`TSK-1032`/`TSK-1033`/`SFT-1032` (Section 09/12 lama) TIDAK diubah — hanya dirujuk secara naratif dari `MiceEvent.changeOrderNote`. `MiceEvent` yang `completed`/`cancelled` bersifat terminal (`updateMiceEvent` menolak edit lebih lanjut), pola sama section 13-15. Client Portal (`app/pages/client/**`) TIDAK disentuh — "Client approval states" direkam internal, BUKAN alur approval self-service client; ini dicatat eksplisit sebagai keputusan scope, bukan gap tersembunyi.

### D-074 — Procurement: Modul Top-Level Baru `/procurement` Mendampingi `/vendors` (Master Data) dan `/supplier` (Self-Service); `RFQ`/`ServiceOrder`/`SupplierInvoice` Entitas Baru Mereuse `Vendor` by ID; Cost Isolation Konsisten Preseden D-070/D-071/D-072/D-073
**Status:** LOCKED · **Date:** 2026-08-01 · **Affected:** `app/types/vendor.ts` (`Vendor` +`category`/`status`/`documents`, `+VendorDocument`, `VendorDetailTab` +`documents`), `app/types/procurement.ts` (baru), `app/types/user.ts` (`ModuleKey` +`procurement`), `app/constants/roles.ts` (`ROLE_MODULE_ACCESS` +kolom `procurement` seluruh role), `app/constants/navigation.ts` (+menu "Procurement", +2 child Supplier Portal), `app/constants/status.ts` (+`VENDOR_STATUSES`/`RFQ_STATUSES`/`SERVICE_ORDER_STATUSES`/`SUPPLIER_INVOICE_STATUSES`), `app/data/vendors.ts` (+`VENDOR_DOCUMENTS`, backfill `category`/`status`), `app/data/procurement.ts` (baru), `app/data/index.ts` (+selector/mutator RFQ/Service Order/Supplier Invoice/Vendor Document, +`getVendorProcurementPerformance`), `app/pages/vendors/**` (+kolom kategori/status, +tab Documents), `app/pages/procurement/**` (baru), `app/pages/supplier/rfq/**`/`app/pages/supplier/service-orders/**` (baru), `app/pages/projects/[id]/index.vue` (+ringkasan RFQ/Service Order), `app/pages/admin/roles.vue` (+kolom matrix)
**Context:** Section 17 (Wajib): "Supplier companies, contacts, categories, documents, status. Catalog/services/rates. RFQ, supplier response, comparison, clarification, selection. Service Order, amendment, acknowledgment, fulfillment. Supplier dashboard dan portal. Supplier quotation, documents, fulfillment updates, invoice submission preview. PT ABC dan PT EFG memiliki produk berbeda. Supplier isolation berdasarkan company. Procurement performance review." Acceptance: "Procurement dan Supplier dapat menjalankan sourcing sampai fulfillment handoff." Audit Section 00/`docs/frontend-known-issues.md` bagian 12 mencatat kondisi sebelum section ini: role `procurement` dan `vendor: MANAGE` sudah ada sejak Section 02 (`PARTIAL`), tapi RFQ formal/comparison/clarification/selection dan Service Order/amendment/acknowledgment/fulfillment sama sekali belum ada — hanya `submitVendorQuotation`/`acceptVendorQuotation`/`rejectVendorQuotation` (submit-accept-reject sederhana, Section 13 lama) yang tersedia. `docs/frontend-known-issues.md` bagian 11 (ditulis Section 16) secara eksplisit menunjuk preseden D-070/D-071/D-072/D-073 sebagai acuan bila Section 17 membutuhkan pola serupa.
**Decision:** (1) **Tiga area terpisah, bukan satu modul digabung**: `/vendors` (Vendor Directory, master data, Section 13 lama, DIPERLUAS aditif dengan `category`/`status`/`documents` — BUKAN diganti), `/supplier` (Supplier Portal, self-service eksternal, Prompt 19, DIPERLUAS dengan RFQ Inbox/Service Order Inbox/Invoice Submission), dan `/procurement` (modul top-level BARU, home role Procurement, RFQ/Service Order/Performance Review) — pola arsitektur "modul top-level baru MENDAMPINGI struktur existing" IDENTIK D-070/D-071/D-072/D-073, TIDAK meninjau ulang D-020 (sudah final sejak Section 13). (2) **`RFQ`/`RFQInvitation`/`RFQResponse`/`RFQClarificationMessage`/`ServiceOrder`/`ServiceOrderAmendment`/`SupplierInvoice`** (baru, `app/types/procurement.ts`) — seluruhnya mereuse `Vendor.id` (bukan entitas Supplier paralel), `VendorQuotation`/`submitVendorQuotation`/`acceptVendorQuotation`/`rejectVendorQuotation` (Section 13 lama) TIDAK diubah/dihapus/digantikan — RFQ formal adalah jalur TAMBAHAN untuk sourcing terstruktur multi-tahap, quotation submit-accept-reject sederhana tetap berlaku untuk assignment cepat per-service (dua jalur, kebutuhan berbeda, konsisten "jangan mengulang atau mengganti hasil selesai tanpa kebutuhan integrasi"). (3) **RFQ lifecycle** (`draft`→`sent`→`responses-in`→`comparison`⇄`clarification`→`selected`→`closed`) — transisi `sent`→`responses-in` OTOMATIS lewat `submitRfqResponse` (bukan tombol manual), transisi lain via `getRfqStatusTransitions`/`moveRfqStatus` (pola sama `FLIGHT_BOOKING_TRANSITIONS`/`PROJECT_STATUS_TRANSITIONS`, D-066/D-070). "Formal Select action" (`selectRfqVendor`) menandai response pemenang `selected`, response lain otomatis `rejected` (satu vendor per RFQ, pola sama `acceptVendorQuotation`). (4) **Comparison table**: DERIVASI murni dari `RFQResponse[]` (diurutkan harga terendah, `getRfqResponses`) — bukan komponen baru, table existing (`Table*`) sudah cukup untuk side-by-side. (5) **Clarification thread**: `RFQClarificationMessage` per (`rfqId`, `vendorId`) — dua arah (`from: 'procurement' | 'supplier'`), pesan dari procurement memajukan status RFQ ke `clarification` bila sebelumnya `responses-in`/`comparison`. (6) **Service Order lifecycle** (`draft`→`sent`→`acknowledged`⇄`amended`→`fulfilled`/`cancelled`) — `amended` BUKAN status terminal (booking bisa diamandemen berkali-kali sebelum fulfilled, pola sama `HotelBookingStatus.amended`, D-071), `ServiceOrderAmendment` append-only (pola sama seluruh amendment/histori section 13-16). Handoff RFQ→Service Order via tombol "Buat Service Order" di RFQ detail (hanya saat status `selected`/`closed`, sekali per RFQ) — line items dan harga di-seed dari RFQ/response pemenang, BUKAN duplikasi manual. (7) **Supplier Invoice** (preview/mock murni, resolusi Q12) — hanya dapat diajukan terhadap Service Order `fulfilled` milik vendor yang sama (guard isolasi), status `submitted`→`under-review`→`approved`/`rejected`, TIDAK ADA payment gateway/processing nyata (larangan protokol eksplisit) — `reviewSupplierInvoice` murni mengubah status+catatan, tidak pernah memicu "pembayaran". (8) **Internal cost isolation**: `ServiceOrder.netCostIdr`/`sellPriceIdr` terpisah, visibilitas digerbangi `canManageProcurement || canViewFinancials` (pola IDENTIK `canViewMiceFinancials` dkk.) — Supplier Portal (`/supplier/service-orders/[id]`) HANYA menampilkan `sellPriceIdr`, `netCostIdr` TIDAK PERNAH dikirim/dirender di halaman tsb (bukan hanya disembunyikan CSS — computed halaman supplier tidak pernah membaca field itu). (9) **Vendor Directory diperluas**: `Vendor.category` (label bebas granular, opsional), `Vendor.status` (`active`/`inactive`/`pending`, default `active` untuk vendor existing — regression-safe), `VendorDocument[]` (preview mock, tab "Documents" baru di Vendor Detail, 5 tab→6 tab) — seluruh field ADITIF, tab lain (Overview/Services/Quotations/Products/Contacts) TIDAK disentuh. (10) **Procurement Performance Review**: `getVendorProcurementPerformance` — DERIVASI murni dari `RFQ`/`RFQInvitation`/`RFQResponse`/`ServiceOrder` existing (pola sama `getCostSheetBreakdown`/`getMiceBoqTotals`), BUKAN field tersimpan. "On-time %" DISEDERHANAKAN sebagai rasio Service Order `fulfilled` terhadap total Service Order vendor (tidak ada field due-date terpisah untuk dibandingkan — keputusan didokumentasikan, bukan gap tersembunyi/fabrikasi mesin bisnis). (11) Fixture: `VND-006` (PT ABC) dan `VND-007` (PT EFG) dipakai luas sebagai vendor RFQ dengan kategori/produk berbeda (melanjutkan `VENDOR_PRODUCTS` Prompt 19 — "PT ABC dan PT EFG memiliki produk berbeda" TERPENUHI sejak Prompt 19, DILANJUTKAN bukan diulang di section ini) — RFQ-003 mendemokan clarification thread aktif dengan PT ABC; RFQ-004 SENGAJA TIDAK menautkan `serviceId` ke `SVC-1034` (Ground Transportation PRJ-103, tetap milik skenario comparison VQ-009/VQ-010 Section 13 lama yang masih `pending-confirmation`) — kebutuhan VIP transfer terpisah, menghindari dua mekanisme keputusan vendor yang bertentangan untuk baris service yang sama.
**Reason:** Preseden D-070/D-071/D-072/D-073 dinyatakan eksplisit sebagai acuan (`docs/frontend-known-issues.md` bagian 11) — mengulang analisis arsitektur dari nol akan bertentangan dengan protokol "jangan mengulang pekerjaan yang sudah diputuskan". Tiga area terpisah (bukan digabung ke satu modul) dipilih karena masing-masing punya audiens dan siklus hidup berbeda: `/vendors` adalah master data yang jarang berubah, `/supplier` adalah akses self-service eksternal ter-isolasi, `/procurement` adalah workspace transaksional lintas-project untuk role Procurement — memaksakan ketiganya jadi satu modul akan mencampur konsep direktori/portal/workflow yang secara semantik berbeda. RFQ formal sebagai jalur TAMBAHAN (bukan pengganti) `VendorQuotation` submit-accept-reject dipilih karena keduanya melayani skenario nyata berbeda: RFQ untuk sourcing kompetitif multi-vendor terstruktur (butuh perbandingan formal), quotation sederhana untuk assignment cepat satu-vendor-satu-service (sudah teruji sejak Section 13, tidak boleh rusak). Guard isolasi ketat pada `submitSupplierInvoice` (harus `fulfilled` + vendor yang sama) mencegah supplier mengajukan invoice palsu atas nama vendor lain atau sebelum layanan benar-benar selesai — konsisten hard rule "Client dan Supplier hanya boleh melihat data company mereka".
**Consequence:** `docs/frontend-known-issues.md` bagian 12 diperbarui — RFQ formal/comparison/clarification/selection dan Service Order/amendment/acknowledgment/fulfillment RESOLVED; Q12 (`docs/mockup-open-questions.md`) RESOLVED (invoice submission self-service kini ada, meski scope-nya "preview" bukan payment nyata, sesuai batasan protokol). `ProjectService`/`VendorQuotation`/`Vendor` (Foundation/Section 13 lama) TIDAK diubah shape secara destruktif — `Vendor` diperluas aditif, `VendorQuotation` tidak disentuh sama sekali. `ServiceOrder` yang `fulfilled`/`cancelled` bersifat terminal (`updateServiceOrder` menolak edit lebih lanjut) — booking pengganti dibuat sebagai `ServiceOrder` baru terpisah, pola sama `HotelBooking`/`FlightBooking`/`TransportBooking`/`MiceEvent` (D-070/D-071/D-072/D-073). Client Portal (`app/pages/client/**`) TIDAK disentuh — Procurement/RFQ/Service Order murni internal↔supplier, di luar scope literal section ini.

### D-075 — Booking dan Service Orders: Consolidation/Orchestration LAYER Fully Additive di Atas Flight/Hotel/Transport/MICE (Section 13-16); `BookingOrchestrationRecord`/`BookingTimelineEntry` Terpisah dari Empat Entitas Booking dan dari `ServiceOrder` Procurement (Section 17)
**Status:** LOCKED · **Date:** 2026-08-01 · **Affected:** `app/types/booking-orchestration.ts` (baru), `app/data/booking-orchestration.ts` (baru), `app/types/user.ts` (`ModuleKey` +`bookings`), `app/constants/roles.ts` (`ROLE_MODULE_ACCESS` +kolom `bookings` seluruh role), `app/constants/navigation.ts` (+menu "Booking & Service Order Center"), `app/constants/status.ts` (+`BOOKING_PAYMENT_GATE_STATUSES`/`BOOKING_ATTEMPT_OUTCOMES`), `app/data/index.ts` (+selector/mutator Booking Orchestration lengkap, +hook payment gate di 4 fungsi `update*BookingStatus`/`updateMiceEventStatus`), `app/pages/bookings/**` (baru), `app/pages/ticketing/index.vue`/`app/pages/accommodation/index.vue`/`app/pages/transportation/index.vue` (+duplicate booking prevention), `app/pages/projects/[id]/index.vue` (refactor 4 blok ringkasan booking jadi 1 SectionCard "Booking Timeline"), `app/pages/admin/roles.vue` (+kolom matrix)
**Context:** Section 18 (Wajib): "Semua Flight/Hotel/Transport/MICE service requirement dalam satu timeline. Booking references, holds, confirmations, deadlines, vouchers. Internal/supplier/client-visible status mapping. Split supplier. Dependency antar-service. Confirmation and payment gates. Failure/retry/manual fallback simulation. Duplicate booking prevention. Exception list." Acceptance: "Operations memiliki satu sumber kebenaran seluruh service." `docs/frontend-known-issues.md` bagian 13 (ditulis Section 17) mencatat disambiguasi eksplisit yang WAJIB dipatuhi: `ServiceOrder` (`app/types/procurement.ts`, Section 17) adalah dokumen formal Procurement→vendor, BUKAN "Service Order konsolidasi lintas jenis layanan" yang dimaksud gap ini. Section 13-16 (D-070/D-071/D-072/D-073) sudah membangun lifecycle detail LENGKAP per domain (`FlightBooking`/`HotelBooking`/`TransportBooking`/`MiceEvent`) — Section 18 TIDAK mengulang pekerjaan itu, murni menambah lapisan konsolidasi di atasnya.
**Decision:** (1) **Fully additive, bukan restrukturisasi**: `BookingOrchestrationRecord` (baru, `app/types/booking-orchestration.ts`) — satu record per booking existing, menautkan `bookingType`+`bookingId` ke salah satu dari 4 entitas booking Section 13-16 via ID (pola sama `CostSheet.opportunityId`/`FlightBooking.serviceId` opsional, D-067/D-070) — TIDAK ADA field baru ditambahkan ke `FlightBooking`/`HotelBooking`/`TransportBooking`/`MiceEvent`, dan `createFlightBooking`/`createHotelBooking`/`createTransportBooking`/`createMiceEvent` (LOCKED, `docs/frontend-implementation-roadmap.md`) TIDAK disentuh sama sekali. (2) **Nama sengaja dijauhkan dari "Service Order"**: entitas/istilah di sini adalah "Booking Orchestration"/"Booking Timeline" (bukan "Service Order" apa pun), label nav "Booking & Service Order Center" adalah istilah UI-only untuk menjembatani bahasa Wajib literal Section 18 tanpa membuat tipe baru bernama serupa `ServiceOrder` Section 17 — resolusi eksplisit terhadap catatan disambiguasi `docs/frontend-known-issues.md` bagian 13. (3) **`BookingTimelineEntry` sebagai DERIVED view-model**, dihitung `getBookingTimeline` (`app/data/index.ts`), pola IDENTIK `getServiceReadinessMatrix`/`getDepartureReadiness`/`getProjectAttentionQueue` (Section 12 baru) — BUKAN entitas tersimpan, tidak bisa stale. (4) **Internal/supplier/client-visible status mapping**: TIGA field terpisah (`internalStatus` = label penuh vocabulary status existing per domain, `supplierVisibleStatus`/`clientVisibleStatus` = bucket 4-kategori "Diproses/Dikonfirmasi/Selesai/Dibatalkan" [client] dan "Menunggu Aksi/Dalam Pengerjaan/Terpenuhi/Dibatalkan" [supplier] yang disederhanakan dari vocabulary status YANG SAMA, bukan enum baru) — sesuai instruksi literal "reusing each domain's existing status vocabulary". Supplier di sini bersifat SIMULATIF (keempat domain booking tidak punya Supplier Portal sendiri — vendor-facing sesungguhnya tetap `ServiceOrder` Procurement Section 17) — bucket ini murni pelabelan konsolidasi untuk operasional internal, TIDAK mengekspos halaman baru ke role `supplier`. (5) **Dependency**: `BookingOrchestrationRecord.dependsOn` (opsional) — cross-domain, dinilai "satisfied" bila booking dependency sudah confirmed-equivalent (`BOOKING_CONFIRMED_STATUSES` per domain); tidak terpenuhi → masuk `exceptions`. (6) **Payment gate**: `paymentGateStatus` (`not-required`/`pending`/`cleared`) bergerak otomatis `not-required`→`pending` saat booking mencapai status confirmed-equivalent (hook di 4 fungsi `update*Status`), aksi "Mark Payment Cleared" (Operations/Finance-facing, `/bookings`) MURNI mengubah flag mock — TIDAK menyentuh `app/data/finance.ts`/`Invoice`/`Payment` (Section 20 baru masih PARTIAL, larangan protokol "jangan mengarang integrasi/mesin bisnis nyata"). (7) **Failure/retry/manual fallback**: `BookingAttempt[]` narasi/log murni (D-006) — diseed (FLT-1021: gagal→manual fallback) dan dapat ditambah lewat `appendBookingAttempt` dari `/bookings`. (8) **Duplicate booking prevention**: guard di level UI (`findActiveBookingConflicts`, dipanggil SEBELUM `createFlightBooking`/`createHotelBooking`/`createTransportBooking` di 3 halaman create — BUKAN di dalam fungsi create itu sendiri, agar signature Section 13-15 tetap 100% tidak berubah) — dialog konfirmasi eksplisit wajib sebelum lanjut, `flagBookingOrchestrationDuplicate` mencatat `ActivityEntry` + `flaggedDuplicate: true` pada record (masuk `exceptions`). MICE SENGAJA dikecualikan (satu event per project, duplikasi tidak relevan secara semantik, sesuai literal Wajib). (9) **Exception list**: `BookingTimelineEntry.exceptions` mengagregasi flag domain existing (`hasScheduleChange`/status `amended` [hotel, tidak punya field `hasChange` terpisah]/`hasChange`+`hasIncident`/`hasChangeOrder`+`hasIncident`+`getMiceScheduleConflicts` [reuse derivasi Section 16]) DITAMBAH kondisi baru orchestration (dependency belum terpenuhi, percobaan terakhir gagal, duplicate flag) — `getBookingExceptionQueue()` memfilter seluruh timeline dengan `exceptions.length > 0`. (10) **Refactor tab Project Detail**: 4 blok ringkasan terpisah (Flight/Hotel/Transport/MICE Bookings, Section 13-16) DIGANTI 1 SectionCard "Booking Timeline" (`getBookingTimeline(projectId)`, pre-filtered) — tombol "Buat X Booking" quick-create per tipe TETAP ada (dipindah ke footer masing-masing `SectionCard` per tipe layanan di dalam loop existing), bukan dihapus. (11) **Modul `bookings`**: `operations` `MANAGE` (pemilik "satu sumber kebenaran seluruh service", acceptance literal), `project-manager`/`management`/`finance`/`viewer` `VIEW`, role lain (termasuk `ticketing`/`accommodation`/`transportation`/`mice` — TETAP mengelola modul masing-masing langsung, bukan lewat konsolidasi ini) `NONE`. Net cost pada `/bookings` digerbangi `canViewFinancials` MURNI (bukan `canManage('bookings')`) — Operations TIDAK otomatis melihat net cost lintas domain yang bukan wewenangnya, konsisten pola D-070/D-071/D-072/D-073 tiap domain asal.
**Reason:** Consolidation layer aditif (bukan menggabung/memindahkan 4 entitas booking jadi satu tabel besar) dipilih karena hard rule "jangan membuat dataset paralel" berlaku DUA ARAH — memaksa 4 model yang sudah punya lifecycle detail sangat berbeda (fare/PNR vs room block vs multi-leg dispatch vs BOQ) menjadi satu struktur seragam akan mencemari model-model itu (preseden D-070 poin "Reason", diperluas eksplisit ke Section 18 sebagai turunan/extension pertama dari pola tsb yang bersifat cross-domain, bukan single-domain). Nama "Booking Orchestration"/"Booking Timeline" (bukan "Service Order") dipilih SEMATA untuk mematuhi instruksi eksplisit `docs/frontend-known-issues.md` bagian 13 (yang secara eksplisit sudah mewanti-wanti risiko tabrakan nama sebelum section ini dikerjakan) — bukan sekadar preferensi gaya penamaan. Duplicate-check dilakukan di level pemanggilan (bukan di dalam fungsi create) secara sengaja mengikuti instruksi literal brief section ini ("Do NOT touch/replace the existing create-booking flows... beyond the minimal addition described") sekaligus menjaga LOCKED protection notes Section 13-15 (`docs/frontend-implementation-roadmap.md`) yang eksplisit melarang perubahan signature/behavior fungsi create tsb tanpa keputusan baru.
**Consequence:** `docs/frontend-known-issues.md` bagian 13 diperbarui — gap Section 18 RESOLVED, catatan disambiguasi dipertahankan (bukan dihapus) karena tetap relevan untuk section berikutnya. `FlightBooking`/`HotelBooking`/`TransportBooking`/`MiceEvent`/`ProjectService` (Section 13-16/Foundation) TIDAK diubah shape-nya — nol risiko regresi. Ini adalah preseden ARSITEKTUR PERTAMA untuk "consolidation/orchestration layer lintas-domain" (berbeda dari D-070/D-071/D-072/D-073 yang seluruhnya "modul top-level baru MENDAMPINGI tab" untuk SATU domain) — bila section berikutnya (mis. Section 19 Changes/Cancellation/Refund/Incident) butuh pola serupa (agregasi lintas beberapa entitas existing tanpa menduplikasi data), preseden `getBookingTimeline`/`BookingOrchestrationRecord` dapat dijadikan acuan.

### D-076 — Changes, Cancellation, Refund dan Incident: `ChangeRequest`/`CancellationRecord`/`RefundRequest`/`Incident` Fully Additive di Atas Section 13-18 dan `ActivityEntry` (Section 14 Lama); Refund Tetap Self-Contained (Bukan Menyentuh `Invoice`/`Payment`); "Versioned History" = Append-Only Log
**Status:** LOCKED · **Date:** 2026-08-01 · **Affected:** `app/types/change-incident.ts` (baru), `app/data/change-incident.ts` (baru), `app/data/activity.ts` (+3 `ActivityEntry` baru `CHG-1032`/`1033`/`1034`, aditif), `app/types/user.ts` (`ModuleKey` +`changes`), `app/constants/roles.ts` (`ROLE_MODULE_ACCESS` +kolom `changes` seluruh role), `app/constants/navigation.ts` (+menu "Changes & Incidents"), `app/constants/status.ts` (+`CHANGE_REQUEST_SOURCES`/`CHANGE_REQUEST_STATUSES`/`REFUND_REQUEST_STATUSES`/`REFUND_CREDIT_STATUSES`/`INCIDENT_SEVERITIES`/`INCIDENT_STATUSES`), `app/data/index.ts` (+selector/mutator Changes/Cancellation/Refund/Incident lengkap, +`getQuotationById`), `app/pages/changes/**` (baru), `app/pages/ticketing/[id]/index.vue`/`app/pages/accommodation/[id]/index.vue`/`app/pages/transportation/[id]/index.vue`/`app/pages/mice/[id]/index.vue` (+hook `createCancellationRecord` pada dialog status cancel-equivalent), `app/pages/projects/[id]/index.vue` (+4 SectionCard baru di tab Activity & Changes), `app/pages/client/project-orders/[id]/index.vue` (tab "Change Request" → "Changes & Incidents", diperkaya sanitized Incident, `submitChangeRequest` beralih ke `createChangeRequest`), `app/pages/admin/roles.vue` (+kolom matrix).
**Context:** Section 19 (Wajib): "Change request dari Client/Internal/Supplier. Before/after dan affected entities. Operational, commercial, financial, timeline impact. Approval states. Additional quotation/change order. Cancellation and penalty. Refund request, approval, partial/full, credit status mock. Incident severity, owner, escalation, communication, resolution. Versioned itinerary/order history. Client-safe visibility." Acceptance: "High-change project dapat diselesaikan tanpa menghapus histori." Audit kode mengonfirmasi kondisi sebelum section ini: tab "Activity & Changes" (`ActivityEntry` + `ChangeCategory`/`ChangeApprovalStatus`, Section 14 lama) menangani "Change" generik dengan baik, tapi TIDAK ADA kategori/field khusus Cancellation (penalty seragam lintas domain), Refund (partial/full/credit status), atau Incident (severity/escalation/communication) — `docs/frontend-known-issues.md` bagian 14 mencatat gap ini eksplisit. D-075 (Section 18) mendokumentasikan preseden "consolidation/orchestration layer lintas-domain" (`BookingOrchestrationRecord`/`getBookingTimeline`) sebagai acuan eksplisit untuk section yang butuh pola serupa — Section 19 adalah turunan PERTAMA dari preseden itu.
**Decision:** (1) **Fully additive, empat entitas baru** (`app/types/change-incident.ts`): `ChangeRequest` (before/after summary, `affectedEntities: AffectedEntityRef[]` menautkan ID lintas-domain [flight/hotel/transport/mice/itinerary/order/project], dampak operasional/komersial/finansial/timeline, `source: 'client'|'internal'|'supplier'`, status 5-nilai `submitted→under-review→approved/rejected`, `approved→implemented`, `linkedQuotationId` opsional untuk "additional quotation/change order"), `CancellationRecord` (penalty-tracking SERAGAM lintas 4 domain booking — hari ini hanya `HotelBooking` punya field penalty sendiri, D-071), `RefundRequest` (partial/full, status 5-nilai `requested→under-review→approved/rejected`, `approved→processed`, `creditStatus` field MOCK `pending`/`issued`/`not-applicable`), `Incident` (severity 4-nilai, `ownerId`, `escalatedTo` opsional, `communicationLog: IncidentCommunicationEntry[]`, `resolutionNote`+`resolvedAt`). Pola arsitektur IDENTIK preseden D-070 s.d. D-075 poin 1: entitas baru menautkan ID ke entitas existing, TIDAK PERNAH memutasinya — ZERO field baru di `FlightBooking`/`HotelBooking`/`TransportBooking`/`MiceEvent`/`Invoice`/`Payment`. (2) **`ChangeRequest` TIDAK menggantikan `ActivityEntry`** (Section 14 lama, LOCKED sebagai audit trail tunggal, `docs/mockup-information-architecture.md` bagian 4) — `createChangeRequest` (`app/data/index.ts`) SELALU memanggil `createChangeEntry` existing dan menyimpan hasilnya di `ChangeRequest.activityEntryId`; `approveChangeRequest`/`rejectChangeRequest` SELALU memanggil `approveChangeEntry`/`rejectChangeEntry` existing pada entry yang ditaut. `ChangeRequest` adalah lapisan STRUKTUR TAMBAHAN (before/after terstruktur, affected entities, dampak 4-dimensi, additional quotation link) di atas audit trail yang sudah ada — bukan audit trail kedua yang bisa berbeda dari yang pertama. (3) **"Versioned itinerary/order history"** (Wajib) dipenuhi lewat sifat APPEND-ONLY `ChangeRequest` — setiap `ChangeRequest` dibuat baru dan tidak pernah diedit/dihapus setelah dibuat (hanya `status` yang berpindah lewat transition map eksplisit `CHANGE_REQUEST_TRANSITIONS`), diorder per `projectId`/`submittedAt` sebagai "Change History" — BUKAN entitas versioning baru (mis. `ChangeRequestVersion`) yang akan menciptakan model paralel untuk konsep yang sudah cukup direpresentasikan oleh list kronologis. (4) **Cancellation and penalty**: `createCancellationRecord` dipanggil dari HOOK UI-level di 4 halaman detail booking (`app/pages/ticketing/[id]/index.vue` dkk.) — SETELAH `update*BookingStatus` berhasil bertransisi ke status cancel-equivalent (`cancelled`/`refunded`/`no-show`), dialog status existing (mandatory-reason, Section 13-16, LOCKED) diperkaya field opsional "Penalty (Rp)" + checkbox "Refund Eligible" HANYA saat status target adalah cancel-equivalent — guard/transition-map/reason-wajib pada `update*BookingStatus` itu sendiri TIDAK disentuh. (5) **Refund request, approval, partial/full, credit status mock**: `RefundRequest.creditStatus` SENGAJA field MOCK SELF-CONTAINED — `updateRefundRequestStatus` mengubah `creditStatus` menjadi `issued` saat status `processed` dan `not-applicable` saat `rejected`, TIDAK PERNAH menyentuh `app/types/finance.ts`/`Invoice`/`Payment` (Section 20 baru masih PARTIAL, larangan protokol "jangan mengarang integrasi/mesin bisnis nyata") — `invoiceId`/`cancellationId` adalah referensi ID READ-ONLY murni untuk konteks tampilan. (6) **Incident severity, owner, escalation, communication, resolution**: `escalateIncident` (status→`escalated`, set `escalatedTo`, tambah entri `communicationLog` otomatis), `appendIncidentCommunication` (murni log, tidak mengubah status), `resolveIncident` (resolution note WAJIB, status→`resolved`), `updateIncidentStatus` (transisi umum lain, `INCIDENT_TRANSITIONS` eksplisit). (7) **Client-safe visibility**: `/client/project-orders/[id]` tab "Changes & Incidents" (rename dari "Change Request") menampilkan `ChangeRequest` HANYA `status`+`beforeSummary`/`afterSummary` (TIDAK PERNAH `operationalImpact`/`commercialImpactIdr`/`financialImpactNote`, bahkan untuk request `source: 'client'` milik Client itu sendiri — konsisten instruksi literal "even then keep numbers sanitized") dan `Incident` HANYA `status`+`resolutionNote` (TIDAK PERNAH `severity`/`escalatedTo`/`communicationLog`) — pola sanitasi IDENTIK D-065/D-067/D-070 dkk. (client hanya `sellPriceIdr`, tidak pernah `netCostIdr`). `submitChangeRequest` (form existing Client Portal) dialihkan dari `createChangeEntry` langsung menjadi `createChangeRequest` (source `'client'`) — TIDAK ADA regresi fungsional, hasil akhirnya tetap satu `ActivityEntry` yang sama seperti sebelumnya, kini plus satu `ChangeRequest` terstruktur. (8) **Modul baru `/changes`** (`ModuleKey` `changes`) — list 4-tab (query-param, pola sama `/procurement`), detail 4 route (`/changes/[id]` untuk `ChangeRequest`, `/changes/cancellations/[id]`, `/changes/refunds/[id]`, `/changes/incidents/[id]`) — pola sub-route mengikuti preseden `/procurement/rfq/[id]` vs `/procurement/service-orders/[id]` (Section 17) dan `/bookings` vs `/bookings/exceptions` (Section 18). `operations`/`project-manager` `MANAGE` (pemilik operasional harian), `management`/`finance`/`viewer` `VIEW` — approval komersial (`ChangeRequest`/`RefundRequest` approve/reject) TETAP digerbangi `canApprove('project')` existing (rank Management pada modul `project`, BUKAN rank baru pada modul `changes`), pola IDENTIK `canApproveChanges` Section 14 lama — konsisten hard rule protokol "Hanya Management yang melakukan commercial approval" tanpa menciptakan mekanisme approval kedua. (9) Fixture (`app/data/change-incident.ts`) ditautkan ke data existing: `ChangeRequest` menautkan 3 `activityEntryId` ke `CHG-1021`/`1023`/`1031` existing (narasi identik) dan 3 `ActivityEntry` BARU (`CHG-1032`/`1033`/`1034`, ditambahkan aditif ke `app/data/activity.ts` — entri existing TIDAK diubah) untuk `ChangeRequest` yang tidak punya padanan lama; `CancellationRecord` menautkan booking yang statusnya SUDAH `cancelled`/`refunded` (`FLT-1023`/`HTL-1023`/`TRN-1037`); `RefundRequest` menautkan `CancellationRecord` di atas + `Invoice` existing (`INV-1021`/`1022`/`1011`/`1032`, read-only); `Incident` mendemokan seluruh 4 severity, satu `escalated` (ke Operations `USR-009`) dengan `communicationLog` multi-entri, satu project-level tanpa `bookingId` (peringatan cuaca ekstrem Palu, severity `critical`).
**Reason:** Fully additive (bukan menggabungkan ke `ActivityEntry`/entitas booking existing) dipilih karena hard rule "jangan membuat dataset paralel" berlaku DUA ARAH (preseden D-070/D-075) — memaksakan field Cancellation/Refund/Incident yang kaya (penalty/credit-status/severity-escalation-communication) ke `ActivityEntry` generik akan mencemari model log yang dipakai SELURUH kategori perubahan (traveler/itinerary/service/vendor/budget/other), sementara membangun `ChangeRequest` sebagai audit trail KEDUA yang terpisah dari `ActivityEntry` akan melanggar LOCKED "satu sumber kebenaran" (`docs/mockup-information-architecture.md` bagian 4) — solusinya adalah `ChangeRequest` sebagai lapisan struktur TAMBAHAN yang WAJIB menaut balik ke `ActivityEntry`, bukan salah satu dari dua ekstrem tsb. Refund tetap self-contained (bukan menyentuh `Invoice`/`Payment`) karena Section 20 (Project Finance) masih berstatus PARTIAL saat section ini dikerjakan — mengintegrasikan `creditStatus` ke `Invoice.status` nyata akan mengarang mesin bisnis finansial (kapan credit note benar-benar mengurangi outstanding invoice, bagaimana partial refund memecah invoice) yang literal Wajib-nya adalah domain Section 20, melanggar hard rule "jangan mengarang integrasi/mesin bisnis nyata" (protokol) sekaligus prinsip "jangan mengulang/mendahului pekerjaan section lain". "Versioned history" sebagai append-only `ChangeRequest` log (bukan entitas versioning baru) dipilih karena acceptance literal ("dapat diselesaikan tanpa menghapus histori") sudah terpenuhi oleh sifat log kronologis yang tidak pernah dihapus — menambah entitas `ChangeRequestVersion` terpisah akan menciptakan struktur data yang lebih kompleks dari yang literal Wajib-nya minta, tanpa manfaat tambahan yang jelas.
**Consequence:** `docs/frontend-known-issues.md` bagian 14 diperbarui — gap Section 19 RESOLVED; bagian 15 (Section 20/Project Finance) diperbarui dengan catatan forward dependency eksplisit (`RefundRequest.creditStatus` mock self-contained, real `CreditNote` integration tetap tanggung jawab Section 20). `FlightBooking`/`HotelBooking`/`TransportBooking`/`MiceEvent`/`Invoice`/`Payment`/`ActivityEntry` (Section 13-16/Foundation/Section 14 lama) TIDAK diubah shape-nya sama sekali — nol risiko regresi terhadap consumer existing. `update*BookingStatus` (Section 13-16, LOCKED) TIDAK diubah guard/transition-map/reason-wajib-nya — hook Cancellation murni efek samping tambahan SETELAH transisi berhasil, pada record terpisah. Ini adalah turunan KEDUA dari preseden "consolidation/orchestration layer lintas-domain" D-075 (turunan pertama) — bila section berikutnya butuh pola serupa (agregasi/exception-tracking lintas beberapa entitas existing tanpa menduplikasi data), preseden `createChangeRequest`+`activityEntryId` (menaut balik ke audit trail existing, bukan menggantikannya) dapat dijadikan acuan tambahan.

### D-077 — Project Finance: Invoice/Payment Mutator Aditif Langsung ke `app/types/finance.ts` (Berbeda dari Pola D-070 s.d. D-076); `CreditNote`/`DebitNote` Baru; `Invoice.amountIdr` Tidak Pernah Ditulis Ulang; Financial Closure Gate Menutup Forward Dependency D-076

**Status:** LOCKED · **Date:** 2026-08-01 · **Affected:** `app/types/finance.ts` (diperluas — `+InvoiceCurrency`/`InvoiceType`/`ExchangeRateSnapshot`/`+CreditNote`/`DebitNote`, `InvoiceStatus` `+'void'`, `Invoice` `+currency`/`exchangeRateSnapshot`/`invoiceType`/`voidedAt`/`voidReason`, `Payment` `+method`/`recordedBy`), `app/data/finance.ts` (backfill fixture existing + `+INV-1041`/`+CREDIT_NOTES`/`+DEBIT_NOTES`), `app/types/procurement.ts` (`SupplierInvoice` `+paymentScheduleDate`/`+matchStatus`, aditif), `app/data/procurement.ts` (backfill `matchStatus`/`paymentScheduleDate` fixture existing), `app/utils/attention.ts` (`isInvoiceOverdue` mengecualikan `'void'`), `app/data/index.ts` (+seluruh mutator/selektor Finance baru, +hook satu baris di `updateRefundRequestStatus`), `app/constants/status.ts` (+`INVOICE_CURRENCIES`/`INVOICE_TYPES`/`SUPPLIER_INVOICE_MATCH_STATUSES`/`CREDIT_NOTE_STATUSES`/`DEBIT_NOTE_STATUSES`, `INVOICE_STATUSES` +`'void'`), `app/constants/navigation.ts` (+2 child nav Finance), `app/pages/finance/invoices.vue` (+Create/Record Payment/Void/Issue Credit Note), `app/pages/finance/notes.vue` (baru), `app/pages/finance/reconciliation.vue` (baru), `app/pages/finance/index.vue` (+link + Financial Closure Readiness), `app/pages/projects/[id]/index.vue` (tab Finance diperkaya + Closure Checklist item `financeSettled` digerbangi), `app/pages/client/project-orders/[id]/index.vue` (+badge DP/currency, sell-side saja).

**Context:** Section 20 (Wajib): "Budget, estimate, committed cost, actual cost, variance, margin [sudah ada, Section 15 lama]. Multi-currency display dan exchange-rate snapshot mock. Client invoice, DP, payment, outstanding, aging [invoice/payment/aging sudah ada Section 15 lama — DP/multi-currency belum]. Supplier invoice/AP, payment schedule, match status. Refund/credit/debit note states. Reconciliation workspace mock. Financial permissions." Acceptance: "Finance dapat menjalankan frontend workflow AR/AP sampai settlement dan closure gate." Ini SATU-SATUNYA section pada roadmap Section 00–24 sejauh ini yang literal Wajib-nya secara eksplisit meminta section BARU untuk memperluas `app/types/finance.ts` secara langsung (D-070 s.d. D-076 seluruhnya SENGAJA menghindarinya, membangun entitas baru yang hanya menaut ID) — Section 20 adalah pemilik domain finance itu sendiri, jadi pola "entitas baru menaut ID tanpa memutasi" tidak berlaku di sini secara harfiah untuk `Invoice`/`Payment`/`SupplierInvoice` (justru section-section itulah yang dimiliki oleh Section 15/17 lama dan kini diperluas oleh pemilik forward-dependency-nya). `docs/frontend-known-issues.md` bagian 15 dan D-076 poin 5 mencatat forward dependency eksplisit: `RefundRequest.creditStatus` (Section 19) MOCK self-contained, real `CreditNote` integration adalah tanggung jawab literal Section 20.

**Decision:** (1) **Perluasan aditif langsung ke `app/types/finance.ts`** (BUKAN entitas baru yang menaut ID) — `Invoice`/`Payment`/`SupplierInvoice` MENDAPAT field baru langsung (currency/invoiceType/void/method/recordedBy/paymentScheduleDate/matchStatus), karena Section 20 adalah PEMILIK domain finance, bukan konsumer eksternal yang harus menghormati boundary entitas orang lain (kontras eksplisit dengan D-070 s.d. D-076 yang seluruhnya dimiliki OLEH domain lain). Backfill fixture existing 100% additive — nilai efektif `currency: 'IDR'` untuk seluruh 5 invoice lama TIDAK mengubah hasil kalkulasi apa pun; `invoiceType` diturunkan dari konteks label/urutan invoice per project apa adanya (bukan tebakan sembarangan — mis. "Termin Awal"→`dp`, "Termin 1"→`dp`, "Termin 2"→`final`). `recordedBy` (Payment, WAJIB diisi) di-backfill `USR-008` (Budi Santoso, role `finance`) untuk 3 payment lama — user internal plausible, konsisten pola `reviewedBy` section lain. (2) **`InvoiceStatus` +`'void'`** — transisi terminal baru (`voidInvoice`, alasan wajib). KONSEKUENSI WAJIB: seluruh selektor yang sebelumnya memakai `status !== 'paid'` sebagai proksi tunggal "outstanding/perlu perhatian" harus turut mengecualikan `'void'`, atau invoice yang sudah dibatalkan akan salah muncul sebagai outstanding/overdue. Titik sentuh yang diperbarui: `isInvoiceOverdue` (`app/utils/attention.ts`), `getInvoiceOutstandingIdr` (return 0 langsung untuk `'void'`), `getOutstandingInvoices`, `getDepartureReadiness` (`outstandingInvoiceCount`, Section 12 lama — SATU baris, murni menambah exclude yang sama, tidak mengubah struktur/return-type). TIDAK ADA fixture yang diseed dengan status `'void'` (hanya reachable lewat aksi `voidInvoice` runtime) — nol perubahan terhadap angka Outstanding/Margin/Variance yang sudah divalidasi presisi sejak Section 15/16 untuk PRJ-101/102/103 (diverifikasi ulang lewat smoke test — lihat bagian 13 laporan section). (3) **Multi-currency + exchange-rate snapshot MOCK** — `Invoice.currency`/`exchangeRateSnapshot` murni tampilan; `amountIdr` TETAP satu-satunya ledger/source of truth (nilai Rupiah), snapshot dicatat SEKALI saat invoice diterbitkan (D-006, bukan integrasi kurs pasar nyata). Fixture demo BARU `INV-1041` (PRJ-104, currency `USD`) SENGAJA ditaruh di project yang sebelumnya nol invoice (tidak pernah divalidasi angka apa pun oleh section manapun) agar mendemokan fitur tanpa risiko mengubah satu pun angka yang sudah divalidasi. (4) **`CreditNote`/`DebitNote` baru** (`app/types/finance.ts`) — `Invoice.amountIdr` TIDAK PERNAH ditulis ulang oleh mutator mana pun (histori invoice tetap utuh, pola sama sejak Section 13 `VendorQuotation`); `getInvoiceOutstandingIdr` diperbarui mengurangi outstanding dengan total `CreditNote` `issued`/`applied` on-the-fly. `DebitNote` MURNI informasional (Wajib eksplisit) — TIDAK pernah menambah `Invoice.amountIdr` atau kalkulasi outstanding apa pun (hanya tercatat/ditampilkan). (5) **Mutator baru** (`app/data/index.ts`): `createInvoice`/`voidInvoice`/`recordPayment`/`issueCreditNote`/`issueDebitNote`/`updateSupplierInvoiceMatchStatus` — seluruhnya menerima objek input (BUKAN parameter positional seperti draft awal brief `recordPayment(invoiceId, amountIdr, method?, recordedBy)`, karena `method?` opsional tidak sah diletakkan sebelum `recordedBy` wajib dalam TypeScript positional params; objek input juga konsisten dengan pola `CreateXInput` yang sudah dipakai di SELURUH mutator sejak Section 07) — deviasi kecil, didokumentasikan di sini dan di laporan section, tidak mengubah perilaku/acceptance. `updateSupplierInvoiceMatchStatus(id, status, actorId, note?)` menambah parameter `actorId` (tidak ada di draft literal brief) — mengikuti pola SELURUH mutator lain di codebase (mis. `reviewSupplierInvoice` persis di sebelahnya) yang selalu butuh actor untuk `ActivityEntry`; brief tanpa `actorId` akan membuat audit trail tidak bisa diatribusikan, dinilai lebih tepat mengikuti preseden codebase daripada literal draft. `recordPayment` MEREUSE `getInvoiceOutstandingIdr` (BUKAN menghitung ulang math outstanding) untuk menentukan status baru (`partially-paid`/`paid`). (6) **Hook satu-satunya yang diizinkan eksplisit ke Section 19**: `updateRefundRequestStatus` (`app/data/index.ts`, LOCKED sejak D-076) — begitu status mencapai `processed` DAN `request.invoiceId` terisi, memanggil `issueCreditNote` BARU. Guard/transition-map/reason-wajib fungsi tsb TIDAK disentuh — hook murni aditif SETELAH `request.status`/`creditStatus` di-set, persis pola hook `createCancellationRecord` (D-076 poin 4). Hook ini bersifat PROSPEKTIF (berlaku untuk transisi status baru sejak Section 20 berjalan) — `REF-001` (fixture lama, sudah `processed`/`creditStatus: 'issued'` sejak Section 19) SENGAJA TIDAK di-backfill `CreditNote` retroaktif, karena akan mengurangi Outstanding PRJ-102 yang sudah divalidasi presisi (Rp95.000.000) sejak Section 15/16 — demo `CreditNote` seeded (`CN-001`) sengaja ditaruh di invoice yang SUDAH `paid` (`INV-1011`, outstanding sudah 0, floor `Math.max(...,0)` menjaga nol perubahan efektif) untuk mendemokan entitas tanpa risiko regresi. (7) **AP scheduling/match-status** (`SupplierInvoice` +`paymentScheduleDate`/+`matchStatus`, `app/types/procurement.ts`) — additive optional, TIDAK menyentuh flow submit/review existing Section 17. Reconciliation workspace (`/finance/reconciliation`) murni worklist mock (D-006) — filter `matchStatus === 'unmatched' || 'disputed'` (invoice tanpa `matchStatus` sama sekali TIDAK muncul, dianggap "belum ditriase" bukan "perlu direkonsiliasi"), aksi "Mark Matched" (satu klik, pola transisi positif) dan "Flag Disputed" (dialog, catatan wajib, TIDAK ada field baru untuk menyimpan catatan — dicatat sebagai `ActivityEntry` project terkait saja, konsisten pola `createCancellationRecord`). (8) **Financial closure gate** — `evaluateFinanceClosureGate(projectId)` (derivasi murni, pola sama `getCostSheetBreakdown`/`getServiceReadinessMatrix`): `ready: true` hanya bila TIDAK ADA (a) invoice project ini dengan `getInvoiceOutstandingIdr > 0`, (b) `RefundRequest` non-terminal (`requested`/`under-review`/`approved`), (c) `SupplierInvoice` dengan `matchStatus` terisi tapi bukan `matched`. `closeProjectFinance` MEREUSE `updateProjectClosureChecklist` existing (Section 09/D-066, TIDAK menulis ulang shape-nya) — mengisi `financeSettled = true` HANYA bila gate `ready`. Checkbox manual `financeSettled` di Closure Checklist tab Overview (Section 09, shell inert sejak awal) kini SELALU disabled dengan hint mengarahkan ke aksi "Close Finance" tab Finance — satu-satunya jalur mengubah field tsb sekarang lewat gate, 3 item checklist lain (`documentsArchived`/`feedbackCollected`/`assetsReturned`) TETAP toggle manual seperti sebelumnya (shell, belum digerbangi section manapun — sengaja tidak diperluas, di luar scope literal Section 20). (9) **Client Portal** — HANYA badge tipe invoice (DP/Termin/Final) + label currency ditambahkan ke tabel invoice sell-side existing; TIDAK ADA Credit/Debit Note, AP, atau Close Finance yang bocor ke Client (pola sanitasi identik D-065/D-067/D-070/D-076).

**Reason:** Perluasan langsung ke `app/types/finance.ts` (bukan entitas menaut-ID seperti D-070 s.d. D-076) dipilih karena precedent itu murni menghindari section BARU memutasi domain MILIK section LAIN — Section 20 di sini justru ADALAH pemilik literal domain finance, jadi memaksakan pola "menaut ID, jangan mutasi" akan menciptakan `InvoiceExtension`/`PaymentExtension` paralel yang bertentangan langsung dengan hard rule "jangan membuat dataset paralel", tanpa manfaat proteksi apa pun (tidak ada section lain yang perlu dilindungi dari section pemilik domainnya sendiri). `Invoice.amountIdr` tidak pernah ditulis ulang (Credit/Debit Note sebagai entitas terpisah, bukan mutasi field) dipilih agar histori invoice tetap dapat diaudit — pola identik "jangan menimpa data historis" yang sudah konsisten sejak Section 13 (`VendorQuotation`, harga tidak pernah ditimpa, hanya duplikasi versi). Hook prospektif-saja (tidak retroaktif ke `REF-001`) dipilih murni untuk menghormati "Perubahan lintas section harus minimal" — migrasi data historis Section 19 ke bentuk baru akan mengubah angka yang sudah divalidasi presisi berulang kali sejak Section 15, risiko regresi yang tidak proporsional dengan manfaat demo tambahan (fitur credit note tetap terdemokan penuh lewat `CN-001`/hook prospektif untuk refund BARU).

**Consequence:** `docs/frontend-known-issues.md` bagian 15 — forward dependency D-076 RESOLVED (didokumentasikan detail di laporan section, bukan dihapus begitu saja — payment gateway nyata TETAP di luar scope selamanya sesuai protokol, dicatat eksplisit sebagai batas permanen bukan gap). `Invoice`/`Payment` (Foundation) dan `SupplierInvoice` (Section 17) TIDAK diubah shape field lama sama sekali (murni tambahan) — nol risiko regresi terhadap consumer existing (`getCostSheetBreakdown` dkk. tidak menyentuh field ini). Ini adalah preseden ARSITEKTUR BARU ("section memperluas domain type miliknya sendiri secara langsung", berbeda dari D-070 s.d. D-076 "section baru menaut ID ke domain section lain") — bila Section 21/22/23 kelak butuh memperluas domain yang literal Wajib-nya secara eksplisit menjadi milik mereka sendiri (bukan domain section lain), preseden ini (perluasan aditif + backfill fixture non-destruktif + hook prospektif untuk forward dependency) dapat dijadikan acuan.

### D-078 — Documents, Communication dan Notifications: `Document`/`Message`/`Notification` Fully Additive di Atas `ProjectDocument`/`VendorDocument`; Generated Document Menaut ke 9 Preview Route Existing (Bukan Duplikasi Generator); Hook Notification-Push Kurasi (4 Titik); Unified Activity Timeline Access-Filtered

**Status:** LOCKED · **Date:** 2026-08-01 · **Affected:** `app/types/document-comms.ts` (baru), `app/data/document-comms.ts` (baru), `app/types/user.ts` (`ModuleKey` +`documents`), `app/constants/roles.ts` (`ROLE_MODULE_ACCESS` +kolom `documents`), `app/constants/navigation.ts` (+menu "Documents & Communication"), `app/constants/status.ts` (+`DOCUMENT_ENTITY_TYPES`/`DOCUMENT_ACCESS_LEVELS`/`MESSAGE_CHANNELS`/`MESSAGE_DELIVERY_STATUSES`/`NOTIFICATION_TYPES`), `app/utils/attention.ts` (+`isDocumentExpired`/`isDocumentExpiringSoon`), `app/data/index.ts` (+selector/mutator Documents/Messages/Notifications/Unified Timeline lengkap, +4 hook `pushNotification` di `escalateIncident`/`approveChangeRequest`/`rejectChangeRequest`/`createProjectTask`/`updateProjectTask`), `app/pages/documents/**` (baru), `app/components/layout/NotificationPanel.vue` (rewire penuh), `app/pages/projects/[id]/index.vue` (tab Documents diperkaya, tab Activity & Changes +Communication +Unified Timeline), `app/pages/admin/roles.vue` (+kolom matrix).

**Context:** Section 21 (Wajib): "Document folders per entity/project. Categories, versions, expiry, access level. Internal/client/supplier visibility. Generated document previews. Internal notes, client messages, supplier messages. In-app notification center. Email/WhatsApp delivery status simulation tanpa klaim integrasi. Mentions, assignments, reminders, escalation. Unified activity timeline dengan filtering akses." Acceptance: "Dokumen dan komunikasi dapat ditelusuri ke entity dan tidak bocor lintas role." Audit kode mengonfirmasi kondisi sebelum section ini (`docs/frontend-known-issues.md` bagian 16): dokumen tersebar (`ProjectDocument` Project Detail — hanya id/projectId/name/uploadedAt, tanpa category/version/expiry/access-level; `getDocumentsByParty` Customer Detail, Prompt 19, murni union `getDocumentsByProject`), `VendorDocument` (Section 17) domain terpisah untuk dokumen vendor; `NotificationPanel.vue` sudah ada sejak starter-template TAPI selalu memakai `ref<Notification[]>` hardcoded fake (interface lokal generik "success/warning/info/error" dengan pesan PM-SaaS umum, "Website Redesign project completed"/"$12,500 payment received" — ZERO koneksi ke data aplikasi ini), `TopHeader.vue` sudah punya bell icon+badge yang bekerja via `defineExpose({ unreadCount })` tapi menampilkan angka dari data fake tsb; tidak ada `Message`/communication-log terstruktur terpisah dari `PartyActivity`/`ActivityEntry` (yang scoped internal); tidak ada unified timeline access-filtered. D-075 (Section 18) dan D-076 (Section 19) mendokumentasikan preseden "consolidation/orchestration layer lintas-domain" sebagai acuan — Section 21 adalah turunan KETIGA dari pola tsb (setelah Booking Orchestration dan Changes/Incidents).

**Decision:** (1) **Tiga entitas baru fully additive** (`app/types/document-comms.ts`): `Document` (entityType/entityId generik lintas 12 tipe entitas — project/party/vendor/traveler/quotation/flight/hotel/transport/mice/invoice/change-request/incident — category/version/expiry/accessLevel/sourceType), `Message` (channel `internal-note`/`client-message`/`supplier-message`, `deliveryStatus`/`deliveryChannel` mock label), `Notification` (userId target tunggal, 8 `NotificationType`). Pola arsitektur IDENTIK preseden D-070 s.d. D-076 poin 1: entitas baru menautkan ID ke entitas existing, TIDAK PERNAH memutasinya — ZERO field baru di `ProjectDocument`/`VendorDocument`/`ActivityEntry`/`PartyActivity`. (2) **`ProjectDocument`/`getDocumentsByProject`/`getDocumentsByParty` (Section 14 lama/Prompt 19) dan `VendorDocument` (Section 17) TIDAK diganti** — `getDocumentsForProject` (baru) adalah UNION view (dokumen baru + dokumen legacy diberi default `category: 'Legacy'`/`accessLevel: 'internal'` untuk tampilan yang wajar), dipakai MENDAMPINGI (bukan menggantikan) `documents`/`recentDocuments` computed lama di Project Detail (widget Overview tetap memakai selector lama apa adanya, nol regresi). (3) **Generated document TIDAK PERNAH menduplikasi generator** — `Document.previewRoute` (untuk `sourceType: 'generated'`) HANYA diisi dengan salah satu dari 9 route preview yang SUDAH ADA dan diverifikasi manual (`/crm/opportunities/[id]/quotation-preview`, `/ticketing/[id]/eticket-preview`, `/accommodation/[id]/voucher-preview`, `/transportation/[id]/service-order-preview`+`driver-sheet-preview`, `/mice/[id]/rundown-preview`+`boq-preview`, `/projects/[id]/run-sheet-preview`+`manifest-preview`) — fixture (`DOC-C002` s.d. `DOC-C010`) menaut ke ID booking/opportunity/project REAL (`FLT-1011`, `HTL-1033`, `TRN-1034`, `MICE-1035`, `OPP-001`, `PRJ-103`), bukan route fiktif. Section 21 TIDAK membuat generator dokumen baru apa pun — "Document Center" murni katalog+link, konsisten hard rule "jangan membuat dataset paralel". (4) **`Message.deliveryStatus`/`deliveryChannel` sebagai generalisasi preseden `Quotation.sentToClientAt`** (Section 05, satu timestamp flip mock) — `sendMessage` settle DETERMINISTIK (`internal-note`→`sent`, `client-message`/`supplier-message`→`delivered`) TANPA fake async timer (D-006); skenario `failed`/`queued` didemokan lewat fixture seed (`MSG-003`/`MSG-008`), bukan dari jalur create runtime — pola sama Section 20 "`'void'` tidak diseed, hanya reachable lewat aksi". (5) **Notification-push HANYA dari 4 hook kurasi**, bukan seluruh mutator lintas codebase (preseden D-075 "representatif, bukan menyeluruh"): `sendMessage` (mentions→`mention`), `escalateIncident` (Section 19, LOCKED→`escalation`), `approveChangeRequest`/`rejectChangeRequest` (Section 19, LOCKED→`change`), `createProjectTask`/`updateProjectTask` (Section 09 lama, LOCKED→`assignment`, guard mencegah notifikasi berulang saat `assignedTo` tidak berubah). Lihat `docs/mockup-change-impact-log.md` CI-051 untuk detail baris-per-baris dan konfirmasi nama fungsi sesungguhnya (brief menyebut approx. "Section 12's task-assignment mutator" — VERIFIKASI menemukan ini sesungguhnya `createProjectTask`/`updateProjectTask` milik Section 09 lama, dicatat sebagai deviasi minor dari nama approx.). (6) **`NotificationPanel.vue` di-REWIRE, bukan didesain ulang** — data source diganti dari `ref<Notification[]>` hardcoded ke `getNotificationsForUser`/`getUnreadNotificationCount`/`markNotificationRead`/`markAllNotificationsRead`/`removeNotification` (Section 21) sambil mempertahankan shell UI (popover styling, mark-as-read/remove interaction, `defineExpose({ unreadCount })`) 100% sama — ini AKTIVASI boilerplate mati (fitur yang sudah ADA di kode tapi tidak pernah terhubung ke data nyata sejak awal proyek), BUKAN mengubah fitur yang sudah bekerja. `TopHeader.vue` TIDAK disentuh sama sekali (kontrak `defineExpose` dipertahankan). Tujuan kanonik "View all notifications" adalah `/documents?tab=notifications` (SATU-SATUNYA UI daftar notifikasi lengkap — tidak ada halaman `/notifications` kedua yang divergen). (7) **`getUnifiedActivityTimeline(entityType, entityId, viewerAccessLevel)`** — derived view-model (pola sama `getBookingTimeline`/`getServiceReadinessMatrix`), menggabungkan `ActivityEntry`+`SystemEvent`+`Message`+`Document` (upload/generate) kronologis; untuk `entityType: 'project'`, Document/Message diagregasi via `projectId` (bukan hanya `entityType: 'project'` sempit) agar dokumen/pesan sub-entity seperti booking/incident yang tertaut project yang sama tetap muncul; `SystemEvent` SELALU `internalOnly: true`; filter `internalOnly` diterapkan bila `viewerAccessLevel !== 'internal'` — memenuhi acceptance literal "tidak bocor lintas role" secara langsung di level selector (bukan hanya di level UI). Disurfaced di tab "Activity & Changes" Project Detail sebagai SectionCard "Unified Activity Timeline (Internal View)" — versi client/supplier-safe di Client/Supplier Portal DIDEFER sebagai stretch goal (selector generik sudah mendukungnya, tinggal dipanggil dengan `viewerAccessLevel: 'client'`/`'supplier'` di halaman portal terkait — dicatat eksplisit sebagai gap yang genuinely tersisa, bukan disembunyikan). (8) **Modul baru `/documents`** (`ModuleKey` `documents`) — 3 tab query-param (pola sama `/changes`, preseden konsolidasi ter-dekat): Documents (filter category/access-level/entity-type/expiry-status, upload mock), Messages (filter channel, compose+mentions), Notifications (tujuan kanonik). `management`/`project-manager`/`operations` `MANAGE`, mayoritas role internal lain `VIEW`, `client`/`supplier` `NONE` (mereka tetap pakai `getDocumentsByParty`/portal existing, TIDAK diperluas Section 21 — scope portal-side document/message view didefer, lihat known issues).

**Reason:** Fully additive (bukan menggabungkan ke `ProjectDocument`/`VendorDocument`/`ActivityEntry`) dipilih karena hard rule "jangan membuat dataset paralel" berlaku DUA ARAH (preseden D-070/D-075/D-076) — memaksakan field category/version/expiry/access-level/deliveryStatus yang kaya ke `ProjectDocument`/`PartyActivity` generik akan mencemari model yang dipakai luas oleh section lain, sementara TIDAK membuat entitas sama sekali akan gagal memenuhi Wajib literal. Generated document menaut ke route preview existing (bukan generator baru) dipilih karena 9 halaman preview tsb SUDAH memenuhi kebutuhan "dokumen yang bisa di-generate" secara lengkap (quotation/e-ticket/voucher/service-order/driver-sheet/rundown/BOQ/run-sheet/manifest) — membangun generator kedua akan melanggar "jangan mengulang pekerjaan section lain" TANPA manfaat tambahan. Hook notification-push kurasi (4 titik, bukan menyeluruh) dipilih mengikuti preseden D-075 eksplisit — menghook SETIAP mutator lintas ~3500 baris `app/data/index.ts` akan menjadi cross-section touch masif yang melanggar "Perubahan lintas section harus minimal, dicatat, dan regression-tested"; 4 titik yang dipilih adalah yang paling representatif terhadap 4 `NotificationType` literal Wajib ("Mentions, assignments, reminders, escalation" — `change` mewakili "reminders" secara longgar sebagai notifikasi keputusan yang perlu ditindaklanjuti, item "reminder" murni juga tersedia sebagai `NotificationType` untuk pemakaian manual/fixture).

**Consequence:** `docs/frontend-known-issues.md` bagian 16 diperbarui — gap Section 21 RESOLVED, dengan catatan eksplisit: versi client/supplier-safe Unified Activity Timeline di Portal DIDEFER (selector sudah generik, tinggal pemanggilan tambahan di halaman portal — bukan keterbatasan arsitektur). `ProjectDocument`/`getDocumentsByProject`/`getDocumentsByParty`/`VendorDocument`/`ActivityEntry`/`SystemEvent` TIDAK diubah shape-nya — nol risiko regresi. `escalateIncident`/`approveChangeRequest`/`rejectChangeRequest` (Section 19)/`createProjectTask`/`updateProjectTask` (Section 09 lama) — hook SATU-DUA baris aditif di akhir fungsi, guard/transition-map/reason-wajib/signature TIDAK disentuh. `NotificationPanel.vue` — AKTIVASI boilerplate mati, `TopHeader.vue` tidak disentuh. Ini adalah turunan KETIGA dari preseden "consolidation/orchestration layer lintas-domain" (D-075→D-076→D-078) — bila section berikutnya (Section 22 Dashboards/Reports/Activity Center) butuh pola serupa (agregasi lintas beberapa entitas existing tanpa menduplikasi data, atau hook notification-push tambahan), preseden `pushNotification`+4-hook-kurasi dan `getUnifiedActivityTimeline` dapat dijadikan acuan.

### D-079 — Dashboards, Reports, Lead Recap dan Activity Center: SSR Loading-Skeleton Fix (Hilangkan `isLoading`, Bukan Delay Client-Only), Saved Views Centralized Mock State (Bukan `localStorage`), Mock SLA Threshold Eksplisit, Gap-Fill Sempit Menghormati D-031

**Status:** LOCKED · **Date:** 2026-08-01 · **Affected:** `app/pages/index.vue`, `app/pages/reports/index.vue`, `app/pages/activity-center/index.vue`, `app/pages/supplier/index.vue`, `app/types/reporting.ts` (baru), `app/data/reporting.ts` (baru), `app/data/index.ts` (+selector Saved Views).

**Context:** Section 22 (Wajib): "Dashboard seluruh role, Customer Journey, Lead Source Recap, Activity Center Super Admin, pipeline/conversion/SLA/quotation performance, project readiness/departure/changes/incidents, revenue/margin/AR-AP/aging, supplier performance, client and supplier action centers, filters/saved views/drill-down/export preview." Audit kode mengonfirmasi (per `docs/mockup-section-reports/section-06-dashboard.md` dan `section-16-reports.md`, OLD SCHEME) bahwa Dashboard (`/`, D-031 LOCKED "satu komponen, widget kondisional per role") dan Reports (`/reports`, 6 SectionCard granular) SUDAH substansial — Section 22 BUKAN rebuild, melainkan kurasi gap-fill sempit yang sudah disepakati eksplisit dengan user: (1) fix bug SSR loading-skeleton (`docs/frontend-known-issues.md` bagian 15, ditemukan Section 20 tapi di luar kepemilikan file section tsb), (2) Export mock Reports (dicatat sejak Section 16 lama, belum dikerjakan), (3) Saved Views, (4) drill-down Reports+Activity Center, (5) section baru "SLA dan Quotation Performance" di Reports, (6) Supplier Action Center, (7) cross-link Vendor Summary→Procurement Performance, (8) verifikasi konten Dashboard 11 role, (9) koreksi klaim stale "Widget role baru belum ada" di roadmap (sudah RESOLVED Section 02/CI-030).

**Decision:** (1) **SSR fix — `isLoading` dihilangkan (selalu `false`), BUKAN diberi `import.meta.server` guard dengan delay client-only.** Root cause bug: `isLoading = ref(true)` + `onMounted(() => setTimeout(() => isLoading.value = false, 400))` — `onMounted` tidak pernah berjalan saat SSR, sehingga HTML hasil render server selamanya berupa `LoadingState` skeleton. Opsi yang dipertimbangkan (guard `import.meta.server` agar `isLoading` awal `false` di server tapi `true` di client, delay hanya client-only) DITOLAK karena akan menciptakan **hydration value mismatch** (nilai awal ref berbeda antara server render dan client initial render sebelum `onMounted`) DAN, bila dihindari dengan menunda toggle `isLoading=true` sampai SETELAH hydration (`onMounted` client-only mengubah `false→true→false`), akan membuat konten yang SUDAH BENAR dari SSR sempat "hilang" lagi jadi skeleton setelah hydration selesai — regresi UX baru yang tidak proporsional untuk data yang 100% fixture sinkron (tidak ada fetch async nyata yang genuinely perlu ditunggu). Opsi yang DIPILIH: `isLoading = ref(false)` permanen, `onMounted`/`setTimeout` dihapus total dari kedua halaman — SSR HTML kini langsung berisi konten nyata (dikonfirmasi lewat curl+grep judul widget, lihat laporan section bagian 13), tidak ada flash/mismatch apa pun. (2) **Saved Views — `SavedView` (baru, `app/types/reporting.ts`) + centralized reactive mock state (`app/data/reporting.ts`, `SAVED_VIEWS`), BUKAN `localStorage`/`sessionStorage`.** Protokol mengizinkan `localStorage` "hanya bila dibutuhkan untuk simulasi interaksi" — tapi SELURUH state mutable lain di codebase ini (Opportunity/Project/RFQ/dst.) memakai `reactive()` array terpusat, bukan browser storage; memakai `localStorage` khusus untuk Saved Views akan menciptakan DUA pola persistence mock yang tidak konsisten tanpa alasan kuat (role-switching `useCurrentUser` adalah SATU-SATUNYA use case `localStorage` existing, dan itu pun karena kebutuhan spesifik "bertahan lintas reload tanpa login form nyata" — Saved Views tidak punya kebutuhan setara). `filters: Record<string, string>` generik (bukan union type per-halaman) karena Dashboard (5 filter: status/type/client/owner/period) dan Reports (3 filter: status/type/period) punya bentuk yang mirip tapi tidak identik — `applySavedView` HANYA mengembalikan objek tersimpan, TIDAK memutasi ref komponen (data layer tidak boleh bergantung pada Vue ref halaman) — pemanggil (masing-masing halaman) yang menuliskannya kembali ke filter existing miliknya sendiri. TIDAK ADA filter baru ditambahkan — murni membungkus filter yang SUDAH ADA. (3) **Export mock** — dialog page-level (bukan per-section) di Reports, pilih bagian (7 SectionCard + "Seluruh Laporan") + format (CSV/PDF), `showToast` menampilkan nama file simulasi (`{section}-{DEMO_REFERENCE_DATE}.{format}`) — TIDAK ADA file yang benar-benar dihasilkan (larangan protokol eksplisit, konsisten D-006). (4) **SLA dan Quotation Performance — metrik dihitung dari `Opportunity.createdAt` → `Quotation.createdAt`, BUKAN `Quotation.sentToClientAt`.** Audit field mengonfirmasi `sentToClientAt` (Section 05) HANYA terisi untuk 1 dari seluruh `QUOTATIONS` fixture saat ini (field opsional, "send to client" adalah aksi eksplisit yang belum dilakukan untuk quotation lain) — terlalu sparse untuk jadi metrik cycle-time utama yang bermakna. `createdAt` pada kedua entitas SELALU terisi (field wajib) sehingga dipakai sebagai metrik utama "cycle time Opportunity Created → Quotation Dibuat"; `sentToClientAt` tetap ditampilkan sebagai stat terpisah ("Quotation Terkirim ke Client") dengan jumlah apa adanya, bukan disamarkan sebagai data lengkap. **Threshold SLA (3 hari) adalah ASUMSI MOCK eksplisit** — ditampilkan dengan label "Threshold mock" di UI, BUKAN diklaim sebagai SLA kontraktual nyata dengan client (larangan protokol "jangan mengarang integrasi/komitmen nyata" turut berlaku untuk angka bisnis yang belum disepakati). **"Approval cycle time" (disebut literal brief) TIDAK dihitung** — `Quotation` (`app/types/opportunity.ts`, LOCKED sejak Section 08) tidak memiliki field timestamp `approvedAt` tersimpan (hanya `approvedBy`/`approvalNote`, di-set `approveQuotation`) — menambah field baru ke `Quotation` untuk metrik derivasi SATU section akan melanggar semangat "jangan menambah field tanpa kebutuhan struktural nyata"; opsi menaksir dari `PartyActivity.createdAt` (log approval) DITOLAK karena `createPartyActivity` selalu memakai `DEMO_REFERENCE_DATE` ("hari ini") sebagai `createdAt`, bukan tanggal historis asli — akan menghasilkan angka yang menyesatkan (bukan representasi waktu approval sesungguhnya), lebih baik didokumentasikan sebagai gap genuine (`docs/frontend-known-issues.md` bagian 17) daripada memproduksi metrik palsu. (5) **Drill-down — HANYA ke destinasi yang sungguhan mendukung filter/route yang relevan, didokumentasikan eksplisit mana yang TIDAK ditautkan.** Sales Pipeline (Reports) → `/crm/opportunities?stage=X` (query-filter sungguhan, dikonfirmasi `crm/opportunities/index.vue` sudah membaca `route.query.stage`). Invoice Aging (Reports) → `/projects/[id]?tab=finance` (Project Detail tab Finance, BUKAN filter langsung di `/finance/invoices` — halaman tsb tidak punya query-filter support dan berada di luar touch-point Section 22 untuk ditambahkan). Project Performance (status/tipe breakdown) — TIDAK ditautkan (`/projects` tidak punya query-filter support, halaman di luar touch-point). Activity Center → `getOpportunityById`/`getQuotationById`(via `opportunityId`)/`getPartyById`/`getProjectById`/`VENDOR_QUOTATIONS.find` (module `vendor`, `entityId` adalah ID VendorQuotation seperti `VQ-009`, BUKAN ID Vendor — ditautkan ke `vendorQuotation.vendorId`) — module `lead`/`finance`/`user` SENGAJA tidak menghasilkan link (tidak ada route `[id]` yang aman/jelas untuk ketiganya). (6) **Supplier Action Center** — SectionCard baru di `/supplier`, pola IDENTIK Client Portal Action Center (`app/pages/client/index.vue`, computed `actionItems: {key,label,to,tone}[]`) — pending RFQ response (`status` `sent`/`responses-in`)/klarifikasi terbuka (`status` `clarification`)/pending Service Order acknowledgment (`status` `sent`)/pending invoice-submission (`status` `fulfilled` tanpa `SupplierInvoice`, `getSupplierInvoicesByServiceOrder` baru diimpor) — seluruhnya REUSE selector Section 17 yang SUDAH di-scope `vendorScopeId` di halaman ini (`rfqs`/`serviceOrders`), tidak ada selector/entitas baru. (7) **Cross-link Vendor Summary → Procurement Performance** — tombol di `#actions` SectionCard, BUKAN duplikasi `getVendorProcurementPerformance` (Section 17) ke Reports. (8) **D-031 dihormati penuh** — TIDAK ADA restrukturisasi widget/filter Dashboard atau 6 section Reports existing; seluruh 7 poin di atas adalah penambahan aditif (SectionCard baru, `#actions` slot, dialog) di ATAS struktur yang sudah final.

**Reason:** SSR fix dipilih menghilangkan `isLoading` (bukan mempertahankan delay dengan guard SSR) karena kompleksitas hydration-mismatch tidak proporsional dengan manfaatnya — data di kedua halaman 100% fixture sinkron, TIDAK ADA alasan genuine untuk mensimulasikan loading sama sekali (berbeda dari D-006 "loading dapat disimulasikan" yang dimaksudkan untuk kasus di mana simulasi menambah realisme demo tanpa mengorbankan korektnes SSR — di sini korban SSR terlalu besar). Saved Views centralized state (bukan `localStorage`) dipilih murni konsistensi arsitektur — SATU pola persistence mock di seluruh 22 section jauh lebih mudah diaudit/direset (`app/utils/mock-reset.ts`) daripada state yang tersebar di dua tempat berbeda. Metrik SLA berbasis `createdAt` (bukan `sentToClientAt`/`approvedAt` yang sparse/tidak ada) dipilih karena "Setiap metric dapat ditelusuri ke data sumber" (acceptance literal Section 22) — metrik yang traceable ke field TIDAK LENGKAP lebih baik didokumentasikan sebagai gap daripada dipaksakan dari data yang tidak representatif (`PartyActivity.createdAt` yang selalu "hari ini").

**Consequence:** `docs/frontend-known-issues.md` bagian 17 diperbarui — export mock RESOLVED, klaim "widget role baru belum ada" dikoreksi sebagai stale (bukan gap baru), SSR bug RESOLVED, Saved Views/drill-down/SLA section/Supplier Action Center/cross-link RESOLVED (dengan sub-gap yang didokumentasikan eksplisit: approval cycle time tidak dihitung, beberapa drill-down target tidak tersedia). `app/pages/index.vue`/`app/pages/reports/index.vue` — struktur widget/filter/6-section TIDAK diubah (D-031 protection note dihormati). `app/pages/activity-center/index.vue`/`app/pages/supplier/index.vue` — perubahan aditif murni (link function, SectionCard baru). Ini adalah preseden bahwa "gap-fill sempit" (bukan rebuild) tetap dapat menghasilkan section COMPLETED penuh bila literal Wajib section sudah sebagian besar terpenuhi oleh section pemilik sebelumnya — pola serupa dapat dijadikan acuan bila Section 23/24 menemukan kondisi serupa (fondasi besar sudah ada, sisanya kurasi gap kecil).

### D-080 — Administration, Master Data dan Audit: Master Data Menjadi Reactive/Editable (Migrasi `app/constants/master-data.ts` → `app/data/master-data.ts`), 11 Kategori Master Data Baru dengan Batas Non-Integrasi Eksplisit Terhadap Business Logic LOCKED, Organization Profile Singleton (Bukan Multi-Tenancy), SystemEvent Sebagai Mekanisme Admin-Audit (Bukan Entitas Baru)

**Status:** LOCKED · **Date:** 2026-08-01 · **Affected:** `app/types/master-data.ts` (baru), `app/data/master-data.ts` (baru), `app/constants/master-data.ts` (diubah — static exports → re-export dari `~/data/master-data`, dipertahankan sebagai jaring pengaman kompatibilitas), `app/types/activity.ts` (`SystemEventModule` +`'administration'`), `app/types/user.ts` (`User` +`status`/`suspendedReason`/`suspendedAt`, +`UserStatus`), `app/data/users.ts` (`USERS` → `reactive()`, backfill `status`, 2 seed `suspended`), `app/utils/mock-reset.ts` (comment update, registry TIDAK diperluas), `app/data/index.ts` (+import/export seluruh array master-data baru, +`pushSystemEvent`/`createMasterDataRecord`/`updateMasterDataRecord`/`deactivateMasterDataRecord`/`reactivateMasterDataRecord`/`getMasterDataUsageCount`/`getMasterDataCategoryMeta`/`updateOrganizationProfile`/`suspendUser`/`reactivateUser`), `app/pages/admin/master-data.vue` (rewrite total — 3 kelompok x 15 sub-tab, CRUD dialog generik, usage-warning dialog), `app/pages/admin/users.vue` (+tab Access Review, +suspend/reactivate, aditif ke list+search+role-switch existing), `app/pages/admin/audit-trail.vue` (+search box, +SectionCard "Log Sistem (Non-Project)" untuk `SystemEvent`, aditif ke 3 filter dropdown existing), `app/pages/admin/organization.vue` (baru), `app/pages/admin/index.vue` (+kartu ke-5), `app/constants/navigation.ts` (+nav child ke-5 "Organization Profile").

**Context:** Section 23 (Wajib): "Users, roles, permissions, organizations. Destinations, airports, airlines, hotels, service/project types. Currencies, taxes, payment terms, cancellation rules. Numbering, templates, readiness gates, assignment rules. Audit trail search. Access review and suspended user state. Feature/configuration forms frontend. Historical snapshot warning ketika master berubah." Acceptance: "Super Admin dapat mengelola seluruh konfigurasi frontend tanpa menjadi approver komersial." Audit `docs/mockup-section-reports/section-17-administration.md` (OLD SCHEME, dibaca penuh) mengonfirmasi kondisi sebelum section ini: `/admin/master-data` (Section 17 lama) EKSPLISIT read-only by design comment ("Data bersifat read-only — perubahan production dilakukan oleh Super Admin", `app/pages/admin/master-data.vue` lama), hanya mencakup 4 kategori (`MASTER_PROJECT_TYPES`/`MASTER_SERVICE_TYPES`/`MASTER_DESTINATIONS`/`MASTER_VENDOR_CATEGORIES`, `app/constants/master-data.ts` lama — static array, BUKAN `reactive()`); `/admin/users` list+role-switch TANPA CRUD/suspend; `/admin/roles` read-only matrix (LOCKED, TIDAK disentuh section ini); `/admin/audit-trail` 3 filter dropdown atas `ActivityEntry` TANPA search box. `docs/frontend-known-issues.md` bagian 18 mencatat gap ini eksplisit sebelum section berjalan. D-079 (Section 22) secara eksplisit mengantisipasi Section 23 kemungkinan butuh "genuinely new work, not just gap-curation" — dikonfirmasi benar: mayoritas Wajib literal section ini (9 kategori master data baru, Organization Profile, suspend/access-review, audit search) SAMA SEKALI belum ada implementasinya di section mana pun sebelumnya, berbeda dari Section 22 yang murni kurasi gap sempit di atas fondasi besar yang sudah ada.

**Decision:** (1) **Master data 4 kategori lama bermigrasi dari `app/constants/master-data.ts` (static array) menjadi `app/data/master-data.ts` (`reactive()`)** — ID/label/description/`isActive` dipertahankan PERSIS SAMA (tidak ada renumbering/rename apa pun) agar konsumen lama tetap kompatibel. `app/constants/master-data.ts` DIPERTAHANKAN (bukan dihapus) sebagai re-export murni dari `~/data/master-data` — audit grep mengonfirmasi HANYA SATU konsumen aktual di seluruh codebase (`app/pages/admin/master-data.vue`, yang di-rewrite total section ini untuk memakai `~/data` langsung), tapi file lama dipertahankan sebagai jaring pengaman kompatibilitas berbiaya nol, bukan dihapus tanpa kebutuhan struktural (hard rule protokol "jangan menghapus tanpa kebutuhan integrasi"). (2) **11 kategori master data BARU, seluruhnya REFERENCE/CONFIGURATION data dengan batas non-integrasi EKSPLISIT terhadap business logic LOCKED** — `Airport`/`Airline`/`Hotel` (Operational Reference) SENGAJA TIDAK ditautkan sebagai foreign key ke `FlightBooking`/`HotelBooking` (Section 13-14, LOCKED, yang tetap memakai field bebas teks/bisnis apa adanya); `MasterCurrency`/`TaxRule`/`PaymentTerm`/`CancellationRule` (Commercial & Finance) berelasi KONSEPTUAL dengan `Invoice.currency` (Section 20) tapi TIDAK memutasi type `Invoice`, dan TIDAK menyentuh transition guard `CancellationRecord`/`update*BookingStatus` (Section 13-19, LOCKED); `NumberingScheme`/`DocumentTemplate`/`ReadinessGateConfig`/`AssignmentRule` (System Configuration) adalah DISPLAY/CONFIG PREVIEW MURNI — `NumberingScheme.nextNumberPreview` bukan counter nyata yang dipakai `nextSequentialId` (LOCKED, ID generation tetap hardcoded per-fixture), `ReadinessGateConfig` tidak me-rewire derivasi departure-readiness (Section 12, LOCKED), `AssignmentRule` tidak me-rewire mutator lead-routing (Section 04, LOCKED). Batas ini didokumentasikan eksplisit di komentar type/data DAN di UI (deskripsi tiap kategori di `/admin/master-data`), bukan gap tersembunyi. (3) **CRUD generik (`createMasterDataRecord`/`updateMasterDataRecord`/`deactivateMasterDataRecord`/`reactivateMasterDataRecord`, dipanggil dengan `MasterDataCategoryKey`)** — bukan 15×3 fungsi bernama-spesifik per kategori — dipilih karena 15 kategori dengan shape berbeda-beda akan menghasilkan ~45 fungsi thin-wrapper nyaris identik bila dibuat spesifik; brief eksplisit mengizinkan opsi generik. Registry `MASTER_DATA_REGISTRY` (`app/data/index.ts`) memetakan key→{array reactive, prefix ID, label} — page tetap mengimpor tipe spesifik per kategori untuk form-nya sendiri, tidak kehilangan type-safety di titik pemanggilan UI. (4) **Historical snapshot warning (`getMasterDataUsageCount`)** — genuine usage-count check HANYA untuk 4 kategori dengan cross-reference bersih (`currency` exact-match `Invoice.currency`; `destination` fuzzy-match prefix nama kota terhadap `Project.destination` bebas-teks, karena format tidak selalu identik persis mis. "Abu Dhabi, UAE" vs "Abu Dhabi, Uni Emirat Arab"; `project-type`/`service-type` peta ID-tetap→field typed `Project.characteristic`/`ProjectService.type`, HANYA berlaku untuk ID seed asli). 10 kategori lain (`vendor-category` — `Vendor.category` bebas-teks TIDAK match label secara andal, mis. "Flight Consolidator" vs "Maskapai / Airline"; `airport`/`airline`/`hotel`/`tax-rule`/`payment-term`/`cancellation-rule`/`numbering-scheme`/`document-template`/`readiness-gate`/`assignment-rule` — genuinely tanpa consumer existing) mengembalikan `null` — UI menampilkan peringatan GENERIK ("Data ini mungkin digunakan di tempat lain — tidak ada cek referensi otomatis untuk kategori ini"), bukan angka fabrikasi, sesuai instruksi eksplisit "be honest in the UI copy". (5) **Organization Profile sebagai SINGLETON** (`ORGANIZATION_PROFILE`, satu `reactive()` object, bukan array) — diinterpretasikan sebagai profil PERUSAHAAN travel agency itu sendiri (legal name/alamat/kontak/default currency/business hours), BUKAN multi-tenancy (JUDGMENT CALL eksplisit — tidak ada satu pun indikasi sistem multi-tenant di 22 section sebelumnya: `Party`/`Vendor` adalah company EKSTERNAL/counterparty, bukan tenant sistem). Ditempatkan di `/admin/organization` (route baru), discoverable dari kartu ke-5 `/admin` hub dan nav child ke-5 Administration — TIDAK merestrukturisasi 4 link card/nav child existing. (6) **User suspend/reactivate + Access Review** — `User.status: 'active'|'suspended'` (baru), `suspendUser` mandatory-reason (pola SAMA seluruh transisi destruktif lain di codebase ini), `reactivateUser` mengosongkan `suspendedReason`/`suspendedAt`. `USERS` (sebelumnya static array, TIDAK PERNAH dimutasi sejak Foundation) kini `reactive()` — perubahan pertama pada array ini di 22 section. Access Review TIDAK memfabrikasi field "last login" (tidak ada di data model `User`, LOCKED) — scope disederhanakan jujur menjadi: tab filterable user aktif dengan aksi suspend satu-klik, additive di `/admin/users` (tab baru, TIDAK merestrukturisasi list+search+role-switch existing). 2 user diseed `suspended` (`USR-011` Viewer cuti panjang, `USR-016` Supplier vendor tidak aktif) — dipilih KARENA keduanya tidak berada di tengah skenario demo aktif section lain manapun (Viewer read-only, VND-007 tidak dipakai project mana pun), suspend tidak merusak alur demo. (7) **Audit trail SEBAGAI `SystemEvent` (mekanisme SUDAH ADA sejak Prompt 19), BUKAN entitas ketiga** — `SystemEventModule` +`'administration'` (satu union member baru), `pushSystemEvent` (helper baru, `app/data/index.ts`) adalah mutator PERTAMA yang benar-benar menulis ke `SYSTEM_EVENTS` (sebelumnya didokumentasikan sebagai "log statis" di `app/utils/mock-reset.ts`, comment diperbarui menjelaskan perubahan ini). Setiap aksi tulis admin (master data create/edit/deactivate/reactivate, update Organization Profile, suspend/reactivate user) memanggil `pushSystemEvent`. `/admin/audit-trail` mendapat search box (aditif ke 3 filter dropdown existing, TIDAK dihapus/direstrukturisasi) yang mencari lintas `ActivityEntry.message`/`reason`/`impactNote`/`category`/`beforeValue`/`afterValue` (existing) DAN `SystemEvent.message`/`type`/`module` (SectionCard baru "Log Sistem (Non-Project)", dipisah dari list `ActivityEntry` karena scope/shape berbeda — project-scoped vs lintas-modul level-atas). Ini SECARA EKSPLISIT menghormati D-076 "`ActivityEntry` adalah SATU-SATUNYA audit trail untuk perubahan project-scoped" — `SystemEvent` bukan audit trail kedua yang BARU dibuat section ini, melainkan mekanisme TERPISAH yang sudah ada sejak Prompt 19 khusus untuk log admin/lintas-modul non-project-scoped, section ini hanya menambah SATU modul (`'administration'`) padanya. (8) **`/admin/roles` dan role-switch demo TIDAK disentuh sama sekali** — matrix read-only (LOCKED sejak Section 02/old-Section 17) tetap seperti semula, tidak ada kolom/baris baru ditambahkan (Organization Profile berada di bawah `ModuleKey` `administration` yang sudah ada, tidak butuh `ModuleKey` baru).

**Reason:** Migrasi ke `reactive()` (bukan menulis ulang dari nol) dipilih karena literal Wajib section ini secara eksplisit meminta Super Admin dapat "mengelola" (manage) konfigurasi — bukan sekadar melihatnya — dan comment kode existing ("read-only — perubahan production dilakukan oleh Super Admin") mengonfirmasi ini SUDAH scope yang belum diselesaikan sejak Section 17 lama, bukan redesign fitur yang sudah final. CRUD generik dipilih murni pertimbangan maintainability atas 15 kategori heterogen — precedent codebase (fungsi bernama-spesifik) tetap dipertahankan di LAPISAN PAGE (form per kategori tetap spesifik lewat `FieldDef[]`), hanya mutator-level yang digeneralisasi. Batas non-integrasi eksplisit (Airport/Airline/Hotel tidak jadi FK booking, NumberingScheme tidak wire ID generation, dst.) dipilih karena mengintegrasikannya akan berarti me-rewire business logic LOCKED milik section lain (larangan protokol "jangan mengarang integrasi/mesin bisnis nyata" + "jangan mengulang/mendahului pekerjaan section lain") — kategori-kategori ini genuinely BARU tanpa consumer, jadi kejujuran defaultnya adalah "referensi murni", bukan pura-pura terintegrasi. `SystemEvent` (bukan entitas audit baru) dipilih karena mekanisme ini SUDAH ADA sejak Prompt 19 khusus untuk kasus PERSIS ini (log lintas-modul non-project-scoped) — membuat entitas audit ketiga akan melanggar "jangan membuat dataset paralel" tanpa alasan, sementara memaksakan aksi admin ke `ActivityEntry` (project-scoped) akan melanggar D-076 LOCKED secara langsung.

**Consequence:** `docs/frontend-known-issues.md` bagian 18 diperbarui — gap master data terbatas RESOLVED (9 kategori baru + migrasi 4 lama jadi editable), historical snapshot warning RESOLVED (dengan catatan eksplisit 10/15 kategori pakai peringatan generik, bukan count genuine — didokumentasikan sebagai batas jujur, bukan gap tersembunyi). `FlightBooking`/`HotelBooking`/`Invoice`/`CancellationRecord`/`update*BookingStatus`/`nextSequentialId`/departure-readiness derivation/lead-routing mutator (seluruhnya LOCKED, Section 04/12/13-19/20) TIDAK diubah shape/perilakunya sama sekali — nol risiko regresi terhadap business logic yang sudah divalidasi presisi di section-section sebelumnya. `/admin/roles` (LOCKED) TIDAK disentuh. Ini adalah preseden bahwa sebuah section BISA menjadi "genuinely new work" (D-079 mengantisipasi ini) sambil tetap 100% menghormati batas LOCKED section lain lewat referensi/konfigurasi murni — bila Section 24 (Full Regression) menemukan kategori master data lain yang perlu ditambahkan di masa depan, pola registry generik + `FieldDef[]` + batas non-integrasi eksplisit ini dapat dijadikan acuan.

### D-081 — Project Closed Mechanism (Pola D-077 Direplikasi Persis) dan Kebijakan Tidak Menjalankan `eslint --fix` Massal

**Status:** LOCKED · **Date:** 2026-08-01 · **Affected:** `app/types/project.ts` (`ProjectClosureChecklist`+`Project`, aditif), `app/data/index.ts` (+`evaluateProjectClosureGate`/`closeProject`/`getProjectClosureSummary`), `app/pages/projects/[id]/index.vue` (+SectionCard "Project Closure"), `.eslintrc.cjs` (baru), `package.json` (+script `lint`/`typecheck`).

**Context:** Section 24 Wajib literal: "Project Closed membutuhkan: services completed, finance finalized, unresolved issues handled, documents complete, client feedback/final note, closure summary." `ProjectClosureChecklist` sudah ada sebagai SHELL sejak Section 09 (D-066) — 4 item manual, TIDAK digerbangi apa pun, `Project.closedAt` bisa di-set tapi tidak ada mutator yang benar-benar melakukannya lewat gate. Section 20 (D-077) sudah membangun pola identik untuk Finance ("Financial Closure Gate" — `evaluateFinanceClosureGate`/`closeProjectFinance`) untuk mengisi SATU dari 4 item checklist (`financeSettled`) dengan logic nyata. Terpisah, audit lint (Q8, baru terpasang section ini) menghasilkan ~9.428 temuan, hampir seluruhnya gaya kode.

**Decision:** (1) **`evaluateProjectClosureGate(projectId)` → `{ready, blockers[]}`** — derivasi murni, mengecek: `project.status === 'completed'`; seluruh `ProjectService`/`FlightBooking`/`HotelBooking`/`TransportBooking`/`MiceEvent` milik project berstatus terminal; `closureChecklist.financeSettled` (REUSE gate Section 20, TIDAK dihitung ulang logic-nya); tidak ada `Incident` project ini berstatus `open`/`investigating`/`escalated`; tidak ada `ChangeRequest` project ini berstatus `submitted`/`under-review`; tidak ada `Document` project ini berstatus kedaluwarsa (best-effort — TIDAK ADA konsep "daftar dokumen wajib per tipe project" di data model manapun untuk dicek kelengkapannya terhadap, didokumentasikan jujur sebagai keterbatasan, BUKAN pengecekan kelengkapan genuine). (2) **`closeProject(projectId, actorId, finalNote, clientFeedback?)`** — mensyaratkan `evaluateProjectClosureGate(...).ready === true` DAN `finalNote` non-kosong (mandatory-reason, pola SAMA seluruh transisi destruktif/final lain di codebase ini); mengisi 3 field DERIVASI BARU pada `ProjectClosureChecklist` (`servicesCompleted`/`unresolvedIssuesHandled`/`documentsComplete` — snapshot boolean pada saat closing, BUKAN field yang terus live-update setelahnya) + `clientFeedback?`/`finalNote?`; men-stamp `Project.closedAt` (REUSE field existing sejak Section 09, TIDAK diduplikasi jadi field kedua) dan `Project.closedBy` (BARU, aditif); mencatat `ActivityEntry`. (3) **`getProjectClosureSummary(projectId)`** — agregat derivasi murni (total service/booking, invoiced/paid, incident resolved, change request implemented) untuk ditampilkan setelah closing, TIDAK ADA field tersimpan yang bisa stale. (4) **UI**: SectionCard "Project Closure" baru, MENDAMPINGI (bukan menggantikan) Closure Checklist shell existing di tab Overview `/projects/[id]` — 8-tab shell (D-026/D-027, LOCKED) TIDAK ditambah tab baru. Role gate: Management/PM/Super Admin (pola sama Close Finance Section 20). (5) **Kebijakan lint**: `eslint --fix` (auto-fix massal) SENGAJA TIDAK dijalankan terhadap ~9.428 temuan style — akan menghasilkan diff raksasa lintas SELURUH codebase (24 section) yang melanggar prinsip protokol "perubahan lintas section minimal, dicatat, dan regression-tested" serta berisiko menyembunyikan perubahan substantif di antara ribuan perubahan kosmetik saat review. Temuan lint dikatalogkan sebagai backlog non-blocking (`docs/frontend-known-issues.md` bagian 19), bukan dibersihkan.

**Reason:** Project Closed mekanisme mereplikasi PERSIS pola Financial Closure Gate (D-077) karena kedua kasus punya bentuk masalah identik: gate derivasi murni + mutator mandatory-reason yang mengisi field snapshot pada shell yang sudah ada — konsistensi pola mengurangi beban kognitif reviewer dan risiko desain baru yang tidak teruji. `closedAt` REUSE (bukan field kedua) mengikuti prinsip "satu sumber kebenaran" yang konsisten dipakai sepanjang D-070→D-080. Kebijakan tidak menjalankan `eslint --fix` mengikuti instruksi eksplisit protokol Section 24 ("Perbaiki bug nyata tanpa redesign besar") — mass-reformat gaya kode adalah redesign kosmetik besar, bukan bug fix, meski otomatis.

**Consequence:** `ProjectOrderStatus.closed` (D-066, LOCKED — dirivasi dari `project.status === 'completed' && project.closedAt`) kini punya jalur mutator resmi pertama kali untuk benar-benar dicapai lewat UI, bukan hanya lewat manipulasi fixture manual. Tidak ada fixture project berstatus `completed` saat ini — jalur sukses (`ready:true`) TIDAK dapat divalidasi via SSR content check, hanya jalur "belum ready" (blocker list) yang genuinely dapat di-smoke-test — dicatat jujur di `docs/frontend-known-issues.md` bagian 20, dimitigasi code review (pola sama Section 20). Lint 9.428 temuan TETAP ada sebagai backlog eksplisit — siapa pun yang melanjutkan proyek dapat menjalankan `eslint --fix` dengan sadar risiko diff besar, bukan warisan tersembunyi.

### D-082 — Accommodation/`HotelBooking` Schedule-Change Flag: Parity Gap Ditemukan, Sengaja TIDAK Diperbaiki

**Status:** DEFERRED · **Date:** 2026-08-01 · **Affected:** `app/types/booking-orchestration.ts`/`app/types/accommodation.ts` (lokasi type `HotelBooking`, TIDAK diubah section ini).

**Context:** Regresi penuh Section 24 terhadap 4 domain booking (Flight/Hotel/Transport/MICE, Section 13-16) menemukan `FlightBooking.hasScheduleChange`, `TransportBooking.hasChange`, dan `MiceEvent.hasChangeOrder` — tiga dari empat domain punya flag boolean penanda "ada perubahan jadwal/perubahan" yang dipakai widget/indicator terkait. `HotelBooking` TIDAK punya padanan flag ini — satu-satunya dari 4 domain yang tidak konsisten dalam hal ini.

**Decision:** Gap ini DICATAT (`docs/frontend-known-issues.md` bagian 20) tapi SENGAJA TIDAK DIPERBAIKI section ini, meski perbaikannya sendiri sepele (satu field boolean opsional aditif). `HotelBooking` (Section 14, D-071) adalah type LOCKED sejak section itu selesai — Section 24 adalah section PALING AKHIR dari roadmap 25-tahap, dan protokol eksplisit meminta "perubahan lintas section minimal" serta "perbaiki bug nyata tanpa redesign besar". Ini bukan bug (tidak ada perilaku salah/rusak yang teramati — `ChangeRequest`/Section 19 tetap berfungsi penuh untuk Accommodation lewat jalur generik yang tidak bergantung pada flag per-domain ini), murni gap konsistensi kosmetik antar-type.

**Reason:** Menyentuh type LOCKED (`HotelBooking`) di detik-detik terakhir roadmap untuk gap yang TIDAK menyebabkan kerusakan fungsional apa pun dinilai berisiko lebih besar (potensi menyentuh konsumen `HotelBooking` yang tidak terduga, butuh regresi ulang Section 14+18+19 yang saling bertautan) daripada manfaatnya (parity kosmetik). Prinsip "jika ragu, dokumentasikan jangan sentuh" dipilih di atas "perbaiki semua yang ditemukan" karena secara eksplisit inilah section TERAKHIR — tidak ada section berikutnya untuk menangkap regresi bila perbaikan ini ternyata berdampak tak terduga.

**Consequence:** `docs/frontend-known-issues.md` bagian 20 mencatat gap ini sebagai `KNOWN_GAP`, jujur, dengan rekomendasi eksplisit bagi siapa pun yang melanjutkan proyek: tambah `hasScheduleChange?: boolean` aditif murni ke `HotelBooking` — aman dikerjakan KAPAN PUN karena field opsional baru tidak mempengaruhi kode existing, tapi TIDAK dikerjakan sekarang.

## Referensi Silang Open Questions

Lihat `docs/mockup-open-questions.md` untuk status akhir seluruh pertanyaan. Per Section 24 (2026-08-01, FINAL): **Q7** `DEFERRED_PERMANENTLY` (tidak pernah jadi blocker sepanjang roadmap, keputusan final). **Q8** `RESOLVED` (tooling lint/typecheck terpasang). **Q9/Q10/Q11** tetap `DEFERRED` (tidak berubah, tidak pernah blocking). Pertanyaan yang sudah diresolusi lebih awal (Q1–Q6, Q12–Q16) diarsipkan di sana, tidak diulang di sini; hasil resolusinya ada di D-018 s/d D-082 di atas.

---

## Kelompok H — Keputusan Implementasi Foundation (Prompt 5)

Prompt 5 adalah tahap coding pertama. Keputusan berikut bersifat teknis-implementasi (bukan keputusan IA/route/role baru — itu semua sudah LOCKED di Prompt 3), dicatat untuk traceability.

### D-041 — Default Demo User: Super Admin
**Status:** LOCKED · **Date:** 2026-07-29 · **Affected:** `app/composables/useCurrentUser.ts`
**Context:** Current user mock butuh nilai default sebelum user memilih lewat role switcher.
**Decision:** Default demo user adalah `USR-010` (Super Admin) agar seluruh navigasi terlihat penuh saat pertama kali dibuka, bukan role dengan akses terbatas.
**Reason:** Menghindari kebingungan "kenapa menu saya sedikit" saat pertama kali mencoba mockup; role switcher di halaman Settings memungkinkan eksplorasi role lain kapan saja.
**Consequence:** Pilihan role dipersist ke `localStorage` (`manovaCurrentUserId`) agar konsisten antar reload, mengikuti pola persistence existing (`isAuthenticated`/`userEmail`).

### D-042 — Tanggal Acuan Demo Fixed (Bukan `new Date()`)
**Status:** LOCKED · **Date:** 2026-07-29 · **Affected:** `app/utils/attention.ts` (`DEMO_REFERENCE_DATE`)
**Context:** Perhitungan "upcoming departure" dan "invoice overdue" butuh titik acuan "hari ini".
**Decision:** Memakai konstanta `DEMO_REFERENCE_DATE = '2026-07-29'` (sesuai `docs/mockup-data-scenarios.md`), bukan `new Date()` runtime.
**Reason:** Bila memakai tanggal sungguhan, demo akan "rusak" secara visual seiring waktu berjalan (project yang sekarang "upcoming" akan terlihat sudah lewat beberapa bulan kemudian) — tanggal fixed menjaga demo tetap konsisten kapan pun dibuka, sesuai Prompt 0 aturan teknis #12.
**Consequence:** Bila fixture data diperbarui di masa depan dengan tanggal baru, `DEMO_REFERENCE_DATE` harus disesuaikan bersamaan.

### D-043 — Tab Project Detail via Query Param, Role Switcher di Settings
**Status:** LOCKED · **Date:** 2026-07-29 · **Affected:** `app/pages/projects/[id]/index.vue`, `app/pages/settings.vue`
**Context:** Merealisasikan D-027 (tab satu route) dan D-022 (Settings minimal) menjadi kode nyata.
**Decision:** Tab Project Detail disinkronkan ke `?tab=` query param (deep-link tanpa nested route); role switcher demo ditempatkan sebagai section di halaman Settings (bukan dropdown di header/sidebar) agar tidak mengganggu chrome utama.
**Reason:** Konsisten dengan keputusan desain yang sudah LOCKED; Settings sudah punya alasan eksplisit untuk ada (D-022), jadi lokasi switcher di sana tidak menambah menu baru.
**Consequence:** `TopHeader.vue` disederhanakan — title halaman per-route yang dulu hardcoded "Dashboard" dihapus (digantikan `PageHeader` di tiap halaman) karena judul ganda (header + body) dinilai redundan; `TopHeader` kini hanya menampilkan indikator role aktif + notifikasi.

### D-044 — Eksekusi Cleanup Fisik (AIAssistant, `cn.ts`, `.gradient-primary`)
**Status:** LOCKED (dieksekusi) · **Date:** 2026-07-29 · **Affected:** `app/components/dashboard/AIAssistant.vue` (dihapus), `app/utils/cn.ts` (dihapus), `assets/css/tailwind.css` (`.gradient-primary` dihapus)
**Context:** D-023 (Prompt 3) sudah mengunci "tidak dilanjutkan" untuk AIAssistant dengan catatan eksekusi fisik menunggu Prompt 5; D-017 (DEFERRED) mencatat duplikasi `cn()` menunggu momen aman; `.gradient-primary` sudah dikonfirmasi tidak dipakai sejak audit Prompt 1.
**Decision:** Ketiganya dieksekusi (dihapus) di Prompt 5 setelah dependency check ulang mengonfirmasi nol pemakaian.
**Reason:** Prompt 5 secara eksplisit adalah tahap cleanup aman dengan syarat dependency check — ketiga item ini memenuhi syarat (locked decision untuk AIAssistant; nol dependency terverifikasi untuk `cn.ts` dan `.gradient-primary`).
**Consequence:** `app/lib/utils.ts` menjadi satu-satunya sumber `cn()` (menyelesaikan sebagian D-017 — duplikasi Dialog `DialogContent`/`DialogScrollContent` **masih belum** dikonsolidasi, tetap DEFERRED karena risikonya berbeda — keduanya aktif dipakai, bukan dead code).

### D-045 — Tidak Menginstal Tooling Lint/Typecheck di Prompt 5
**Status:** DEFERRED · **Date:** 2026-07-29 · **Affected:** `package.json` (tidak diubah)
**Context:** `docs/mockup-open-questions.md` Q8 mencatat tooling ini "harus diselesaikan di dalam fase Foundation" (ditulis di Prompt 4).
**Decision:** Prompt 5 **tidak** menginstal `eslint` inti maupun `vue-tsc`, meski Q8 sebelumnya menyiratkan penyelesaian di fase ini.
**Reason:** Prompt 5 sendiri tidak secara eksplisit memerintahkan instalasi package baru di teksnya (section N hanya bilang "jalankan lint/typecheck", mengasumsikan tool sudah ada); menginstal package baru tanpa instruksi eksplisit dinilai melampaui scope "cleanup aman dan foundation" dan berisiko dianggap keputusan sepihak yang seharusnya dikonfirmasi dulu.
**Consequence:** Q8 tetap berstatus terbuka/blocking-sebelum-CRM (lihat update di `docs/mockup-open-questions.md`); build tetap divalidasi penuh (sukses) sebagai bentuk quality gate yang tersedia saat ini.

## D-081 — Revisi 9-Modul: RBAC Dinamis, Alur 6 Step Project Order, dan Restrukturisasi Navigasi

**Konteks.** Struktur 26 entri navigasi dan 16 role yang tumbuh organik per-section tidak lagi cocok dengan
kebutuhan bisnis yang diminta klien (9 modul). Selain itu permission bersifat compile-time sehingga
Administration tidak dapat menambah role atau mengatur menu, dan alur menjalankan satu project tersebar di
banyak halaman.

**Keputusan.**

1. **RBAC menjadi data runtime.** `ROLE_MODULE_ACCESS` (konstanta 16×17) diganti empat array `reactive()` di
   `app/data/rbac.ts`: `ROLE_DEFINITIONS`, `ROLE_MODULE_GRANTS`, `ROLE_MENU_GRANTS`, `ROLE_CAPABILITY_GRANTS`.
   `RoleId`/`ModuleKey` menjadi `string` (union TS tidak mungkin untuk role yang dibuat saat runtime);
   kompensasinya `KnownRoleId`/`KnownModuleKey` untuk autocomplete, `resolveModuleKey()` dengan dev-warning,
   dan capability bernama menggantikan perbandingan role literal.

2. **Modul lama tetap valid lewat alias.** `LEGACY_MODULE_ALIAS` (`app/constants/modules.ts`) memetakan
   `project`/`bookings`/`ticketing`/`procurement`/dst. ke 9 modul baru, sehingga 92 dari 108 call-site
   `canView`/`canManage`/`canApprove` tidak perlu disentuh. Hanya 20 site `'crm'` yang bermakna Sales
   disweep manual — `'crm'` adalah key kanonik sekaligus key lama, jadi tidak bisa dialiaskan.

3. **Role 16 → 13.** Digabung lewat `LEGACY_ROLE_ALIAS`: sub-domain operasional + `project-manager` →
   `operations`; `account-executive` + `product-planner` → `sales`; `viewer` → `management`; `procurement`
   → `vendor-partner`; `supplier` → `vendor`. Dijamin tidak ada akses yang hilang oleh test
   `app/data/rbac.test.ts` yang membandingkan terhadap snapshot matriks 16-role lama.

4. **Anti self-lockout adalah syarat, bukan pelengkap.** Tanpa backend, role yang salah dikonfigurasi tidak
   bisa dipulihkan dari server. Karena itu: `super-admin` mem-bypass matriks (`isSuperAdmin`), perubahan yang
   mencabut `administration` dari role pengguna sendiri ditolak, role sistem/berpenghuni tidak bisa dihapus,
   Super Admin terakhir tidak bisa dipindah role, dan `resetRbacToDefaults()` tersedia di `/settings` —
   halaman yang tidak digerbangi modul apa pun.

5. **Alur 6 step Project Order diderivasi, bukan direstrukturisasi.** `ProjectStatus` (8 nilai, LOCKED sejak
   D-028) TIDAK diubah. Step Drafting → Confirmed → Start → Departure → On Progress → Done diturunkan lewat
   `getProjectOrderStep()`, mengikuti preseden `getProjectOrderStatus()` (D-066). Setiap step punya gerbang
   yang seluruhnya memanggil selector yang sudah ada (`getDepartureReadiness`, `getTravelerReadiness`,
   `evaluateProjectClosureGate`) sehingga workspace baru tidak mungkin menjawab berbeda dari halaman lama.

6. **Satu-satunya perubahan pada peta transisi status.** `PROJECT_STATUS_TRANSITIONS['in-progress']`
   ditambah `'ongoing-trip'`. Sebelumnya status `ongoing-trip` mustahil dicapai lewat `updateProjectStatus`
   meski sudah lama ada di `ProjectStatus` dan sudah punya transisi keluar — membuat step Departure → On
   Progress tidak bisa dijalankan. Perubahan bersifat aditif; tidak ada transisi lama yang dihapus.

7. **Modul baru mengekspor selectornya sendiri.** `app/data/index.ts` sudah 6.600+ baris. HR, Inventory,
   Marketing, Finance-ext, CRM-engagement, dan Geo masing-masing berdiri di file sendiri.
   `project-order-workflow.ts` sengaja TIDAK di-re-export dari barrel karena ia mengimpor dari barrel
   tersebut — halaman mengimpornya langsung.

8. **Peta perencanaan tanpa library eksternal.** `RegionMapPicker.vue` memproyeksikan koordinat asli
   (equirectangular) ke SVG inline alih-alih memakai Leaflet/tile server. Konsekuensi yang disadari: peta
   bersifat skematik, bukan peta jalan. Dipilih agar demo berjalan penuh tanpa koneksi internet dan tanpa
   API key; jarak dan posisi relatif tetap akurat karena koordinatnya nyata.

9. **Kalender adalah view, bukan entitas.** `useScheduleEvents()` menggabungkan jadwal yang sudah tersebar
   (project, itinerary, milestone, booking flight/hotel/transport/MICE, maintenance aset) menjadi satu
   `ScheduleEvent[]` terderivasi — tidak ada entitas jadwal baru yang bisa menyimpang dari sumbernya.

10. **`closureChecklist.assetsReturned` akhirnya punya sumber data.** Field ini sudah lama ada tapi tidak
    pernah terisi. Sejak modul Inventory dibangun, `areProjectAssetsReturned()` menjadikannya gerbang nyata
    pada step Done.

**Dihapus.** `app/pages/tasks.vue`, `app/pages/expenses.vue`, `app/pages/projects/create.vue`,
`app/pages/projects/[id]/edit.vue`, dan `app/pages/customer-journey/project-orders/**` — seluruhnya sisa
template awal berbahasa Inggris/USD yang tidak tersambung ke domain MANOVA. Keempatnya di-redirect lewat
`routeRules` di `nuxt.config.ts` agar bookmark lama tidak jatuh ke 404.
