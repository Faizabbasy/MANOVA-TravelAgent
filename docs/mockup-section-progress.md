# Mockup Section Progress — MANOVA

Log **append-only** sesuai `prompts/01-PROTOKOL WAJIB — CONTINUITY, DOKUMENTASI PER SECTION, DAN PERLINDUNGAN HASIL.md` bagian A. Entri baru ditambahkan di bagian bawah; histori lama tidak dihapus atau ditimpa.

Dokumen ini memakai skema field standar protokol (Section ID/nama, Status, Scope, Files, Routes, Components, Data/types/constants, Validation, Known issues, Cross-section impact, Next action) — berbeda dari `docs/mockup-progress.md` yang memakai skema naratif lama (Date/Phase/Completed/Decisions/dst.). Kedua dokumen mencatat fakta yang sama untuk Section 00–05; tidak ada isi yang bertentangan. Entri di bawah adalah **rekonstruksi retroaktif** ke skema baru berdasarkan `docs/mockup-progress.md` Entri 1–6 dan pemeriksaan langsung codebase, disusun saat protokol ini mulai diberlakukan secara formal (2026-07-29) — bukan ditulis real-time saat section aslinya dikerjakan.

Status yang dipakai: `NOT_STARTED`, `IN_PROGRESS`, `COMPLETED`, `BLOCKED`, `NEEDS_REVIEW`, `DEFERRED`.

---

## Section 00 — Konteks Bisnis dan Aturan Kerja

- **Tanggal:** 2026-07-29
- **Status:** COMPLETED
- **Scope dan completed items:** Membaca dan mengonfirmasi domain MANOVA, tipe project, alur bisnis, entitas, 11 role, kebutuhan dashboard, skenario data demo, prinsip reuse, aturan teknis, source of truth dokumentasi, aturan pelaporan. Murni pembacaan konteks — tidak ada kode/halaman/package yang diubah.
- **Files created/changed/removed:** Tidak ada.
- **Routes affected:** Tidak ada.
- **Components reused/created:** Tidak ada.
- **Data/types/constants affected:** Tidak ada.
- **Validation results:** Tidak berlaku (tidak ada kode diubah).
- **Known issues:** Tidak ada pada tahap ini.
- **Cross-section impact:** Tidak ada.
- **Next action:** Lanjut ke Section 01 — Audit Template dan Codebase.

## Section 01 — Audit Template dan Codebase

- **Tanggal:** 2026-07-29
- **Status:** COMPLETED
- **Scope dan completed items:** Audit read-only menyeluruh: foundation project (versi/package/config), struktur codebase, UI & design system, fitur/halaman, data & state, kualitas codebase (build/lint/typecheck/test). Tidak ada implementasi, rename, penggantian data, penghapusan, instalasi package, atau refactor pada tahap ini.
- **Files created/changed/removed:** Dibuat — `docs/template-audit.md`, `docs/mockup-progress.md`.
- **Routes affected:** Tidak ada (audit read-only).
- **Components reused/created:** Tidak ada.
- **Data/types/constants affected:** Tidak ada.
- **Validation results:** `npm run build` sukses (exit 0, dijalankan 2x); lint/typecheck/test tidak dapat dijalankan (tooling belum dikonfigurasi — dicatat sebagai temuan).
- **Known issues:** Data mock (Project/Task/Expense) tidak konsisten lintas halaman (2–3 shape per entitas, 1 project orphan `PRJ-005`); bug nyata `handleDelete` tidak terdefinisi di `expenses.vue`; 9 dari 13 menu sidebar dead-link; tidak ada RBAC/role mock; tidak ada formatter currency/date bersama; tidak ada tooling lint/typecheck/test berfungsi.
- **Cross-section impact:** Tidak ada (temuan menjadi input Section 02).
- **Next action:** Lanjut ke Section 02 — Gap Analysis dan Template Reuse Mapping.

## Section 02 — Gap Analysis dan Template Reuse Mapping

- **Tanggal:** 2026-07-29
- **Status:** COMPLETED
- **Scope dan completed items:** Gap analysis dan reuse mapping murni dokumentasi. Mapping Matrix 9 route existing → kategori reuse; Component Reuse Matrix 19 kebutuhan komponen; Data Model Gap; Navigation Gap (13 menu, 9 dead link); fitur tidak relevan dengan rekomendasi tanpa eksekusi hapus; rekomendasi phasing 11 tahap.
- **Files created/changed/removed:** Dibuat — `docs/template-reuse-mapping.md`, `docs/mockup-scope.md`, `docs/mockup-design-decisions.md`, `docs/mockup-open-questions.md`. Diupdate — `docs/mockup-progress.md`.
- **Routes affected:** Tidak ada (tidak ada route/sidebar diubah, tidak ada halaman baru).
- **Components reused/created:** Tidak ada.
- **Data/types/constants affected:** Tidak ada.
- **Validation results:** Tidak berlaku — tidak ada kode diubah.
- **Known issues:** 8 open question awal (Q1–Q8) dicatat, belum diresolusi.
- **Cross-section impact:** Tidak ada.
- **Next action:** Lanjut ke Section 03 — Information Architecture, Route, Role, dan Workflow.

## Section 03 — Information Architecture, Route, Role, dan Workflow

- **Tanggal:** 2026-07-29
- **Status:** COMPLETED
- **Scope dan completed items:** Finalisasi IA — 9 kelompok baseline dievaluasi; Operations & Travelers melebur jadi tab Project Detail, Vendor tetap top-level; CRM disederhanakan 4 sub-menu. Route inventory lengkap per kelompok. Project Detail dikonsolidasi 6→8 tab final, single-route dengan state client-side. Model Party/Prospect/Client dirinci. Opportunity-to-Project workflow lengkap (diagram + checklist efek Won + role model dua-langkah). Project Status (8 status) dan Service Status generik dirancang. Role & Access Matrix 11 role x 6 modul x 5 level. Dashboard Role Behavior dirancang.
- **Files created/changed/removed:** Dibuat — `docs/mockup-information-architecture.md`, `docs/route-and-role-matrix.md`. Diupdate — `docs/mockup-design-decisions.md` (+14 entri LOCKED), `docs/mockup-open-questions.md` (Q1–Q6 RESOLVED, +Q9), `docs/mockup-scope.md`, `docs/mockup-progress.md`.
- **Routes affected:** Tidak ada perubahan kode — murni rancangan/desain route yang akan diimplementasikan section berikutnya.
- **Components reused/created:** Tidak ada (rancangan reuse dicatat, belum dieksekusi).
- **Data/types/constants affected:** Tidak ada.
- **Validation results:** Tidak berlaku — tidak ada kode diubah.
- **Known issues:** Q7 (vee-validate/zod) dan Q8 (tooling) tetap terbuka; Q9 baru (threshold numerik attention, sengaja ditunda).
- **Cross-section impact:** Keputusan D-018 s/d D-031 di section ini menjadi dasar LOCKED untuk seluruh section implementasi berikutnya (Section 05 ke atas) — termasuk keputusan eksplisit bahwa penghapusan fisik `AIAssistant.vue` (D-023) ditunda sampai Section 05. Lihat `docs/mockup-change-impact-log.md` entri CI-001.
- **Next action:** Lanjut ke Section 04 — Membuat dan Memperbarui Dokumentasi.

## Section 04 — Membuat dan Memperbarui Dokumentasi

- **Tanggal:** 2026-07-29
- **Status:** COMPLETED
- **Scope dan completed items:** Merapikan seluruh `docs/` menjadi source of truth sebelum coding. `mockup-design-decisions.md` direstrukturisasi total ke format decision-record (D-001–D-040). `mockup-scope.md` dilengkapi (objective, context, users, assumptions, constraints, acceptance criteria, DoD). `mockup-data-scenarios.md` dibuat baru (3 skenario: Normal/Manila, High-Change/Abu Dhabi, Complex/Palu + 1 Lost Opportunity + 7 skenario tambahan). `mockup-open-questions.md` direstrukturisasi (ID/Category/Impact/Recommendation/Owner/Status/Blocking). `route-and-role-matrix.md` dan `template-reuse-mapping.md` dilengkapi kolom wajib. Document consistency check lintas 9 file dijalankan.
- **Files created/changed/removed:** Diupdate — `mockup-design-decisions.md`, `mockup-scope.md`, `mockup-open-questions.md`, `route-and-role-matrix.md`, `template-reuse-mapping.md`, `mockup-progress.md`. Dibuat baru — `mockup-data-scenarios.md`.
- **Routes affected:** Tidak ada — tidak ada file `app/**` yang diubah pada tahap ini.
- **Components reused/created:** Tidak ada.
- **Data/types/constants affected:** Tidak ada (skenario data baru masih dokumentasi, belum jadi fixture kode).
- **Validation results:** Tidak berlaku untuk kode; consistency check antar-dokumen dijalankan dan lulus (nama module/route/role/status konsisten, tidak ada referensi file yang tidak ada).
- **Known issues:** Q7 non-blocking; Q8 blocking sebelum implementasi modul (harus selesai dalam fase Foundation); Q9/Q10/Q11 deferred.
- **Cross-section impact:** Tidak ada.
- **Next action:** Lanjut ke Section 05 — Bersihkan Template dan Siapkan Foundation (menunggu perintah user).

## Section 05 — Bersihkan Template dan Siapkan Foundation

- **Tanggal:** 2026-07-29
- **Status:** COMPLETED (foundation coding pertama; modul bisnis penuh — CRM, Opportunity, Operations, Vendor, Finance — **belum** dikerjakan, sesuai batasan tahap ini)
- **Scope dan completed items:** Coding pertama di atas seluruh dokumentasi Section 00–04. App identity rebrand ke MANOVA; navigation dibangun ulang dari satu source of truth (`NAV_ITEMS`) dengan role-visibility mock; type foundation, status/role constants, dummy data terpusat (3 project + 4 party + fixture terkait), formatter Rupiah/tanggal, helper attention, current user & role mock + permission helper; 11 shared UI component + primitive `ui/tabs`; route foundation aktif untuk seluruh IA (sebagian shell nyata dengan fixture, sebagian `ModulePlaceholder` berlabel). Detail lengkap: `docs/mockup-section-reports/section-05-foundation.md`.
- **Files created:** 8 file type (`app/types/*.ts`), 3 file constant (`app/constants/*.ts`), 8 file data + barrel (`app/data/*.ts`), `app/utils/format.ts`, `app/utils/attention.ts`, `app/composables/useCurrentUser.ts`, `app/composables/usePermissions.ts`, 11 komponen `app/components/shared/*.vue`, 4 file + index `app/components/ui/tabs/`, route baru `app/pages/crm/*`, `app/pages/vendors/index.vue`, `app/pages/finance/*`, `app/pages/reports/index.vue`, `app/pages/admin/*`, `app/pages/settings.vue`.
- **Files changed:** `nuxt.config.ts`, `app/components/layout/AppSidebar.vue`, `app/components/layout/TopHeader.vue`, `app/pages/index.vue`, `app/pages/login.vue`, `app/pages/projects/index.vue`, `app/pages/projects/[id]/index.vue`, `assets/css/tailwind.css`.
- **Files removed:** `app/utils/cn.ts` (duplikat `lib/utils.ts`, dependency-checked), `app/components/dashboard/AIAssistant.vue` (sesuai D-023, dependency-checked).
- **Routes affected:** Lihat `docs/mockup-implementation-state.md` bagian 2 untuk klasifikasi lengkap (aktif-nyata vs placeholder vs lama-tidak-ditautkan vs excluded).
- **Components reused:** 15 primitive `ui/*` lama, `dashboard/StatsCard.vue`, layout `dashboard.vue`/`AppSidebar.vue`/`TopHeader.vue` (struktur dipertahankan), `useSidebar.ts`/`useIsMobile.ts`.
- **Components created:** 11 shared component + 4 primitive `ui/tabs` (rincian di atas).
- **Data/types/constants affected:** Seluruh type/constant/fixture di atas dibuat baru pada section ini (belum ada sebelumnya).
- **Validation results:** `pnpm exec nuxt prepare` sukses; `pnpm run build` sukses (exit 0, 3x run); lint tidak dapat dijalankan (belum ada script/`eslint` core); typecheck tidak dapat dijalankan (`vue-tsc` belum terpasang); test — "No test files found" (pre-existing, bukan regresi); smoke test manual 11 route via curl — seluruhnya HTTP 200. **Divalidasi ulang saat penyusunan dokumen continuity ini (2026-07-29): build kembali sukses, hasil test/lint/typecheck konsisten dengan temuan asli.**
- **Known issues:** Q8 (tooling lint/typecheck) tetap terbuka, eksplisit blocking sebelum Section 06; bug `handleDelete` di `expenses.vue` belum diperbaiki (route belum ditautkan navigasi).
- **Cross-section impact:** Eksekusi keputusan D-023 (removal `AIAssistant.vue`, diputuskan Section 03) — lihat `docs/mockup-change-impact-log.md` entri CI-001.
- **Next action:** Selesaikan Q8 (tooling lint/typecheck) sebelum atau di awal Section 06 (Dashboard), lalu lanjut sesuai phasing `docs/template-reuse-mapping.md` bagian I. Menunggu perintah user — tidak dieksekusi otomatis.

## Section 06 — Dashboard

- **Tanggal:** 2026-07-29
- **Status:** COMPLETED
- **Scope dan completed items:** Menyelesaikan Dashboard final di `/` berbasis fixture terpusat, role-aware, sesuai `docs/route-and-role-matrix.md` bagian 6 (D-031, LOCKED). Widget diimplementasikan 1:1 per role: Management/Viewer (Opportunity Pipeline, Active Projects by Status, Budget vs Actual, Outstanding Invoices, Attention, Recent Activity), Sales (Pipeline, Quotations Menunggu Keputusan), Project Manager (Active Projects Milik Saya, Upcoming Departures, Attention Milik Saya, Milestone/Task Mendatang, Change History Ringkas), Operations/Ticketing/Accommodation/Transportation/MICE (Service Readiness ter-scope per sub-domain, Upcoming Departures), Finance (Budget vs Actual, Cost Breakdown, Outstanding), Super Admin (superset + Ringkasan Administrasi). Filter (status, tipe project, client, owner, periode keberangkatan) ditambahkan dan berlaku ke seluruh widget berbasis Project.
- **Files created:** `app/components/shared/StatusBreakdownList.vue`.
- **Files changed:** `app/pages/index.vue` (rewrite total), `app/components/dashboard/BudgetChart.vue`, `ExpenseCategories.vue`, `RecentActivity.vue` (diadaptasi dari hardcoded fake data ke fixture nyata), `app/utils/attention.ts` (+`isTaskUpcoming`, +`UPCOMING_TASK_WINDOW_DAYS`), `app/data/index.ts` (+4 selector), `app/data/opportunities.ts` (+3 Opportunity, +2 Quotation), `app/data/activity.ts` (+2 Task).
- **Files removed:** Tidak ada.
- **Routes affected:** `/` (final, sebelumnya shell Section 05).
- **Components reused:** `StatsCard.vue` (apa adanya), 15 primitive `ui/*` termasuk `ui/select` (pemakaian nyata pertama).
- **Components created:** `StatusBreakdownList.vue`.
- **Data/types/constants affected:** Fixture `OPPORTUNITIES`/`QUOTATIONS`/`TASKS` diperluas (lihat `docs/mockup-change-impact-log.md` CI-002); tidak ada perubahan `app/types/*`/`app/constants/*`.
- **Validation results:** `npx nuxi prepare` sukses; `npm run build` sukses (2x run); smoke test curl 9 route → seluruhnya HTTP 200; verifikasi tekstual bundle server mengonfirmasi widget baru ter-compile; `npx vitest run` → 0 test file (pre-existing); `npx nuxi typecheck` → gagal, `vue-tsc` tidak terpasang (Q8, pre-existing); lint tidak tersedia (Q8, pre-existing). Verifikasi browser interaktif/hydrated **tidak dilakukan** — tidak ada tool headless browser tersedia di lingkungan ini.
- **Known issues:** Q8 tetap terbuka (user memerintahkan Section 06 dikerjakan langsung tanpa menyelesaikannya lebih dulu); "Follow-up/activity mendatang milik sendiri" (Sales) belum dapat diimplementasikan (data model Activity level-Party/Opportunity belum ada); "Cost breakdown" Finance per-project bukan per jenis layanan (keterbatasan fixture).
- **Cross-section impact:** Lihat `docs/mockup-change-impact-log.md` CI-002 (perluasan fixture Section 05) dan CI-003 (adaptasi komponen dashboard dan rewrite halaman Dashboard Section 05).
- **Next action:** Section 07 — CRM Party. Rekomendasi eksplisit: selesaikan Q8 (tooling lint/typecheck) sebelum atau di awal Section 07. Menunggu perintah user — tidak dieksekusi otomatis.

## Section 07 — CRM Party

- **Tanggal:** 2026-07-29
- **Status:** COMPLETED
- **Scope dan completed items:** Party/customer list dan detail, Prospect list, Client list, Contact person, Activity history (level-Party, entitas baru `PartyActivity`), summary opportunity/project, search/filter/sort, create/edit frontend mock (Prospect, Contact, Activity — mutasi nyata via `reactive()` fixture), empty/not-found states, role behavior. Party Detail 5-tab: Overview/Contacts/Opportunities/Activities/Projects* (kondisional Client + ≥1 project). Widget Dashboard Sales "Follow-up Mendatang" (deferred Section 06) diisi.
- **Files created:** `app/pages/crm/parties/[id]/index.vue`.
- **Files changed:** `app/pages/crm/prospects.vue`, `app/pages/crm/clients.vue` (rewrite total dari `ModulePlaceholder`), `app/pages/index.vue` (+widget Follow-up Mendatang, minimal), `app/data/parties.ts` (`reactive()` + `PARTY_ACTIVITIES`), `app/data/index.ts` (+6 selector/helper), `app/types/party.ts` (+3 type), `app/constants/status.ts` (+`PARTY_ACTIVITY_TYPES`), `app/constants/navigation.ts` (`comingSoon` dihapus dari Prospects/Clients), `app/utils/attention.ts` (+2 export), `app/components/shared/SectionCard.vue` (+slot `#actions`, backward-compatible).
- **Files removed:** Tidak ada.
- **Routes affected:** `/crm/prospects` (real), `/crm/clients` (real), `/crm/parties/[id]` (baru).
- **Components reused:** `SectionCard`, `EmptyState`, `RoleAccessState`, `StatusBadge`, `DetailMetadataList`, `PageHeader`, `Tabs`/`TabsList`/`TabsTrigger`/`TabsContent`, `Table*`, `Dialog*`, `Label`, `Input`, `Button` (Dialog dipakai nyata pertama kali di luar `expenses.vue`).
- **Components created:** Tidak ada file baru (SectionCard diperluas, bukan komponen baru).
- **Data/types/constants affected:** `Party`/`ContactPerson` tidak berubah shape; `+PartyActivity`. `PARTIES`/`CONTACTS`/`PARTY_ACTIVITIES` jadi `reactive()` — lihat CI-004.
- **Validation results:** `npx nuxi prepare` dan `npm run build` sukses; smoke test curl 9 route (termasuk `/crm/parties/PTY-999` not-found) seluruhnya HTTP 200 dengan konten nyata terkonfirmasi; `vitest`/`typecheck`/lint tetap pre-existing gap (Q8). Interactive/hydrated browser verification tidak dilakukan (keterbatasan tooling).
- **Known issues:** Q8 tetap terbuka (section ketiga berturut-turut); pagination mock sengaja tidak dibuat (volume data terlalu kecil untuk affordance nyata); `/crm/opportunities` belum di-groupby-stage (Section 08).
- **Cross-section impact:** `docs/mockup-change-impact-log.md` CI-004 (fixture Party jadi reactive + entitas baru, milik Section 05), CI-005 (widget Dashboard Follow-up Mendatang, milik Section 06), CI-006 (SectionCard `#actions` slot, milik Section 05).
- **Next action:** Section 08 — Opportunity dan Quotation. Rekomendasi kuat: selesaikan Q8 sebelum atau di awal Section 08. Menunggu perintah user — tidak dieksekusi otomatis.

## Section 08 — Opportunity dan Quotation

- **Tanggal:** 2026-07-30
- **Status:** COMPLETED
- **Scope dan completed items:** Opportunity list (`/crm/opportunities`) dengan pipeline visualization (reuse `StatusBreakdownList`), filter stage/party/search; Opportunity Detail (`/crm/opportunities/[id]`, baru) dengan stage stepper (Draft s/d WonRequested, Lost, On Hold), Owner/value/requirement/destination/travel date/traveler estimate/service scope, Quotation summary dengan create-mock (saat lanjut ke Proposal) dan revisi (version mock), Activity/follow-up (reuse `PartyActivity` Section 07 dengan `opportunityId` opsional baru). Won penuh (approve + Project) sengaja TIDAK dikerjakan (scope Section 09).
- **Files created:** `app/pages/crm/opportunities/index.vue`, `app/pages/crm/opportunities/[id]/index.vue`.
- **Files changed:** `app/types/opportunity.ts` (+7 field `Opportunity`, +2 field `Quotation`), `app/types/party.ts` (+`opportunityId?` pada `PartyActivity`), `app/data/opportunities.ts` (`reactive()`, field lengkap, QUO-005 versi 2), `app/data/parties.ts` (backfill `opportunityId`), `app/data/index.ts` (+5 selector/mutator), `app/pages/crm/parties/[id]/index.vue` (tab Opportunities kini link ke detail).
- **Files removed:** `app/pages/crm/opportunities.vue` (dipindah isinya ke `opportunities/index.vue` — lihat bug routing di bawah).
- **Routes affected:** `/crm/opportunities` (rewrite lokasi file), `/crm/opportunities/[id]` (baru).
- **Components reused:** `StatusBreakdownList`, `SectionCard` (+`#actions`), `Dialog*`, `Table*`, `StatusBadge`, `DetailMetadataList`, `PageHeader`, `EmptyState`, `RoleAccessState`.
- **Components created:** Tidak ada.
- **Data/types/constants affected:** `Opportunity`/`Quotation` diperluas (lihat bagian Files changed); `OPPORTUNITIES`/`QUOTATIONS` jadi `reactive()` (CI-007).
- **Validation results:** `nuxi prepare` + `npm run build` sukses 2x. **Bug ditemukan saat verifikasi konten** (bukan hanya status code): `/crm/opportunities/[id]` ter-resolve ke 404 catch-all karena konflik file datar `opportunities.vue` vs direktori `opportunities/[id]/`, meski build sukses dan curl awal melaporkan HTTP 200. Diperbaiki dengan memindah list page ke `opportunities/index.vue`; rebuild dan re-verifikasi konten (title dinamis, string spesifik per skenario) mengonfirmasi benar. `vitest`/`typecheck`/lint tetap pre-existing gap (Q8).
- **Known issues:** Q8 tetap terbuka (4 section berturut-turut); "Stepper adaptasi create.vue" dan "pipeline chart baru" dari rancangan awal disederhanakan (badge stepper, reuse StatusBreakdownList) — didokumentasikan, bukan penyimpangan diam-diam.
- **Cross-section impact:** `docs/mockup-change-impact-log.md` CI-007 (fixture Opportunity/Quotation jadi reactive + field baru, milik Section 05/06), CI-008 (PartyActivity `opportunityId` + backfill, milik Section 07), CI-009 (Party Detail Opportunities tab ditaut ke detail, milik Section 07).
- **Next action:** Section 09 — Opportunity Won to Project. Rekomendasi sangat kuat: selesaikan Q8 sebelum Section 09. Menunggu perintah user — tidak dieksekusi otomatis.

## Section 09 — Opportunity Won to Project

- **Tanggal:** 2026-07-30
- **Status:** COMPLETED
- **Scope dan completed items:** Permission check (`canApprove('crm')` — Management/Super Admin), confirmation dialog (Approve dan Reject), requirement validation (`getOpportunityMissingRequirements` — destinasi/tanggal/traveler/quotation), stage `WonRequested → Won`, Project otomatis dibuat (seluruh checklist LOCKED bagian 2.2: partyId+lifecycle transition, data dasar, service scope, budget/quotation reference, activity log, toast feedback, redirect ke Project Detail), duplicate prevention (guard `projectId` sudah ada), mock persistence jujur (`reactive()` array, bukan backend).
- **Files created:** `app/composables/useToast.ts`, `app/components/shared/ToastContainer.vue`.
- **Files changed:** `app/types/project.ts` (+`sourceQuotationId?`), `app/data/projects.ts` (`reactive()`), `app/data/activity.ts` (`reactive()`), `app/data/opportunities.ts` (OPP-005 → `won-requested`), `app/data/index.ts` (+`getOpportunityMissingRequirements`, `+approveOpportunityWon`, `+rejectOpportunityWon`), `app/layouts/dashboard.vue` (+`<ToastContainer />`), `app/pages/crm/opportunities/[id]/index.vue` (+Approve/Reject UI, requirement validation display).
- **Files removed:** Tidak ada.
- **Routes affected:** Tidak ada route baru — interaksi terjadi di `/crm/opportunities/[id]` (Section 08), hasil muncul di `/projects`/`/projects/[id]` (Section 05).
- **Components reused:** `Dialog*`, `Button`, `Label`, `Input`, `SectionCard`.
- **Components created:** `ToastContainer.vue` (diekstrak dari pola lokal `expenses.vue`).
- **Data/types/constants affected:** `Project.sourceQuotationId` (baru); `PROJECTS`/`ACTIVITIES` jadi `reactive()`; `OPP-005` dimajukan ke `won-requested` (seed, demonstrasi siap-pakai).
- **Validation results:** `nuxi prepare` + `npm run build` sukses. Smoke test konten (bukan hanya status code, mengikuti pelajaran Section 08) mengonfirmasi kondisi pra-aksi benar: `OPP-005` menampilkan tombol Approve/Reject untuk Super Admin, `PTY-004` masih Prospect, `/projects` masih 3 project. **Verifikasi klik-interaktif end-to-end tidak dapat dilakukan** (tidak ada tool browser headless) — dimitigasi lewat code review ketat terhadap checklist LOCKED. `vitest`/`typecheck`/lint tetap pre-existing gap (Q8).
- **Known issues:** Q8 tetap terbuka (5 section berturut-turut); verifikasi interaktif end-to-end tidak dilakukan langsung; `DEFAULT_PROJECT_OWNER_ID` (PM default) adalah keputusan sementara tanpa alur assignment manual.
- **Cross-section impact:** `docs/mockup-change-impact-log.md` CI-010 (Project/Activity fixture jadi reactive + field baru, milik Section 05), CI-011 (toast diekstrak dari `expenses.vue`, milik Section 05/pre-existing template), CI-012 (OPP-005 dimajukan ke won-requested, milik Section 08).
- **Next action:** Section 10 — Project Core. Rekomendasi sangat kuat: selesaikan Q8 sebelum Section 10. Menunggu perintah user — tidak dieksekusi otomatis.

## Section 10 — Project Core

- **Tanggal:** 2026-07-30
- **Status:** COMPLETED
- **Scope dan completed items:** Project list dengan search/filter (status, tipe, client, owner)/sort (tanggal, nama, budget); status/type/client/destination/travel date/owner/progress (bar linear berdasarkan urutan status)/attention seluruhnya tampil di card; Project Detail Overview tab diperkaya dengan Service Summary, Milestone/Task Summary, Document Summary, dan Recent Activity (reuse `StatusBreakdownList`); conditional section berdasarkan service (sudah alami dari data PROJECT_SERVICES); tiga skenario (Normal/High-Change/Complex) diverifikasi konsisten; loading/empty/not-found states dipertahankan/diperkaya.
- **Files created:** Tidak ada.
- **Files changed:** `app/pages/projects/index.vue` (filter/sort/progress ditambahkan), `app/pages/projects/[id]/index.vue` (Overview tab diperkaya, tab lain tidak diubah).
- **Files removed:** Tidak ada.
- **Routes affected:** `/projects`, `/projects/[id]` (tidak ada route baru).
- **Components reused:** `StatusBreakdownList`, `SectionCard` (+`#actions`), `EmptyState`, `StatusBadge`, `AttentionIndicator`.
- **Components created:** Tidak ada.
- **Data/types/constants affected:** Tidak ada — murni presentasi di atas data existing.
- **Validation results:** `nuxi prepare` + `npm run build` sukses. Smoke test konten (bukan hanya status code) memverifikasi progress bar (40%/20%/60% untuk PRJ-101/102/103, dihitung ulang manual dan cocok), breakdown Service/Task Summary cocok persis fixture, document count benar, regresi tab lain (`?tab=itinerary-services/tasks/finance`) tidak berubah. `vitest`/`typecheck`/lint tetap pre-existing gap (Q8). Verifikasi interaktif klik filter/dropdown tidak dilakukan (tidak ada browser headless).
- **Known issues:** Q8 tetap terbuka (6 section berturut-turut); `ProjectsTable.vue` sengaja tidak disatukan (didokumentasikan, bukan gap tersembunyi — card-grid existing sudah memenuhi seluruh scope literal).
- **Cross-section impact:** Tidak ada entri change-impact-log baru — Section 10 menyelesaikan kepemilikan yang memang didesain untuknya (Section 05 secara eksplisit men-defer isi Overview/filter list ke "phase later", bukan modifikasi tak terduga atas section lain).
- **Next action:** Section 11 — Traveler and Participant. Rekomendasi sangat kuat: selesaikan Q8 sebelum Section 11. Menunggu perintah user — tidak dieksekusi otomatis.

## Section 11 — Traveler and Participant

- **Tanggal:** 2026-07-30
- **Status:** COMPLETED
- **Scope dan completed items:** Mengisi penuh tab "Travelers" pada Project Detail (baseline Foundation sejak Section 05): traveler/participant list dan profile, group dan rooming list, passport/travel document metadata, emergency contact dan special request, missing document indicator, add/edit/remove/import mock, filter (search/group/dokumen belum lengkap), states, dan role behavior (`canManageTravelers` — PM/Super Admin). Tab lain Project Detail tidak disentuh; Overview (Section 10) tidak diubah sama sekali.
- **Files created/changed/removed:** Dibuat — `docs/mockup-section-reports/section-11-traveler-participant.md`. Diubah — `app/types/project.ts` (`Traveler`/`TravelerGroup` diperluas, `+RoomType`, `+RoomAssignment`), `app/data/projects.ts` (`TRAVELER_GROUPS`/`TRAVELERS` jadi `reactive()`, fixture diperluas 1→18 baris, `+ROOM_ASSIGNMENTS`), `app/data/index.ts` (+7 selector/mutator traveler), `app/utils/attention.ts` (+`isTravelerDocumentMissing`, `+PASSPORT_EXPIRY_WARNING_DAYS`), `app/constants/status.ts` (`+ROOM_TYPES`), `app/pages/projects/[id]/index.vue` (tab Travelers ditulis ulang total).
- **Routes affected:** Tidak ada route baru — `/projects/[id]` (tab `travelers`) diisi penuh.
- **Components reused/created:** Reused — `Dialog*`, `Label`, `Input`, `Button`, `Table*`, `SectionCard`, `StatusBadge`, `EmptyState`, `useToast`/`ToastContainer`, dan `Checkbox` (pemakaian nyata pertama). Tidak ada komponen file baru.
- **Data/types/constants affected:** `Traveler`/`TravelerGroup` diperluas (aditif), `+RoomAssignment` (entitas baru), `+ROOM_TYPES` (constant baru). Lihat CI-013.
- **Validation results:** `nuxi prepare` + `npm run build` sukses. Smoke test konten (curl+grep) mengonfirmasi 6/6 traveler "Dokumen Lengkap" di PRJ-101, 2/6 "Dokumen Belum Lengkap" di PRJ-102 (paspor expiring + belum diisi), 1/6 "Dokumen Belum Lengkap" di PRJ-103, rooming list dan group filter tampil benar; regresi tab lain (`?tab=itinerary-services`, `?tab=finance`, `?tab=overview`) dikonfirmasi tidak berubah. `vitest`/`typecheck`/lint tetap pre-existing gap (Q8).
- **Known issues:** Q8 tetap terbuka (tujuh section berturut-turut); CRUD `TravelerGroup` sengaja tidak diimplementasikan (di luar scope literal); profil traveler tetap sampel representatif (6/6/6 dari headcount 6/18/60), didokumentasikan transparan; verifikasi interaktif klik/toggle tidak dilakukan (keterbatasan tooling).
- **Cross-section impact:** `docs/mockup-change-impact-log.md` CI-013 (Traveler/TravelerGroup jadi reactive + field baru + entitas RoomAssignment, milik Section 05).
- **Next action:** Section 12 — Itinerary and Operations. Rekomendasi sangat kuat: selesaikan Q8 sebelum Section 12. Menunggu perintah user — tidak dieksekusi otomatis.

## Section 12 — Itinerary and Operations

- **Tanggal:** 2026-07-30
- **Status:** COMPLETED
- **Scope dan completed items:** Mengisi penuh tab "Itinerary & Services" pada Project Detail (baseline Foundation sejak Section 05): daily itinerary, conditional sub-section Flight/Hotel/Transportation/MICE/Additional Service, booking/reference mock, service status dan readiness (dengan update status role-gated), operational tasks preview, timeline/list pattern (bukan kalender baru), change markers untuk High-Change Project, states, dan role behavior granular per sub-domain (`canManageServiceType`). Tab lain (Vendors, Finance) tidak disentuh; Overview dan Travelers (Section 10/11) tidak diubah kodenya.
- **Files created/changed/removed:** Dibuat — `docs/mockup-section-reports/section-12-itinerary-operations.md`. Diubah — `app/types/project.ts` (`ServiceTypeKey` +`additional`, `ProjectService` +`bookingReference`, `+ItineraryItem`), `app/data/projects.ts` (`PROJECT_SERVICES` jadi `reactive()`, +`bookingReference` pada 5 baris, +1 baris `SVC-1036` additional, `+ITINERARY_ITEMS` 15 baris), `app/data/index.ts` (+`getItineraryItems`, `+updateServiceStatus`), `app/constants/status.ts` (`SERVICE_TYPES` +`additional`), `app/utils/format.ts` (+`formatDayLabel`), `app/pages/projects/[id]/index.vue` (tab Itinerary & Services ditulis ulang total).
- **Routes affected:** Tidak ada route baru — `/projects/[id]` (tab `itinerary-services`) diisi penuh.
- **Components reused/created:** Reused — `SectionCard`, `Table*`, `StatusBadge`, `EmptyState`, `Button`, `useToast`/`ToastContainer`, pola list `divide-y` existing. Tidak ada komponen file baru.
- **Data/types/constants affected:** `ProjectService` diperluas (aditif), `+ItineraryItem` (entitas baru), `SERVICE_TYPES` +`additional` (constant). Lihat CI-014.
- **Validation results:** `nuxi prepare` + `npm run build` sukses. Smoke test konten (curl+grep) mengonfirmasi conditional service section bekerja tepat (PRJ-101 hanya Flight, tidak ada Hotel/Transportation/MICE), banner "Penanda Perubahan" tampil untuk PRJ-102, "Additional Service" tampil untuk PRJ-103 dengan booking reference, readiness fraction presisi per section (Flight 1/2, Hotel 1/1, Transportation 0/1, MICE 1/1, Additional 1/1). Efek fixture ke Overview Service Summary (Confirmed 3→4) diverifikasi presisi, bukan diasumsikan. `vitest`/`typecheck`/lint tetap pre-existing gap (Q8).
- **Known issues:** Q8 tetap terbuka (delapan section berturut-turut); daily itinerary read-only (tidak diminta CRUD di scope); CRUD `ProjectService` (tambah/hapus baris) tidak diimplementasikan (di luar scope literal — hanya update status); verifikasi interaktif klik/ganti-role tidak dilakukan (keterbatasan tooling).
- **Cross-section impact:** `docs/mockup-change-impact-log.md` CI-014 (ProjectService jadi reactive + field baru + entitas ItineraryItem, milik Section 05; dampak terverifikasi ke Service Summary Overview, milik Section 10).
- **Next action:** Section 13 — Vendor Management. Rekomendasi sangat kuat: selesaikan Q8 sebelum Section 13. Menunggu perintah user — tidak dieksekusi otomatis.

## Section 13 — Vendor Management

- **Tanggal:** 2026-07-30
- **Status:** COMPLETED
- **Scope dan completed items:** Membangun `/vendors` (list, filter, create — Super Admin), `/vendors/[id]` (Vendor Detail baru, 4 tab Overview/Services/Quotations/Contacts), dan mengisi tab "Vendors" Project Detail (baseline Foundation) dengan vendor assignment per service, perbandingan quotation, dan aksi Accept/Reject yang mengubah confirmation status (reuse `updateServiceStatus` Section 12). Ditemukan dan dikoreksi inkonsistensi dokumentasi lama (`docs/route-and-role-matrix.md` bagian 0/1.4) soal role Vendor yang tidak sinkron dengan kode `ROLE_MODULE_ACCESS` sejak Foundation.
- **Files created/changed/removed:** Dibuat — `app/pages/vendors/[id]/index.vue`, `docs/mockup-section-reports/section-13-vendor-management.md`. Diubah — `app/types/vendor.ts` (+`VendorContact`, `+VendorQuotation`, `+VendorActivity`, `+VendorDetailTab`), `app/data/vendors.ts` (`VENDORS` jadi `reactive()`, `+VENDOR_CONTACTS`/`VENDOR_QUOTATIONS`/`VENDOR_ACTIVITIES`), `app/data/index.ts` (+9 selector/mutator), `app/constants/status.ts` (`+VENDOR_QUOTATION_STATUSES`), `app/pages/vendors/index.vue` (ditulis ulang), `app/pages/projects/[id]/index.vue` (tab Vendors ditulis ulang).
- **Routes affected:** `/vendors/[id]` (baru); `/vendors` (Section 05 shell) diisi penuh.
- **Components reused/created:** Reused — `Dialog*`, `Table*`, `SectionCard`, `StatusBadge`, `EmptyState`, `Tabs*`, `DetailMetadataList`, `useToast`, dan `canManageServiceType` (Section 12, reuse langsung untuk gerbang Accept/Reject). Tidak ada komponen file baru.
- **Data/types/constants affected:** `Vendor` jadi `reactive()`; `+VendorContact`/`VendorQuotation`/`VendorActivity` (entitas baru); `+VENDOR_QUOTATION_STATUSES` (constant baru). Lihat CI-015.
- **Validation results:** `nuxi prepare` + `npm run build` sukses. Smoke test konten (curl+grep) mengonfirmasi vendor list/detail/not-found benar, skenario comparison Ground Transportation (PRJ-103, Rp45jt vs Rp52jt) tampil tepat dengan tombol Terima/Tolak, regresi tab lain Project Detail tidak berubah. `vitest`/`typecheck`/lint tetap pre-existing gap (Q8).
- **Known issues:** Q8 tetap terbuka (sembilan section berturut-turut); CRUD hapus vendor/contact/quotation tidak diimplementasikan (di luar scope literal); verifikasi interaktif klik Accept/Reject/ganti-role tidak dilakukan (keterbatasan tooling).
- **Cross-section impact:** `docs/mockup-change-impact-log.md` CI-015 (Vendor jadi reactive + entitas baru, milik Section 05; tab Vendors Project Detail diisi, belum ada owner sebelumnya; koreksi dokumentasi role Vendor di route-and-role-matrix.md bagian 0/1.4).
- **Next action:** Section 14 — Project Changes. Rekomendasi sangat kuat: selesaikan Q8 sebelum Section 14. Menunggu perintah user — tidak dieksekusi otomatis.

## Section 14 — Project Changes

- **Tanggal:** 2026-07-30
- **Status:** COMPLETED
- **Scope dan completed items:** Mengisi penuh tab "Activity & Changes" Project Detail: change list/detail (category, reason, requester, before/after), dampak traveler/itinerary/service/vendor/budget, status dan approval mock (Setujui/Tolak, `canApprove('project')`), change timeline kronologis, attention indicator, dan role access (`canLogChange` untuk mengajukan). `ActivityEntry` diperluas aditif — bukan entitas Change paralel (LOCKED IA bagian 4). 4 entri `CHG-*` existing diperkaya (bukan record baru); `CHG-1023` (PRJ-102) jadi skenario approval hidup (`pending`).
- **Files created/changed/removed:** Dibuat — `docs/mockup-section-reports/section-14-project-changes.md`. Diubah — `app/types/activity.ts` (`+ChangeCategory`, `+ChangeApprovalStatus`, `ActivityEntry` +7 field opsional), `app/data/activity.ts` (4 baris `CHG-*` diperkaya), `app/constants/status.ts` (`+CHANGE_CATEGORIES`, `+CHANGE_APPROVAL_STATUSES`), `app/data/index.ts` (+`createChangeEntry`/`approveChangeEntry`/`rejectChangeEntry`), `app/pages/projects/[id]/index.vue` (tab Activity & Changes diperkaya).
- **Routes affected:** Tidak ada route baru — `/projects/[id]` (tab `activity-changes`) diisi penuh.
- **Components reused/created:** Reused — `Dialog*`, `Label`, `Input`, `Button`, `SectionCard`, `StatusBadge`, `AttentionIndicator`, `EmptyState`, `useToast`. Tidak ada komponen file baru.
- **Data/types/constants affected:** `ActivityEntry` diperluas (aditif), `+CHANGE_CATEGORIES`/`CHANGE_APPROVAL_STATUSES` (constant baru). Lihat CI-016.
- **Validation results:** `nuxi prepare` + `npm run build` sukses. Smoke test konten (curl+grep) mengonfirmasi 3 change PRJ-102 (2 Disetujui, 1 Menunggu Approval dengan tombol Setujui/Tolak), before/after dan requester tampil tepat, 1 change PRJ-103 (Disetujui, 20→25 pax). Regresi tab lain (Overview/Travelers/Itinerary & Services/Vendors) dan Dashboard tidak berubah. `vitest`/`typecheck`/lint tetap pre-existing gap (Q8).
- **Known issues:** Q8 tetap terbuka (sepuluh section berturut-turut); tidak ada detail-route terpisah per Change (inline, konsisten single-route); CRUD hapus Change tidak diimplementasikan; verifikasi interaktif tidak dilakukan (keterbatasan tooling).
- **Cross-section impact:** `docs/mockup-change-impact-log.md` CI-016 (ActivityEntry diperluas aditif, milik Section 05; `reviewed` Section 06 tidak diubah maknanya; integrasi Dashboard terjadi otomatis lewat selector existing tanpa perubahan kode Dashboard).
- **Next action:** Section 15 — Project Finance. Rekomendasi sangat kuat: selesaikan Q8 sebelum Section 15. Menunggu perintah user — tidak dieksekusi otomatis.

## Section 15 — Project Finance

- **Tanggal:** 2026-07-30
- **Status:** COMPLETED
- **Scope dan completed items:** Membangun `/finance/invoices` dan `/finance/payments` (dari `ModulePlaceholder`) menjadi list/detail penuh dengan aging dan payment history; merestrukturisasi tab "Finance" Project Detail dengan Budget/Actual/Variance/Quotation/Committed Vendor Cost/Margin dan **visibilitas berbasis role** (Tier 0: Sales/Ops/subdomain hanya Quotation+Outstanding; Tier 1: PM/Finance/Management/SuperAdmin/Viewer full breakdown; Margin dikecualikan untuk PM). Tidak ada perubahan type/fixture — murni selektor turunan baru + tampilan.
- **Files created/changed/removed:** Dibuat — `docs/mockup-section-reports/section-15-project-finance.md`. Diubah — `app/utils/attention.ts` (+`invoiceAgingDays`), `app/data/index.ts` (+`getInvoiceOutstandingIdr`/`getProjectOutstandingIdr`/`getCommittedVendorCostIdr`), `app/pages/finance/invoices.vue` (ditulis ulang dari placeholder), `app/pages/finance/payments.vue` (ditulis ulang dari placeholder), `app/pages/projects/[id]/index.vue` (tab Finance ditulis ulang, role-tiered).
- **Routes affected:** Tidak ada route baru — `/finance/invoices`, `/finance/payments`, dan `/projects/[id]` (tab `finance`) diisi penuh.
- **Components reused/created:** Reused — `StatsCard`, `Table*`, `SectionCard`, `Dialog*`, `DetailMetadataList`, `EmptyState`. Tidak ada komponen file baru.
- **Data/types/constants affected:** Tidak ada — `Invoice`/`Payment` dipakai apa adanya, murni selektor turunan baru.
- **Validation results:** `nuxi prepare` + `npm run build` sukses. Smoke test konten (curl+grep) mengonfirmasi aging invoice (28 & 9 hari overdue) presisi, dan Margin/Variance/Committed Vendor Cost/Outstanding untuk PRJ-101/102/103 seluruhnya cocok persis dengan `docs/mockup-data-scenarios.md` bagian 1.3/2.3/3.3 yang sudah ada sejak Prompt 4. Regresi Section 06/08/10/13/14 terverifikasi tidak berubah. `vitest`/`typecheck`/lint tetap pre-existing gap (Q8).
- **Known issues:** Q8 tetap terbuka (sebelas section berturut-turut); tidak ada CRUD invoice/payment (di luar scope literal); verifikasi interaktif ganti-role tidak dilakukan (keterbatasan tooling).
- **Cross-section impact:** Tidak ada entri change-impact-log baru — tidak ada data/type section lain yang diubah, hanya selektor turunan baru dan pengisian tab/route yang memang belum dikerjakan section manapun (dicatat sejak Section 10 sebagai "menyusul Section 15").
- **Next action:** Section 16 — Reports. Rekomendasi sangat kuat: selesaikan Q8 sebelum Section 16. Menunggu perintah user — tidak dieksekusi otomatis.

## Section 16 — Reports

- **Tanggal:** 2026-07-30
- **Status:** COMPLETED
- **Scope dan completed items:** Membangun `/reports` (dari `ModulePlaceholder` Section 05) menjadi satu halaman dengan 6 section granular: Sales Pipeline, Project Performance, Upcoming Departure dan Service Readiness, Vendor Summary, Budget vs Actual dan Margin, Invoice Aging dan Outstanding — dengan filter (status/tipe/periode project), chart/table (`StatusBreakdownList`/`BudgetChart`/`Table`), states (loading/empty/unauthorized), dan visibilitas per role (pemetaan didokumentasikan di `docs/route-and-role-matrix.md` bagian 1.6 catatan implementasi Section 16). Tidak ada dataset report terpisah — seluruh angka reuse fixture/selektor existing (terutama selektor finansial Section 15).
- **Files created/changed/removed:** Dibuat — `app/pages/reports/index.vue` (rewrite total dari `ModulePlaceholder`), `docs/mockup-section-reports/section-16-reports.md`. Diubah — `app/constants/navigation.ts` (flag `comingSoon` item Reports dihapus, satu baris).
- **Routes affected:** `/reports` (tidak ada route baru — shell Section 05 diisi penuh).
- **Components reused/created:** Reused — `PageHeader`, `SectionCard`, `RoleAccessState`, `LoadingState`, `EmptyState`, `StatusBreakdownList`, `StatsCard`, `BudgetChart`, `Table*`, `Select*`. Tidak ada komponen file baru.
- **Data/types/constants affected:** Tidak ada — murni agregasi read-only di atas `PROJECTS`/`OPPORTUNITIES`/`QUOTATIONS`/`VENDOR_QUOTATIONS`/`INVOICES` dan selektor `app/data/index.ts` existing.
- **Validation results:** `nuxi prepare` + `npm run build` sukses (2x run — run kedua setelah fix `comingSoon`). Smoke test HTTP 8 route seluruhnya 200. Verifikasi bundle server (`reports-*.mjs`) mengonfirmasi 6 judul section ter-compile. Verifikasi angka manual terhadap fixture: Budget/Actual/Variance/Quotation/Margin agregat 3 project, Invoice Aging (28 & 9 hari overdue, cocok Section 15), Committed Vendor Cost Rp1.365.000.000, Sales Pipeline Win Rate 75% — seluruhnya presisi. `vitest`/`typecheck`/lint tetap pre-existing gap (Q8). Verifikasi interaktif ganti-role tidak dilakukan (keterbatasan tooling, konsisten sejak Section 06).
- **Known issues:** Q8 tetap terbuka (dua belas section berturut-turut); export mock tidak dikerjakan (belum disepakati, sesuai hard rule literal); pemetaan role 2 section baru (Upcoming Departure/Service Readiness, Vendor Summary) adalah keputusan implementasi Section 16, bukan perluasan tabel LOCKED; nav item Invoices/Payments masih bertanda `comingSoon: true` (oversight Section 15, ditemukan namun sengaja tidak diperbaiki — di luar scope Section 16).
- **Cross-section impact:** `docs/mockup-change-impact-log.md` CI-017 (flag `comingSoon` nav Reports dihapus, milik Section 05).
- **Next action:** Section 17 — Administration. Rekomendasi sangat kuat: selesaikan Q8 sebelum Section 17. Menunggu perintah user — tidak dieksekusi otomatis.

## Section 17 — Administration

- **Tanggal:** 2026-07-30
- **Status:** COMPLETED
- **Scope dan completed items:** 
  1. User List & Detail Mock: Mengubah `/admin/users` menjadi daftar user lengkap dengan pencarian, filter role, indikator user yang sedang login aktif, serta dialog detail yang menunjukkan permission matrix personal user dan tombol beralih user.
  2. Role Switcher Demo: Membangun kontrol switcher reaktif di `/admin/index.vue` dan `/admin/users.vue` (dialog detail) menggunakan source of truth dari `useCurrentUser()`.
  3. Role & Permission Matrix: Meningkatkan `/admin/roles.vue` dengan visualisasi matrix grid berwarna, penjelasan level akses legend, ringkasan peranan tiap role, dan action flags khusus.
  4. Master Data: Mengisi `/admin/master-data.vue` dengan data referensi tipe project, jenis layanan, destinasi perjalanan, dan kategori vendor menggunakan file konstanta master-data baru.
  5. Audit Trail: Menyelesaikan `/admin/audit-trail.vue` dengan summary stats, filter project/tipe/review status, dan tampilan detail log perubahan (kategori, requester, approval status, before/after, dampak).
  6. Navigasi: Menghapus flag `comingSoon` pada sub-item Administration dan Finance.
- **Files created/changed/removed:**
  - Dibuat: `app/constants/master-data.ts`, `docs/mockup-section-reports/section-17-administration.md`.
  - Diubah: `app/constants/navigation.ts`, `app/pages/admin/index.vue`, `app/pages/admin/users.vue`, `app/pages/admin/roles.vue`, `app/pages/admin/master-data.vue`, `app/pages/admin/audit-trail.vue`, `docs/mockup-change-impact-log.md`.
- **Routes affected:** `/admin`, `/admin/master-data`, `/admin/users`, `/admin/roles`, `/admin/audit-trail`, `/finance/invoices`, `/finance/payments` (tidak ada route baru, fungsionalitas placeholder diisi penuh).
- **Components reused/created:** Reused: `PageHeader`, `SectionCard`, `RoleAccessState`, `EmptyState`, `StatusBadge`, `Table*`, `Dialog*`, `Button`, `Input`.
- **Data/types/constants affected:** Dibuat konstanta baru `MASTER_PROJECT_TYPES`/`MASTER_SERVICE_TYPES`/`MASTER_DESTINATIONS`/`MASTER_VENDOR_CATEGORIES` di `app/constants/master-data.ts`.
- **Validation results:** `nuxi prepare` sukses. `npm run build` sukses melakukan kompilasi penuh client & server build (10.77 detik).
- **Known issues:** Q8 tetap terbuka (tiga belas section berturut-turut); verifikasi interaktif visual ganti-role tidak dilakukan secara headless otomatis (keterbatasan tooling).
- **Cross-section impact:** `docs/mockup-change-impact-log.md` CI-018 (menghapus flag `comingSoon` pada Finance dan Administration).
- **Next action:** Section 18 — Regression and Demo Readiness. ✅ SELESAI.

## Section 18 — Regression and Demo Readiness

- **Tanggal:** 2026-07-30
- **Status:** COMPLETED
- **Scope dan completed items:**
  1. **Audit route dan dead navigation:** Inventarisasi seluruh 28 route ter-generate Nuxt; identifikasi 4 route "locked/excluded" (template lama tidak di sidebar), 1 route placeholder (`/crm/quotations`), 23 route active; dikatalogkan di `docs/mockup-final-route-inventory.md`.
  2. **Audit breadcrumbs dan sidebar flags:** Tidak ditemukan breadcrumb kosong atau link sidebar mati pada route aktif. Flag `comingSoon: true` hanya tersisa pada `/crm/quotations` — sudah tepat.
  3. **Bug fix CI-019:** `handleDelete` undefined di `app/pages/expenses.vue` (ditemukan Section 01, deferred selama 17 section) diperbaiki → `requestDelete`. Satu baris, risiko rendah.
  4. **Build validation:** `npm run build` dikonfirmasi sukses (hanya pre-existing Tailwind warning dan EBUSY Windows lock — bukan error kompilasi).
  5. **Dokumen final:**
     - `docs/mockup-demo-script.md` — alur demo 10-step dari login hingga Administration.
     - `docs/mockup-final-known-issues.md` — katalog 14 known issues (A-001 – C-003), semua sudah terdokumentasi.
     - `docs/mockup-final-route-inventory.md` — inventarisasi 28 route lengkap dengan status/peran/tab.
  6. **Section report:** `docs/mockup-section-reports/section-18-regression-demo-readiness.md`.
- **Files created/changed/removed:**
  - Dibuat: `docs/mockup-demo-script.md`, `docs/mockup-final-known-issues.md`, `docs/mockup-final-route-inventory.md`, `docs/mockup-section-reports/section-18-regression-demo-readiness.md`.
  - Diubah: `app/pages/expenses.vue` (bug fix CI-019, satu baris), `docs/mockup-change-impact-log.md` (CI-019), `docs/mockup-implementation-state.md` (status COMPLETE), `docs/mockup-section-progress.md` (entri ini).
- **Routes affected:** Tidak ada route baru — bug fix hanya menyentuh `/expenses` (route locked/excluded).
- **Components reused/created:** Tidak ada — Section 18 adalah audit dan dokumentasi.
- **Data/types/constants affected:** Tidak ada — tidak ada perubahan data model.
- **Validation results:** `npm run build` sukses (hanya pre-existing warnings). Audit route manual 23 active + 5 non-active. Bug fix CI-019 dikonfirmasi via `grep` (tidak ada consumer lain dari `handleDelete`). `vitest`/`typecheck`/lint tetap pre-existing gap (Q8).
- **Known issues:** Q8 tetap terbuka (empat belas section berturut-turut, A-003/A-004/A-005 di `docs/mockup-final-known-issues.md`); verifikasi interaktif ganti-role tidak dilakukan secara headless (C-002); route template lama masih accessible via URL (B-003).
- **Cross-section impact:** `docs/mockup-change-impact-log.md` CI-019 (fix `handleDelete`, milik `expenses.vue` template lama sejak Section 01).
- **Next action:** **Section 18 adalah section final.** Tidak ada section 19 atau backend. Mockup MANOVA Travel Agent selesai sepenuhnya.

---

## Prompt 19 — Change Request: Customer Journey, Account Executive, Supplier, dan Commercial Approval

- **Tanggal:** 2026-07-30
- **Status:** COMPLETED
- **Scope dan completed items:** Change request di atas mockup 18-section COMPLETED (`prompts/21-PROMPT-19-CHANGES-&-UPDATE.md`, dijalankan via `prompts/99-RUN-CURRENT-SECTION.md`). Ringkasan (detail penuh: `docs/mockup-section-reports/change-customer-journey-ae-supplier.md`): 2 role baru (Account Executive, Supplier — 13 role total); entitas baru `Lead`/`LeadActivity`/`VendorProduct`/`SystemEvent`; Commercial Approval workflow pada `Quotation` (terpisah dari Won approval existing, D-049); modul baru Customer Journey (`/customer-journey/*` — Leads Table/Kanban/Inbox+drawer, Customers, Project Orders, Lead Source Recap; Customers/Project Orders reuse `Party`/`Project` existing, bukan dataset paralel — D-050); Activity Center (`/activity-center`, Super Admin saja); Supplier Portal (`/supplier/*`, vendor-isolated via `User.vendorId` — D-048) + tab "Products" baru di Vendor Detail; `Opportunity.ownerId` di-reassign dari Sales ke AE; Dashboard (`/`) mendapat widget tambahan untuk AE/Supplier agar tidak kosong.
- **Files created/changed/removed:** Lihat laporan perubahan bagian "Files Created, Changed, dan Removed" untuk daftar lengkap (folder baru `app/pages/customer-journey/`, `app/pages/activity-center/`, `app/pages/supplier/`, file baru `app/types/lead.ts`, `app/data/leads.ts`; ~24 file existing diubah).
- **Routes affected:** `/customer-journey/*` (7 route baru), `/activity-center` (baru), `/supplier/*` (3 route baru), `/vendors/[id]` (+tab Products), `/crm/opportunities/[id]` (+Commercial Approval), `/reports`, `/` (Dashboard) — perluasan visibilitas widget.
- **Components reused/created:** Reused — seluruh shared component + primitive `ui/*` existing, termasuk `ui/sheet/*` (dipakai pertama kali — drawer Lead Detail). Tidak ada komponen shared baru.
- **Data/types/constants affected:** Lihat `docs/mockup-implementation-state.md` bagian 4 dan `docs/mockup-change-impact-log.md` untuk daftar lengkap entitas/field baru dan reassignment data existing.
- **Validation results:** `nuxi prepare` + `npm run build` sukses (beberapa run menemui `EBUSY` pre-existing pada cleanup `.output`, diatasi dengan mematikan proses node sebelum retry). Smoke test HTTP 32 route (baru + existing) seluruhnya 200. Smoke test konten memverifikasi Commercial Approval, isolasi Supplier, angka Lead Source Recap (dihitung ulang manual, cocok), Vendor Detail Products tab, Activity Center (22 event). Regresi route existing dikonfirmasi tidak berubah. `vitest`/`typecheck` tetap pre-existing gap (Q8).
- **Known issues:** Q8 tetap terbuka; Q12 baru (self-service submit quotation Supplier, deferred); verifikasi interaktif ganti-role tidak dilakukan headless (keterbatasan tooling, konsisten sejak Section 06).
- **Cross-section impact:** Lihat `docs/mockup-change-impact-log.md` (entri CI baru) — menyentuh Section 06 (Dashboard), Section 07 (canManageParty), Section 08 (Opportunity ownerId/canManageOpportunity), Section 09 (OPP-005 restage), Section 13 (Vendor Detail +tab), Section 16 (Reports showSalesPipeline).
- **Next action:** Tidak ada section/change baku selanjutnya — menunggu perintah user.

## Prompt 20 — Change Request: Sales Qualification to Account Executive Opportunity Flow

- **Tanggal:** 2026-07-31
- **Status:** COMPLETED
- **Scope dan completed items:** Change request di atas Prompt 19 (`prompts/22-PROMPT-20-Change-Request-Sales-Qualification-to-AE-OpportunityF.md`, dijalankan via `prompts/99-RUN-CURRENT-SECTION.md`). Ringkasan (detail penuh: `docs/mockup-section-reports/change-sales-qualification-ae-opportunity.md`):
  1. **Lead Qualification** — 13 field baru pada `Lead` (7 wajib, 6 opsional), tab "Qualification" baru di drawer Leads dengan completion indicator + missing-field warning, gate `getLeadMissingQualification` (UI dan mutator), aksi "Simpan Draft" (`updateLeadQualification`) dan "Mark as Unqualified" (`markLeadUnqualified`) baru. `handedOverTo` (AE tujuan) kini diisi eksplisit oleh Sales lewat form, bukan otomatis saat klik Qualify.
  2. **`qualifyLeadAndCreateOpportunity`** digerbangi kelengkapan qualification, membawa seluruh data (destinasi/tanggal/traveler/service scope/ringkasan/contact name/expected close) ke Opportunity baru, mencatat 3 activity ("Lead Qualified", "Lead Assigned to Account Executive", "Opportunity Created") yang sebelumnya tidak ada.
  3. **AE Requirement Detail** — section baru Opportunity Detail (14 field: itinerary concept, departure city, dst. — `Opportunity.requirementDetail`), dialog "Edit Requirement" (AE), field aditif `contactName`/`leadId`/`expectedCloseDate`.
  4. **Requirement Gate sebelum Quotation** — `getOpportunityRequirementGate` baru (terpisah dari `getOpportunityMissingRequirements` Section 09), men-disable tombol "Buat Quotation" bila belum lengkap.
  5. **Quotation komersial** — field baru `discountIdr`/`estimatedCostIdr`/`estimatedMarginIdr`/`paymentTerms`/`serviceBreakdown`, mutator `updateQuotationDetails`, dialog "Edit Quotation" terpisah dari "Create New Version".
  6. **Mark as Won satu-langkah** — tombol AE menggantikan "Ajukan sebagai Won" + Approve/Reject Won Management terpisah (D-053), reuse mutator existing.
  7. **Status workflow baru** — `getOpportunityWorkflowStatus` + `OPPORTUNITY_WORKFLOW_STATUSES` (Pending Requirement/Ready for Quotation/Quotation Draft/Pending Management Approval/Approved/Won/Lost), badge utama di PageHeader; label `won-requested` direname.
  8. **Fixture:** `LED-001` (qualification lengkap), `LED-004` (qualification sebagian), `OPP-009` (Ready for Quotation), `OPP-010` (Quotation Draft, `QUO-010`), `requirementDetail` pada `OPP-005`/`OPP-006`, `leadId` pada `OPP-001`/`OPP-005`.
  9. **Konsistensi:** teks `ROLE_NOTES` di `/admin/roles` disinkronkan dengan model role Prompt 19/20 (CI-029).
- **Files created/changed/removed:**
  - Diubah: `app/types/lead.ts`, `app/types/opportunity.ts`, `app/constants/status.ts`, `app/data/index.ts`, `app/data/leads.ts`, `app/data/opportunities.ts`, `app/pages/customer-journey/leads/index.vue`, `app/pages/crm/opportunities/[id]/index.vue`, `app/pages/admin/roles.vue`, seluruh `docs/*.md` terdampak (lihat bagian dokumentasi laporan).
  - Dibuat: `docs/mockup-section-reports/change-sales-qualification-ae-opportunity.md` (laporan ini).
  - Dihapus: Tidak ada.
- **Routes affected:** `/customer-journey/leads` (tab Qualification baru), `/crm/opportunities/[id]` (Requirement Detail, Edit Quotation, Mark as Won, badge status baru), `/admin/roles` (teks saja). Tidak ada route baru.
- **Components reused/created:** Reused penuh — `Sheet`/`Tabs`/`Dialog`/`DialogScrollContent`/`SectionCard`/`DetailMetadataList`/`StatusBadge`/`EmptyState` existing. Tidak ada komponen shared baru.
- **Data/types/constants affected:** 13 field baru `Lead`, 4 field baru + `OpportunityRequirementDetail` pada `Opportunity`, 5 field baru + `QuotationServiceItem` pada `Quotation`, `OpportunityWorkflowStatus` (type baru, dirivasi). 3 constant baru (`LEAD_SERVICE_CATEGORIES`, `LEAD_URGENCY_LEVELS`, `OPPORTUNITY_WORKFLOW_STATUSES`), 1 label diubah. 7 selector/mutator baru di `app/data/index.ts`.
- **Validation results:** `npx nuxi prepare` sukses. `npm run build` sukses (chunk `leads-*`/`opportunities-*` ter-compile). Smoke test HTTP ~27 route (baru+existing representatif) seluruhnya 200, tidak ada string error di HTML. Smoke test konten: badge workflow status sesuai per skenario (OPP-005 "Pending Management Approval", OPP-006 "Approved"+tombol Mark as Won aktif, OPP-007 "Pending Requirement", OPP-009 "Ready for Quotation"+tombol Buat Quotation, OPP-010 "Quotation Draft"+Edit Quotation+Service Breakdown). Lead Source Recap (10/3/2/1) tidak berubah. `npx vitest run` — "No test files found" (pre-existing). `npx nuxi typecheck` — gagal, `vue-tsc` tidak terpasang (pre-existing, Q8).
- **Known issues:** Q8 tetap terbuka (tidak berubah). Payment terms/margin-cost summary sengaja tidak digerbangi hard-block pada Requirement Gate (D-055, kondisional tanpa mekanisme konfigurasi eksplisit). Verifikasi interaktif (isi form Qualification/Requirement Detail secara live, klik Mark as Won) tidak dilakukan headless — keterbatasan tooling konsisten sejak Section 06, dimitigasi lewat code review ketat terhadap seluruh gate baru dan smoke test SSR konten.
- **Cross-section impact:** `docs/mockup-change-impact-log.md` CI-027 (Mark as Won satu-langkah, menyentuh Section 09), CI-028 (label `won-requested`, menyentuh Section 05/Foundation), CI-029 (teks `admin/roles.vue`, menyentuh Section 17).
- **Next action:** Tidak ada section/change baku selanjutnya — menunggu perintah user.

---

## [Skema Roadmap Baru Section 00–24] Section 00 — Current Progress Reconciliation

Mulai dari entri ini, section memakai **skema penomoran baru** (D-057, `docs/mockup-design-decisions.md` Kelompok K) — "Section 00" di sini BUKAN Section 00–04 dokumentasi murni skema lama (lihat catatan di `docs/mockup-section-reports/README.md`), melainkan section pertama roadmap `prompts/01-PROTOKOL-WAJIB.md` versi "FRONTEND-ONLY CONTINUATION".

- **Tanggal:** 2026-08-01
- **Status:** COMPLETED
- **Scope dan completed items:** Audit progres frontend terhadap roadmap Section 00–24 baru (`prompts/Section 00 — Current Progress Reconciliation.md`, dijalankan via `prompts/99-RUN-CURRENT-SECTION.md`). Inventaris route/pages/data/types/roles aktual; klasifikasi COMPLETED/PARTIAL/NOT_STARTED per Section 00–24; pencocokan terhadap 9 area kunci (Sales qualification, AE quotation, Management approval, Customer Journey, Client, Supplier, Project Order, Activity Center, Lead Source Recap); penyusunan dependency order dan daftar fitur yang harus dilindungi. Tidak ada perubahan application code (audit-only, sesuai batasan "Jangan mengubah application code kecuali memperbaiki broken import/build blocker yang jelas" — tidak ditemukan blocker).
- **Files created/changed/removed:**
  - Dibuat: `docs/frontend-module-map.md`, `docs/frontend-workflow-map.md`, `docs/frontend-implementation-roadmap.md`, `docs/frontend-known-issues.md`, `docs/mockup-section-reports/section-00-current-progress-reconciliation.md` (laporan ini).
  - Diubah: `docs/mockup-implementation-state.md` (bagian 0 baru + update bagian 1/7/8), `docs/mockup-design-decisions.md` (+D-057), `docs/mockup-open-questions.md` (+Q13–Q16), `docs/mockup-progress.md` (+Entri 9), `docs/mockup-section-progress.md` (entri ini).
  - Dihapus: Tidak ada.
- **Routes affected:** Tidak ada — audit-only.
- **Components reused/created:** Tidak ada — audit-only.
- **Data/types/constants affected:** Tidak ada — audit-only.
- **Validation results:** `npx nuxi prepare` sukses. `npm run build` sukses (tanpa perubahan kode apa pun, membuktikan tidak ada blocker tersisa dari Prompt 0–20).
- **Known issues:** 4 open question baru (Q13–Q16, `docs/mockup-open-questions.md`) dan daftar gap lengkap per section di `docs/frontend-known-issues.md` — seluruhnya `NEEDS_VALIDATION`/`KNOWN_GAP`/`NOT_STARTED`, bukan bug pada hasil Prompt 0–20 (yang tetap COMPLETED apa adanya).
- **Cross-section impact:** Tidak ada perubahan pada hasil section/prompt manapun dari skema lama — murni dokumentasi pemetaan baru di atasnya.
- **Next action:** Section 01 (Frontend Foundation dan State Governance) direkomendasikan berbasis dependency (`docs/frontend-implementation-roadmap.md`) — menunggu perintah eksplisit user, tidak dieksekusi otomatis.

---

## [Skema Roadmap Baru Section 00–24] Section 01 — Frontend Foundation dan State Governance

- **Tanggal:** 2026-08-01
- **Status:** COMPLETED
- **Scope dan completed items:** Lengkapi gap foundation frontend yang ditemukan Section 00 (`prompts/Section 01 — Frontend Foundation dan State Governance.md`, dijalankan via `prompts/99-RUN-CURRENT-SECTION.md`). Audit konfirmasi: domain types/status constants terpusat, centralized stores per domain, stable ID scheme, mock persistence (`localStorage`), format currency/date, activity generator/transition helper, permission/data-scope helper (`usePermissions`), dan shared UI kit (`PageHeader`/`SectionCard`/`Sheet`/`Table*`/`StatusBadge`/`EmptyState`/`ErrorState`/`LoadingState`/`RoleAccessState`/`Dialog*`/`Tabs*`) **sudah COMPLETED** sejak Foundation lama (Prompt 5) — tidak diubah/diulang (sesuai instruksi eksplisit "Jangan mengganti foundation existing yang sudah sehat"). Satu gap konkret ditemukan dan ditutup: **state reset / seed scenario** belum ada — diimplementasikan `app/utils/mock-reset.ts` (snapshot+restore) dan `app/plugins/mock-reset.client.ts` (capture snapshot client-only saat app dimuat), diekspos lewat tombol "Reset Demo Data" di `/settings`. "Mock repository/service layer agar pages tidak membaca fixture langsung" diklarifikasi TERPENUHI oleh `app/data/index.ts` existing (single barrel module, tidak ada halaman dengan fixture terduplikasi) — didokumentasikan sebagai D-058, bukan dibangun lapisan paralel baru (menghindari premature abstraction/dead code).
- **Files created/changed/removed:**
  - Dibuat: `app/utils/mock-reset.ts`, `app/plugins/mock-reset.client.ts`, `docs/mockup-section-reports/section-01-frontend-foundation-state-governance.md` (laporan ini).
  - Diubah: `app/pages/settings.vue` (+SectionCard "Mock Data Management", tombol "Reset Demo Data"), `docs/mockup-design-decisions.md` (+D-058), `docs/frontend-module-map.md`, `docs/frontend-implementation-roadmap.md`, `docs/frontend-known-issues.md`, `docs/mockup-implementation-state.md`, `docs/mockup-progress.md`, `docs/mockup-section-progress.md` (entri ini).
  - Dihapus: Tidak ada.
- **Routes affected:** `/settings` (section baru). Tidak ada route baru.
- **Components reused/created:** Reused — `SectionCard`/`Dialog*`/`Button`/`useToast`. Tidak ada shared component baru (existing sudah lengkap, dikonfirmasi via audit).
- **Data/types/constants affected:** Tidak ada perubahan type/entitas. `app/utils/mock-reset.ts` membaca seluruh reactive array terpusat existing (`LEADS`, `OPPORTUNITIES`, `QUOTATIONS`, `PARTIES`, `CONTACTS`, `PARTY_ACTIVITIES`, `PROJECTS`, `PROJECT_SERVICES`, `TRAVELER_GROUPS`, `TRAVELERS`, `ROOM_ASSIGNMENTS`, `ITINERARY_ITEMS`, `VENDORS`, `VENDOR_CONTACTS`, `VENDOR_QUOTATIONS`, `VENDOR_ACTIVITIES`, `VENDOR_PRODUCTS`, `INVOICES`, `PAYMENTS`, `ACTIVITIES`, `DOCUMENTS`, `TASKS`, `LEAD_ACTIVITIES`) tanpa memodifikasi shape-nya.
- **Validation results:** `npx nuxi prepare` sukses. `npm run build` sukses (chunk `settings-*` bertambah, `precomputed.mjs` bertambah ~2kB dari plugin baru). Smoke test HTTP 11 route representatif (termasuk `/settings`) — seluruhnya HTTP 200, tidak ada string error di HTML. `npx vitest run` — "No test files found" (pre-existing, Q8, tidak berubah).
- **Known issues:** Tidak ada known issue baru dari perubahan ini. Q8 tetap terbuka (tidak berubah). Gap Section 02–24 lainnya tetap seperti dicatat `docs/frontend-known-issues.md` (di luar scope Section 01).
- **Cross-section impact:** Tidak ada — perubahan murni aditif (file baru + 1 section baru di halaman Settings), tidak menyentuh hasil section/prompt manapun dari skema lama maupun Section 00.
- **Next action:** Section 02 (Role, Access dan Navigation) direkomendasikan berbasis dependency (`docs/frontend-implementation-roadmap.md`) — menunggu perintah eksplisit user.

---

## [Skema Roadmap Baru Section 00–24] Section 02 — Role, Access dan Navigation

- **Tanggal:** 2026-08-01
- **Status:** COMPLETED
- **Scope dan completed items:** Finalisasi role dan navigation frontend (`prompts/Section 02 — Role, Access dan Navigation.md`, dijalankan via `prompts/99-RUN-CURRENT-SECTION.md`).
  1. **Role final 16** — `RoleId` +`product-planner`/+`procurement`/+`client` (D-059), mengikuti pola D-046.
  2. **`client-portal` module** — `ModuleKey` baru (pola identik `supplier-portal`), `User.clientPartyId`, `usePermissions().clientScopeId`.
  3. **Client Portal shell minimal** — route baru `/client`: profil company + list Opportunity + list Project Order, ter-isolasi per `clientPartyId`, TANPA nilai komersial (larangan eksplisit protokol). Fitur bisnis penuh tetap Section 08.
  4. **Procurement** — `vendor: MANAGE` (owner fungsional direktori Vendor, mengaktifkan "Tambah Vendor" yang sebelumnya hanya Super Admin).
  5. **Product Planner** — VIEW read-only ke `crm`/`project`/`vendor`/`reports` sebagai placeholder sampai Section 10.
  6. **Demo user baru** — `USR-017` (Product Planner), `USR-018` (Procurement), `USR-019`/`USR-020` (Client, masing-masing `PTY-001`/`PTY-002` — mendemokan isolasi "Client A tidak melihat Client B").
  7. **Matrix View dilengkapi** — `/admin/roles` (dikonfirmasi SUDAH literal grid ModuleKey x Role sejak Section 17 lama, Wajib Section 02 terpenuhi tanpa dibangun baru) diperluas 6→8 kolom (+`supplier-portal`/+`client-portal`, gap pre-existing sejak Prompt 19), `ROLE_NOTES` +5 entri yang sebelumnya terlewat.
  8. **Dashboard tidak kosong** — ditemukan dan ditutup dalam section yang sama: 3 role baru sebelumnya tidak punya widget Dashboard sama sekali (bug regresi UX bila dibiarkan) — ditambahkan 3 `SectionCard` welcome/redirect bersyarat (CI-030).
  9. **Route guards** — dikonfirmasi pola existing (`RoleAccessState` + `canView(...)` per halaman, middleware `auth` untuk login-gate) sudah memadai, diterapkan konsisten ke `/client` baru.
- **Files created/changed/removed:**
  - Dibuat: `app/pages/client/index.vue`, `docs/mockup-section-reports/section-02-role-access-navigation.md` (laporan ini).
  - Diubah: `app/types/user.ts`, `app/constants/roles.ts`, `app/composables/usePermissions.ts`, `app/data/users.ts`, `app/constants/navigation.ts`, `app/pages/admin/roles.vue`, `app/pages/index.vue` (Dashboard), `docs/mockup-design-decisions.md` (+D-059), `docs/mockup-open-questions.md` (Q13 RESOLVED), `docs/mockup-change-impact-log.md` (+CI-030/CI-031), `docs/frontend-module-map.md`, `docs/frontend-implementation-roadmap.md`, `docs/frontend-known-issues.md`, `docs/mockup-implementation-state.md`, `docs/mockup-progress.md`, `docs/mockup-section-progress.md` (entri ini), `docs/mockup-section-reports/README.md`.
  - Dihapus: Tidak ada.
- **Routes affected:** `/client` (baru). `/` (Dashboard, +3 widget bersyarat). `/admin/roles` (+2 kolom, +5 catatan role). `/settings` tidak berubah (role switcher generik, otomatis menampilkan 4 user baru).
- **Components reused/created:** Reused — `PageHeader`/`SectionCard`/`RoleAccessState`/`StatsCard`/`DetailMetadataList`/`StatusBadge`/`EmptyState`. Tidak ada shared component baru.
- **Data/types/constants affected:** `RoleId` 13→16, `ModuleKey`+1, `User.clientPartyId` (field baru), `ROLE_MODULE_ACCESS` +3 baris +1 kolom, `ROLES` +3 entri, 4 `USER` baru (`USR-017`–`USR-020`). Tidak ada entitas existing yang diubah shape-nya secara breaking.
- **Validation results:** `npx nuxi prepare` sukses. `npm run build` sukses (chunk `client-*` baru ter-compile). Smoke test HTTP 30 route (baru+existing) — seluruhnya HTTP 200. Smoke test konten: `/client` (default Super Admin) menampilkan `RoleAccessState` "Anda tidak memiliki akses" (isolasi bekerja, tidak bocor data company manapun); `/admin/roles` menampilkan label "Client Portal"/"Supplier Portal"/"Product Planner"/"Procurement"; `/admin/users` otomatis merefleksikan 4 user baru tanpa perubahan kode (mengonfirmasi desain reaktif existing generic, konsisten temuan Prompt 19). `npx vitest run` — "No test files found" (pre-existing, Q8).
- **Known issues:** Q13 RESOLVED. Sisa fitur bisnis penuh Client Portal (Section 08), RFQ/Service Order Procurement (Section 17), modul Product Planning (Section 10) tetap tanggung jawab section masing-masing (dicatat `docs/frontend-known-issues.md`). Verifikasi interaktif ganti-role/isolasi antar-Client tidak dilakukan headless (keterbatasan tooling konsisten).
- **Cross-section impact:** `docs/mockup-change-impact-log.md` CI-030 (Dashboard, menyentuh Section 06 lama), CI-031 (Matrix View, menyentuh Section 17 lama) — keduanya perbaikan aditif murni, regression-tested.
- **Next action:** Section 03 (Public Lead Intake) direkomendasikan berbasis dependency (`docs/frontend-implementation-roadmap.md`) — menunggu perintah eksplisit user.

---

## [Skema Roadmap Baru Section 00–24] Section 03 — Public Lead Intake

- **Tanggal:** 2026-08-01
- **Status:** COMPLETED
- **Scope dan completed items:** Implementasi public lead intake frontend (`prompts/Section 03 — Public Lead Intake.md`, dijalankan via `prompts/99-RUN-CURRENT-SECTION.md`).
  1. **Route publik baru `/lead-intake`** — `layout: false`, tanpa `middleware: 'auth'`, tidak masuk `NAV_ITEMS` (Wajib "Public tidak mendapat dashboard internal").
  2. **4 kategori** (Corporate Travel/Group Travel/Individual Travel/MICE — Event) via selector pill, dapat di-preset lewat `?type=`. Copy/label form menyesuaikan kategori terpilih.
  3. **Form dasar** — Nama Kontak (wajib), Nama Company (opsional), Sumber (select, default terisi otomatis dari `utm_source` bila ada), Telepon/Email (minimal satu wajib). Field opsional tambahan (reuse `Lead` qualification fields existing): Destinasi, Perkiraan Jumlah Traveler, Ceritakan Kebutuhan Anda.
  4. **Consent checkbox** (wajib dicentang untuk submit) + ringkasan kebijakan privasi mock (collapsible, eksplisit ditandai "Mock — bukan dokumen legal").
  5. **UTM/source/referrer preview** — panel transparan menampilkan `utm_source`/`utm_medium`/`utm_campaign` (query string) dan `document.referrer` (client-only), atau pesan "kunjungan langsung" bila kosong.
  6. **Validation** — Nama Kontak, kontak minimal satu (telepon/email), consent — ditampilkan sebagai warning list setelah percobaan submit pertama (bukan sebelum, demi UX).
  7. **Duplicate suggestion** — non-blocking, mencocokkan telepon/email terhadap `LEADS` existing, menampilkan info (bukan larangan submit).
  8. **Success/error state** — sukses menampilkan nomor referensi (`Lead.id`) + tombol "Ajukan Permintaan Baru"; error memakai `ErrorState.vue` (dipakai pertama kali sejak Foundation, dikonfirmasi Section 01 tersedia tapi belum pernah dipakai).
  9. **Submission** — reuse `createLead()`+`updateLeadQualification()` (Prompt 20) apa adanya, menulis ke `LEADS` centralized state yang sama dipakai `/customer-journey/leads` dan `/customer-journey/lead-sources` — memenuhi acceptance "Lead public dapat ditelusuri ke Lead list dan Lead Source Recap" secara otomatis (single source of truth, bukan dataset paralel).
  10. **Discoverability** — `/login` (satu-satunya halaman publik lain) mendapat 1 baris link ke `/lead-intake`.
- **Files created/changed/removed:**
  - Dibuat: `app/pages/lead-intake/index.vue`, `docs/mockup-section-reports/section-03-public-lead-intake.md` (laporan ini).
  - Diubah: `app/pages/login.vue` (+link discoverability), `docs/mockup-design-decisions.md` (+D-060), `docs/mockup-open-questions.md` (Q15 RESOLVED), `docs/mockup-change-impact-log.md` (+CI-032), `docs/frontend-module-map.md`, `docs/frontend-workflow-map.md`, `docs/frontend-implementation-roadmap.md`, `docs/frontend-known-issues.md`, `docs/mockup-implementation-state.md`, `docs/mockup-progress.md`, `docs/mockup-section-progress.md` (entri ini), `docs/mockup-section-reports/README.md`.
  - Dihapus: Tidak ada.
- **Routes affected:** `/lead-intake` (baru). `/login` (+link, tidak ada perubahan logic autentikasi mock).
- **Components reused/created:** Reused — `Input`/`Label`/`Button`/`Checkbox`/`ErrorState` (pertama kali dipakai). Tidak ada shared component baru.
- **Data/types/constants affected:** Tidak ada — reuse penuh `Lead`/`LeadServiceCategory`/`LeadSource` (types) dan `createLead`/`updateLeadQualification` (mutators) existing dari Prompt 20/Section 02, tanpa perubahan shape apa pun.
- **Validation results:** `npx nuxi prepare` sukses. `npm run build` sukses (chunk `lead-intake-*` baru ter-compile). Smoke test HTTP (`/`, `/login`, `/lead-intake` termasuk dengan query `?type=mice-event&utm_source=instagram&utm_medium=cpc&utm_campaign=demo`, `/customer-journey/leads`, `/customer-journey/lead-sources`, + 13 route existing representatif) — seluruhnya HTTP 200, tidak ada string error. Konten `/lead-intake` terverifikasi menampilkan kategori sesuai query param. `npx vitest run` — "No test files found" (pre-existing, Q8).
- **Known issues:** Q15 RESOLVED. Verifikasi interaktif submit form (klik Kirim Permintaan, cek Lead baru muncul di `/customer-journey/leads`) tidak dilakukan headless — dimitigasi lewat code review (mutator yang dipakai identik dengan yang sudah divalidasi Prompt 20). Merge-duplicate PENUH tetap Section 04.
- **Cross-section impact:** `docs/mockup-change-impact-log.md` CI-032 (link `/login`, menyentuh halaman Foundation/template baseline yang belum pernah disentuh section manapun) — aditif murni, regression-tested.
- **Next action:** Section 04 (Sales Leads dan Qualification) direkomendasikan berbasis dependency (`docs/frontend-implementation-roadmap.md`) — menunggu perintah eksplisit user.

---

## [Skema Roadmap Baru Section 00–24] Section 04 — Sales Leads dan Qualification

- **Tanggal:** 2026-08-01
- **Status:** COMPLETED
- **Scope dan completed items:** Lengkapi workflow Sales end-to-end (`prompts/Section 04 — Sales Leads dan Qualification.md`, dijalankan via `prompts/99-RUN-CURRENT-SECTION.md`). Audit Section 00 mengonfirmasi mayoritas Wajib sudah COMPLETED sejak Prompt 20 (Leads Table/Kanban/Inbox, drawer 4-tab, Qualification draft/final lengkap 13 field, gate Qualify & Create Opportunity, idempotency, activity/follow-up history, Sales tidak bisa quotation/approve/Mark as Won). 3 gap konkret ditutup:
  1. **Edit Lead** — dialog baru (Overview tab) untuk field kontak dasar (nama/company/source/phone/email), sebelumnya hanya bisa diisi sekali saat create.
  2. **Reopen** — tombol baru di drawer footer (muncul saat lead archived), kebalikan Archive.
  3. **Merge suggestion** — diimplementasikan sebagai archive-dengan-referensi (D-061): deteksi otomatis (`getLeadDuplicateCandidates`, cocok phone/email), panel "Lead Serupa Terdeteksi" di Overview tab, badge "Possible Duplicate" di Table view, aksi "Tandai sebagai Duplikat" (`mergeLeadAsDuplicate`, mengarsipkan lead duplikat dengan catatan referensi ke lead canonical — TIDAK menggabungkan field, kedua record tetap ada sebagai histori).
  4. **DRY dengan Section 03** — selector deteksi duplikat dipusatkan, `/lead-intake` direfactor untuk reuse (CI-033), bukan logic paralel.
  5. **New Lead dialog** — ditambahkan info non-blocking duplikat saat mengisi phone/email, pola sama dengan `/lead-intake`.
  6. **Fixture** — `LED-011` baru (email sengaja sama dengan `LED-007`) untuk mendemokan fitur tanpa mengubah data lama.
- **Files created/changed/removed:**
  - Dibuat: `docs/mockup-section-reports/section-04-sales-leads-qualification.md` (laporan ini).
  - Diubah: `app/data/index.ts` (+`reopenLead`, +`updateLeadContact`, +`getLeadDuplicateCandidates`, +`mergeLeadAsDuplicate`), `app/pages/customer-journey/leads/index.vue` (+Edit Lead, +Reopen, +merge suggestion UI, +duplicate hint New Lead), `app/pages/lead-intake/index.vue` (refactor reuse selector, CI-033), `app/data/leads.ts` (+`LED-011`), `docs/mockup-design-decisions.md` (+D-061), `docs/mockup-change-impact-log.md` (+CI-033), `docs/mockup-data-scenarios.md` (+4l, update total 10→11), `docs/frontend-module-map.md`, `docs/frontend-workflow-map.md`, `docs/frontend-implementation-roadmap.md`, `docs/frontend-known-issues.md`, `docs/mockup-implementation-state.md`, `docs/mockup-progress.md`, `docs/mockup-section-progress.md` (entri ini), `docs/mockup-section-reports/README.md`.
  - Dihapus: Tidak ada.
- **Routes affected:** `/customer-journey/leads` (fitur baru, route sama). `/lead-intake` (refactor internal, perilaku tidak berubah).
- **Components reused/created:** Reused sepenuhnya — `Dialog*`/`SectionCard`/`StatusBadge`/`Button`/`Input`/`Label`. Tidak ada shared component baru.
- **Data/types/constants affected:** Tidak ada perubahan type. 4 mutator/selector baru di `app/data/index.ts` (murni fungsi, tidak mengubah shape `Lead`). 1 fixture baru (`LED-011`).
- **Validation results:** `npx nuxi prepare` sukses. `npm run build` sukses (2x run, termasuk setelah penambahan fixture `LED-011`). Smoke test HTTP ~25 route (baru+existing) — seluruhnya HTTP 200. Smoke test konten: badge "Possible Duplicate" muncul 2x (LED-007 dan LED-011, simetris); Lead Source Recap Total Leads bertambah 10→11, Qualified/Opportunities/Won tetap 3/2/1 (tidak terpengaruh). Tidak ditemukan string error di HTML manapun. `npx vitest run` — "No test files found" (pre-existing, Q8).
- **Known issues:** Merge suggestion TIDAK melakukan true field-merge (dicatat D-061, evolusi lanjutan bila dibutuhkan). Verifikasi interaktif (klik Tandai sebagai Duplikat, Reopen, Edit Lead) tidak dilakukan headless — dimitigasi lewat code review dan smoke test SSR konten. Q8 tetap terbuka.
- **Cross-section impact:** `docs/mockup-change-impact-log.md` CI-033 (refactor `/lead-intake`, menyentuh Section 03 yang baru saja COMPLETED) — pure refactor, perilaku identik, regression-tested.
- **Next action:** Section 05 (Account Executive Opportunity dan Quotation) direkomendasikan berbasis dependency (`docs/frontend-implementation-roadmap.md`) — menunggu perintah eksplisit user.

---

## [Skema Roadmap Baru Section 00–24] Section 05 — Account Executive Opportunity dan Quotation

- **Tanggal:** 2026-07-31
- **Status:** COMPLETED
- **Scope dan completed items:** Lengkapi workflow Account Executive Opportunity/Quotation (`prompts/Section 05 — Account Executive Opportunity dan Quotation.md`). Audit Section 00 mengonfirmasi mayoritas Wajib sudah COMPLETED sejak Section 08/Prompt 20 (Assigned Leads dan Opportunity pipeline/detail, Requirement Detail + completion indicator, edit requirement tanpa menghapus qualification history, product/service scope/traveler composition/preferences/payment terms/risks/commercial-operational notes, Quotation create/edit/version/revise, line items, submit approval ke Management, stage dan activity history, Mark as Lost). 5 gap konkret ditutup:
  1. **Duplicate Quotation** — `duplicateQuotationVersion` (salinan persis sebagai versi baru, `approvalStatus` direset draft), berbeda dari "Create New Version"/`reviseQuotation` (mengosongkan nilai).
  2. **Compare Versions** — panel toggle "Bandingkan dengan versi sebelumnya", terbatas pada nilai total (`amountIdr` vs `supersededAmountIdr`) karena model `Quotation` existing tidak menyimpan snapshot breakdown penuh per versi — disclaimer eksplisit di UI (D-062).
  3. **Send to Client / Withdraw** — `sendQuotationToClient` (mock timestamp `sentToClientAt`) dan `withdrawQuotationSubmission` (revert `submitted`→`draft`, guard status).
  4. **Line item tax/fee/markup terpisah** — `Quotation` +`taxIdr`/`markupIdr`/`currency`/`validUntil`/`termsAndConditions`/`inclusions`/`exclusions`, seluruhnya di "Edit Quotation".
  5. **PDF/Print Preview** — halaman baru `/crm/opportunities/[id]/quotation-preview` (`layout: false`, cetak via `window.print()` browser — bukan generator PDF nyata).
  6. **Client Confirmation** — `recordClientConfirmation` (`Opportunity.clientConfirmedAt`/`clientConfirmationNote`) + dialog AE-facing, gerbang TAMBAHAN (bukan pengganti `approvalStatus === 'approved'`) sebelum tombol "Mark as Won" aktif — literal Wajib "AE belum dapat Mark as Won sebelum approved + client confirmation".
- **Files created/changed/removed:**
  - Dibuat: `app/pages/crm/opportunities/[id]/quotation-preview.vue`, `docs/mockup-section-reports/section-05-ae-opportunity-quotation.md` (laporan ini).
  - Diubah: `app/pages/crm/opportunities/[id]/index.vue` (Edit Quotation +7 field, +Duplicate Quotation, +Compare panel, +Withdraw, +Send to Client, +Client Confirmation, gerbang Mark as Won), `app/data/opportunities.ts` (`QUO-010` +tax/markup/currency/validity/inclusions/exclusions/terms; `QUO-006` +`sentToClientAt`), `app/types/opportunity.ts` (+7 field `Quotation`, +2 field `Opportunity` — sudah ada di working tree sebelum sesi ini dilanjutkan), `app/data/index.ts` (+4 mutator — sudah ada di working tree sebelum sesi ini dilanjutkan), `docs/mockup-design-decisions.md` (+D-062), `docs/mockup-change-impact-log.md` (+CI-034), `docs/mockup-open-questions.md` (Q14 sebagian resolved), `docs/mockup-data-scenarios.md`, `docs/frontend-module-map.md`, `docs/frontend-workflow-map.md`, `docs/frontend-implementation-roadmap.md`, `docs/frontend-known-issues.md`, `docs/mockup-implementation-state.md`, `docs/mockup-progress.md` (+Entri 14), `docs/mockup-section-progress.md` (entri ini), `docs/mockup-section-reports/README.md`.
  - Dihapus: Tidak ada.
- **Routes affected:** `/crm/opportunities/[id]/quotation-preview` (baru). `/crm/opportunities/[id]` (fitur baru, route sama).
- **Components reused/created:** Reused sepenuhnya — `Dialog*`/`SectionCard`/`StatusBadge`/`Button`/`Input`/`Label`/`EmptyState`/`RoleAccessState`/`NuxtLink`. Tidak ada shared component baru.
- **Data/types/constants affected:** `Quotation` +7 field aditif, `Opportunity` +2 field aditif — tidak ada breaking change. 4 mutator baru di `app/data/index.ts`. 2 fixture existing diperkaya (`QUO-006`/`QUO-010`), tidak ada fixture baru.
- **Validation results:** `npx nuxi prepare` + `npm run build` sukses (chunk `quotation-preview-*` baru ter-compile). Smoke test HTTP (opportunity detail representatif OPP-001/002/003/004/005/006/007/008/009/010, quotation-preview OPP-010/OPP-006/OPP-999 (not-found)/OPP-007 (tanpa quotation), plus route regresi Dashboard/CRM/Customer Journey/Reports/Projects/Finance/Party Detail) — seluruhnya HTTP 200. Smoke test konten (curl+grep): `OPP-006` menampilkan badge "Terkirim ke Client" dan tombol Mark as Won `disabled title="Client confirmation belum dicatat"`; `OPP-010` quotation-preview menampilkan Tax/Fee, Markup, Inclusions, Exclusions, Terms & Conditions; `OPP-005` menampilkan tombol "Withdraw Submission"; `OPP-010` detail menampilkan "Duplicate Quotation" dan "PDF / Print Preview". `vitest`/`typecheck`/lint tetap pre-existing gap (Q8, tidak berubah).
- **Known issues:** Q8 tetap terbuka. Compare Versions terbatas nilai total (D-062, keputusan disengaja bukan gap tersembunyi). PDF/Print Preview memakai `window.print()` browser (bukan generator PDF nyata, sesuai batasan protokol). Verifikasi interaktif (klik Duplicate/Withdraw/Send to Client/Catat Client Confirmation) tidak dilakukan headless — dimitigasi lewat code review ketat dan smoke test SSR konten per skenario.
- **Cross-section impact:** `docs/mockup-change-impact-log.md` CI-034 — gerbang Mark as Won bertambah syarat Client Confirmation, mengubah perilaku skenario demo `OPP-006` yang sebelumnya didokumentasikan Prompt 20 sebagai "Approved, tombol Mark as Won aktif" (disengaja, bukan regresi).
- **Next action:** Section 06 — Management Approval, Won dan Client Activation. Menunggu perintah user — tidak dieksekusi otomatis.

---

## [Skema Roadmap Baru Section 00–24] Section 06 — Management Approval, Won dan Client Activation

- **Tanggal:** 2026-07-31
- **Status:** COMPLETED
- **Scope dan completed items:** Lengkapi commercial approval dan conversion frontend (`prompts/SECTION_06_Management_Approval_Won_Client_Activation.md`). Audit mengonfirmasi mayoritas Wajib sudah COMPLETED sejak Prompt 19/Section 05 (Approve/Reject Commercial Approval per-Opportunity, hanya Management yang approve, AE melihat hasil dan merevisi bila rejected, Client confirmation mock AE-facing, AE Mark as Won hanya setelah approved+confirmed di level UI, transaksi Won — Prospect→Client/Opportunity→Won/Project Order/PM assignment/activity — sudah ada sejak Section 09). 4 gap konkret ditutup:
  1. **Management Approval Queue** — `/crm/quotations` (placeholder sejak Foundation) ditulis ulang total: tab "Menunggu Approval" (`getQuotationsPendingApproval`, dialog review margin/discount/tax/markup/terms/complexity-dirivasi/risk + aksi Approve/Reject), tab "Menunggu Client Confirmation" (`getOpportunitiesPendingClientConfirmation`, visibilitas Management), tab "Semua Quotation". `comingSoon` dihapus dari nav.
  2. **Approve/Reject/Return for Revision dengan notes/history** — "Reject" berfungsi sekaligus sebagai "Return for Revision" (D-063, tidak ada status paralel baru — `rejectQuotation` sudah membawa quotation kembali dapat direvisi AE lewat "Create New Version"). `submitQuotationForApproval`/`approveQuotation`/`rejectQuotation` kini mencatat `PartyActivity` per keputusan — histori yang sebelumnya tidak ada (hanya `approvalNote` tunggal, tertimpa).
  3. **Gerbang Won di level data** — `approveOpportunityWon` (Section 09) diperkaya guard `quotation.approvalStatus === 'approved' && opportunity.clientConfirmedAt` — forbidden flow benar-benar diblokir di mutator, bukan hanya tombol UI disabled (literal Wajib "dapat diuji melalui role switcher").
  4. **Account Owner AE** — `approveOpportunityWon` menegaskan ulang `party.accountOwnerId = opportunity.ownerId` saat Won (checklist transaksi, aditif — sudah terisi sejak Party dibuat, kini dijamin konsisten di titik Won juga).
  5. **Duplicate Client dan Project Order prevention** — diverifikasi ulang: sudah ada sejak Section 09 (`projectId` guard) dan `qualifyLeadAndCreateOpportunity` (dedup Party by name) — tidak ada perubahan diperlukan.
- **Files created/changed/removed:**
  - Dibuat: `docs/mockup-section-reports/section-06-management-approval-won.md` (laporan ini).
  - Diubah: `app/pages/crm/quotations.vue` (ditulis ulang total dari `ModulePlaceholder`), `app/constants/navigation.ts` (`comingSoon` dihapus), `app/data/index.ts` (+2 selector, 4 mutator existing diperkaya perilakunya), `docs/mockup-design-decisions.md` (+D-063), `docs/mockup-change-impact-log.md` (+CI-036), `docs/mockup-open-questions.md` (Q14 RESOLVED), `docs/frontend-module-map.md`, `docs/frontend-workflow-map.md`, `docs/frontend-implementation-roadmap.md`, `docs/frontend-known-issues.md`, `docs/mockup-implementation-state.md`, `docs/mockup-progress.md` (+Entri 15), `docs/mockup-section-progress.md` (entri ini), `docs/mockup-section-reports/README.md`.
  - Dihapus: Tidak ada.
- **Routes affected:** `/crm/quotations` (isi penuh dari placeholder, route sama). Tidak ada route baru.
- **Components reused/created:** Reused sepenuhnya — `PageHeader`/`SectionCard`/`RoleAccessState`/`Tabs*`/`Table*`/`StatusBadge`/`Dialog*`/`DialogScrollContent`/`DetailMetadataList`/`Button`/`Input`/`Label`/`NuxtLink`/`useToast`. Tidak ada shared component baru.
- **Data/types/constants affected:** Tidak ada perubahan type/shape. 2 selector baru; 4 mutator existing (`submitQuotationForApproval`/`approveQuotation`/`rejectQuotation`/`approveOpportunityWon`) diperkaya perilaku (logging, guard, field reaffirmation) tanpa perubahan signature.
- **Validation results:** `npx nuxi prepare` + `npm run build` sukses (2x run — run pertama sempat terganjal stale build lock file `.nuxt/nuxt.lock` dari proses `dev` lama, dibersihkan sebelum retry). Smoke test HTTP ~29 route (baru+existing representatif lintas seluruh modul) — seluruhnya HTTP 200. Smoke test konten (curl+grep): tab count "Menunggu Approval (1)"/"Menunggu Client Confirmation (1)"/"Semua Quotation (8)" cocok fixture (`QUO-005` submitted, `OPP-006` approved-belum-confirmed, 8 quotation total); `QUO-005` muncul di tab pending-approval (`?tab=pending-approval`, default); `OPP-006` ("Manila Repeat Business Q4 2026") muncul di tab pending-confirmation (`?tab=pending-confirmation`, tab state query-param, pola sama Project Detail); `OPP-001` (won lama) tetap menampilkan link "Lihat Project hasil konversi" tanpa perubahan. `vitest`/`typecheck`/lint tetap pre-existing gap (Q8).
- **Known issues:** Q8 tetap terbuka. Notifikasi push/email Management untuk quotation baru masuk queue TIDAK diimplementasikan (di luar scope, larangan integrasi nyata). Verifikasi interaktif (klik Approve/Reject dari queue) tidak dilakukan headless — dimitigasi lewat reuse mutator yang sudah tervalidasi Prompt 19/Section 05 dan smoke test SSR konten per skenario kunci.
- **Cross-section impact:** `docs/mockup-change-impact-log.md` CI-036 — guard `approveOpportunityWon` dipindah ke level data (menyentuh Section 09, pemilik asli mutator; dan Section 05, pemilik gerbang UI existing) — aditif, regression-tested, tidak mengubah perilaku permitted flow.
- **Next action:** Section 07 — Customer Journey. Menunggu perintah user — tidak dieksekusi otomatis.

---

## [Skema Roadmap Baru Section 00–24] Section 07 — Customer Journey

- **Tanggal:** 2026-07-31
- **Status:** COMPLETED
- **Scope dan completed items:** Lengkapi Customer Journey Dashboard untuk Super Admin dan Account Executive (`prompts/SECTION_07_Customer_Journey.md`). Audit mengonfirmasi mayoritas Wajib sudah COMPLETED sejak Prompt 19: Leads Table/Kanban/Inbox dengan drawer, Customers/Companies list, Customer detail 6-tab PERSIS literal (Overview/Contacts/Opportunities/Project Orders/Activities/Documents), Project Orders list dan detail, seluruh entitas reuse `Party`/`Project`/`Opportunity` (tidak ada dataset paralel, acceptance terpenuhi). 2 gap konkret ditutup:
  1. **Overview funnel Lead→Qualified→Opportunity→Approved→Won→Client→Project Order** — panel baru "Customer Journey Funnel" di `/customer-journey`, 7 tahap dihitung snapshot independen dari kondisi data terkini (bukan cohort historis, D-064 — codebase tidak menyimpan event-log transisi), menampilkan conversion % dari tahap sebelumnya, bar proporsional.
  2. **Drill-down dari metrics ke records** — setiap tahap funnel adalah `NuxtLink` yang deep-link ke halaman list terkait dengan query filter otomatis ter-seed: Lead→`/customer-journey/leads`, Qualified→`?stage=qualified`, Opportunity→`/crm/opportunities`, Approved→`/crm/quotations?tab=all&status=approved` (filter `status` baru ditambahkan ke tab "Semua"), Won→`/crm/opportunities?stage=won`, Client→`/customer-journey/customers?status=client`, Project Order→`/customer-journey/project-orders`. Halaman tujuan (`leads`/`opportunities`/`customers`) diperbarui untuk membaca filter awal dari `route.query` (pola sama `projects/[id]/index.vue`).
  3. **Filters by source, owner, stage, client, date, project type** — Leads (source/owner/stage, existing) + Customers (+owner, baru) + Project Orders (client/status/type/AE/PM existing, +periode keberangkatan 30/60/90 hari, baru) — 6 dimensi literal lengkap.
  4. **AE data scope ke portfolio miliknya; Super Admin seluruh data** — bug ditemukan dan ditutup dalam section yang sama (CI-037): `scopedLeads`/"Active Clients" di dashboard utama sebelumnya TIDAK ter-scope untuk AE sama sekali (hanya Opportunity/Project Order yang benar sejak Prompt 19). Diperbaiki (`handedOverTo`/`getPartiesByAccountOwner` baru) + diperluas konsisten ke `/customer-journey/customers` dan `/customer-journey/project-orders` via toggle "Hanya Portfolio Saya" (default ON untuk AE, pola sama `assignedToMeOnly` Leads Section 04 — bukan hard filter, AE tetap bisa browse semua data bila perlu).
- **Files created/changed/removed:**
  - Dibuat: `docs/mockup-section-reports/section-07-customer-journey.md` (laporan ini).
  - Diubah: `app/pages/customer-journey/index.vue` (rewrite — funnel, scoping fix), `app/pages/customer-journey/leads/index.vue` (+seed `stageFilter` dari query), `app/pages/customer-journey/customers/index.vue` (+owner filter, +toggle portfolio, +seed `statusFilter` dari query), `app/pages/customer-journey/project-orders/index.vue` (+date filter, +toggle portfolio), `app/pages/crm/opportunities/index.vue` (+seed `stageFilter` dari query), `app/pages/crm/quotations.vue` (+filter `status` pada tab "Semua"), `app/data/index.ts` (+`getPartiesByAccountOwner`), `docs/mockup-design-decisions.md` (+D-064), `docs/mockup-change-impact-log.md` (+CI-037), `docs/frontend-module-map.md`, `docs/frontend-workflow-map.md`, `docs/frontend-implementation-roadmap.md`, `docs/frontend-known-issues.md`, `docs/mockup-implementation-state.md`, `docs/mockup-progress.md` (+Entri 16), `docs/mockup-section-progress.md` (entri ini), `docs/mockup-section-reports/README.md`.
  - Dihapus: Tidak ada.
- **Routes affected:** Tidak ada route baru — `/customer-journey`, `/customer-journey/leads`, `/customer-journey/customers`, `/customer-journey/project-orders`, `/crm/opportunities`, `/crm/quotations` seluruhnya mendapat fitur/filter baru pada route yang sama.
- **Components reused/created:** Reused sepenuhnya — `PageHeader`, `SectionCard`, `RoleAccessState`, `StatsCard`, `StatusBreakdownList`, `NuxtLink`, `Checkbox` (pemakaian ke-2 sejak Section 11). Tidak ada shared component baru — funnel ditulis sebagai markup lokal halaman (bukan komponen shared) karena baru dipakai di satu tempat.
- **Data/types/constants affected:** Tidak ada perubahan type/shape. 1 selector baru (`getPartiesByAccountOwner`) di `app/data/index.ts`.
- **Validation results:** `npx nuxi prepare` + `npm run build` sukses (2x run — run pertama gagal kompilasi Vue SFC karena tag `<p>` tanpa penutup dari edit awal `customer-journey/index.vue`, ditemukan dan diperbaiki sebelum retry). Smoke test HTTP ~30 route (Customer Journey lintas sub-halaman + regresi CRM/Projects/Vendors/Finance/Reports/Admin/Supplier/Client) — seluruhnya HTTP 200. Smoke test konten (curl+grep): funnel Super Admin (default role) menampilkan Lead 10/Qualified 3/Opportunity 10/Approved 5/Won 4/Client 3/Project Order 4 — dihitung ulang manual terhadap fixture dan cocok persis (10 lead aktif dari 11 total −1 archived; 3 party berstatus client dari 4; 4 project dari `PRJ-101–104`; 5 quotation approved `QUO-001/002/003/006/008`; 4 opportunity won `OPP-001/002/003/008`). Drill-down dikonfirmasi via inspeksi `<option value="..." selected>` di halaman tujuan untuk seluruh 4 target yang mendukung filter (`leads?stage=qualified`, `opportunities?stage=won`, `customers?status=client`, `quotations?tab=all&status=approved` — quotation approved 5 ID dikonfirmasi persis cocok). `vitest`/`typecheck`/lint tetap pre-existing gap (Q8).
- **Known issues:** Q8 tetap terbuka. Funnel bersifat snapshot (bukan cohort historis per-lead) — didokumentasikan eksplisit di UI dan D-064, bukan gap tersembunyi. `/crm/opportunities`/`/crm/quotations` (di luar folder Customer Journey) TIDAK mendapat AE portfolio scoping — drill-down ke sana tetap lintas-AE, hanya ter-filter stage/status. Verifikasi interaktif (klik toggle portfolio, klik funnel) tidak dilakukan headless — dimitigasi lewat smoke test SSR konten dan inspeksi state filter via query param.
- **Cross-section impact:** `docs/mockup-change-impact-log.md` CI-037 — bug fix AE portfolio scoping pada `/customer-journey/index.vue` yang dimiliki Prompt 19 (Change Request), disengaja/dituju perbaikan (bukan regresi section ini), regression-tested terhadap role non-AE.
- **Next action:** Section 08 — Client Portal. Menunggu perintah user — tidak dieksekusi otomatis.

---

## [Skema Roadmap Baru Section 00–24] Section 08 — Client Portal

- **Tanggal:** 2026-07-31
- **Status:** COMPLETED
- **Scope dan completed items:** Implementasikan role Client secara lengkap pada frontend (`prompts/SECTION_08_Client_Portal.md`), di atas shell minimal Section 02 (`/client`, `clientScopeId` isolation, `RoleId`/`ModuleKey` `client-portal` sudah ada). Wajib literal ditutup seluruhnya:
  1. **Client dashboard** — `/client` ditulis ulang total: stats, Action Center, Profil Company, Support (AE), Contacts, list Opportunity, list Project Order.
  2. **Company profile dan contacts** — Profil Company (read-only) + Contacts list dengan dialog "Tambah Kontak" (reuse `createContact`, Section 07).
  3. **Travel request creation** — dialog di `/client`, membuat `Lead` baru (`source: 'client-portal'`, field baru aditif) + `updateLeadQualification` langsung mengisi draft detail (destinasi/tanggal/traveler/service scope/ringkasan), `handedOverTo`/`ownerId` diisi `party.accountOwnerId` (AE yang sudah menangani company).
  4. **Quotation view, revision request, accept/reject confirmation mock** — `/client/opportunities/[id]` (baru): quotation tersanitasi (amount/discount/tax/currency/validity/terms/inclusions/exclusions/service breakdown, TANPA cost/margin internal), "Minta Revisi" (`createPartyActivity`), "Setujui"/"Tolak" (Setujui = `recordClientConfirmation` self-service, CI-038; Tolak = `createPartyActivity` komunikasi, TIDAK memutasi `approvalStatus`).
  5. **Project Orders milik company** — list di `/client` (link diperbaiki dari `/projects/[id]` yang sebelumnya tidak accessible untuk Client, ke `/client/project-orders/[id]` baru).
  6. **Shared itinerary** — tab "Itinerary" di `/client/project-orders/[id]` (reuse `getItineraryItems`, Section 12).
  7. **Traveler/participant submission** — tab "Travelers", form tambah/edit (reuse `createTraveler`/`updateTraveler`, Section 11).
  8. **Shared documents** — tab "Documents", read-only (keputusan disengaja, D-065 — `DOCUMENTS` bukan `reactive()`).
  9. **Invoice/payment status sell-side** — tab "Finance", Invoice+riwayat Payment (reuse `getInvoicesByProject`/`getPaymentsByInvoice`, Section 15) — TANPA budget/actual cost/margin.
  10. **Change request** — tab "Change Request", list + dialog ajukan (reuse `createChangeEntry`, Section 14; kategori dibatasi `traveler`/`itinerary`/`service`/`other`, menyembunyikan `vendor`/`budget` internal).
  11. **Support/contact AE dan PM** — kontak AE di dashboard utama, kontak AE+PM per Project Order di tab Overview.
  12. **Notifications/action center** — panel "Action Center" di dashboard, agregat quotation pending confirmation + traveler dokumen belum lengkap + invoice overdue.
  13. **Isolasi/larangan margin-cost-client lain** — diverifikasi eksplisit lewat grep audit: nol kecocokan `estimatedCostIdr`/`estimatedMarginIdr`/`markupIdr`/`budgetIdr`/`actualCostIdr`/`approvedBy`/`approvalNote` di 3 file; `clientScopeId` + pengecekan `partyId`/`projectId` milik company sendiri di kedua halaman detail baru (not-found bila bukan milik sendiri).
- **Files created/changed/removed:**
  - Dibuat: `app/pages/client/opportunities/[id]/index.vue`, `app/pages/client/project-orders/[id]/index.vue`, `docs/mockup-section-reports/section-08-client-portal.md` (laporan ini).
  - Diubah: `app/pages/client/index.vue` (ditulis ulang total), `app/types/lead.ts` (`LeadSource` +`client-portal`), `app/constants/status.ts` (`LEAD_SOURCES` +1 entri), `app/constants/navigation.ts` (komentar diperbarui), `docs/mockup-design-decisions.md` (+D-065), `docs/mockup-change-impact-log.md` (+CI-038), `docs/frontend-module-map.md`, `docs/frontend-workflow-map.md`, `docs/frontend-implementation-roadmap.md`, `docs/frontend-known-issues.md`, `docs/mockup-implementation-state.md`, `docs/mockup-progress.md` (+Entri 17), `docs/mockup-section-progress.md` (entri ini), `docs/mockup-section-reports/README.md`.
  - Dihapus: Tidak ada.
- **Routes affected:** `/client/opportunities/[id]` (baru), `/client/project-orders/[id]` (baru). `/client` (rewrite total, route sama).
- **Components reused/created:** Reused sepenuhnya — `PageHeader`, `SectionCard`, `RoleAccessState`, `StatsCard`, `EmptyState`, `StatusBadge`, `DetailMetadataList`, `Dialog*`, `DialogScrollContent`, `Tabs*`, `Table*`, `Button`, `Input`, `Label`, `NuxtLink`, `useToast`. Tidak ada shared component baru.
- **Data/types/constants affected:** `LeadSource` +`'client-portal'` (aditif). Tidak ada mutator/selector baru di `app/data/index.ts` — seluruh aksi reuse mutator generik existing lintas 6 section sebelumnya (04/05/07/11/14).
- **Validation results:** `npx nuxi prepare` + `npm run build` sukses. Smoke test HTTP ~20 route (`/client`, `/client/opportunities/OPP-001`+`OPP-999` not-found, `/client/project-orders/PRJ-101`+`PRJ-999` not-found+`?tab=travelers`/`finance`/`changes`, plus regresi lintas modul) — seluruhnya HTTP 200. Grep audit konten mengonfirmasi nol kecocokan field internal terlarang di 3 file. **Verifikasi interaktif konten role Client TIDAK dapat dilakukan via smoke test SSR/curl** — role-switching adalah mekanisme client-only (`localStorage`), SSR selalu merender sebagai default demo user Super Admin (D-041) yang tidak punya `clientScopeId`, sehingga `/client/*` di bawah curl SELALU menampilkan `RoleAccessState` (perilaku BENAR, tapi tidak membuktikan konten Client-facing sesungguhnya — dicatat eksplisit sebagai keterbatasan tooling yang LEBIH KETAT dari section lain, bukan diabaikan). Dimitigasi lewat code review ketat + grep audit field terlarang + cross-check fixture manual (`PTY-001`/`USR-019` — 2 Project Order Won + 1 Opportunity approved-belum-confirmed; `PTY-002`/`USR-020` — untuk verifikasi isolasi). `vitest`/`typecheck`/lint tetap pre-existing gap (Q8).
- **Known issues:** Q8 tetap terbuka. Shared documents read-only (D-065, keputusan disengaja). Verifikasi interaktif role Client tidak dilakukan headless (keterbatasan lebih ketat dari section lain, dicatat eksplisit — lihat Validation results).
- **Cross-section impact:** `docs/mockup-change-impact-log.md` CI-038 — `recordClientConfirmation` (Section 05) kini dipanggil dari titik akses baru self-service Client, selain titik akses AE-facing existing — aditif, tidak mengubah mutator, regression-tested terhadap flow AE-facing existing.
- **Next action:** Section 09 — Project Order dan Handover. Menunggu perintah user — tidak dieksekusi otomatis.

---

## [Skema Roadmap Baru Section 00–24] Section 09 — Project Order dan Handover

- **Tanggal:** 2026-07-31
- **Status:** COMPLETED
- **Scope dan completed items:** Lengkapi Project Order frontend (`prompts/SECTION_09_Project_Order_Handover.md`). Q16 (blocking) diresolusi sesuai rekomendasi asli (derivasi aditif, pola D-053/D-056):
  1. **`ProjectOrderStatus` 10-nilai** (Created/Handover Pending/Planning/Confirmed/Ready/In Progress/Completed/Closed/On Hold/Cancelled) — dirivasi via `getProjectOrderStatus()` dari `ProjectStatus` (8 nilai, LOCKED D-028, TIDAK direstrukturisasi) + field `handoverAcceptedAt`/`readyAt`/`closedAt` baru pada `Project` (D-066). Badge `PageHeader` `/projects/[id]` diganti dari `PROJECT_STATUSES` mentah ke hasil derivasi ini; status mentah tetap ditampilkan sebagai "Status Internal" di metadata (traceability).
  2. **Source Opportunity dan approved Quotation** — SectionCard "Ringkasan Layanan" diperkaya menampilkan nilai quotation approved via `getQuotationByOpportunity`; bug ditemukan+diperbaiki bersamaan: link opportunity sebelumnya ke list (`/crm/opportunities`), bukan detail spesifik (CI-039).
  3. **AE Account Owner, PM Operational Owner** — AE (dari `sourceOpportunity.ownerId`) kini ditampilkan eksplisit berdampingan PM (`project.ownerId`, sudah ada) di badge Tim Project dan metadata.
  4. **AE-to-PM handover checklist, PM Accept/Return Handover dengan reason** — `acceptProjectHandover`/`returnProjectHandover` (baru), guard hanya dari `status === 'draft'` dan belum accepted; Return TIDAK mengubah status (tetap Handover Pending), mencatat alasan sebagai Activity visible. Acceptance criterion "PM dapat menerima handover dan memulai planning tanpa kehilangan data komersial" terpenuhi otomatis (quotation/budget sudah terisi penuh sejak `approveOpportunityWon`, mutator baru tidak menyentuhnya).
  5. **Team assignment dan role responsibilities** — `addProjectTeamMember`/`removeProjectTeamMember` (baru, `teamUserIds` sebelumnya tanpa mutator sama sekali) + dialog tambah + tombol hapus per anggota, role ditampilkan berdampingan nama.
  6. **Milestones, tasks, dependencies** — `ProjectTask` +`isMilestone`/`dependsOnTaskId`/`assignedTo` (aditif), `TASKS` jadi `reactive()`, Tasks tab ditulis ulang total (sebelumnya read-only murni — tidak ada create/edit meski `dueAt` sudah ada di type sejak Foundation) — dialog "+ Tambah Task", status dropdown per baris, badge Milestone, label "Depends on:".
  7. **Documents, activity** — dikonfirmasi sudah ada sejak Foundation/Section 14, tidak perlu perubahan.
  8. **Risks** — `ProjectRisk` entitas baru (severity/status/lifecycle sendiri, tidak dipaksakan ke model existing), SectionCard "Risks" + dialog catat + cycle status (klik badge Open→Mitigated→Closed→Open).
  9. **Status: 10 nilai literal** — lihat poin 1.
  10. **Transition guards dan visible reason** — `updateProjectStatus` dengan peta `PROJECT_STATUS_TRANSITIONS` eksplisit, reason WAJIB untuk On Hold/Cancelled, dicatat sebagai `ActivityEntry` (reuse tab Activity & Changes existing).
  11. **Closure checklist shell** — `ProjectClosureChecklist` (4 item toggle), SectionCard "Closure Checklist", sengaja TIDAK menggerbangi transisi status apa pun (diserahkan ke Section 24 sesuai instruksi literal).
- **Files created/changed/removed:**
  - Dibuat: `docs/mockup-section-reports/section-09-project-order-handover.md` (laporan ini).
  - Diubah: `app/types/project.ts` (`+ProjectOrderStatus`, `+ProjectClosureChecklist`, `Project` +7 field), `app/types/activity.ts` (`ProjectTask` +3 field, `+ProjectRisk`/`ProjectRiskSeverity`/`ProjectRiskStatus`), `app/constants/status.ts` (`+PROJECT_ORDER_STATUSES`/`RISK_SEVERITIES`/`RISK_STATUSES`), `app/data/activity.ts` (`TASKS` jadi `reactive()`, `+PROJECT_RISKS`), `app/data/projects.ts` (backfill `handoverAcceptedAt` PRJ-101/102/103), `app/data/index.ts` (+13 mutator/selector), `app/pages/projects/[id]/index.vue` (Overview +4 SectionCard, Tasks tab ditulis ulang, bug fix link opportunity), `docs/mockup-design-decisions.md` (+D-066), `docs/mockup-change-impact-log.md` (+CI-039), `docs/mockup-open-questions.md` (Q16 RESOLVED), `docs/frontend-module-map.md`, `docs/frontend-workflow-map.md`, `docs/frontend-implementation-roadmap.md`, `docs/frontend-known-issues.md`, `docs/mockup-implementation-state.md`, `docs/mockup-progress.md` (+Entri 18), `docs/mockup-section-progress.md` (entri ini), `docs/mockup-section-reports/README.md`.
  - Dihapus: Tidak ada.
- **Routes affected:** Tidak ada route baru — `/projects/[id]` (tab Overview dan Tasks) diisi penuh pada route yang sama.
- **Components reused/created:** Reused sepenuhnya — `SectionCard`, `StatusBadge`, `Dialog*`, `DialogScrollContent`, `Button`, `Input`, `Label`, `Checkbox`, `DetailMetadataList`, `EmptyState`, `useToast`. Tidak ada shared component baru.
- **Data/types/constants affected:** `Project` +7 field aditif, `ProjectTask` +3 field aditif, `+ProjectRisk` (entitas baru), `+ProjectOrderStatus`/`ProjectClosureChecklist`/`ProjectRiskSeverity`/`ProjectRiskStatus` (type baru, sebagian dirivasi). 13 mutator/selector baru. `TASKS` jadi `reactive()`.
- **Validation results:** `npx nuxi prepare` + `npm run build` sukses (percobaan pertama, tidak ada error kompilasi). Smoke test HTTP ~18 route (`/projects/PRJ-101`–`104`, `?tab=tasks`, plus regresi CRM/Customer Journey/Client/Vendors/Finance/Reports/Admin/Settings) — seluruhnya HTTP 200. Smoke test konten (curl+grep): `PRJ-104` menampilkan "Handover Pending" (2×) + tombol "Accept Handover"/"Return Handover"; `PRJ-101` menampilkan "Confirmed" + "Tandai Ready" + "Handover diterima"; `PRJ-102` menampilkan "Planning"; `PRJ-103` menampilkan judul 2 risk yang diseed ("Ketersediaan venue MICE...", "Cuaca ekstrem...") dan tab Tasks menampilkan 3× badge "Milestone" + 1× "Depends on:"; link opportunity `PRJ-101` mengarah persis ke `/crm/opportunities/OPP-001` (bukan list) dan menampilkan nilai quotation approved "Rp 95.000.000". `vitest`/`typecheck`/lint tetap pre-existing gap (Q8).
- **Known issues:** Q8 tetap terbuka. Q16 RESOLVED sepenuhnya. Closure Checklist tetap shell tanpa gating (disengaja, D-066, milik Section 24). Dependency task bersifat single-link sederhana (bukan DAG penuh) — evolusi lanjutan bila dibutuhkan. Verifikasi interaktif (klik Accept/Return Handover, transisi status, tambah task/risk) tidak dilakukan headless — dimitigasi lewat code review ketat dan smoke test SSR konten per skenario kunci (4 project dengan derived status berbeda).
- **Cross-section impact:** `docs/mockup-change-impact-log.md` CI-039 — bug fix link opportunity asal di tab Overview (dimiliki Section 10 — Project Core), aditif murni, regression-tested.
- **Next action:** Section 10 — Product Planning dan Costing. Menunggu perintah user — tidak dieksekusi otomatis.

## Section 10 (Roadmap Baru) — Product Planning dan Costing

- **Tanggal:** 2026-07-31
- **Status:** COMPLETED
- **Scope dan completed items:** Modul baru penuh untuk Product Planner/Travel Consultant (`NOT_STARTED` sepenuhnya per audit Section 00): (1) **Product/package templates** — `ProductTemplate` (entitas baru), katalog `/product-planning` (list+detail, create/edit, status draft→active→archived). (2) **Itinerary concept** — field teks ringkasan konsep per template. (3) **Service alternatives** — `serviceAlternatives[]` embedded (label/biaya per pax/rekomendasi/catatan), tabel perbandingan di detail. (4) **Cost sheet, markup, tax/fee, contingency, currency** — `CostSheet` (entitas baru), `/product-planning/cost-sheets` (list+detail), kalkulasi derivasi (`getCostSheetBreakdown`: base cost→+contingency→+markup→+tax=total sell). (5) **Traveler-based costing** — seluruh line item dikalikan `travelerCount`. (6) **Scenario/version comparison** — Cost Sheet dengan `opportunityId` sama = skenario berbeda (demo: CS-003/CS-004 "Economy"/"Premium" pada OPP-009), panel compare + "Duplicate as New Version" (pola sama `duplicateQuotationVersion`). (7) **Inclusions, exclusions, assumptions, validity** — field pada `ProductTemplate` dan `CostSheet`. (8) **Collaboration dengan AE, Operations, Finance** — modul baru `product-planning` (`ModuleKey`), akses `VIEW` untuk AE/Operations/Finance/PM/Management/Viewer, `MANAGE` untuk Product Planner; ringkasan Cost Sheet + link ditambahkan di Opportunity Detail. (9) **Snapshot konsep ketika dipakai pada quotation/project** — `applyCostSheetToQuotation` menyalin hasil kalkulasi ke `Quotation` (dibuat baru atau update draft) lalu mengunci Cost Sheet (`status: 'final'`, tidak dapat diedit lagi). (10) **Internal costing tidak terlihat Client** — dikonfirmasi lewat grep audit: `CostSheet`/`ProductTemplate` tidak diimpor di `app/pages/client/**`; field yang disalin ke Quotation sudah termasuk daftar field tersaring Client Portal sejak D-065.
- **Files created:** `app/types/product.ts`, `app/data/products.ts`, `app/pages/product-planning/index.vue`, `app/pages/product-planning/[id]/index.vue`, `app/pages/product-planning/cost-sheets/index.vue`, `app/pages/product-planning/cost-sheets/[id]/index.vue`, `docs/mockup-section-reports/section-10-product-planning-costing.md`.
- **Files changed:** `app/types/user.ts` (`ModuleKey` +`product-planning`), `app/types/opportunity.ts` (`Quotation` +`costSheetId?`), `app/constants/roles.ts` (`ROLE_MODULE_ACCESS` +kolom `product-planning` seluruh 16 role), `app/constants/navigation.ts` (+menu "Product Planning" 2 sub-item), `app/data/index.ts` (+2 selector dasar, `+getCostSheetBreakdown`, +4 mutator Product Template, +4 mutator Cost Sheet termasuk `applyCostSheetToQuotation`), `app/pages/crm/opportunities/[id]/index.vue` (+SectionCard ringkasan Cost Sheet), `app/pages/index.vue` (widget Product Planner diperbarui dari placeholder), `app/pages/admin/roles.vue` (+1 kolom matrix, +update 2 catatan role).
- **Files removed:** Tidak ada.
- **Routes affected:** `/product-planning` (baru), `/product-planning/[id]` (baru), `/product-planning/cost-sheets` (baru), `/product-planning/cost-sheets/[id]` (baru). Tidak ada route existing yang dihapus/di-rename.
- **Components reused:** `SectionCard`, `PageHeader`, `DetailMetadataList`, `StatusBadge`, `EmptyState`, `RoleAccessState`, `Table*`, `Dialog*`/`DialogScrollContent`, `Tabs*` (tidak dipakai — cukup 1 halaman per entitas), `Button`, `Input`, `Label`, `useToast`. Tidak ada primitive `ui/*` baru.
- **Components created:** Tidak ada komponen file baru (murni page-level, konsisten pola Vendor/Finance).
- **Data/types/constants affected:** `+ProductTemplate`/`ProductServiceAlternative`/`CostSheet`/`CostSheetLineItem` (entitas baru, `app/types/product.ts`), `Quotation` +`costSheetId?` (aditif), `ModuleKey` +`product-planning`. Lihat `docs/mockup-change-impact-log.md` CI-040.
- **Validation results:** `npx nuxi prepare` + `npm run build` sukses (2x run, termasuk setelah update widget Dashboard/Matrix Role). Smoke test HTTP: `/product-planning`, `/product-planning/PRD-001`, `/product-planning/PRD-999` (not-found), `/product-planning/cost-sheets`, `/product-planning/cost-sheets/CS-001`/`CS-003`/`CS-005`, `/product-planning/cost-sheets/CS-999` (not-found), `/crm/opportunities/OPP-001`/`OPP-009`, `/admin/roles` — seluruhnya HTTP 200. Smoke test konten (curl+grep): breakdown CS-003 (Economy Scenario) dihitung ulang manual dan cocok persis (base Rp216.250.000 → +contingency 5% Rp10.812.500 → Rp227.062.500 → +markup 12% Rp27.247.500 → Rp254.310.000 → +tax 5% Rp12.715.500 → **total sell Rp267.025.500**, margin Rp39.963.000); CS-001 (applied) menampilkan badge "Final (Locked)" + pesan snapshot, TANPA tombol Edit/Apply; CS-005 (standalone, tanpa Opportunity) menampilkan Edit+Duplicate TANPA tombol Apply to Quotation; `/crm/opportunities/OPP-009` menampilkan kedua skenario "Economy"/"Premium"; `/admin/roles` menampilkan kolom "Product Planning" baru. Regresi 9 route representatif lintas modul lain dikonfirmasi tidak berubah. `npx nuxi typecheck` gagal (`vue-tsc` belum terpasang, Q8, pre-existing — 10 section berturut-turut); lint tidak tersedia (Q8, pre-existing); `npx vitest run` → 0 test file (pre-existing).
- **Known issues:** Q8 tetap terbuka (10 section berturut-turut). Verifikasi interaktif (klik Apply to Quotation, Duplicate as New Version, toggle scenario compare, ganti role ke Product Planner) tidak dilakukan headless — dimitigasi lewat code review ketat + smoke test konten SSR per skenario kunci (applied/standalone/live-scenario). Currency tetap mock IDR-sentris (D-006, konsisten `Quotation.currency` sejak Section 05) — tidak ada konversi kurs nyata meski field `currency` ada di kedua entitas.
- **Cross-section impact:** `docs/mockup-change-impact-log.md` CI-040 — `Quotation.costSheetId` aditif (dimiliki Section 05/08), SectionCard ringkasan Cost Sheet di Opportunity Detail (dimiliki Section 05/08), widget Dashboard Product Planner diperbarui (dimiliki Section 02, CI-030), kolom Matrix Role `/admin/roles` diperbarui (dimiliki Section 17 lama/Section 02).
- **Next action:** Section 11 — Traveler dan Travel Documents (gap PARTIAL: visa/tiket/asuransi belum di-track), berbasis dependency roadmap. Menunggu perintah user — tidak dieksekusi otomatis.

## Section 11 (Roadmap Baru) — Traveler dan Travel Documents

- **Tanggal:** 2026-07-31
- **Status:** COMPLETED
- **Scope dan completed items:** Melengkapi fondasi traveler/participant (COMPLETED sejak Section 11 lama/Prompt 11) dengan seluruh gap literal Wajib: (1) **Passport/ID/visa metadata** — `Traveler` +`idNumber`/`visaNumber`/`visaExpiryDate`. (2) **Expiry and missing document warnings** — `isTravelerDocumentMissing` diperluas mengevaluasi visa (kondisional, hanya bila `visaNumber` diisi). (3) **Emergency contact, dietary, accessibility, special request** — +`dietaryRestrictions`/`accessibilityNeeds` terpisah dari `specialRequest`. (4) **Group, rooming, companion** — +`companionOfTravelerId` (link ke traveler lain). (5) **Client self-submission** — dikonfirmasi sudah RESOLVED sejak Section 08; form `/client/project-orders/[id]` diperkaya field baru yang sama. (6) **Bulk import preview dan error report mock** — `previewTravelerImportMock`/`commitTravelerImport` (baru) menggantikan `importTravelersMock` lama (langsung commit tanpa preview), lihat CI-041. (7) **Internal verification** — `toggleTravelerVerification` (baru), badge "Terverifikasi"/"Belum Diverifikasi" per traveler, PM/Super Admin saja. (8) **Sensitive values masked sesuai role** — `maskDocumentNumber` (baru, `app/utils/format.ts`), hanya PM/Super Admin melihat nomor paspor/ID/visa penuh. (9) **Manifest/rooming list export preview** — `/projects/[id]/manifest-preview` (baru, print-friendly, pola sama `quotation-preview.vue`). (10) **Readiness indicator** — `getTravelerReadiness` (baru, derivasi: dokumen lengkap/terverifikasi/rooming ditugaskan/persentase readiness), ditampilkan sebagai 4 stat tile di tab Travelers.
- **Files created:** `app/pages/projects/[id]/manifest-preview.vue`, `docs/mockup-section-reports/section-11-traveler-documents.md`.
- **Files changed:** `app/types/project.ts` (`Traveler` +7 field), `app/utils/attention.ts` (`isTravelerDocumentMissing` diperluas), `app/utils/format.ts` (+`maskDocumentNumber`), `app/data/index.ts` (`importTravelersMock` dihapus, +4 mutator/selector baru, `CreateTravelerInput` +6 field), `app/data/projects.ts` (backfill selektif field baru + 2 kondisi visa demo), `app/pages/projects/[id]/index.vue` (tab Travelers diperkaya total: readiness tile, kolom dokumen/catatan, masking, verifikasi, dialog import preview), `app/pages/client/project-orders/[id]/index.vue` (form traveler +5 field).
- **Files removed:** Tidak ada (fungsi `importTravelersMock` dihapus, bukan file).
- **Routes affected:** `/projects/[id]/manifest-preview` (baru). Tidak ada route existing yang dihapus/di-rename.
- **Components reused:** `SectionCard`, `Table*`, `Dialog*`/`DialogScrollContent`, `StatusBadge`, `EmptyState`, `Checkbox`, `useToast`. Tidak ada shared component baru.
- **Data/types/constants affected:** `Traveler` +7 field aditif (lihat bagian Files changed). Lihat `docs/mockup-change-impact-log.md` CI-041.
- **Validation results:** `npx nuxi prepare` + `npm run build` sukses (2x run). Smoke test HTTP `/projects/PRJ-101`/`102`/`103?tab=travelers`, `/projects/PRJ-101/manifest-preview`, `/projects/PRJ-999/manifest-preview` (not-found), `/client/project-orders/PRJ-101?tab=travelers`, plus regresi `/`, `/crm/opportunities`, `/product-planning`, `/vendors`, `/finance`, `/admin/roles`, `/settings` — seluruhnya HTTP 200. Smoke test konten: PRJ-101 readiness tile menunjukkan "Dokumen Lengkap" 6/6, "Terverifikasi" 1/6 (dihitung ulang manual, cocok fixture), companion "Mendampingi: Hendra Wijaya" tampil untuk Nadia Puspita; PRJ-102 menunjukkan 3/6 "Dokumen Belum Lengkap" (1 tanpa paspor + 1 paspor expiring + 1 visa tanpa tanggal kedaluwarsa — kondisi visa baru dikonfirmasi berfungsi); manifest preview PRJ-103 menampilkan section MANIFEST + Rooming List (Twin 101/Suite VIP 1). `vitest`/`typecheck`/lint tetap pre-existing gap (Q8).
- **Known issues:** Q8 tetap terbuka (11 section berturut-turut). Tiket dan asuransi TETAP tidak ada field/UI — di luar Wajib literal Section 11, `KNOWN_GAP` eksplisit (bukan gap tersembunyi). Verifikasi interaktif (klik dialog import preview, toggle verifikasi, ganti role untuk melihat masking) tidak dilakukan headless — dimitigasi lewat code review ketat + smoke test konten SSR yang membuktikan kalkulasi readiness dan kondisi visa baru bekerja benar.
- **Cross-section impact:** `docs/mockup-change-impact-log.md` CI-041 — `importTravelersMock` (Section 11 lama) dihapus diganti alur preview+commit, form traveler Client Portal (Section 08) diperkaya field baru — keduanya aditif/regression-tested.
- **Next action:** Section 12 — Itinerary, Operations, Tasks dan Readiness (readiness checklist/matrix agregat lintas-domain, departure countdown/alert, run sheet — status PARTIAL), berbasis dependency roadmap. Menunggu perintah user — tidak dieksekusi otomatis.

## Section 12 (Roadmap Baru) — Itinerary, Operations, Tasks dan Readiness

- **Tanggal:** 2026-08-01
- **Status:** COMPLETED
- **Scope dan completed items:** "Operational command center" — melengkapi seluruh gap literal di atas fondasi Day-by-day itinerary dan Tasks/owner/deadline/dependency (COMPLETED sejak Section 12 lama/Section 09): (1) **Timezone-aware schedule** — `ItineraryItem` +`timezone` (label teks, ditampilkan berdampingan jam). (2) **Internal vs client-shared itinerary** — +`visibleToClient` (default `true`), `getClientVisibleItineraryItems` (selector filter, satu-satunya titik dipakai Client Portal), toggle "Jadikan Internal"/"Tampilkan ke Client" di halaman internal. (3) **Tasks, checklist, owner, deadline, blocker, dependency** — `ProjectTask` +`isBlocked`/`blockedReason` (baru), dialog blokir dengan alasan wajib, tombol buka blokir. (4) **Service readiness matrix** — `getServiceReadinessMatrix` (baru, derivasi), SectionCard tabel agregat per tipe layanan. (5) **Departure readiness gates** — `getDepartureReadiness` (baru, derivasi lintas-section: traveler/service/task/risk/invoice), badge Ready/Belum Siap + breakdown, bersifat ADVISORY (tidak memblokir transisi status). (6) **Run sheet, contacts, emergency info** — `/projects/[id]/run-sheet-preview` (baru, print-friendly: jadwal + team contacts + vendor contacts + traveler emergency info dengan masking). (7) **Attention/exception queue** — `getProjectAttentionQueue` (baru, agregator lintas tab dengan navigasi klik-langsung). (8) **On-trip updates dan shift notes mock** — `ShiftNote` (entitas baru), SectionCard "On-Trip Updates / Shift Notes" dengan create dialog. (9) **Calendar/timeline views** — toggle List/Timeline pada Daily Itinerary (CSS murni, data sama).
- **Files created:** `app/pages/projects/[id]/run-sheet-preview.vue`, `docs/mockup-section-reports/section-12-itinerary-operations-readiness.md`.
- **Files changed:** `app/types/project.ts` (`ItineraryItem` +2 field), `app/types/activity.ts` (`ProjectTask` +2 field, `+ShiftNote`/`ShiftPeriod`), `app/data/index.ts` (+10 selector/mutator baru), `app/data/activity.ts` (+`SHIFT_NOTES`, backfill `isBlocked` pada `TSK-1021`), `app/data/projects.ts` (backfill `timezone` seluruh item + 3 `ItineraryItem` internal-only baru), `app/pages/projects/[id]/index.vue` (tab Itinerary & Services +4 SectionCard, tab Tasks +blocker UI), `app/pages/client/project-orders/[id]/index.vue` (filter itinerary `visibleToClient`).
- **Files removed:** Tidak ada.
- **Routes affected:** `/projects/[id]/run-sheet-preview` (baru). Tidak ada route existing yang dihapus/di-rename.
- **Components reused:** `SectionCard`, `Table*`, `Dialog*`, `StatusBadge`, `EmptyState`, `RoleAccessState`, `useToast`. Tidak ada shared component baru (halaman run-sheet-preview mengikuti pola `quotation-preview.vue`/`manifest-preview.vue` persis).
- **Data/types/constants affected:** `ItineraryItem` +2 field aditif, `ProjectTask` +2 field aditif, `+ShiftNote`/`ShiftPeriod` (entitas baru). Lihat `docs/mockup-change-impact-log.md` CI-042.
- **Validation results:** `npx nuxi prepare` + `npm run build` sukses. Smoke test HTTP `/projects/PRJ-101`/`102`/`103?tab=itinerary-services`/`tab=tasks`, `/projects/PRJ-101/run-sheet-preview`, `/projects/PRJ-999/run-sheet-preview` (not-found), `/client/project-orders/PRJ-101`/`102?tab=itinerary`, plus regresi 8 route lain — seluruhnya HTTP 200. Smoke test konten: PRJ-101 menampilkan "Ready to Depart" + timezone "Asia/Jakarta"/"Asia/Manila" + badge "Internal Only"; PRJ-102 menampilkan "Belum Siap" + Attention Queue berisi pesan task diblokir + Tasks tab menampilkan badge "Blocked"; PRJ-103 menampilkan 2 Shift Notes yang diseed; Client Portal PRJ-102 dikonfirmasi TIDAK menampilkan item internal-only baru tapi tetap menampilkan item lama. `vitest`/`typecheck`/lint tetap pre-existing gap (Q8).
- **Known issues:** Q8 tetap terbuka (12 section berturut-turut). Readiness gate bersifat advisory (disengaja, D-069) — tidak menggerbangi transisi status apa pun. Halaman/dashboard dedicated per sub-domain (Ticketing/Accommodation/Transportation/MICE) tetap milik Section 13-16. Verifikasi interaktif (klik toggle internal-only, blokir task, catat shift note, toggle timeline) tidak dilakukan headless — dimitigasi lewat code review ketat + smoke test konten SSR.
- **Cross-section impact:** `docs/mockup-change-impact-log.md` CI-042 — filter itinerary Client Portal (Section 08), tab Itinerary & Services (Section 12 lama) dan Tasks (Section 09) diperkaya — seluruhnya aditif/regression-tested.
- **Next action:** Section 13 — Ticketing (halaman dedicated, PNR/e-ticket mock, dashboard tersendiri — status PARTIAL), berbasis dependency roadmap. Menunggu perintah user — tidak dieksekusi otomatis.

## Section 13 (Roadmap Baru) — Ticketing

- **Tanggal:** 2026-08-01
- **Status:** COMPLETED
- **Scope dan completed items:** Melengkapi lifecycle flight frontend penuh — modul baru `/ticketing`, entitas baru `FlightBooking` (terpisah dari `ProjectService` generik existing). (1) **Flight request/options** — `FlightOption[]` embedded, dibuat sebagai status awal `requested`. (2) **Fare/cabin/baggage/ancillary display** — field per opsi, tabel perbandingan + tombol "Pilih" (single-select). (3) **PNR/order reference mock** — field `pnr`, ditautkan ke `bookingReference` existing untuk booking yang sudah ada. (4) **Segments dan traveler assignment** — `FlightSegment[]` + `travelerIds[]`, SEKALIGUS berfungsi sebagai "Group booking/name list". (5) **Ticketing deadline** — field tanggal, ditampilkan di ringkasan dan list. (6) **Hold/Confirm/Issue/Reissue/Cancel/Refund state simulation** — `FlightBookingStatus` 7 nilai, peta transisi eksplisit, alasan wajib untuk Cancel/Refund (dicatat sebagai `ActivityEntry`). (7) **Schedule change/disruption** — flag + catatan, banner di halaman detail. (8) **Fare rules and financial impact** — teks bebas + breakdown finansial. (9) **Internal net cost vs client sell price** — dua field terpisah, `netCostIdr` hanya untuk Ticketing/role finansial penuh, TIDAK PERNAH tampil di dokumen client-facing. (10) **Documents/ticket preview** — `/ticketing/[id]/eticket-preview` (print-friendly, sanitized). Modul baru `ticketing` (`ModuleKey`) — Ticketing `MANAGE`, Management/Finance/PM/Operations/Viewer `VIEW`. Resolusi eksplisit ketegangan D-020 vs Wajib literal — didokumentasikan sebagai D-070, preseden untuk Section 14–16.
- **Files created:** `app/types/ticketing.ts`, `app/data/ticketing.ts`, `app/pages/ticketing/index.vue`, `app/pages/ticketing/[id]/index.vue`, `app/pages/ticketing/[id]/eticket-preview.vue`, `docs/mockup-section-reports/section-13-ticketing.md`.
- **Files changed:** `app/types/user.ts` (`ModuleKey` +`ticketing`), `app/constants/roles.ts` (`ROLE_MODULE_ACCESS` +kolom `ticketing` seluruh role), `app/constants/navigation.ts` (+menu "Ticketing"), `app/constants/status.ts` (+`FLIGHT_BOOKING_STATUSES`/`CABIN_CLASSES`), `app/data/index.ts` (+selector/mutator Flight Booking lengkap), `app/pages/projects/[id]/index.vue` (+ringkasan Flight Booking di sub-section flight), `app/pages/admin/roles.vue` (+1 kolom matrix, +update catatan role Ticketing).
- **Files removed:** Tidak ada.
- **Routes affected:** `/ticketing` (baru), `/ticketing/[id]` (baru), `/ticketing/[id]/eticket-preview` (baru). Tidak ada route existing yang dihapus/di-rename.
- **Components reused:** `SectionCard`, `PageHeader`, `DetailMetadataList`, `StatusBadge`, `EmptyState`, `RoleAccessState`, `Table*`, `Dialog*`/`DialogScrollContent`, `Checkbox`, `useToast`. Tidak ada shared component baru.
- **Data/types/constants affected:** `+FlightBooking`/`FlightOption`/`FlightSegment`/`FlightBookingStatus`/`CabinClass` (entitas dan type baru). `ModuleKey` +`ticketing`. Lihat `docs/mockup-change-impact-log.md` CI-043.
- **Validation results:** `npx nuxi prepare` + `npm run build` sukses. Smoke test HTTP `/ticketing`, `/ticketing/FLT-1011`/`1021`/`1032`/`1033`, `/ticketing/FLT-9999` (not-found), `/ticketing/FLT-1011/eticket-preview`, plus regresi `/projects/PRJ-101`/`103?tab=itinerary-services`, `/admin/roles` — seluruhnya HTTP 200. Smoke test konten: list menampilkan 6 Flight Booking dengan 5 dari 7 status lifecycle terwakili (Requested/Hold/Issued/Reissued/Refunded — Confirmed/Cancelled sengaja tidak di-seed, tetap reachable lewat transisi UI); FLT-1011 menampilkan opsi terpilih "Garuda Nusantara" + Net Cost/Sell Price/Margin; FLT-1021 menampilkan banner Schedule Change merujuk CHG-1021; e-ticket preview FLT-1011 dikonfirmasi HANYA menampilkan Sell Price (Rp95.000.000), TIDAK menampilkan Net Cost (Rp75.000.000) — sanitasi terverifikasi. `vitest`/`typecheck`/lint tetap pre-existing gap (Q8).
- **Known issues:** Q8 tetap terbuka (13 section berturut-turut). Accommodation/Transportation/MICE (Section 14-16) tetap `KNOWN_GAP` — pola arsitektur D-070 didokumentasikan sebagai preseden. Verifikasi interaktif (klik pilih opsi, transisi status dengan dialog alasan, toggle traveler assignment) tidak dilakukan headless — dimitigasi lewat code review ketat + smoke test konten SSR.
- **Cross-section impact:** `docs/mockup-change-impact-log.md` CI-043 — ringkasan Flight Booking di tab Itinerary & Services (Section 12 lama) dan kolom Matrix Role (Section 02/17 lama) — seluruhnya aditif/regression-tested.
- **Next action:** Section 14 — Accommodation (halaman dedicated, room block/voucher mock, dashboard tersendiri — status PARTIAL), berbasis dependency roadmap, mengikuti preseden arsitektur D-070. Menunggu perintah user — tidak dieksekusi otomatis.

## Section 14 (Roadmap Baru) — Accommodation

- **Tanggal:** 2026-08-01
- **Status:** COMPLETED
- **Scope dan completed items:** Melengkapi lifecycle hotel frontend penuh — modul baru `/accommodation`, entitas baru `HotelBooking` (terpisah dari `ProjectService` generik existing), pola arsitektur IDENTIK D-070 (Section 13/Ticketing). (1) **Hotel sourcing/options** — `HotelOption[]` embedded, dibuat sebagai status awal `requested`. (2) **Property, room type, rate plan, meal, policies** — field per opsi, tabel perbandingan + tombol "Pilih" (single-select). (3) **Room block, occupancy, rooming list** — SENGAJA TIDAK diduplikasi: `HotelBooking.groupId` (opsional) menautkan ke `TravelerGroup` (Section 11) existing, `getHotelRoomingList` (selector baru, read-only) membaca `RoomAssignment` existing apa adanya. (4) **Check-in/out, early/late request** — field tanggal + flag boolean, ditampilkan sebagai badge. (5) **Quote, booking, confirmation, voucher** — `confirmationNumber` (pola sama `pnr`), `voucherIssuedAt`, halaman `/accommodation/[id]/voucher-preview` (print-friendly, sanitized). (6) **Amendment, cancellation, no-show, deadline, penalty** — `HotelBookingStatus` 7 nilai, peta transisi eksplisit (`amended` BUKAN status terminal — berbeda dari pola `reissued` pada Flight), alasan wajib untuk Cancel/No-Show (dicatat sebagai `ActivityEntry`), `cancellationPenaltyIdr`/`noShowPenaltyIdr` opsional. (7) **Traveler special requests** — SENGAJA TIDAK diduplikasi: `travelerIds` menunjuk ke `Traveler` existing, halaman detail membaca `specialRequest`/`dietaryRestrictions`/`accessibilityNeeds` (Section 11) langsung. (8) **Internal cost isolation** — `netCostIdr`/`sellPriceIdr` dua field terpisah, `netCostIdr` hanya untuk Accommodation/role finansial penuh, TIDAK PERNAH tampil di dokumen client-facing. Acceptance "individual maupun group" didemokan dalam satu entitas yang sama (`travelerIds` panjang 1 = individual, banyak = group — bukan flag/type terpisah). Modul baru `accommodation` (`ModuleKey`) — Accommodation `MANAGE`, Management/Finance/PM/Operations/Viewer `VIEW`.
- **Files created:** `app/types/accommodation.ts`, `app/data/accommodation.ts`, `app/pages/accommodation/index.vue`, `app/pages/accommodation/[id]/index.vue`, `app/pages/accommodation/[id]/voucher-preview.vue`, `docs/mockup-section-reports/section-14-accommodation.md`.
- **Files changed:** `app/types/user.ts` (`ModuleKey` +`accommodation`), `app/constants/roles.ts` (`ROLE_MODULE_ACCESS` +kolom `accommodation` seluruh role), `app/constants/navigation.ts` (+menu "Accommodation"), `app/constants/status.ts` (+`HOTEL_BOOKING_STATUSES`/`MEAL_PLANS`), `app/data/index.ts` (+selector/mutator Hotel Booking lengkap, +`getHotelRoomingList`), `app/pages/projects/[id]/index.vue` (+ringkasan Hotel Booking di sub-section hotel), `app/pages/admin/roles.vue` (+1 kolom matrix, +update catatan role Accommodation).
- **Files removed:** Tidak ada.
- **Routes affected:** `/accommodation` (baru), `/accommodation/[id]` (baru), `/accommodation/[id]/voucher-preview` (baru). Tidak ada route existing yang dihapus/di-rename.
- **Components reused:** `SectionCard`, `PageHeader`, `DetailMetadataList`, `StatusBadge`, `EmptyState`, `RoleAccessState`, `Table*`, `Dialog*`/`DialogScrollContent`, `Checkbox`, `useToast`. Tidak ada shared component baru.
- **Data/types/constants affected:** `+HotelBooking`/`HotelOption`/`HotelBookingStatus`/`MealPlan` (entitas dan type baru). `ModuleKey` +`accommodation`. Lihat `docs/mockup-change-impact-log.md` CI-044.
- **Validation results:** `npx nuxi prepare` + `npm run build` sukses. Smoke test HTTP `/accommodation`, `/accommodation/HTL-1022`/`1023`/`1033`/`1034`/`1035`/`1036`, `/accommodation/HTL-9999` (not-found), `/accommodation/HTL-1022/voucher-preview`, plus regresi `/projects/PRJ-101`/`102`/`103?tab=itinerary-services`, `/admin/roles` — seluruhnya HTTP 200. Smoke test konten: list menampilkan 6 Hotel Booking dengan 5 dari 7 status lifecycle terwakili (Requested/Quoted/Confirmed/Amended/Cancelled — Completed/No-Show sengaja tidak di-seed, tetap reachable lewat transisi UI); HTL-1022 menampilkan banner Amendment + opsi terpilih "Corniche Suite Wing" + Net Cost/Sell Price/Margin; HTL-1033 rooming list menampilkan `ROOM-001` (Twin 101, Michael Tanuwijaya/Putri Anggraeni) — dikonfirmasi reuse persis, bukan data baru; HTL-1035 menampilkan catatan aksesibilitas kursi roda (reuse `Traveler.accessibilityNeeds`); voucher preview HTL-1022 dikonfirmasi menampilkan HANYA Sell Price (Rp152.900.000), teks "Net Cost" dan angka Rp129.600.000 sama sekali TIDAK muncul — sanitasi terverifikasi. `vitest`/`typecheck`/lint tetap pre-existing gap (Q8).
- **Known issues:** Q8 tetap terbuka (14 section berturut-turut). Transportation/MICE (Section 15-16) tetap `KNOWN_GAP` — pola arsitektur D-070/D-071 didokumentasikan sebagai preseden. Verifikasi interaktif (klik pilih opsi, transisi status dengan dialog alasan, toggle traveler assignment) tidak dilakukan headless — dimitigasi lewat code review ketat + smoke test konten SSR.
- **Cross-section impact:** `docs/mockup-change-impact-log.md` CI-044 — ringkasan Hotel Booking di tab Itinerary & Services (Section 12 lama) dan kolom Matrix Role (Section 02/17 lama) — seluruhnya aditif/regression-tested. Rooming list Hotel Booking Detail membaca `RoomAssignment`/`TravelerGroup` (Section 11) read-only, tidak menulis/mengubah shape-nya.
- **Next action:** Section 15 — Transportation (halaman dedicated, driver/vehicle mock, dashboard tersendiri — status PARTIAL), berbasis dependency roadmap, mengikuti preseden arsitektur D-070/D-071. Menunggu perintah user — tidak dieksekusi otomatis.

## Section 15 (Roadmap Baru) — Transportation

- **Tanggal:** 2026-08-01
- **Status:** COMPLETED
- **Scope dan completed items:** Melengkapi lifecycle transportasi darat frontend penuh — modul baru `/transportation`, entitas baru `TransportBooking` (terpisah dari `ProjectService` generik existing), pola arsitektur IDENTIK D-070/D-071 (Section 13/Ticketing, Section 14/Accommodation). (1) **Transfer/service request** — field `transferType` (airport-pickup/dropoff/point-to-point/full-day-charter/multi-day-charter). (2) **Pickup/drop-off, route, schedule** + **Multi-leg dispatch** — `TransportLeg[]` embedded, satu booking dapat memiliki banyak leg. (3) **Vehicle type, capacity, luggage, accessibility** — `TransportOption[]` embedded, tabel perbandingan + tombol "Pilih" (single-select). (4) **Supplier, vehicle, driver, contact** — supplier DIREUSE dari `ProjectService.vendorId`/`Vendor` existing; `assignedVehiclePlateNumber`/`driverName`/`driverPhone` field baru. (5) **Manifest/group allocation** — SENGAJA TIDAK diduplikasi: `groupId` menautkan ke `TravelerGroup` (Section 11), `travelerIds` menunjuk ke `Traveler` existing (1 entri = individual, banyak = group). (6) **Standby/overtime/toll** — field eksplisit terpisah dari rate dasar. (7) **Quote, assignment, confirmation, service order, driver sheet** — `TransportBookingStatus` 7 nilai mengikuti urutan literal (`requested`/`quoted`/`assigned`/`confirmed`/`completed`/`cancelled`/`no-show`), alasan wajib untuk Cancel/No-Show (dicatat sebagai `ActivityEntry`); DUA halaman dokumen terpisah — `service-order-preview.vue` (client-facing, sanitized, hanya sell price) dan `driver-sheet-preview.vue` (internal driver, TIDAK ada informasi harga sama sekali). (8) **Change, cancellation, incident, no-show** — `hasChange`/`changeNote` dan `hasIncident`/`incidentNote` sebagai flag terpisah (bukan status lifecycle tambahan), TIDAK ada field "penalty" (berbeda dari Accommodation — literal Wajib Section 15 tidak menyebutkannya). Acceptance "merencanakan dan menutup seluruh service" didemokan lewat transisi lengkap requested→completed/cancelled/no-show pada fixture. Modul baru `transportation` (`ModuleKey`) — Transportation `MANAGE`, Management/Finance/PM/Operations/Viewer `VIEW`.
- **Files created:** `app/types/transportation.ts`, `app/data/transportation.ts`, `app/pages/transportation/index.vue`, `app/pages/transportation/[id]/index.vue`, `app/pages/transportation/[id]/service-order-preview.vue`, `app/pages/transportation/[id]/driver-sheet-preview.vue`, `docs/mockup-section-reports/section-15-transportation.md`.
- **Files changed:** `app/types/user.ts` (`ModuleKey` +`transportation`), `app/constants/roles.ts` (`ROLE_MODULE_ACCESS` +kolom `transportation` seluruh role), `app/constants/navigation.ts` (+menu "Transportation"), `app/constants/status.ts` (+`TRANSPORT_BOOKING_STATUSES`/`VEHICLE_TYPES`), `app/data/index.ts` (+selector/mutator Transport Booking lengkap), `app/pages/projects/[id]/index.vue` (+ringkasan Transport Booking di sub-section transportation), `app/pages/admin/roles.vue` (+1 kolom matrix, +update catatan role Transportation).
- **Files removed:** Tidak ada.
- **Routes affected:** `/transportation` (baru), `/transportation/[id]` (baru), `/transportation/[id]/service-order-preview` (baru), `/transportation/[id]/driver-sheet-preview` (baru). Tidak ada route existing yang dihapus/di-rename.
- **Components reused:** `SectionCard`, `PageHeader`, `DetailMetadataList`, `StatusBadge`, `EmptyState`, `RoleAccessState`, `Table*`, `Dialog*`/`DialogScrollContent`, `Checkbox`, `useToast`. Tidak ada shared component baru.
- **Data/types/constants affected:** `+TransportBooking`/`TransportOption`/`TransportLeg`/`TransportBookingStatus`/`VehicleType`/`TransportRateUnit`/`TransferType` (entitas dan type baru). `ModuleKey` +`transportation`. Lihat `docs/mockup-change-impact-log.md` CI-045.
- **Validation results:** `npx nuxi prepare` + `npm run build` sukses. Smoke test HTTP `/transportation`, `/transportation/TRN-1034`/`1035`/`1036`/`1037`/`1038`, `/transportation/TRN-9999` (not-found), `/transportation/TRN-1034/service-order-preview`, `/transportation/TRN-1034/driver-sheet-preview`, plus regresi `/projects/PRJ-103?tab=itinerary-services`, `/admin/roles` — seluruhnya HTTP 200. Smoke test konten: list menampilkan 5 Transport Booking dengan 5 dari 7 status lifecycle terwakili (Quoted/Assigned/Confirmed/Completed/Cancelled — Requested/No-Show sengaja tidak di-seed, tetap reachable lewat transisi UI); TRN-1034 menampilkan unit "DN 1234 AB"/driver "Herman Wijaya"/badge Standby 4 jam+Toll Rp150.000; TRN-1035 menampilkan banner Change ("dimajukan dari 10:00"); TRN-1036 menampilkan banner Incident ("kendala AC") dan catatan aksesibilitas kursi roda (reuse `Traveler.accessibilityNeeds`); Service Order TRN-1034 dikonfirmasi menampilkan HANYA Sell Price (Rp7.200.000), TIDAK menampilkan Net Cost; Driver Sheet TRN-1034 dikonfirmasi TIDAK menampilkan angka Rupiah sama sekali (nol harga) — sanitasi ganda terverifikasi. `vitest`/`typecheck`/lint tetap pre-existing gap (Q8).
- **Known issues:** Q8 tetap terbuka (15 section berturut-turut). MICE (Section 16) tetap `KNOWN_GAP` — pola arsitektur D-070/D-071/D-072 didokumentasikan sebagai preseden. Verifikasi interaktif tidak dilakukan headless — dimitigasi lewat code review ketat + smoke test konten SSR. `requested`/`no-show` tidak di-seed pada fixture, murni keterbatasan jumlah baris demo.
- **Cross-section impact:** `docs/mockup-change-impact-log.md` CI-045 — ringkasan Transport Booking di tab Itinerary & Services (Section 12 lama) dan kolom Matrix Role (Section 02/17 lama) — seluruhnya aditif/regression-tested. `VendorQuotation` VQ-009/VQ-010 (SVC-1034, Section 13 lama) TIDAK diubah statusnya.
- **Next action:** Section 16 — MICE dan Event (halaman dedicated, venue/BOQ/staffing mock, dashboard tersendiri — status PARTIAL), berbasis dependency roadmap, mengikuti preseden arsitektur D-070/D-071/D-072. Menunggu perintah user — tidak dieksekusi otomatis.

## Section 16 (Roadmap Baru) — MICE dan Event

- **Tanggal:** 2026-08-01
- **Status:** COMPLETED
- **Scope dan completed items:** Melengkapi lifecycle MICE/Event frontend penuh — modul baru `/mice`, entitas baru `MiceEvent` (terpisah dari `ProjectService` generik existing), pola arsitektur IDENTIK D-070/D-071/D-072 (Section 13-15) — namun berbeda strukturnya: SATU event kaya-struktur (bukan banyak booking) sesuai realita satu acara MICE per project. (1) **Brief, venue, rooms/sessions, agenda/rundown** — field literal + `MiceSession[]` embedded (roomName/sessionTitle/startAt/endAt/capacity/picUserId/isConfirmed). (2) **Participant categories** + **Attendance** — `MiceParticipantCategory[]` (expectedCount/actualCount dalam satu struct). (3) **Catering, AV, staging, equipment, booths** + **Vendor packages dan BOQ** — `MiceBoqItem[]` embedded (6 kategori), `vendorId` opsional DIREUSE dari `Vendor` existing, total DERIVASI (`getMiceBoqTotals`, bukan field tersimpan). (4) **Staffing/PIC** — `MiceStaffAssignment[]`, `userId` DIREUSE dari `User` existing. (5) **Setup/teardown/rehearsal/permit checklist** + **Deliverables** — embedded array dengan toggle langsung (`toggleMiceChecklistItem`/`toggleMiceDeliverable`). (6) **Capacity and schedule conflicts** — `getMiceScheduleConflicts` (derivasi ADVISORY, tidak memblokir transisi apa pun), mendeteksi overlap sesi di room sama DAN kapasitas vs total peserta; fixture mendemokan kondisi nyata (kapasitas 100 < 125 peserta) menautkan ke `RSK-1031`/`TSK-1032` existing. (7) **Client approval states** — `MiceApprovalStatus` (draft/submitted/approved/rejected) TERPISAH dari `MiceEventStatus` (planning/confirmed/in-progress/completed/cancelled), pola sama Commercial Approval (D-049); `rejected` dapat direvisi-ulang, `approved` stabil (perubahan lanjutan lewat Change Order). (8) **Change order, incident, attendance, deliverables** — `hasChangeOrder`/`hasIncident` sebagai flag terpisah (pola sama Transportation D-072), attendance via `actualCount`, deliverables via checklist terpisah. Acceptance "planning sampai post-event completion" didemokan lewat transisi lengkap pada fixture. DUA dokumen client-facing tersanitasi: `rundown-preview.vue` (agenda, tanpa angka finansial sama sekali) dan `boq-preview.vue` (breakdown BOQ, HANYA sell price). Modul baru `mice` (`ModuleKey`) — MICE `MANAGE`, Management/Finance/PM/Operations/Viewer `VIEW`.
- **Files created:** `app/types/mice.ts`, `app/data/mice.ts`, `app/pages/mice/index.vue`, `app/pages/mice/[id]/index.vue`, `app/pages/mice/[id]/rundown-preview.vue`, `app/pages/mice/[id]/boq-preview.vue`, `docs/mockup-section-reports/section-16-mice.md`.
- **Files changed:** `app/types/user.ts` (`ModuleKey` +`mice`), `app/constants/roles.ts` (`ROLE_MODULE_ACCESS` +kolom `mice` seluruh role), `app/constants/navigation.ts` (+menu "MICE"), `app/constants/status.ts` (+`MICE_EVENT_STATUSES`/`MICE_APPROVAL_STATUSES`/`MICE_BOQ_CATEGORIES`/`MICE_CHECKLIST_TASKS`), `app/data/index.ts` (+selector/mutator MICE Event lengkap), `app/pages/projects/[id]/index.vue` (+ringkasan MICE Event di sub-section mice), `app/pages/admin/roles.vue` (+1 kolom matrix, +update catatan role MICE).
- **Files removed:** Tidak ada.
- **Routes affected:** `/mice` (baru), `/mice/[id]` (baru), `/mice/[id]/rundown-preview` (baru), `/mice/[id]/boq-preview` (baru). Tidak ada route existing yang dihapus/di-rename.
- **Components reused:** `SectionCard`, `PageHeader`, `DetailMetadataList`, `StatusBadge`, `EmptyState`, `RoleAccessState`, `Table*`, `Dialog*`/`DialogScrollContent`, `Checkbox`, `useToast`. Tidak ada shared component baru.
- **Data/types/constants affected:** `+MiceEvent`/`MiceSession`/`MiceParticipantCategory`/`MiceBoqItem`/`MiceStaffAssignment`/`MiceChecklistItem`/`MiceDeliverable`/`MiceEventStatus`/`MiceApprovalStatus`/`MiceBoqCategory`/`MiceChecklistTask` (entitas dan type baru). `ModuleKey` +`mice`. Lihat `docs/mockup-change-impact-log.md` CI-046.
- **Validation results:** `npx nuxi prepare` + `npm run build` sukses. Smoke test HTTP `/mice`, `/mice/MICE-1035`, `/mice/MICE-9999` (not-found), `/mice/MICE-1035/rundown-preview`, `/mice/MICE-1035/boq-preview`, `/mice/MICE-9999/rundown-preview`/`boq-preview` (not-found), plus regresi `/projects/PRJ-103?tab=itinerary-services`, `/admin/roles`, `/accommodation`, `/ticketing`, `/transportation` — seluruhnya HTTP 200. Smoke test konten: MICE-1035 menampilkan banner Capacity/Schedule Conflict ("kapasitas ruangan (100 pax) lebih kecil dari total peserta yang diharapkan (125 pax)") dihitung ulang manual dan cocok persis; banner Change Order dan Incident tampil dengan narasi yang menautkan ke `RSK-1031`/`TSK-1032`/`TSK-1033` existing; BOQ preview menampilkan total Rp282.000.000 (6 baris dijumlah ulang manual, cocok) TANPA kemunculan net cost di manapun; rundown preview TANPA angka finansial sama sekali. `vitest`/`typecheck`/lint tetap pre-existing gap (Q8).
- **Known issues:** Q8 tetap terbuka (16 section berturut-turut). Seluruh 4 sub-domain operasional (Ticketing/Accommodation/Transportation/MICE, Section 13-16) kini RESOLVED — tidak ada lagi `KNOWN_GAP` role-only tersisa di kelompok ini. Verifikasi interaktif tidak dilakukan headless — dimitigasi lewat code review ketat + smoke test konten SSR. `requested`/`completed`/`no-show` (status yang belum di-seed pada fixture tunggal) tetap reachable via UI transisi.
- **Cross-section impact:** `docs/mockup-change-impact-log.md` CI-046 — ringkasan MICE Event di tab Itinerary & Services (Section 12 lama) dan kolom Matrix Role (Section 02/17 lama) — seluruhnya aditif/regression-tested. `RSK-1031`/`TSK-1032`/`TSK-1033`/`SFT-1032` (Section 09/12 lama) HANYA dirujuk secara naratif, tidak diubah.
- **Next action:** Section 17 — Supplier dan Procurement (RFQ formal/comparison/clarification, Service Order/amendment/acknowledgment, supplier invoice submission preview — status PARTIAL), berbasis dependency roadmap, dapat mengikuti preseden arsitektur D-070/D-071/D-072/D-073 bila relevan. Menunggu perintah user — tidak dieksekusi otomatis.

## Section 17 (Roadmap Baru) — Supplier dan Procurement

- **Tanggal:** 2026-08-01
- **Status:** COMPLETED
- **Scope dan completed items:** Melengkapi Procurement dan Supplier Portal penuh — modul top-level baru `/procurement` MENDAMPINGI `/vendors` (master data, Section 13 lama) dan `/supplier` (self-service, Prompt 19), pola arsitektur mengikuti preseden D-070/D-071/D-072/D-073 (D-074). Entitas baru `RFQ`/`RFQInvitation`/`RFQResponse`/`RFQClarificationMessage`/`ServiceOrder`/`ServiceOrderAmendment`/`SupplierInvoice` — seluruhnya mereuse `Vendor.id` existing, bukan Supplier paralel. (1) **Supplier companies, contacts, categories, documents, status** — `Vendor` diperluas aditif `category`/`status`/`documents` (`VendorDocument`, tab "Documents" baru Vendor Detail, 5→6 tab). (2) **Catalog/services/rates** — DIREUSE `VendorProduct` existing (Prompt 19), tidak diduplikasi. (3) **RFQ, supplier response, comparison, clarification, selection** — `RFQ` lifecycle 7-status (`draft`→`sent`→`responses-in`→`comparison`⇄`clarification`→`selected`→`closed`, transisi `sent→responses-in` otomatis lewat `submitRfqResponse`), side-by-side comparison table (`getRfqResponses`, diurutkan harga terendah), clarification thread per vendor dua-arah (`RFQClarificationMessage`), formal "Select" action (`selectRfqVendor`, response lain otomatis `rejected`). (4) **Service Order, amendment, acknowledgment, fulfillment** — `ServiceOrder` lifecycle 6-status (`draft`→`sent`→`acknowledged`⇄`amended`→`fulfilled`/`cancelled`, `amended` BUKAN terminal), handoff RFQ→Service Order via tombol "Buat Service Order" (RFQ detail, status `selected`/`closed`), `ServiceOrderAmendment` append-only. (5) **Supplier dashboard dan portal** — `/supplier` mendapat 2 link card baru (RFQ Inbox, Service Orders) + 2 stat card. (6) **Supplier quotation, documents, fulfillment updates, invoice submission preview** — `/supplier/rfq/[id]` (respons harga per-line-item, resubmit sampai keputusan, clarification reply), `/supplier/service-orders/[id]` (Acknowledge, Tandai Fulfilled, Invoice Submission form — `submitSupplierInvoice`, hanya untuk Service Order `fulfilled` milik vendor sendiri, resolusi Q12). (7) **"PT ABC dan PT EFG memiliki produk berbeda"** — TERPENUHI (fixture RFQ-002 mengundang keduanya untuk kebutuhan berbeda, RFQ-003 clarification thread aktif dengan PT ABC), melanjutkan `VENDOR_PRODUCTS` Prompt 19. (8) **Supplier isolation berdasarkan company** — seluruh halaman `/supplier/rfq`/`/supplier/service-orders` di-scope `vendorScopeId` (pola sama existing), guard eksplisit di `submitSupplierInvoice` (harus vendor yang sama). (9) **Procurement performance review** — `/procurement/performance`, `getVendorProcurementPerformance` DERIVASI murni (win rate, avg response days, on-time fulfillment %, quotation history) dari data RFQ/Service Order existing, bukan field tersimpan. Acceptance "Procurement dan Supplier dapat menjalankan sourcing sampai fulfillment handoff" didemokan lewat 4 RFQ (draft/responses-in/clarification/closed) dan 2 Service Order (plain fulfilled + amended) end-to-end pada fixture. `VendorQuotation` submit/accept/reject (Section 13 lama) TIDAK diubah — jalur RFQ formal adalah TAMBAHAN.
- **Files created:** `app/types/procurement.ts`, `app/data/procurement.ts`, `app/pages/procurement/index.vue`, `app/pages/procurement/rfq/[id]/index.vue`, `app/pages/procurement/service-orders/[id]/index.vue`, `app/pages/procurement/performance/index.vue`, `app/pages/supplier/rfq/index.vue`, `app/pages/supplier/rfq/[id]/index.vue`, `app/pages/supplier/service-orders/index.vue`, `app/pages/supplier/service-orders/[id]/index.vue`, `docs/mockup-section-reports/section-17-supplier-procurement.md`.
- **Files changed:** `app/types/vendor.ts` (+`category`/`status`/`documents` pada `Vendor`, +`VendorDocument`, `VendorDetailTab` +`documents`), `app/data/vendors.ts` (+`VENDOR_DOCUMENTS`, backfill `category`/`status` 7 vendor existing), `app/pages/vendors/index.vue` (+kolom Kategori/Status, +field kategori form Tambah Vendor), `app/pages/vendors/[id]/index.vue` (+tab Documents, +dialog Edit Kategori/Status), `app/pages/supplier/index.vue` (+2 stat card, +2 link card), `app/types/user.ts` (`ModuleKey` +`procurement`), `app/constants/roles.ts` (`ROLE_MODULE_ACCESS` +kolom `procurement` seluruh 16 role), `app/constants/navigation.ts` (+menu "Procurement" 3-child, +2 child Supplier Portal), `app/constants/status.ts` (+`VENDOR_STATUSES`/`RFQ_STATUSES`/`SERVICE_ORDER_STATUSES`/`SUPPLIER_INVOICE_STATUSES`), `app/data/index.ts` (+selector/mutator RFQ/Service Order/Supplier Invoice/Vendor Document lengkap, +`getVendorProcurementPerformance`/`getVendorsWithProcurementActivity`), `app/pages/projects/[id]/index.vue` (+SectionCard "Procurement — RFQ dan Service Order" di sub-section Itinerary & Services), `app/pages/admin/roles.vue` (+1 kolom matrix, +update catatan role Procurement).
- **Files removed:** Tidak ada.
- **Routes affected:** `/procurement` (baru), `/procurement/rfq/[id]` (baru), `/procurement/service-orders/[id]` (baru), `/procurement/performance` (baru), `/supplier/rfq` (baru), `/supplier/rfq/[id]` (baru), `/supplier/service-orders` (baru), `/supplier/service-orders/[id]` (baru). `/vendors`, `/vendors/[id]`, `/supplier` diperluas pada route yang sama (tidak ada rename). Tidak ada route existing yang dihapus.
- **Components reused:** `SectionCard`, `PageHeader`, `DetailMetadataList`, `StatusBadge`, `EmptyState`, `RoleAccessState`, `Table*`, `Dialog*`/`DialogScrollContent`, `Checkbox`, `useToast`. Tidak ada shared component baru — pola halaman mengikuti persis `/ticketing`/`/accommodation`/`/transportation`/`/mice` (list+detail) dan `/supplier/products`/`/supplier/orders` (self-service ter-scope vendor).
- **Data/types/constants affected:** `+RFQ`/`RFQLineItem`/`RFQStatus`/`RFQInvitation`/`RFQInvitationStatus`/`RFQResponse`/`RFQResponseLineItem`/`RFQResponseStatus`/`RFQClarificationMessage`/`ServiceOrder`/`ServiceOrderLineItem`/`ServiceOrderStatus`/`ServiceOrderAmendment`/`SupplierInvoice`/`SupplierInvoiceStatus` (`app/types/procurement.ts`, seluruhnya baru). `Vendor` +`category`/`status`/`documents` (aditif), `+VendorDocument`/`VendorStatus` (`app/types/vendor.ts`). `ModuleKey` +`procurement`. Lihat `docs/mockup-change-impact-log.md` CI-047.
- **Validation results:** `npx nuxi prepare` sukses. `npm run build` sukses (2x run, termasuk setelah perbaikan default clarification thread — vendor dengan thread aktif kini jadi default seleksi, bukan vendor pertama diundang). Smoke test HTTP `/procurement`, `/procurement/rfq/RFQ-001`/`002`/`003`/`004`, `/procurement/rfq/RFQ-999` (not-found), `/procurement/service-orders/SO-001`/`002`, `/procurement/service-orders/SO-999` (not-found), `/procurement/performance`, `/vendors`, `/vendors/VND-006`/`007`/`999` (not-found), `/supplier`, `/supplier/products`, `/supplier/orders`, `/supplier/rfq`, `/supplier/rfq/RFQ-002`, `/supplier/service-orders`, `/supplier/service-orders/SO-001`, plus regresi `/admin/roles`, `/projects/PRJ-101`/`102`/`103`, `/accommodation`, `/transportation`, `/mice`, `/ticketing`, `/` — seluruhnya HTTP 200. Smoke test konten: RFQ-004 menampilkan vendor terpilih "Trans Wahana Logistik" pada tabel comparison; SO-001 menampilkan "Fulfilled"; RFQ-003 menampilkan 2 pesan clarification thread PT ABC ("Mohon konfirmasi kebijakan pembatalan..."/"Pembatalan H-3 dikenakan..."); `/vendors` menampilkan kolom Kategori ("Hotel Budget") dan Status; `/vendors/VND-006?tab=documents` menampilkan "Kontrak Kerjasama PT ABC 2026.pdf"/"Sertifikat Standar Layanan PT ABC.pdf"; `/admin/roles` menampilkan kolom "Procurement"; `/projects/PRJ-103?tab=itinerary-services` menampilkan blok "Procurement — RFQ dan Service Order" dengan "RFQ Transportasi Bandara — Grup VIP Tambahan". `vitest`/`typecheck`/lint tetap pre-existing gap (Q8).
- **Known issues:** Q8 tetap terbuka (17 section berturut-turut). Q12 (self-service invoice submission) **RESOLVED** — lihat `docs/mockup-open-questions.md`. "On-time %" Procurement Performance Review disederhanakan sebagai rasio Service Order `fulfilled` terhadap total (tidak ada field due-date terpisah untuk dibandingkan — dicatat sebagai keputusan D-074, bukan gap tersembunyi). Bagian 2/4 `docs/mockup-implementation-state.md` ditemukan tidak diperbarui sejak Section 09 (Section 10-16 tidak menambah narasi "2x" — dicatat transparan, bukan diperbaiki retroaktif karena di luar scope literal Section 17). Verifikasi interaktif (klik Select vendor, Acknowledge/Fulfill Service Order, submit Invoice) tidak dilakukan headless — dimitigasi lewat code review ketat terhadap seluruh guard (`selectRfqVendor`/`updateServiceOrderStatus`/`submitSupplierInvoice`) + smoke test konten SSR.
- **Cross-section impact:** `docs/mockup-change-impact-log.md` CI-047 — ekstensi aditif ke Vendor Directory (Section 13 lama), Supplier Portal (Prompt 19), Project Detail (Section 12 lama/Foundation), Matrix Role dan `ModuleKey`/`NAV_ITEMS`/`ROLE_MODULE_ACCESS` (Section 02/17 lama) — seluruhnya aditif/regression-tested. `VendorContact`/`VendorQuotation`/`VendorActivity`/`VendorProduct`/`getServicesByVendor`/`submitVendorQuotation`/`acceptVendorQuotation`/`rejectVendorQuotation` (Section 13 lama) TIDAK disentuh sama sekali.
- **Next action:** Section 18 — Booking dan Service Orders (halaman konsolidasi tampilan booking lintas Section 13-16 — BERBEDA dari `ServiceOrder` Procurement Section 17, lihat `docs/frontend-known-issues.md` bagian 13 — status NOT_STARTED), berbasis dependency roadmap. Menunggu perintah user — tidak dieksekusi otomatis.

## Section 18 (Roadmap Baru) — Booking dan Service Orders

- **Tanggal:** 2026-08-01
- **Status:** COMPLETED
- **Scope dan completed items:** Membangun unified booking/service-order center — consolidation/orchestration LAYER fully additive di atas `FlightBooking`/`HotelBooking`/`TransportBooking`/`MiceEvent` (Section 13-16, D-070/D-071/D-072/D-073), BUKAN entitas komersial baru, dan SENGAJA tidak dinamai "Service Order" apa pun (disambiguasi eksplisit dari `ServiceOrder` Procurement Section 17, lihat `docs/frontend-known-issues.md` bagian 13). (1) **Semua Flight/Hotel/Transport/MICE service requirement dalam satu timeline** — `getBookingTimeline(projectId?)` (baru, `app/data/index.ts`), DERIVASI murni (pola sama `getServiceReadinessMatrix`/`getProjectAttentionQueue`), 18 booking existing seluruhnya tercakup (6 Flight + 6 Hotel + 5 Transport + 1 MICE). (2) **Booking references, holds, confirmations, deadlines, vouchers** — `BookingTimelineEntry.reference`/`startDate`/`deadlineDate`/`voucherHref` (link ke `eticket-preview`/`voucher-preview`/`service-order-preview`/`rundown-preview` existing masing-masing domain). (3) **Internal/supplier/client-visible status mapping** — `internalStatus` (label penuh vocabulary existing per domain) TERPISAH dari `supplierVisibleStatus`/`clientVisibleStatus` (bucket 4-kategori disederhanakan dari vocabulary YANG SAMA, bukan enum baru). (4) **Split supplier** — bucket status supplier-facing bersifat simulatif (keempat domain booking tidak punya Supplier Portal sendiri, vendor-facing sesungguhnya tetap `ServiceOrder` Procurement Section 17). (5) **Dependency antar-service** — `BookingOrchestrationRecord.dependsOn` (opsional, cross-domain), dinilai satisfied/blocked; fixture mendemokan `TRN-1035` (Transport) `dependsOn` `HTL-1034` (Hotel, masih `quoted`) — blocked dependency exception nyata. (6) **Confirmation and payment gates** — `paymentGateStatus` (`not-required`/`pending`/`cleared`), otomatis bergerak ke `pending` saat booking mencapai status confirmed-equivalent (hook di 4 fungsi `update*BookingStatus`/`updateMiceEventStatus`), aksi "Mark Payment Cleared" (Operations/Finance-facing, `/bookings`) — mock murni (D-006), TIDAK menyentuh `app/data/finance.ts`/`Invoice`/`Payment`. (7) **Failure/retry/manual fallback simulation** — `BookingAttempt[]` (attempt log), fixture mendemokan `FLT-1021` (failed→manual-fallback), aksi "Catat Percobaan" di `/bookings` (`appendBookingAttempt`). (8) **Duplicate booking prevention** — Flight/Hotel/Transport (bukan MICE, satu event per project) — `findActiveBookingConflicts` dipanggil di level halaman create SEBELUM `createFlightBooking`/`createHotelBooking`/`createTransportBooking` (fungsi create existing TIDAK diubah signature/behavior-nya, LOCKED), dialog konfirmasi eksplisit wajib, `flagBookingOrchestrationDuplicate` mencatat `ActivityEntry` + flag pada record. (9) **Exception list** — `getBookingExceptionQueue()`, mengagregasi flag domain existing (`hasScheduleChange`/status `amended`/`hasChange`+`hasIncident`/`hasChangeOrder`+`hasIncident`+`getMiceScheduleConflicts` reuse) DITAMBAH dependency blocked/percobaan gagal/duplicate flag — halaman `/bookings/exceptions` baru. Acceptance "Operations memiliki satu sumber kebenaran seluruh service" TERPENUHI — modul baru `bookings` (`ModuleKey`), `operations` `MANAGE`. Tab "Itinerary & Services" `/projects/[id]` di-refactor: 4 blok ringkasan booking terpisah (Section 13-16) diganti 1 SectionCard "Booking Timeline" terunifikasi (pre-filtered per project, informasi identik `/bookings`) — tombol "Buat X Booking" quick-create TETAP ada.
- **Files created/changed/removed:**
  - Dibuat: `app/types/booking-orchestration.ts`, `app/data/booking-orchestration.ts`, `app/pages/bookings/index.vue`, `app/pages/bookings/exceptions/index.vue`, `docs/mockup-section-reports/section-18-booking-service-orders.md` (laporan ini).
  - Diubah: `app/types/user.ts` (`ModuleKey` +`bookings`), `app/constants/roles.ts` (`ROLE_MODULE_ACCESS` +kolom `bookings` seluruh 16 role), `app/constants/navigation.ts` (+menu "Booking & Service Order Center" 2-child), `app/constants/status.ts` (+`BOOKING_PAYMENT_GATE_STATUSES`/`BOOKING_ATTEMPT_OUTCOMES`), `app/data/index.ts` (+selector/mutator Booking Orchestration lengkap [`getBookingOrchestrationRecord`/`getBookingTimeline`/`getBookingExceptionQueue`/`setBookingPaymentGateStatus`/`appendBookingAttempt`/`findActiveBookingConflicts`/`flagBookingOrchestrationDuplicate`], +hook `syncBookingPaymentGateOnStatusChange` di 4 fungsi `update*Status`), `app/pages/ticketing/index.vue`/`app/pages/accommodation/index.vue`/`app/pages/transportation/index.vue` (+duplicate booking prevention: dialog konfirmasi + flag), `app/pages/projects/[id]/index.vue` (refactor 4 blok booking terpisah → 1 SectionCard "Booking Timeline" terunifikasi), `app/pages/admin/roles.vue` (+1 baris `modules`, +update teks `ROLE_NOTES['operations']`), `docs/mockup-design-decisions.md` (+D-075), `docs/mockup-change-impact-log.md` (+CI-048), `docs/mockup-data-scenarios.md` (+bagian 4u), `docs/frontend-module-map.md`, `docs/frontend-workflow-map.md`, `docs/frontend-implementation-roadmap.md`, `docs/frontend-known-issues.md` (bagian 13, RESOLVED), `docs/mockup-implementation-state.md`, `docs/mockup-progress.md` (+Entri 27), `docs/mockup-section-progress.md` (entri ini), `docs/mockup-section-reports/README.md`.
  - Dihapus: Tidak ada.
- **Routes affected:** `/bookings` (baru), `/bookings/exceptions` (baru). Tidak ada route existing yang dihapus/di-rename — `/projects/[id]` (tab Itinerary & Services) diperkaya pada route yang sama.
- **Components reused:** `PageHeader`, `SectionCard`, `DetailMetadataList`, `StatusBadge`, `EmptyState`, `RoleAccessState`, `Table*`, `Dialog*`, `Checkbox`, `useToast`. Tidak ada shared component baru — pola halaman mengikuti persis `/procurement` (list+filter+stat, dialog konfirmasi) dan pola SectionCard ringkasan existing di `/projects/[id]`.
- **Data/types/constants affected:** `+BookingDomain`/`BookingPaymentGateStatus`/`BookingAttemptOutcome`/`BookingAttempt`/`BookingOrchestrationDependency`/`BookingOrchestrationRecord`/`BookingTimelineDependencyView`/`BookingTimelineEntry` (entitas dan type baru, `app/types/booking-orchestration.ts`, seluruhnya aditif). `FlightBooking`/`HotelBooking`/`TransportBooking`/`MiceEvent` (Section 13-16) TIDAK diubah shape-nya. `ModuleKey` +`bookings`. Lihat `docs/mockup-change-impact-log.md` CI-048.
- **Validation results:** `npx nuxi prepare` sukses. `npm run build` sukses (2x run, termasuk setelah perbaikan `SectionCard` tanpa `title` yang membuat `description` tidak ter-render pada `/bookings`/`/bookings/exceptions`). Smoke test HTTP `/bookings`, `/bookings/exceptions`, plus regresi `/ticketing`, `/ticketing/FLT-1021`, `/accommodation`, `/accommodation/HTL-1034`, `/transportation`, `/transportation/TRN-1035`, `/mice`, `/mice/MICE-1035`, `/procurement`, `/vendors`, `/finance`, `/reports`, `/product-planning`, `/customer-journey`, `/admin/roles`, `/projects/PRJ-101`–`104?tab=itinerary-services`, `/`, dan `/nonexistent-route-xyz` (not-found) — seluruhnya HTTP 200. Smoke test konten (curl+grep): `/bookings` menampilkan "18 dari 18 booking ditampilkan" (seluruh fixture tercakup) dan 6× tombol "Mark Payment Cleared" (cocok jumlah `paymentGateStatus: pending` diseed), exception `FLT-1021` ("Perubahan jadwal...") dan duplicate flag `HTL-1036` ("Ditandai sebagai duplicate booking yang disengaja...") tampil, kolom "Net Cost / Sell Price" tampil untuk Super Admin (role finansial penuh); `/bookings/exceptions` menampilkan "6 booking membutuhkan perhatian" dan "Dependency belum terpenuhi: Hotel HTL-1034 (menunggu konfirmasi)." untuk `TRN-1035`; `/projects/PRJ-101?tab=itinerary-services` mengonfirmasi 4 heading lama ("Flight Bookings"/"Hotel Bookings"/"Transport Bookings"/"MICE Events") SUDAH TIDAK ADA, digantikan SectionCard "Booking Timeline" yang menampilkan `FLT-1011`, tombol "Buat Flight Booking" tetap ada; `/admin/roles` menampilkan kolom "Bookings"; blok "Procurement — RFQ dan Service Order" (Section 17, `/projects/PRJ-103`) dikonfirmasi TIDAK berubah. `vitest`/`typecheck`/lint tetap pre-existing gap (Q8).
- **Known issues:** Q8 tetap terbuka (18 section berturut-turut). "Supplier-visible status" pada `BookingTimelineEntry` bersifat simulatif (bucket label internal, bukan Supplier Portal baru — keempat domain booking Section 13-16 tidak punya akses Supplier sendiri) — dicatat sebagai keputusan D-075, bukan gap tersembunyi. Verifikasi interaktif (klik Mark Payment Cleared, Catat Percobaan, konfirmasi dialog duplicate booking) tidak dilakukan headless — dimitigasi lewat code review ketat terhadap seluruh guard (`setBookingPaymentGateStatus`/`appendBookingAttempt`/`flagBookingOrchestrationDuplicate`/`findActiveBookingConflicts`) + smoke test konten SSR per skenario kunci (dependency blocked, payment gate pending/cleared, failed→manual-fallback attempt, duplicate flag).
- **Cross-section impact:** `docs/mockup-change-impact-log.md` CI-048 — refactor tab Itinerary & Services (Section 12 lama/Foundation, pemilik asli — hanya blok booking terpisah Section 13-16 yang diganti, blok Procurement Section 17 dan tabel `ProjectService` generik TIDAK disentuh), duplicate-check addition ke 3 halaman create-booking (Section 13-15, TIDAK mengubah fungsi create existing), Matrix Role dan `ModuleKey`/`NAV_ITEMS`/`ROLE_MODULE_ACCESS` (Section 02/17 lama) — seluruhnya aditif/regression-tested. `FlightBooking`/`HotelBooking`/`TransportBooking`/`MiceEvent`/`ServiceOrder`/`RFQ`/`SupplierInvoice` (Section 13-17) TIDAK diubah shape maupun isinya sama sekali.
- **Next action:** Section 19 — Changes, Cancellation, Refund dan Incident (record terstruktur terpisah dari `ActivityEntry` generik — Cancellation dengan penalty, Refund dengan status partial/full/credit, Incident dengan severity/escalation — status PARTIAL), berbasis dependency roadmap. Menunggu perintah user — tidak dieksekusi otomatis.

## Section 19 (Roadmap Baru) — Changes, Cancellation, Refund dan Incident

- **Tanggal:** 2026-08-01
- **Status:** COMPLETED
- **Scope dan completed items:** Melengkapi exception management — empat entitas baru fully additive di atas `FlightBooking`/`HotelBooking`/`TransportBooking`/`MiceEvent` (Section 13-16), `Invoice`/`Payment` (Foundation), dan `ActivityEntry`/`createChangeEntry`/`approveChangeEntry`/`rejectChangeEntry` (Section 14 lama, D-076). (1) **Change request dari Client/Internal/Supplier** — `ChangeRequest` (baru, `app/types/change-incident.ts`), `source: 'client'|'internal'|'supplier'`, fixture `CR-001` s/d `CR-006` mencakup seluruh 3 sumber. (2) **Before/after dan affected entities** — `beforeSummary`/`afterSummary` (string terstruktur, bukan campuran dengan reason) + `affectedEntities: AffectedEntityRef[]` (menautkan ID lintas-domain flight/hotel/transport/mice/itinerary/order/project). (3) **Operational, commercial, financial, timeline impact** — 4 field terpisah (`operationalImpact`/`commercialImpactIdr`/`financialImpactNote`/`timelineImpactNote`), seluruhnya opsional. (4) **Approval states** — `ChangeRequestStatus` 5-nilai (`submitted`/`under-review`/`approved`/`rejected`/`implemented`), peta transisi eksplisit (`getChangeRequestStatusTransitions`), `approveChangeRequest`/`rejectChangeRequest` (mandatory reason untuk reject) SELALU memanggil `approveChangeEntry`/`rejectChangeEntry` existing pada `ActivityEntry` yang ditaut (`activityEntryId`) — audit trail project TETAP satu sumber kebenaran, bukan log kedua. (5) **Additional quotation/change order** — `linkedQuotationId` opsional, menaut ke `Quotation` existing (`getQuotationById`, baru). (6) **Cancellation and penalty** — `CancellationRecord` (baru), lapisan penalty-tracking SERAGAM lintas 4 domain booking (hari ini hanya `HotelBooking` punya field penalty sendiri), dibuat ADITIF dari hook UI-level di 4 halaman detail booking (`app/pages/ticketing/[id]/index.vue`, `accommodation/[id]`, `transportation/[id]`, `mice/[id]`) — dialog status cancel-equivalent existing (mandatory-reason, LOCKED) diperkaya field opsional Penalty + checkbox Refund Eligible, `createCancellationRecord` dipanggil SETELAH `update*BookingStatus` berhasil, TIDAK mengubah guard/transition-map/reason-wajib existing. (7) **Refund request, approval, partial/full, credit status mock** — `RefundRequest` (baru), `type: 'partial'|'full'`, status 5-nilai (`requested`/`under-review`/`approved`/`rejected`/`processed`), `creditStatus` field MOCK self-contained (`pending`/`issued`/`not-applicable`, otomatis `issued` saat `processed`, `not-applicable` saat `rejected`) — SENGAJA TIDAK menyentuh `Invoice`/`Payment` (forward dependency Section 20, `docs/frontend-known-issues.md` bagian 15). (8) **Incident severity, owner, escalation, communication, resolution** — `Incident` (baru), severity 4-nilai, `ownerId`, `escalateIncident` (set `escalatedTo` + entri `communicationLog` otomatis), `appendIncidentCommunication` (log manual), `resolveIncident` (resolution note wajib). (9) **Versioned itinerary/order history** — dipenuhi lewat sifat APPEND-ONLY `ChangeRequest` (tidak pernah diedit/dihapus, hanya berpindah status, diorder kronologis sebagai "Change History") — BUKAN entitas versioning baru. (10) **Client-safe visibility** — `/client/project-orders/[id]` tab "Change Request"→"Changes & Incidents": Change Request hanya `status`+before/after (TIDAK PERNAH dampak operasional/komersial/finansial, bahkan untuk request milik Client sendiri), Incident hanya `status`+resolution note (TIDAK PERNAH severity/escalation/communication log). Modul baru `/changes` (`ModuleKey` `changes`) — `operations`/`project-manager` `MANAGE`, `management`/`finance`/`viewer` `VIEW` (approval komersial tetap `canApprove('project')` existing, BUKAN rank modul `changes` baru). Acceptance "High-change project dapat diselesaikan tanpa menghapus histori" TERPENUHI — seluruh `ChangeRequest`/`CancellationRecord`/`RefundRequest`/`Incident` bersifat append-only/immutable-setelah-dibuat (hanya field status yang berubah lewat mutator eksplisit).
- **Files created/changed/removed:**
  - Dibuat: `app/types/change-incident.ts`, `app/data/change-incident.ts`, `app/pages/changes/index.vue`, `app/pages/changes/[id]/index.vue`, `app/pages/changes/cancellations/[id]/index.vue`, `app/pages/changes/refunds/[id]/index.vue`, `app/pages/changes/incidents/[id]/index.vue`, `docs/mockup-section-reports/section-19-change-cancel-refund-incident.md` (laporan ini).
  - Diubah: `app/data/activity.ts` (+3 `ActivityEntry` baru `CHG-1032`/`1033`/`1034`, aditif — entri existing TIDAK diubah), `app/types/user.ts` (`ModuleKey` +`changes`), `app/constants/roles.ts` (`ROLE_MODULE_ACCESS` +kolom `changes` seluruh 16 role), `app/constants/navigation.ts` (+menu "Changes & Incidents" 4-child), `app/constants/status.ts` (+`CHANGE_REQUEST_SOURCES`/`CHANGE_REQUEST_STATUSES`/`REFUND_REQUEST_STATUSES`/`REFUND_CREDIT_STATUSES`/`INCIDENT_SEVERITIES`/`INCIDENT_STATUSES`), `app/data/index.ts` (+selector/mutator Changes/Cancellation/Refund/Incident lengkap [lihat CI-049], +`getQuotationById`), `app/pages/ticketing/[id]/index.vue`/`app/pages/accommodation/[id]/index.vue`/`app/pages/transportation/[id]/index.vue`/`app/pages/mice/[id]/index.vue` (+hook `createCancellationRecord` pada dialog status cancel-equivalent), `app/pages/projects/[id]/index.vue` (+4 SectionCard baru di tab "Activity & Changes"), `app/pages/client/project-orders/[id]/index.vue` (tab "Change Request"→"Changes & Incidents", `submitChangeRequest` beralih ke `createChangeRequest`, +SectionCard Incidents sanitized), `app/pages/admin/roles.vue` (+1 baris `modules`, +update `ROLE_NOTES['operations']`/`ROLE_NOTES['project-manager']`), `docs/mockup-design-decisions.md` (+D-076), `docs/mockup-change-impact-log.md` (+CI-049), `docs/mockup-data-scenarios.md` (+bagian 4v), `docs/frontend-module-map.md`, `docs/frontend-workflow-map.md`, `docs/frontend-implementation-roadmap.md`, `docs/frontend-known-issues.md` (bagian 14 RESOLVED, bagian 15 diperbarui dengan forward dependency), `docs/mockup-implementation-state.md`, `docs/mockup-progress.md` (+Entri 28), `docs/mockup-section-progress.md` (entri ini), `docs/mockup-section-reports/README.md`.
  - Dihapus: Tidak ada.
- **Routes affected:** `/changes` (baru), `/changes/[id]` (baru), `/changes/cancellations/[id]` (baru), `/changes/refunds/[id]` (baru), `/changes/incidents/[id]` (baru). Tidak ada route existing yang dihapus/di-rename — `/projects/[id]` (tab Activity & Changes) dan `/client/project-orders/[id]` (tab Changes & Incidents) diperkaya pada route yang sama.
- **Components reused:** `PageHeader`, `SectionCard`, `DetailMetadataList`, `StatusBadge`, `EmptyState`, `RoleAccessState`, `Table*`, `Dialog*`/`DialogScrollContent`, `Checkbox`, `StatsCard`, `Tabs*`, `useToast`. Tidak ada shared component baru — pola halaman list mengikuti `/procurement` (query-param tab) dan `/bookings` (list+filter+stat), pola halaman detail mengikuti `/ticketing/[id]` (status dialog mandatory-reason).
- **Data/types/constants affected:** `+ChangeRequestSource`/`ChangeRequestStatus`/`AffectedEntityRef`/`ChangeRequest`/`CancellationRecord`/`RefundRequestStatus`/`RefundRequest`/`IncidentSeverity`/`IncidentStatus`/`IncidentCommunicationEntry`/`Incident` (entitas dan type baru, `app/types/change-incident.ts`, seluruhnya aditif). `FlightBooking`/`HotelBooking`/`TransportBooking`/`MiceEvent`/`Invoice`/`Payment`/`ActivityEntry` (Section 13-16/Foundation/Section 14 lama) TIDAK diubah shape-nya. `ModuleKey` +`changes`. Lihat `docs/mockup-change-impact-log.md` CI-049.
- **Validation results:** `npx nuxi prepare` sukses. `npm run build` sukses (2x run — run pertama menemukan bug sort comparator `incRows` di `app/pages/changes/index.vue` yang menyebabkan HTTP 500 pada `/changes?tab=incidents`, diperbaiki lalu build+smoke-test ulang sukses). Smoke test HTTP: `/changes`, `/changes?tab=cancellations|refunds|incidents`, `/changes/CR-001` s/d `CR-006`, `/changes/cancellations/CNX-001` s/d `CNX-003`, `/changes/refunds/REF-001` s/d `REF-005`, `/changes/incidents/INC-001` s/d `INC-004`, plus ID tidak ada untuk masing-masing (not-found UI), regresi `/projects/PRJ-101` s/d `PRJ-104?tab=activity-changes`, `/client`, `/client/project-orders/PRJ-101`/`PRJ-102`, `/bookings`, `/bookings/exceptions`, `/procurement`, `/admin/roles`, `/ticketing`+detail, `/accommodation`+detail, `/transportation`+detail, `/mice`+detail, `/`, `/nonexistent-route-xyz` — seluruhnya HTTP 200. Smoke test konten (curl+grep): `/changes` menampilkan 4 stat card ("Change Request Terbuka"/"Total Cancellation"/"Refund Terbuka"/"Incident Terbuka"); `/changes/incidents/INC-001` menampilkan badge "Escalated" dan narasi eskalasi ke "Fajar Nugroho" (Operations); `/projects/PRJ-102?tab=activity-changes` menampilkan seluruh 4 SectionCard baru; `/admin/roles` menampilkan kolom "Changes & Incidents". Sanitasi Client Portal diverifikasi lewat CODE REVIEW (grep mengonfirmasi template `client/project-orders/[id]/index.vue` tidak pernah mereferensikan `severity`/`escalatedTo`/`communicationLog`/`operationalImpact`/`commercialImpactIdr`/`financialImpactNote` di luar komentar) — bukan curl konten langsung, karena akses `/client/**` membutuhkan role `client` aktif (role-switcher client-side, tidak terbawa request curl tanpa sesi). `vitest`/`typecheck`/lint tetap pre-existing gap (Q8).
- **Known issues:** Q8 tetap terbuka (19 section berturut-turut). `RefundRequest.creditStatus` bersifat MOCK self-contained (belum terintegrasi ke `Invoice`/`Payment`/`CreditNote` nyata) — forward dependency EKSPLISIT ke Section 20 (Project Finance), didokumentasikan di `docs/frontend-known-issues.md` bagian 15, bukan gap tersembunyi. Verifikasi interaktif (klik approve/reject Change Request, escalate/resolve Incident, ajukan Refund dari Cancellation, isi field Penalty/Refund Eligible di dialog cancel booking) tidak dilakukan headless — dimitigasi lewat code review ketat terhadap seluruh guard (`getChangeRequestStatusTransitions`/`getRefundRequestStatusTransitions`/`getIncidentStatusTransitions`/mandatory-reason pada `rejectChangeRequest`/`updateRefundRequestStatus`/`resolveIncident`) + smoke test konten SSR per skenario kunci.
- **Cross-section impact:** `docs/mockup-change-impact-log.md` CI-049 — hook `createCancellationRecord` ke 4 halaman detail booking (Section 13-16, TIDAK mengubah guard/transition-map/reason-wajib existing), extension tab Activity & Changes (Section 14 lama, list `ActivityEntry` existing TIDAK diubah), extension Client Portal (Section 08, `submitChangeRequest` beralih fungsi tapi hasil akhir `ActivityEntry` yang sama tetap terbentuk), Matrix Role dan `ModuleKey`/`NAV_ITEMS`/`ROLE_MODULE_ACCESS` (Section 02/17 lama) — seluruhnya aditif/regression-tested. `FlightBooking`/`HotelBooking`/`TransportBooking`/`MiceEvent`/`Invoice`/`Payment`/`ActivityEntry`/`createChangeEntry`/`approveChangeEntry`/`rejectChangeEntry` (Section 13-16/Foundation/Section 14 lama) TIDAK diubah shape maupun signature/behavior-nya sama sekali.
- **Next action:** Section 20 — Project Finance (Refund/credit status mock — `RefundRequest.creditStatus` Section 19 adalah forward dependency eksplisit-nya, status PARTIAL), berbasis dependency roadmap. Menunggu perintah user — tidak dieksekusi otomatis.

## Section 20 (Roadmap Baru) — Project Finance

- **Tanggal:** 2026-08-01
- **Status:** COMPLETED
- **Scope dan completed items:** Melengkapi Project Finance frontend AR/AP sampai settlement dan closure gate (D-077) — meng-extend modul `/finance` existing (Section 15 lama) alih-alih membuat modul baru. (1) **Multi-currency dan exchange-rate snapshot mock**: `Invoice` +`currency`/`exchangeRateSnapshot` (mock, dicatat sekali saat invoice diterbitkan, `amountIdr` tetap satu-satunya ledger/source of truth) — seluruh fixture existing di-backfill `currency: 'IDR'` (nilai efektif tidak berubah), demo non-IDR (`INV-1041`, currency USD) sengaja ditaruh di PRJ-104 (sebelumnya nol invoice, tidak pernah divalidasi angka apa pun oleh section manapun) agar tidak mengubah satu pun angka yang sudah divalidasi sejak Section 15/16. (2) **DP/invoice type**: `Invoice.invoiceType` (`dp`/`progress`/`final`), backfill sesuai konteks label/urutan invoice per project apa adanya. (3) **Void Invoice**: `InvoiceStatus` +`'void'` (transisi terminal, alasan wajib, `voidInvoice`) — diblokir untuk invoice `paid`/`void`; SELURUH selektor yang sebelumnya memakai `status !== 'paid'` sebagai proksi outstanding diperbarui turut mengecualikan `'void'` (`isInvoiceOverdue`, `getInvoiceOutstandingIdr`, `getOutstandingInvoices`, `getDepartureReadiness` [Section 12 lama, satu baris]). (4) **Create Invoice dan Record Payment**: `createInvoice`/`recordPayment` (mock ledger update murni, D-006, BUKAN payment gateway nyata) — `recordPayment` mereuse `getInvoiceOutstandingIdr` untuk recompute `Invoice.status` (`unpaid`→`partially-paid`→`paid`), TIDAK menduplikasi math outstanding. (5) **Credit/Debit Note**: `CreditNote`/`DebitNote` (baru, `app/types/finance.ts`) — `Invoice.amountIdr` TIDAK PERNAH ditulis ulang (histori invoice tetap utuh), `getInvoiceOutstandingIdr` mengurangi outstanding dengan Credit Note `issued`/`applied` on-the-fly; `DebitNote` murni informasional. (6) **Menutup forward dependency Section 19 (D-076 poin 5)**: hook SATU BLOK aditif di `updateRefundRequestStatus` (LOCKED) — begitu status mencapai `processed` DAN `invoiceId` terisi, `issueCreditNote` (baru) dipanggil otomatis; hook PROSPEKTIF (tidak retroaktif ke `REF-001` fixture lama yang sudah `processed` sejak Section 19, demi menjaga Outstanding PRJ-102 yang sudah divalidasi presisi sejak Section 15/16 tidak berubah). (7) **AP extension**: `SupplierInvoice` (Section 17) +`paymentScheduleDate`/`matchStatus` (aditif, flow submit/review existing TIDAK disentuh), `updateSupplierInvoiceMatchStatus` (baru). (8) **Reconciliation workspace mock**: `/finance/reconciliation` (baru) — worklist Supplier Invoice `unmatched`/`disputed`, aksi "Mark Matched" (satu klik) dan "Flag Disputed" (dialog, catatan wajib). (9) **Financial closure gate**: `evaluateFinanceClosureGate(projectId)` (derivasi murni: no outstanding invoice, no Refund Request non-terminal, no Supplier Invoice unmatched) + `closeProjectFinance` (mereuse `updateProjectClosureChecklist` existing, mengisi `financeSettled = true` HANYA bila gate `ready`) — mengisi shell inert `ProjectClosureChecklist.financeSettled` (Section 09/D-066) dengan logic nyata untuk PERTAMA KALINYA; checkbox manual item tsb di tab Overview kini selalu disabled dengan hint mengarahkan ke aksi Close Finance tab Finance (3 item checklist lain tetap manual). (10) **Financial permissions**: seluruh aksi mutasi baru digerbangi `canManage('finance')` (reuse existing, hanya `finance`/`super-admin` role — tidak ada constant permission baru dibutuhkan). (11) **Client Portal**: tab Finance `/client/project-orders/[id]` diperkaya badge Tipe (DP/Termin/Final) + label currency SAJA pada tabel invoice sell-side existing — TIDAK ADA budget/margin/AP/Credit-Debit-Note yang ditambahkan (sanitasi dipertahankan). (12) **Supplier Portal**: `/supplier/service-orders/[id]` (Section 17) dikonfirmasi TIDAK diubah/diperluas — scoping `vendorScopeId` existing dipertahankan, tidak ada halaman baru untuk `paymentScheduleDate`/`matchStatus`.
- **Files created/changed/removed:**
  - Dibuat: `app/pages/finance/notes.vue`, `app/pages/finance/reconciliation.vue`, `docs/mockup-section-reports/section-20-project-finance.md` (laporan ini).
  - Diubah: `app/types/finance.ts` (+`InvoiceCurrency`/`InvoiceType`/`ExchangeRateSnapshot`/`CreditNote`/`DebitNote`, `InvoiceStatus` +`'void'`, `Invoice` +5 field, `Payment` +2 field — seluruhnya aditif), `app/data/finance.ts` (backfill fixture existing aditif, +`INV-1041` baru [PRJ-104], +`CREDIT_NOTES`/`DEBIT_NOTES` dengan 1 seed masing-masing), `app/types/procurement.ts` (`SupplierInvoice` +`paymentScheduleDate`/`matchStatus`, aditif), `app/data/procurement.ts` (backfill `matchStatus`/`paymentScheduleDate` pada 3 dari 4 `SUPPLIER_INVOICES` existing), `app/utils/attention.ts` (`isInvoiceOverdue` +exclude `'void'`), `app/data/index.ts` (+14 selector/mutator Finance baru [lihat CI-050], `getInvoiceOutstandingIdr`/`getOutstandingInvoices`/`getDepartureReadiness` diperbarui, `updateRefundRequestStatus` [Section 19, LOCKED] +hook `issueCreditNote`), `app/constants/status.ts` (+`INVOICE_CURRENCIES`/`INVOICE_TYPES`/`SUPPLIER_INVOICE_MATCH_STATUSES`/`CREDIT_NOTE_STATUSES`/`DEBIT_NOTE_STATUSES`, `INVOICE_STATUSES` +`'void'`), `app/constants/navigation.ts` (Finance +2 child nav), `app/pages/finance/invoices.vue` (+Create Invoice/Record Payment/Issue Credit Note/Void Invoice), `app/pages/finance/index.vue` (+link Notes/Reconciliation, +SectionCard Financial Closure Readiness), `app/pages/projects/[id]/index.vue` (tab Finance +badge Tipe/currency, +Credit/Debit Notes, +AP Summary, +Close Finance; Closure Checklist item `financeSettled` digerbangi), `app/pages/client/project-orders/[id]/index.vue` (tab Finance +badge Tipe/currency SAJA), `docs/mockup-design-decisions.md` (+D-077), `docs/mockup-change-impact-log.md` (+CI-050), `docs/mockup-data-scenarios.md` (+bagian 4w), `docs/frontend-module-map.md`, `docs/frontend-workflow-map.md`, `docs/frontend-implementation-roadmap.md`, `docs/frontend-known-issues.md` (bagian 15 RESOLVED), `docs/mockup-implementation-state.md`, `docs/mockup-progress.md` (+Entri 29), `docs/mockup-section-progress.md` (entri ini), `docs/mockup-section-reports/README.md`.
  - Dihapus: Tidak ada.
- **Routes affected:** `/finance/notes` (baru), `/finance/reconciliation` (baru). Tidak ada route existing yang dihapus/di-rename — `/finance` (agregat closure readiness), `/finance/invoices` (Create/Record Payment/Void/Credit Note), `/projects/[id]` (tab Finance), dan `/client/project-orders/[id]` (tab Finance) diperkaya pada route yang sama.
- **Components reused:** `PageHeader`, `SectionCard`, `DetailMetadataList`, `StatusBadge`, `EmptyState`, `RoleAccessState`, `Table*`, `Dialog*`/`DialogScrollContent`, `Tabs*`, `StatsCard`, `useToast`. Tidak ada shared component baru — pola halaman list-tab mengikuti `/changes` (query-param tab, Section 19), pola worklist+aksi mengikuti `/bookings/exceptions`.
- **Data/types/constants affected:** `Invoice`/`Payment` (Foundation) diperluas ADITIF (lihat D-077 untuk rasional kenapa BERBEDA dari pola D-070 s.d. D-076). `SupplierInvoice` (Section 17) diperluas ADITIF. `+CreditNote`/`+DebitNote` (baru, `app/types/finance.ts`). `ProjectClosureChecklist` (Section 09/D-066) TIDAK diubah shape-nya — hanya penggunaan `financeSettled` yang kini digerbangi logic baru. Lihat `docs/mockup-change-impact-log.md` CI-050.
- **Validation results:** `npx nuxi prepare` sukses. `npm run build` sukses. Smoke test HTTP: `/finance`, `/finance/invoices`, `/finance/payments`, `/finance/notes`, `/finance/notes?tab=debit`, `/finance/reconciliation`, `/projects/PRJ-101` s/d `PRJ-104?tab=finance`, `/client/project-orders/PRJ-101`/`PRJ-102`, `/changes/refunds/REF-001`/`REF-002`, `/procurement`+detail, `/supplier/service-orders/SO-001`, `/bookings`, `/bookings/exceptions`, `/admin/roles`, `/`, `/nonexistent-route-xyz` — seluruhnya HTTP 200 (satu isu stale server process dari sesi sebelumnya ditemukan di run pertama, diselesaikan dengan mem-force-kill proses lama dan restart preview server, BUKAN bug kode — dikonfirmasi ulang seluruhnya 200 di run kedua). Smoke test konten (curl+grep): PRJ-101 Margin Rp12.500.000/Committed Rp90.000.000, PRJ-102 Variance -Rp25.000.000/Margin Rp10.000.000/Committed Rp345.000.000/Outstanding Rp95.000.000, PRJ-103 Margin Rp220.000.000/Committed Rp930.000.000/Outstanding Rp700.000.000 — SELURUHNYA identik persis dengan angka yang divalidasi sejak Section 15/16 (nol regresi, dikonfirmasi `CN-001` sengaja ditaruh di invoice yang sudah `paid` [outstanding tetap 0] dan hook Credit Note ke `REF-001` sengaja tidak di-backfill retroaktif). `/finance/notes` menampilkan `CN-001` (tab Credit) dan `DN-001` (tab Debit, via `?tab=debit`). `/finance/reconciliation` menampilkan `SINV-002`/`SINV-004` (unmatched/disputed), TIDAK menampilkan `SINV-001` (matched, terfilter benar). Financial Closure Gate blocker presisi sesuai fixture: PRJ-101 1 blocker (Refund non-terminal), PRJ-102 3 blocker (2 invoice outstanding + Refund + AP unmatched), PRJ-103 3 blocker, PRJ-104 1 blocker (invoice outstanding). `vitest`/`typecheck`/lint tetap pre-existing gap (Q8).
- **Known issues:** Q8 tetap terbuka (20 section berturut-turut). `reports/index.vue` (Section 16 lama) SSR selalu menampilkan `LoadingState` (bukan konten laporan) — `isLoading` di-toggle via `setTimeout` di `onMounted` yang tidak berjalan saat SSR; ditemukan selama regresi Section 20, dikonfirmasi PRE-EXISTING sejak Section 16 (file tidak disentuh sama sekali oleh Section 20), diperbaiki otomatis oleh hydration browser real — dicatat sebagai temuan, bukan diperbaiki (di luar scope kepemilikan Section 20). Payment gateway nyata/integrasi kurs pasar real-time/reconciliation engine algoritmik TETAP di luar scope selamanya (batas permanen protokol eksplisit, bukan gap). Verifikasi interaktif (klik Create Invoice/Record Payment/Void/Issue Credit Note/Mark Matched/Flag Disputed/Close Finance) tidak dilakukan headless — dimitigasi lewat code review ketat + smoke test konten SSR state awal.
- **Cross-section impact:** `docs/mockup-change-impact-log.md` CI-050 — hook `issueCreditNote` SATU BLOK ke `updateRefundRequestStatus` (Section 19, LOCKED, guard/transition-map/reason-wajib TIDAK disentuh), extension `SupplierInvoice` (Section 17, aditif, flow submit/review TIDAK disentuh), `getDepartureReadiness` (Section 12 lama, SATU baris exclude `'void'`), extension tab Finance Project Detail dan Client Portal (Section 15 lama/Section 08), Closure Checklist `financeSettled` (Section 09/D-066, shell inert kini digerbangi — 3 item lain tidak berubah), nav Finance (Section 02/17 lama) — seluruhnya aditif/regression-tested. `Invoice`/`Payment`/`SupplierInvoice` TIDAK diubah field lama-nya sama sekali; nilai efektif fixture existing (Outstanding/Margin/Variance/Committed PRJ-101/102/103) TIDAK berubah, diverifikasi ulang identik dengan angka yang divalidasi sejak Section 15/16.
- **Next action:** Section 21 — Documents, Communication dan Notifications, berbasis urutan roadmap literal. Menunggu perintah user — tidak dieksekusi otomatis.

## Section 21 (Roadmap Baru) — Documents, Communication dan Notifications

- **Tanggal:** 2026-08-01
- **Status:** COMPLETED
- **Scope dan completed items:** Melengkapi cross-module communication (D-078) — modul konsolidasi-style baru `/documents` MENDAMPINGI (bukan menggantikan) `ProjectDocument`/`getDocumentsByProject`/`getDocumentsByParty` (Section 14 lama/Prompt 19) dan `VendorDocument` (Section 17), pola arsitektur turunan KETIGA dari preseden "consolidation/orchestration layer" (D-075→D-076→D-078). (1) **Document folders per entity/project, categories/versions/expiry/access level, internal/client/supplier visibility**: `Document` (baru, `app/types/document-comms.ts`) — `entityType` generik lintas 12 tipe entitas (project/party/vendor/traveler/quotation/flight/hotel/transport/mice/invoice/change-request/incident), `category`/`version`/`expiresAt`/`accessLevel`. `getDocumentsForProject` (union view, `app/data/index.ts`) menggabungkan `Document` baru dengan `ProjectDocument` legacy (default `category: 'Legacy'`) — tab "Documents" Project Detail diperkaya, widget Overview "recentDocuments" TIDAK diubah (tetap pakai selector lama). (2) **Generated document previews**: `Document.previewRoute` (`sourceType: 'generated'`) HANYA menaut ke 9 route preview EXISTING yang diverifikasi manual (quotation/e-ticket/voucher/service-order/driver-sheet/rundown/BOQ/run-sheet/manifest) — TIDAK ADA generator dokumen baru, fixture menaut ID booking/opportunity/project REAL. (3) **Internal notes, client messages, supplier messages**: `Message` (baru) — 3 channel eksplisit, `deliveryStatus`/`deliveryChannel` simulasi mock (Email/WhatsApp, D-006, "tanpa klaim integrasi") — generalisasi preseden `Quotation.sentToClientAt` (Section 05). `sendMessage` settle deterministik (internal-note→sent, client/supplier-message→delivered); skenario `failed`/`queued` didemokan lewat fixture seed, bukan jalur create runtime. (4) **In-app notification center**: `Notification` (baru, 8 tipe) + `getNotificationsForUser`/`getUnreadNotificationCount`/`markNotificationRead`/`markAllNotificationsRead`/`removeNotification`/`pushNotification`. `NotificationPanel.vue` (bell popover, `TopHeader.vue`) DI-REWIRE dari data hardcoded fake starter-template (AKTIVASI boilerplate mati, bukan mengubah fitur yang bekerja) — shell UI/`defineExpose({ unreadCount })` dipertahankan 100%. `/documents?tab=notifications` adalah tujuan kanonik "View all notifications" (satu-satunya, tidak ada halaman kedua yang divergen). (5) **Mentions, assignments, reminders, escalation**: 4 hook `pushNotification` kurasi (bukan menyeluruh, preseden D-075) — `sendMessage` (mentions→`mention`), `escalateIncident` (Section 19, LOCKED→`escalation`), `approveChangeRequest`/`rejectChangeRequest` (Section 19, LOCKED→`change`), `createProjectTask`/`updateProjectTask` (Section 09 lama, LOCKED→`assignment` — koreksi: brief menyebut approx. "task-assignment mutator Section 12", verifikasi kode menemukan fungsi sesungguhnya milik Section 09 lama). Guard di `updateProjectTask` mencegah notifikasi berulang saat `assignedTo` tidak berubah. (6) **Unified activity timeline dengan filtering akses**: `getUnifiedActivityTimeline(entityType, entityId, viewerAccessLevel)` (derived, pola sama `getBookingTimeline`) menggabungkan `ActivityEntry`+`SystemEvent`+`Message`+`Document` kronologis; untuk `entityType: 'project'`, Document/Message diagregasi via `projectId` (bukan hanya `entityType: 'project'` sempit); `SystemEvent` selalu `internalOnly: true`; filter diterapkan di level selector bila `viewerAccessLevel !== 'internal'` — memenuhi acceptance "tidak bocor lintas role" langsung di data layer. Disurfaced sebagai SectionCard "Unified Activity Timeline (Internal View)" di tab "Activity & Changes" Project Detail (must-have, internal-view) — versi client/supplier-safe Portal DIDEFER sebagai stretch goal (selector generik sudah mendukung `viewerAccessLevel` lain, tinggal pemanggilan tambahan, dicatat eksplisit sebagai gap genuine bukan disembunyikan). (7) **Modul baru `/documents`** (`ModuleKey` `documents`) — 3 tab query-param (Documents/Messages/Notifications, pola sama `/changes`): filter category/access-level/entity-type/expiry-status + upload mock dialog (Documents); filter channel + compose+mentions dialog (Messages); list notifikasi milik user login + mark-all-read (Notifications). `management`/`project-manager`/`operations` `MANAGE`, mayoritas role internal lain `VIEW`, `client`/`supplier` `NONE` (tetap pakai `getDocumentsByParty`/portal existing).
- **Files created/changed/removed:**
  - Dibuat: `app/types/document-comms.ts`, `app/data/document-comms.ts`, `app/pages/documents/index.vue`, `docs/mockup-section-reports/section-21-documents-comms-notifications.md` (laporan ini).
  - Diubah: `app/data/index.ts` (+import `DOCUMENT_RECORDS`/`MESSAGE_RECORDS`/`NOTIFICATION_RECORDS`, +seluruh selector/mutator Documents/Messages/Notifications/Unified Timeline baru [lihat CI-051], `escalateIncident`/`approveChangeRequest`/`rejectChangeRequest`/`createProjectTask`/`updateProjectTask` [Section 19/09, LOCKED] +hook `pushNotification`), `app/components/layout/NotificationPanel.vue` (rewire penuh, shell UI dipertahankan), `app/pages/projects/[id]/index.vue` (tab Documents diperkaya via `getDocumentsForProject`, tab Activity & Changes +SectionCard "Communication" dan "Unified Activity Timeline (Internal View)"), `app/types/user.ts` (`ModuleKey` +`documents`), `app/constants/roles.ts` (`ROLE_MODULE_ACCESS` +kolom `documents` seluruh 16 role), `app/constants/navigation.ts` (+menu "Documents & Communication", 3 child), `app/constants/status.ts` (+`DOCUMENT_ENTITY_TYPES`/`DOCUMENT_ACCESS_LEVELS`/`MESSAGE_CHANNELS`/`MESSAGE_DELIVERY_STATUSES`/`NOTIFICATION_TYPES`), `app/utils/attention.ts` (+`DOCUMENT_EXPIRY_WARNING_DAYS`, +`isDocumentExpired`/`isDocumentExpiringSoon`), `app/pages/admin/roles.vue` (+baris matrix `documents`), `docs/mockup-design-decisions.md` (+D-078), `docs/mockup-change-impact-log.md` (+CI-051), `docs/mockup-data-scenarios.md` (+bagian 4x), `docs/frontend-module-map.md`, `docs/frontend-workflow-map.md`, `docs/frontend-implementation-roadmap.md`, `docs/frontend-known-issues.md` (bagian 16 RESOLVED), `docs/mockup-implementation-state.md`, `docs/mockup-progress.md` (+Entri 30), `docs/mockup-section-progress.md` (entri ini), `docs/mockup-section-reports/README.md`.
  - Dihapus: Tidak ada.
- **Routes affected:** `/documents` (baru, 3 tab query-param). Tidak ada route existing yang dihapus/di-rename — `/projects/[id]` (tab Documents diperkaya, tab Activity & Changes diperkaya) diperkaya pada route yang sama.
- **Components reused:** `PageHeader`, `SectionCard`, `DetailMetadataList`, `StatusBadge`, `EmptyState`, `RoleAccessState`, `Table*`, `Dialog*`/`DialogScrollContent`, `Tabs*`, `StatsCard`, `useToast`. Tidak ada shared component baru — pola halaman list-tab mengikuti `/changes` (query-param tab, Section 19).
- **Data/types/constants affected:** `Document`/`Message`/`Notification`/`UnifiedTimelineEntry` (baru, `app/types/document-comms.ts`) — fully additive. `ProjectDocument`/`getDocumentsByProject`/`getDocumentsByParty` (Section 14 lama/Prompt 19) dan `VendorDocument` (Section 17) TIDAK diubah shape-nya. Lihat `docs/mockup-change-impact-log.md` CI-051.
- **Validation results:** `npx nuxi prepare` sukses. `npm run build` sukses (exit 0). Smoke test HTTP: `/documents`, `/documents?tab=messages`, `/documents?tab=notifications`, `/projects/PRJ-101` s/d `PRJ-104` (`?tab=documents`/`?tab=activity-changes`), `/changes`, `/changes?tab=cancellations`, `/bookings`, `/bookings/exceptions`, `/procurement`, `/finance`, `/finance/invoices`, `/finance/notes`, `/finance/reconciliation`, `/admin/roles`, `/ticketing`, `/accommodation`, `/transportation`, `/mice`, `/crm/opportunities`, `/customer-journey`, `/client`, `/supplier`, `/`, `/nonexistent-route-xyz` — seluruhnya HTTP 200. Smoke test konten (curl+grep): `/documents` menampilkan judul "Documents & Communication" dan dokumen expired `DOC-C011` ("Kontrak Vendor PT ABC"); `/projects/PRJ-101?tab=documents` menampilkan dokumen legacy ("Kontrak Kerjasama PRJ-101") DAN dokumen baru ("E-Ticket FLT-1011"); `/projects/PRJ-101?tab=activity-changes` menampilkan SectionCard "Communication" dan "Unified Activity Timeline". `vitest`/`typecheck`/lint tetap pre-existing gap (Q8).
- **Known issues:** Q8 tetap terbuka (21 section berturut-turut). Versi client-safe/supplier-safe Unified Activity Timeline di Client/Supplier Portal DIDEFER — selector `getUnifiedActivityTimeline` sudah generik dan mendukung `viewerAccessLevel` non-internal, tinggal pemanggilan tambahan di halaman portal terkait (stretch goal, dicatat eksplisit, bukan blocker). Email/WhatsApp delivery status TETAP simulasi mock murni selamanya (batas permanen protokol, bukan gap). Verifikasi interaktif (klik bell popover/mark-as-read/kirim pesan/upload dokumen) tidak dilakukan headless — dimitigasi lewat code review ketat + smoke test konten SSR state awal.
- **Cross-section impact:** `docs/mockup-change-impact-log.md` CI-051 — 4 hook `pushNotification` SATU-DUA baris ke `escalateIncident`/`approveChangeRequest`/`rejectChangeRequest` (Section 19, LOCKED, guard/transition-map/reason-wajib TIDAK disentuh) dan `createProjectTask`/`updateProjectTask` (Section 09 lama, LOCKED, signature TIDAK disentuh), rewire `NotificationPanel.vue` (aktivasi boilerplate mati, `TopHeader.vue` tidak disentuh), extension tab Documents/Activity & Changes Project Detail (Section 09/14 lama), nav/roles/ModuleKey — seluruhnya aditif/regression-tested. `ProjectDocument`/`getDocumentsByProject`/`getDocumentsByParty`/`VendorDocument`/`ActivityEntry`/`SystemEvent` TIDAK diubah field lama-nya sama sekali.
- **Next action:** Section 22 — Dashboards, Reports, Lead Recap dan Activity Center, berbasis urutan roadmap literal. Menunggu perintah user — tidak dieksekusi otomatis.
