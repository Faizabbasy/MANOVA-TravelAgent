# Frontend Known Issues — MANOVA (Roadmap Section 00–24)

Dibuat oleh **Section 00 — Current Progress Reconciliation** (2026-08-01). Berisi gap yang ditemukan saat mencocokkan kondisi codebase aktual (hasil Prompt 0–20, COMPLETED) terhadap roadmap Section 00–24 baru. Known issues historis Prompt 0–20 **tidak diulang di sini** — tetap di `docs/mockup-final-known-issues.md` dan `docs/mockup-implementation-state.md` bagian 6 (rujuk dokumen tsb untuk gap lama seperti Q8 tooling).

Status: `NEEDS_VALIDATION` (perlu keputusan/implementasi saat section pemilik dikerjakan), `DEFERRED` (sengaja ditunda, bukan blocker), `KNOWN_GAP` (dicatat, tidak dianggap bug — batas scope Prompt 0–20 yang belum mencakup requirement roadmap baru).

## 0. Section 01–04 — Sudah Diselesaikan (2026-08-01)

Gap foundation berikut ditemukan Section 00 dan sudah ditutup oleh Section 01 (`docs/mockup-section-reports/section-01-frontend-foundation-state-governance.md`), dicatat di sini untuk jejak, bukan lagi open:
- **State reset / seed scenario** — sebelumnya tidak ada mekanisme reset. Sekarang tersedia `app/utils/mock-reset.ts` + `app/plugins/mock-reset.client.ts` + tombol "Reset Demo Data" (`/settings`).
- **Repository/service layer** — diklarifikasi (bukan gap): `app/data/index.ts` sudah memenuhi maksud fungsionalnya (single source, tidak ada fixture terduplikasi per halaman) sejak Foundation lama. Lihat D-058 (`docs/mockup-design-decisions.md`).
- **`ErrorState.vue`/`LoadingState.vue`** — dikonfirmasi tersedia dan reusable (bukan hilang), meski belum dipakai luas — tersedia untuk section berikutnya yang butuh simulasi loading/error eksplisit.

## 0b. BUG — App Gagal Mount di Browser Sejak Section 01 (`DataCloneError`) — RESOLVED (2026-07-31)

**Ini adalah bug nyata, bukan gap scope** — dilaporkan langsung oleh user sebagai "500 Internal Server Error — Failed to execute 'structuredClone' on 'Window': [object Array] could not be cloned."

- **Root cause:** `app/plugins/mock-reset.client.ts` memanggil `captureMockSnapshot()` pada SETIAP page load di browser, yang memanggil `structuredClone()` langsung terhadap array `reactive()` Vue (`OPPORTUNITIES`/`QUOTATIONS`/`PARTIES`/`PROJECTS`/`VENDORS`/`TRAVELERS`/`LEADS`/dst.). `structuredClone()` browser tidak dapat mengkloning `Proxy` (termasuk hasil `reactive()`) — selalu melempar `DataCloneError`, membuat SELURUH app gagal mount di browser manapun sejak Section 01 (2026-08-01) menambahkan plugin ini.
- **Mengapa lolos tidak terdeteksi sejak Section 01–05 (5 section berturut-turut):** Bug hanya terjadi di kode **client-only** (`.client.ts`), yang tidak pernah dieksekusi saat SSR. Seluruh smoke test sejak Section 01 memakai `curl` (HTML hasil SSR — selalu HTTP 200 karena SSR tidak pernah menjalankan plugin ini). Setiap laporan section sejak itu secara eksplisit mencatat "verifikasi interaktif tidak dilakukan headless (keterbatasan tooling)" — kali ini keterbatasan tsb terbukti benar-benar menyembunyikan bug fatal, bukan cuma risiko teoretis.
- **Fix:** `app/utils/mock-reset.ts` — `structuredClone` diganti helper `deepClone` (`JSON.parse(JSON.stringify(...))`), aman karena seluruh fixture adalah data JSON-safe murni. Direproduksi dan diverifikasi lewat skrip Node sebelum dan sesudah fix. Detail: `docs/mockup-change-impact-log.md` CI-035.
- **Status:** `RESOLVED`. **Rekomendasi kuat untuk section berikutnya:** jangan mengandalkan smoke test `curl` saja untuk memvalidasi kode `.client.ts` baru — kelas bug ini (fatal, client-only, tidak terlihat dari SSR) tidak akan terdeteksi oleh metode tsb.

## 1. Role dan Akses — RESOLVED Section 02 (2026-08-01)

- ~~Role `client`, `product-planner`, `procurement` belum ada~~ — **RESOLVED**, `RoleId` kini 16 nilai (D-059).
- ~~Matrix view permission belum dikonfirmasi~~ — **RESOLVED**, `/admin/roles` dikonfirmasi SUDAH berupa grid ModuleKey x Role literal (sejak Section 17 lama) — diperluas 6→8 kolom (CI-031).
- ~~Dashboard kosong untuk role baru~~ — **RESOLVED** (ditemukan+ditutup dalam Section 02 yang sama, CI-030) — 3 widget welcome baru untuk Client/Procurement/Product Planner.
- Sisa pekerjaan role baru tetap milik section pemiliknya: fitur bisnis penuh Client Portal (Section 08), RFQ/Service Order Procurement (Section 17) — role/akses/nav/shell sudah siap sebagai fondasi. Modul Product Planning/Costing (Section 10) sudah **RESOLVED** — lihat bagian 8.
- ~~Public Lead Intake belum ada~~ — **RESOLVED** Section 03 (`/lead-intake`). Duplicate suggestion di form publik bersifat non-blocking preview (mock, cek phone/email terhadap `LEADS`) — merge-duplicate PENUH tetap tanggung jawab Section 04.

## 2. Lead dan Qualification (Section 04) — RESOLVED (2026-08-01)

- ~~Reopen lead archived~~ — **RESOLVED**, `reopenLead` + tombol "Reopen" di drawer footer.
- ~~Merge-duplicate suggestion~~ — **RESOLVED** sebagai archive-dengan-referensi (D-061) — bukan true field-merge (dicatat sebagai simplifikasi eksplisit, evolusi lanjutan bila dibutuhkan).
- ~~Edit Lead (field kontak dasar)~~ — **RESOLVED**, sebelumnya hanya bisa diisi sekali saat create.

## 3. Opportunity dan Quotation (Section 05) — RESOLVED (2026-07-31)

- ~~Quotation duplicate/compare~~ — **RESOLVED**, `duplicateQuotationVersion` ("Duplicate Quotation", salinan persis sebagai versi baru) + panel "Bandingkan dengan versi sebelumnya" (toggle). Compare TERBATAS pada nilai total (`amountIdr` vs `supersededAmountIdr`) — tidak ada breakdown/discount/tax/markup per versi, karena model `Quotation` existing (Section 08) tidak menyimpan snapshot penuh per versi (D-062, disclaimer eksplisit tampil di UI, bukan gap tersembunyi).
- ~~Send mock ke client / withdraw submission~~ — **RESOLVED**, `sendQuotationToClient` ("Send to Client", timestamp mock) dan `withdrawQuotationSubmission` ("Withdraw Submission", revert `submitted`→`draft`, guard hanya dari status `submitted`).
- ~~PDF/print preview~~ — **RESOLVED**, halaman baru `/crm/opportunities/[id]/quotation-preview` (`layout: false`, print via `window.print()` browser — bukan generator PDF nyata, sesuai batasan protokol).
- ~~Line item tax/fee/markup terpisah~~ — **RESOLVED**, `Quotation.taxIdr`/`markupIdr`/`currency`/`validUntil`/`termsAndConditions`/`inclusions`/`exclusions` (field baru, aditif) tampil di "Edit Quotation" dan Quotation Preview.
- **Client Confirmation gate** — ditambahkan sesuai Wajib literal Section 05 ("AE belum dapat Mark as Won sebelum approved + client confirmation"): `recordClientConfirmation` + dialog AE-facing, Mark as Won kini men-disable sampai `Opportunity.clientConfirmedAt` terisi (selain `Quotation.approvalStatus === 'approved'` yang sudah ada). Queue/notifikasi Management-facing dan Client Portal-facing tetap milik Section 06/08 (Q14, belum RESOLVED).

## 4. Management Approval dan Client Activation (Section 06) — RESOLVED (2026-07-31)

- ~~Approval queue agregat belum ada~~ — **RESOLVED**, `/crm/quotations` ditulis ulang dari `ModulePlaceholder` menjadi Management Approval Queue (tab "Menunggu Approval", dialog review detail margin/discount/tax/markup/terms/complexity/risk, aksi Approve/Reject dengan catatan). Lihat Q14 (RESOLVED), D-063.
- ~~Client confirmation belum ada record/aksi eksplisit~~ — **RESOLVED sepenuhnya**: AE-facing (`recordClientConfirmation`, Section 05) + Management-facing visibility (tab "Menunggu Client Confirmation" di `/crm/quotations`, Section 06). Mark as Won digerbangi `quotation.approvalStatus === 'approved'` DAN `clientConfirmedAt` terisi — kini DIVALIDASI DI LEVEL DATA (`approveOpportunityWon`), bukan hanya UI (D-063). Rekaman client confirmation tetap dicatat AE (mock, bukan self-service dari Client Portal) — didokumentasikan sebagai batasan yang disengaja, bukan gap.
- **Approve/Reject/Return for Revision dengan notes/history** — **RESOLVED**: "Reject" berfungsi sekaligus sebagai "Return for Revision" (tidak ada status paralel baru, D-063); `submitQuotationForApproval`/`approveQuotation`/`rejectQuotation` kini mencatat `PartyActivity` per keputusan (histori), reuse tab "Activity / Follow-up" existing di Opportunity Detail.
- **Duplicate Client dan Project Order prevention** — dikonfirmasi SUDAH ada sejak Section 09/Prompt 20 (`if (opportunity.projectId) return getProjectById(...)` di `approveOpportunityWon`; dedup Party by name di `qualifyLeadAndCreateOpportunity`) — tidak perlu perubahan, hanya diverifikasi ulang Section 06.

## 5. Customer Journey (Section 07) — RESOLVED (2026-07-31)

- ~~Funnel drill-down dan conversion metrics~~ — **RESOLVED**, `/customer-journey` mendapat panel "Customer Journey Funnel" 7-tahap (Lead→Qualified→Opportunity→Approved→Won→Client→Project Order), tiap tahap menampilkan conversion % dari tahap sebelumnya dan dapat diklik (drill-down ke `/customer-journey/leads`, `/crm/opportunities`, `/crm/quotations`, `/customer-journey/customers`, `/customer-journey/project-orders` dengan filter query yang sesuai). D-064.
- ~~AE data scope belum konsisten~~ — **RESOLVED** (bug ditemukan+ditutup dalam section yang sama, CI-037): `scopedLeads`/Active Clients dashboard sebelumnya TIDAK ter-scope untuk AE sama sekali (padahal Opportunity/Project Order sudah). Diperbaiki + diperluas ke sub-list `/customer-journey/customers` dan `/customer-journey/project-orders` (toggle "Hanya Portfolio Saya", default ON untuk AE).
- ~~Filters by source/owner/stage/client/date/project type~~ — **RESOLVED**: Leads (source/owner/stage, sejak Prompt 19) + Customers (+owner, baru) + Project Orders (client/status/type/AE/PM sejak Prompt 19, +date/periode keberangkatan, baru) — seluruh 6 dimensi filter literal Wajib kini tersedia lintas modul Customer Journey.

## 6. Client Portal (Section 08) — RESOLVED (2026-07-31)

**Catatan:** Entri asli ("belum ada sama sekali — tidak ada route `/client/*`, tidak ada role `client`") sudah STALE sejak Section 02 (2026-08-01) membangun role/shell minimal — dokumen ini belum diperbarui saat itu (oversight, ditemukan dan dikoreksi di sini).

- ~~Client dashboard, company profile+contacts, travel request, quotation view+revision+accept/reject, Project Orders+itinerary+traveler+dokumen+finance+change request, support contact, action center~~ — **RESOLVED** (Section 08). `/client` (dashboard rewrite total dari shell Section 02), `/client/opportunities/[id]` (baru), `/client/project-orders/[id]` (baru, 6 tab).
- ~~Accept/reject confirmation self-service~~ — **RESOLVED** (CI-038) — sebelumnya dicatat sebagai batasan disengaja di Section 05 ("AE mencatat manual, bukan self-service"), kini Client dapat mengonfirmasi/menyatakan keberatan sendiri lewat `/client/opportunities/[id]` (reuse `recordClientConfirmation`, D-065).
- **Sanitization internal cost/margin/notes** — diverifikasi eksplisit lewat grep audit: TIDAK ADA `estimatedCostIdr`/`estimatedMarginIdr`/`markupIdr`/`budgetIdr`/`actualCostIdr`/`approvedBy`/`approvalNote` di ketiga file Client Portal. Lihat D-065.
- **Shared documents read-only** — Client TIDAK dapat upload dokumen sendiri (keputusan disengaja, D-065 — `DOCUMENTS` bukan `reactive()`, upload akan memerlukan migrasi tambahan di luar scope literal). Evolusi lanjutan bila dibutuhkan.
- **Verifikasi interaktif role Client tidak dilakukan headless** — role-switching adalah mekanisme client-only (`localStorage`), SSR/curl selalu merender sebagai default demo user (Super Admin, D-041) yang TIDAK punya `clientScopeId` — sehingga `/client/*` di bawah curl SELALU menampilkan `RoleAccessState` "tidak memiliki akses" (perilaku BENAR untuk Super Admin, tapi berarti konten Client-facing sesungguhnya tidak dapat diverifikasi lewat smoke test SSR seperti section lain). Dimitigasi lewat code review ketat + grep audit field terlarang + cross-check fixture manual (`PTY-001`/`USR-019` dan `PTY-002`/`USR-020`), TIDAK lewat smoke test konten SSR biasa.

## 7. Project Order dan Handover (Section 09) — RESOLVED (2026-07-31)

- ~~Taksonomi status baru belum diimplementasikan~~ — **RESOLVED**, lihat Q16 (RESOLVED) dan D-066. `ProjectOrderStatus` (10 nilai) dirivasi dari `ProjectStatus` (LOCKED, tidak diubah) + field handover/ready/closure baru.
- ~~AE-to-PM handover accept/return belum ada~~ — **RESOLVED**, `acceptProjectHandover`/`returnProjectHandover` (baru, `app/data/index.ts`) + UI di tab Overview `/projects/[id]`. PM default (`DEFAULT_PROJECT_OWNER_ID`) tetap dipakai untuk penugasan awal (Section 09 tidak mengubah mekanisme assignment PM, hanya menambah langkah accept/return SETELAH assignment) — `PRJ-104` (satu-satunya project `draft`) sengaja dibiarkan Handover Pending untuk demo hidup.
- **Ditemukan sekaligus ditutup dalam section yang sama:** `project.status` sebelumnya TIDAK PERNAH punya mutator (hanya diset sekali saat `approveOpportunityWon`) — kini dapat bertransisi via `updateProjectStatus` dengan guard peta transisi + reason wajib untuk On Hold/Cancelled. Team assignment (`teamUserIds`) sebelumnya tanpa mutator sama sekali — kini `addProjectTeamMember`/`removeProjectTeamMember`. Tasks tab sebelumnya read-only murni (tidak ada create/edit meski `dueAt` sudah ada di type sejak Foundation) — kini `createProjectTask`/`updateProjectTask` + milestone/dependency/assignee. Risk tracking (`ProjectRisk`, entitas baru) dan Closure Checklist (shell) ditambahkan. Link "Project ini berasal dari opportunity" sebelumnya mengarah ke list, bukan detail spesifik — diperbaiki (CI-039).

## 8. Product Planning dan Costing (Section 10) — RESOLVED (2026-07-31)

- ~~Belum ada sama sekali~~ — **RESOLVED**. `ProductTemplate`/`CostSheet` (entitas baru, `app/types/product.ts`), modul `product-planning` (`ModuleKey`), route `/product-planning` (katalog) dan `/product-planning/cost-sheets` (traveler-based costing, markup/tax/contingency, scenario/version comparison via multi-Cost-Sheet per Opportunity). Snapshot ke Quotation lewat `applyCostSheetToQuotation` (mengunci Cost Sheet setelahnya). Detail lengkap `docs/mockup-section-reports/section-10-product-planning-costing.md`.

## 9. Traveler dan Travel Documents (Section 11) — RESOLVED (2026-07-31)

- ~~Dokumen selain paspor belum di-track~~ — **RESOLVED sebagian**. `Traveler` kini punya `idNumber`/`visaNumber`/`visaExpiryDate` (Wajib literal Section 11 hanya menyebut "Passport/ID/visa metadata") — visa ikut dievaluasi `isTravelerDocumentMissing` bila diisi. **Tiket dan asuransi TETAP belum ada field/UI** — di luar Wajib literal Section 11 (tidak disebutkan), dicatat eksplisit sebagai `KNOWN_GAP` yang tersisa, bukan regresi/terlewat.
- Detail lengkap fitur baru: `docs/mockup-section-reports/section-11-traveler-documents.md`.

## 10. Itinerary, Operations, Tasks dan Readiness (Section 12) — RESOLVED (2026-08-01)

- ~~Readiness checklist/matrix terpisah belum ada~~ — **RESOLVED**. `getServiceReadinessMatrix`/`getDepartureReadiness` (baru, `app/data/index.ts`) — agregat kesiapan layanan per tipe + gate keberangkatan (dokumen traveler/layanan confirmed/task blocked/risk open/invoice outstanding), ditampilkan sebagai SectionCard "Departure Readiness Gate" dan "Service Readiness Matrix" di tab Itinerary & Services. Bersifat ADVISORY (tidak memblokir transisi status project, pola sama Closure Checklist D-066). Detail lengkap: `docs/mockup-section-reports/section-12-itinerary-operations-readiness.md`.
- Ticket/insurance document tracking (Traveler, Section 11) TETAP di luar scope literal manapun — tetap `KNOWN_GAP` terpisah (lihat bagian 9).
- **Departure countdown/alert eksplisit** — `isUpcomingDeparture` (util existing) dipakai untuk Attention widget, belum tentu tampil sebagai "countdown" UI khusus. Status: `NEEDS_VALIDATION`.

## 11. Ticketing / Accommodation / Transportation / MICE (Section 13–16) — SELURUHNYA RESOLVED (2026-08-01)

- ~~Belum ada halaman dedicated per domain~~ — **Ticketing (Section 13) RESOLVED.** `/ticketing` (list) + `/ticketing/[id]` (detail lifecycle) + `/ticketing/[id]/eticket-preview` (dokumen) — modul top-level baru dengan `ModuleKey` sendiri, MENDAMPINGI (bukan menggantikan) ringkasan sub-flight di tab "Itinerary & Services" Project Detail. Ini secara eksplisit MENINJAU ULANG ketegangan dengan D-020 yang dicatat di sini sebelumnya — resolusinya BUKAN memindahkan konten tab ke top-level, melainkan menambahkan modul top-level BARU khusus lifecycle detail (options/PNR/segments/deadline/state machine/fare rules/financial) yang literal Wajib Section 13 minta, sementara tab existing (D-020, LOCKED) tetap sebagai ringkasan cepat dalam konteks satu project. Lihat D-070 (`docs/mockup-design-decisions.md`). Detail lengkap: `docs/mockup-section-reports/section-13-ticketing.md`.
- ~~Belum ada halaman dedicated per domain~~ — **Accommodation (Section 14) RESOLVED.** `/accommodation` (list) + `/accommodation/[id]` (detail lifecycle) + `/accommodation/[id]/voucher-preview` (dokumen) — modul top-level baru pola arsitektur IDENTIK Ticketing (D-070/D-071), MENDAMPINGI ringkasan sub-hotel tab "Itinerary & Services". Room block/occupancy/rooming list dan traveler special request DIREUSE dari `TravelerGroup`/`RoomAssignment`/`Traveler` (Section 11) lewat `groupId`/`travelerIds` — bukan dataset paralel. Lihat D-071 (`docs/mockup-design-decisions.md`). Detail lengkap: `docs/mockup-section-reports/section-14-accommodation.md`.
- ~~Belum ada halaman dedicated per domain~~ — **Transportation (Section 15) RESOLVED.** `/transportation` (list) + `/transportation/[id]` (detail lifecycle) + `/transportation/[id]/service-order-preview` (dokumen client-facing) + `/transportation/[id]/driver-sheet-preview` (dokumen internal driver, tanpa informasi harga sama sekali) — modul top-level baru pola arsitektur IDENTIK Ticketing/Accommodation (D-070/D-071/D-072), MENDAMPINGI ringkasan sub-transportation tab "Itinerary & Services". Manifest/group allocation DIREUSE dari `TravelerGroup`/`Traveler` (Section 11); supplier DIREUSE dari `Vendor`/`ProjectService.vendorId` (Section 13 lama) — bukan dataset paralel. Lihat D-072 (`docs/mockup-design-decisions.md`). Detail lengkap: `docs/mockup-section-reports/section-15-transportation.md`.
- ~~Belum ada halaman dedicated per domain~~ — **MICE (Section 16) RESOLVED.** `/mice` (list) + `/mice/[id]` (detail: brief/venue/sessions/participant categories/BOQ/staffing/checklist/deliverables) + `/mice/[id]/rundown-preview` (dokumen client-facing agenda) + `/mice/[id]/boq-preview` (dokumen client-facing budget, sanitized) — modul top-level baru pola arsitektur IDENTIK Ticketing/Accommodation/Transportation (D-070/D-071/D-072), MENDAMPINGI ringkasan sub-mice tab "Itinerary & Services". Staffing/PIC DIREUSE dari `User` (Section 02); vendor package DIREUSE dari `Vendor` (Section 13 lama) — bukan dataset paralel. "Client approval states" TERPISAH dari status event (pola sama Commercial Approval, D-049). "Capacity and schedule conflicts" — derivasi ADVISORY, mendemokan kondisi nyata (venue Hari 2 kapasitas < peserta) yang menautkan ke `RSK-1031`/`TSK-1032` existing. Lihat D-073 (`docs/mockup-design-decisions.md`). Detail lengkap: `docs/mockup-section-reports/section-16-mice.md`.
- **Seluruh 4 sub-domain operasional (Ticketing/Accommodation/Transportation/MICE, Section 13–16) kini RESOLVED** — preseden arsitektur D-070/D-071/D-072/D-073 (modul top-level baru mendampingi tab existing, reuse data Section 11/13, sanitasi cost internal konsisten) dapat dijadikan acuan untuk kebutuhan serupa di section berikutnya (mis. Section 17 Supplier/Procurement RFQ formal, bila membutuhkan pola serupa).

## 12. Supplier dan Procurement (Section 17) — RESOLVED (2026-08-01)

- ~~Role Procurement belum ada~~ — **RESOLVED Section 02** (Q13), diperkuat Section 17 (`ROLE_MODULE_ACCESS.procurement.procurement = 'MANAGE'`).
- ~~RFQ formal/comparison/clarification/selection~~ — **RESOLVED**. Modul top-level baru `/procurement` (D-074): `RFQ`/`RFQInvitation`/`RFQResponse`/`RFQClarificationMessage` (`app/types/procurement.ts`), lifecycle 7-status (`draft`→`sent`→`responses-in`→`comparison`⇄`clarification`→`selected`→`closed`), side-by-side comparison table, clarification thread per vendor (dua arah), formal "Select" action (`selectRfqVendor`). `submitVendorQuotation`/`acceptVendorQuotation`/`rejectVendorQuotation` (Section 13 lama) TIDAK diubah — tetap jalur terpisah untuk assignment cepat satu-vendor-satu-service.
- ~~Service Order/amendment/acknowledgment/fulfillment~~ — **RESOLVED**. `ServiceOrder`/`ServiceOrderAmendment` (baru), lifecycle 6-status (`draft`→`sent`→`acknowledged`⇄`amended`→`fulfilled`/`cancelled`), handoff RFQ→Service Order via tombol "Buat Service Order" di RFQ detail (status `selected`), acknowledgment dan fulfillment status update self-service dari `/supplier/service-orders/[id]`.
- ~~Supplier invoice submission preview~~ — **RESOLVED**, menutup Q12 (`docs/mockup-open-questions.md`, RESOLVED Section 17). `SupplierInvoice` — preview/mock murni, TIDAK ADA payment gateway/processing nyata (larangan protokol), hanya dapat diajukan terhadap Service Order `fulfilled` milik vendor yang sama.
- **Baru (aditif Section 17)**: `Vendor` (Section 13 lama) diperluas `category`/`status`/`documents` (`VendorDocument`, tab "Documents" baru Vendor Detail) — TIDAK mengganti `/vendors` existing. Procurement Performance Review (`/procurement/performance`) — win rate, rata-rata waktu respons, on-time fulfillment, quotation history, seluruhnya DERIVASI dari data RFQ/Service Order existing (`getVendorProcurementPerformance`), bukan field tersimpan.
- **Known gap yang tetap ada (dicatat, bukan tersembunyi)**: "On-time %" Performance Review disederhanakan sebagai rasio Service Order `fulfilled` terhadap total (tidak ada field due-date terpisah untuk dibandingkan — D-074). Client approval/self-service pada RFQ/Service Order TIDAK ada (di luar scope literal, keputusan sama seperti MICE `MiceApprovalStatus` D-073 — approval yang ada adalah "client selected vendor" secara internal, bukan Client Portal). Verifikasi interaktif (klik Select/Acknowledge/submit invoice) tidak dilakukan headless, dimitigasi code review ketat + smoke test SSR konten (konsisten sejak Section 06).

## 13. Booking dan Service Orders (Section 18) — RESOLVED (2026-08-01)

- ~~Belum ada halaman konsolidasi~~ — **RESOLVED.** Modul baru `/bookings` (Timeline, `getBookingTimeline()`) + `/bookings/exceptions` (Exception Queue, `getBookingExceptionQueue()`) — consolidation/orchestration LAYER fully additive di atas `FlightBooking`/`HotelBooking`/`TransportBooking`/`MiceEvent` (Section 13-16). `BookingOrchestrationRecord` (baru, `app/types/booking-orchestration.ts`) — satu record per booking existing, menautkan via `bookingType`+`bookingId`, TIDAK memutasi entitas booking yang mendasarinya. `BookingTimelineEntry` — DERIVED view-model (`getBookingTimeline`, `app/data/index.ts`), menggabungkan booking reference/hold/confirm/deadline/voucher, status internal/supplier/client (bucket disederhanakan dari vocabulary existing, bukan enum baru), dependency antar-service (dinilai satisfied/blocked), payment gate (`not-required`/`pending`/`cleared`, otomatis bergerak ke `pending` saat booking confirmed-equivalent), attempt log (failure/retry/manual fallback, mock D-006), dan exception list (agregasi flag domain existing + dependency blocked + percobaan gagal + duplicate flag). Duplicate booking prevention (Flight/Hotel/Transport, bukan MICE) diimplementasikan di level halaman create (`findActiveBookingConflicts` + dialog konfirmasi) SEBELUM memanggil fungsi create existing — fungsi `createFlightBooking`/`createHotelBooking`/`createTransportBooking`/`createMiceEvent` (Section 13-16) TIDAK diubah signature/behavior-nya. Tab "Itinerary & Services" `/projects/[id]` di-refactor: 4 blok ringkasan booking terpisah (Section 13-16) diganti 1 SectionCard "Booking Timeline" terunifikasi (pre-filtered per project, informasi identik `/bookings`). Modul baru `bookings` (`ModuleKey`) — `operations` `MANAGE` (acceptance literal "satu sumber kebenaran seluruh service"), `management`/`project-manager`/`finance`/`viewer` `VIEW`, role lain `NONE`. Lihat D-075 (`docs/mockup-design-decisions.md`). Detail lengkap: `docs/mockup-section-reports/section-18-booking-service-orders.md`.
- **Catatan disambiguasi (Section 17, DIPERTAHANKAN — masih relevan untuk section berikutnya)**: `ServiceOrder` (`app/types/procurement.ts`, Section 17) adalah dokumen formal Procurement→vendor (dari RFQ atau engagement langsung), BUKAN "Service Order konsolidasi lintas jenis layanan" yang dimaksud Section 18 (yang merujuk pada tampilan gabungan `FlightBooking`/`HotelBooking`/`TransportBooking`/`MiceEvent` per project). Kedua konsep bernama mirip tapi BERBEDA. Section 18 SENGAJA TIDAK membuat entitas baru bernama "Service Order" apa pun — istilah yang dipakai adalah "Booking Orchestration"/"Booking Timeline"/label nav "Booking & Service Order Center" (UI-only, bukan nama tipe) untuk menghindari tabrakan nama. Section berikutnya yang menyentuh area serupa harus tetap eksplisit menyatakan mana yang dimaksud.

## 14. Changes, Cancellation, Refund dan Incident (Section 19) — RESOLVED (2026-08-01)

- ~~Cancellation/Refund/Incident record terstruktur belum ada~~ — **RESOLVED.** Modul baru `/changes` (Change Requests/Cancellations/Refunds/Incidents, 4 tab query-param) + `/changes/[id]` (Change Request detail) + `/changes/cancellations/[id]`/`/changes/refunds/[id]`/`/changes/incidents/[id]` (detail masing-masing) — fully additive di atas `FlightBooking`/`HotelBooking`/`TransportBooking`/`MiceEvent` (Section 13-16), `Invoice`/`Payment` (Foundation), dan `ActivityEntry`/`createChangeEntry`/`approveChangeEntry`/`rejectChangeEntry` (Section 14 lama). `ChangeRequest` (baru, `app/types/change-incident.ts`) — before/after summary, affected entities, dampak operasional/komersial/finansial/timeline, sumber Client/Internal/Supplier, status lifecycle 5-nilai (`submitted`/`under-review`/`approved`/`rejected`/`implemented`), link opsional ke additional quotation/change order (`linkedQuotationId`) — setiap `ChangeRequest` WAJIB menautkan satu `ActivityEntry` (`activityEntryId`) lewat `createChangeRequest` (memanggil `createChangeEntry` existing), memastikan `ActivityEntry` tetap SATU-SATUNYA audit trail project (bukan log kedua yang terpisah). "Versioned itinerary/order history" dipenuhi lewat sifat append-only `ChangeRequest` (tidak pernah diedit/dihapus, hanya berpindah status) — BUKAN entitas versioning baru. `CancellationRecord` (baru) — lapisan penalty-tracking SERAGAM lintas 4 domain booking (hari ini hanya `HotelBooking` yang punya field penalty sendiri), dibuat ADITIF dari hook UI-level di 4 halaman detail booking (`app/pages/ticketing/[id]/index.vue` dkk.) begitu status berpindah ke cancel-equivalent — TIDAK mengubah guard/transition-map/reason-wajib `update*BookingStatus` existing (Section 13-16, LOCKED). `RefundRequest` (baru) — partial/full, status lifecycle 5-nilai (`requested`/`under-review`/`approved`/`rejected`/`processed`), `creditStatus` field MOCK self-contained (`pending`/`issued`/`not-applicable`) — TIDAK menyentuh `Invoice`/`Payment` (lihat bagian 15 di bawah, forward dependency Section 20). `Incident` (baru) — severity 4-nilai, owner, escalation (`escalatedTo`), communication log narasi murni, resolution note — project-level atau tertaut ke booking spesifik lintas 4 domain. Client Portal (`/client/project-orders/[id]`, tab "Changes & Incidents") menampilkan versi sanitized: Change Request hanya status + before/after summary (TIDAK PERNAH `operationalImpact`/`commercialImpactIdr`/`financialImpactNote`, bahkan untuk request milik Client sendiri), Incident hanya status + resolution note (TIDAK PERNAH `severity`/`escalatedTo`/`communicationLog`). Modul baru `changes` (`ModuleKey`) — `operations`/`project-manager` `MANAGE`, `management`/`finance`/`viewer` `VIEW` (approval komersial tetap lewat `canApprove('project')` existing, pola sama Section 14 lama, BUKAN rank modul `changes`), role lain `NONE`. Lihat D-076 (`docs/mockup-design-decisions.md`). Detail lengkap: `docs/mockup-section-reports/section-19-change-cancel-refund-incident.md`.

## 15. Project Finance (Section 20) — RESOLVED (2026-08-01)

- **Update Section 20 (D-077):** Forward dependency `RefundRequest.creditStatus` → real `CreditNote` integration (dicatat sejak Section 19) SELESAI. `app/types/finance.ts` kini punya `CreditNote`/`DebitNote` nyata; `updateRefundRequestStatus` (Section 19, LOCKED) memanggil `issueCreditNote` (Section 20) begitu status mencapai `processed` DAN `invoiceId` terisi — `Invoice.amountIdr` TIDAK PERNAH ditulis ulang, `getInvoiceOutstandingIdr` mengurangi outstanding dengan Credit Note `issued`/`applied` on-the-fly. Hook bersifat PROSPEKTIF (transisi status baru sejak Section 20 berjalan) — `REF-001` (fixture lama, sudah `processed` sejak Section 19) SENGAJA TIDAK di-backfill Credit Note retroaktif untuk menjaga Outstanding PRJ-102 yang sudah divalidasi presisi sejak Section 15/16 tetap tidak berubah.
- **Scope Section 20 lengkap terpenuhi:** Multi-currency + exchange-rate snapshot mock (`Invoice.currency`/`exchangeRateSnapshot`), DP/invoice type (`invoiceType`), Void Invoice (transisi terminal, alasan wajib), Create Invoice + Record Payment (mock ledger, reuse `getInvoiceOutstandingIdr`), Supplier Invoice AP scheduling + match-status (`app/types/procurement.ts`, aditif), Reconciliation workspace mock (`/finance/reconciliation`), Financial closure gate (`evaluateFinanceClosureGate`/`closeProjectFinance`, mengisi `ProjectClosureChecklist.financeSettled` shell Section 09/D-066 dengan logic nyata pertama kalinya).
- **Batas permanen (BUKAN gap)** — sesuai protokol, selamanya di luar scope: payment gateway nyata, integrasi kurs pasar real-time, reconciliation engine otomatis (matching algoritmik) — seluruhnya mock/manual by design (D-006). Status: `PERMANENT_SCOPE_BOUNDARY`, bukan `KNOWN_GAP`.
- **Gap kecil yang genuinely tersisa:** Verifikasi interaktif (klik Record Payment/Void/Issue Credit Note/Mark Matched/Close Finance) tidak dilakukan headless (keterbatasan tooling, konsisten sejak Section 06) — dimitigasi lewat code review ketat + smoke test konten SSR pada state awal. `reports/index.vue` (Section 16 lama) SSR selalu menampilkan `LoadingState` (bukan konten laporan) karena `isLoading` di-toggle lewat `setTimeout` di `onMounted` yang tidak berjalan saat SSR — ditemukan selama regresi Section 20, PRE-EXISTING sejak Section 16 (bukan disebabkan perubahan Section 20, dikonfirmasi `reports/index.vue` tidak disentuh sama sekali oleh Section 20), diperbaiki oleh browser real via hydration client-side. Dicatat di sini sebagai temuan, bukan diperbaiki (di luar scope literal Section 20 — pemilik file adalah Section 16).

## 16. Documents, Communication dan Notifications (Section 21) — RESOLVED (2026-08-01)

- ~~Document center konsolidasi lintas modul belum ada~~ — **RESOLVED.** `/documents` (baru, 3 tab query-param: Documents/Messages/Notifications) — `Document`/`Message`/`Notification` (baru, `app/types/document-comms.ts`), fully additive di atas `ProjectDocument`/`getDocumentsByProject`/`getDocumentsByParty` (Section 14 lama/Prompt 19, TIDAK diganti) dan `VendorDocument` (Section 17, TIDAK diganti). `getDocumentsForProject` (union view) dipakai memperkaya tab "Documents" Project Detail dengan category/version/expiry/access-level. Dokumen `sourceType: 'generated'` menaut ke 9 route preview existing (Section 05/09/13-16) via `previewRoute` — TIDAK ADA generator dokumen baru.
- ~~Notification center in-app belum ada~~ — **RESOLVED.** `getNotificationsForUser`/`getUnreadNotificationCount`/`markNotificationRead`/`markAllNotificationsRead`/`removeNotification`/`pushNotification` (baru) — `NotificationPanel.vue` (bell popover, `TopHeader.vue`) di-REWIRE dari data hardcoded fake starter-template (AKTIVASI boilerplate mati, bukan mengubah fitur yang bekerja) ke data real. Dipicu dari 4 hook kurasi: `sendMessage` (mentions), `escalateIncident` (Section 19), `approveChangeRequest`/`rejectChangeRequest` (Section 19), `createProjectTask`/`updateProjectTask` (Section 09 lama — koreksi: brief menyebut approx. "task-assignment mutator Section 12", verifikasi kode menemukan ini sesungguhnya milik Section 09 lama). Tab "Notifications" `/documents?tab=notifications` adalah tujuan kanonik "View all notifications".
- ~~Komunikasi client/supplier terstruktur belum ada~~ — **RESOLVED.** `Message` (baru) — 3 channel eksplisit (`internal-note`/`client-message`/`supplier-message`), `deliveryStatus`/`deliveryChannel` simulasi mock (Email/WhatsApp, tanpa klaim integrasi nyata, D-006). `PartyActivity`/`LeadActivity`/`VendorActivity` (existing) TIDAK diubah — `Message` adalah lapisan terpisah, bukan pengganti.
- **Unified activity timeline** — `getUnifiedActivityTimeline(entityType, entityId, viewerAccessLevel)` (derived, `app/data/index.ts`) menggabungkan `ActivityEntry`+`SystemEvent`+`Message`+`Document` kronologis dengan filter `internalOnly` — disurfaced di tab "Activity & Changes" Project Detail (internal-view, must-have). **Genuinely tersisa (KNOWN_GAP, bukan disembunyikan):** versi client-safe/supplier-safe (viewer non-internal) di Client Portal/Supplier Portal DIDEFER sebagai stretch goal — selector `getUnifiedActivityTimeline` sudah generik dan mendukung `viewerAccessLevel: 'client'`/`'supplier'`, tinggal pemanggilan tambahan di halaman portal terkait (`/client/project-orders/[id]`, `/supplier/service-orders/[id]`) yang belum dikerjakan karena scope waktu section ini diprioritaskan ke must-have (internal view + document/message/notification core). Rekomendasi: section berikutnya yang menyentuh portal tsb dapat menambahkannya sebagai perluasan aditif kecil.
- Lihat D-078 (`docs/mockup-design-decisions.md`) dan CI-051 (`docs/mockup-change-impact-log.md`) untuk detail lengkap. Laporan section: `docs/mockup-section-reports/section-21-documents-comms-notifications.md`.

## 17. Dashboards, Reports, Lead Recap dan Activity Center (Section 22) — RESOLVED (2026-08-01)

- ~~Export mock (CSV/PDF placeholder) — belum ada di Reports~~ — **RESOLVED.** Dialog "Export" (`app/pages/reports/index.vue`, PageHeader `#actions`) — pilih bagian (7 section + "Seluruh Laporan") + format (CSV/PDF), toast konfirmasi nama file mock (mis. `sla-quotation-performance-2026-08-01.csv`) — TIDAK ADA file yang benar-benar dihasilkan (larangan protokol eksplisit).
- ~~Widget role baru (Client/Product Planner/Procurement)~~ — **KLAIM STALE, DIKOREKSI.** Entri ini sudah TIDAK BENAR sejak Section 02 (CI-030, 2026-08-01) menambahkan 3 widget welcome (`showClientWelcome`/`showProcurementWelcome`/`showProductPlannerWelcome`, `app/pages/index.vue`) — dikonfirmasi ulang lewat pembacaan kode langsung saat Section 22 dikerjakan. `docs/frontend-implementation-roadmap.md` baris Section 22 turut dikoreksi (sebelumnya masih mengutip klaim stale ini sebagai prasyarat belum terpenuhi).
- ~~SSR loading-skeleton bug (`/` dan `/reports` selalu menampilkan `LoadingState` saat SSR)~~ — **RESOLVED**, ditemukan Section 20 (dicatat bagian 15 di atas), root cause `isLoading` hanya di-toggle lewat `setTimeout` di `onMounted` (tidak pernah berjalan saat SSR). Fix: `isLoading` dihilangkan (selalu `false`) di KEDUA halaman — bukan diberi delay client-only, karena delay akan membuat konten SSR yang sudah benar sempat "hilang" lagi jadi skeleton setelah hydration (regresi UX baru). Lihat D-079.
- ~~Saved Views~~ — **RESOLVED.** `SavedView` (baru, `app/types/reporting.ts`) + centralized reactive mock state (`app/data/reporting.ts`, BUKAN localStorage/sessionStorage) — "Simpan View"/terapkan/hapus di Dashboard DAN Reports, membungkus filter existing masing-masing halaman (tidak ada filter baru).
- ~~Drill-down~~ — **RESOLVED sebagian, didokumentasikan eksplisit mana yang TIDAK dapat ditautkan:** Sales Pipeline (Reports) → `/crm/opportunities?stage=X` (filter sungguhan). Invoice Aging (Reports) → tab Finance Project Detail terkait (`/projects/[id]?tab=finance`) — BUKAN filter per-invoice (`/finance/invoices` tidak punya query-filter support, di luar touch-point Section 22 untuk menambahkannya). Project Performance (status/tipe breakdown, Reports) — **TIDAK ditautkan**, `/projects` tidak punya query-filter support dan halaman tsb di luar touch-point Section 22. Activity Center → entity detail untuk module `opportunity`/`quotation`/`client`/`project-order`/`vendor` (reuse selector `~/data` existing) — module `lead` (tidak ada route `[id]`, hanya drawer `/customer-journey/leads`), `finance` (`entityId` INV-xxx/PAY-xxx, tidak ada route per-ID), dan `user` (`/admin/users` tidak ada route `[id]`) **SENGAJA TIDAK ditautkan** — bukan gap tersembunyi, dicatat eksplisit sebagai batas mapping yang aman.
- ~~SLA dan Quotation Performance~~ — **RESOLVED sebagian.** SectionCard baru ke-7 di Reports menghitung cycle time `Opportunity.createdAt` → `Quotation.createdAt` (bukan `Quotation.sentToClientAt`, field opsional yang HANYA terisi untuk 1 dari seluruh quotation saat ini — dicatat sebagai data sparse, ditampilkan sebagai stat terpisah "Quotation Terkirim ke Client" bukan metrik utama), dengan threshold SLA mock eksplisit 3 hari (bukan SLA kontraktual nyata). **"Approval cycle time" TIDAK dapat dihitung** — `Quotation` (`app/types/opportunity.ts`) tidak memiliki field timestamp `approvedAt` tersimpan (hanya `approvedBy`/`approvalNote`, di-set oleh `approveQuotation`) — didokumentasikan sebagai keterbatasan data model, BUKAN diimplementasikan dengan fabrikasi tanggal.
- ~~Supplier Action Center~~ — **RESOLVED.** SectionCard baru di `/supplier` (pola sama Client Portal Action Center) — pending RFQ response/klarifikasi terbuka, pending Service Order acknowledgment, pending invoice-submission (Service Order `fulfilled` tanpa `SupplierInvoice`), seluruhnya reuse selector Section 17 (`rfqs`/`serviceOrders` sudah di-scope `vendorScopeId`, +`getSupplierInvoicesByServiceOrder`).
- ~~Cross-link Vendor Summary → Procurement Performance~~ — **RESOLVED.** Tombol "Procurement Performance Review" di `#actions` SectionCard "Vendor Summary" (Reports) → `/procurement/performance` (Section 17 existing) — TIDAK menduplikasi `getVendorProcurementPerformance`.
- **Verifikasi per-role Dashboard** — dilakukan lewat code-review sistematis `visibleTo(...)` per widget (bukan interactive browser headless, keterbatasan konsisten sejak Section 06 — role-switching client-only via `localStorage`, SSR/curl selalu Super Admin) + cross-check fixture manual (mis. `USR-002` PM memiliki 3 project aktif untuk widget "Active Projects Milik Saya"). Hasil lengkap per role: lihat `docs/mockup-section-reports/section-22-dashboards-reports-activity.md` bagian 12.
- Verifikasi interaktif (klik Export/Save View/Apply View/drill-down) tidak dilakukan headless — dimitigasi code review ketat + smoke test SSR konten (konsisten sejak Section 06).

## 18. Administration, Master Data dan Audit — RESOLVED Section 23 (2026-08-01)

- ~~Master data terbatas~~ — **RESOLVED**. `app/constants/master-data.ts` (Section 17 lama, static) bermigrasi jadi `app/data/master-data.ts` (`reactive()`, D-080) — 4 kategori lama (`MASTER_PROJECT_TYPES`/`MASTER_SERVICE_TYPES`/`MASTER_DESTINATIONS`/`MASTER_VENDOR_CATEGORIES`) kini editable, PLUS 11 kategori baru: Operational Reference (Airport/Airline/Hotel), Commercial & Finance (Currency/Tax Rule/Payment Term/Cancellation Rule), System Configuration (Numbering Scheme/Document Template/Readiness Gate/Assignment Rule). `/admin/master-data` direstrukturisasi 3 kelompok x 15 sub-tab, Add/Edit/Deactivate/Reactivate mock lewat mutator generik `createMasterDataRecord`/`updateMasterDataRecord`/`deactivateMasterDataRecord`/`reactivateMasterDataRecord`.
- ~~Historical snapshot warning~~ — **RESOLVED** sebagai `getMasterDataUsageCount`. **Catatan jujur, BUKAN gap tersembunyi**: genuine usage-count hanya feasible untuk 4/15 kategori yang punya cross-reference bersih ke entitas existing (`currency`→`Invoice.currency`, `destination`→`Project.destination` fuzzy-match, `project-type`→`Project.characteristic`, `service-type`→`ProjectService.type`, hanya untuk ID seed asli). 10 kategori sisanya (`vendor-category`/`airport`/`airline`/`hotel`/`tax-rule`/`payment-term`/`cancellation-rule`/`numbering-scheme`/`document-template`/`readiness-gate`/`assignment-rule`) menampilkan peringatan GENERIK saat edit/deactivate ("mungkin digunakan di tempat lain, tidak ada cek referensi otomatis") — bukan angka fabrikasi, sesuai instruksi brief "be honest in the UI copy". Dicatat eksplisit sebagai batas jujur.
- ~~Users, roles, permissions, organizations (bagian "organizations")~~ — **RESOLVED** sebagai Organization Profile SINGLETON (`/admin/organization`, baru) — profil perusahaan travel agency itu sendiri, BUKAN multi-tenancy (judgment call eksplisit, D-080).
- ~~Access review and suspended user state~~ — **RESOLVED** — `User.status`/`suspendUser`/`reactivateUser` (mandatory-reason), tab "Access Review" baru di `/admin/users`. **Catatan jujur**: TIDAK ADA field "terakhir login" (tidak difabrikasi, tidak ada di data model `User`) — scope disederhanakan menjadi filterable view user aktif + suspend satu-klik, sesuai instruksi brief eksplisit.
- ~~Audit trail search~~ — **RESOLVED** — search box baru di `/admin/audit-trail`, mencari lintas `ActivityEntry` (existing) DAN `SystemEvent` (SectionCard baru "Log Sistem (Non-Project)"). `SystemEventModule`+`'administration'` (baru); setiap aksi tulis admin baru (master data/organization profile/suspend/reactivate) menghasilkan `SystemEvent` baru, visible langsung.
- **NumberingScheme/ReadinessGateConfig/AssignmentRule/DocumentTemplate — display/config preview murni, SENGAJA TIDAK di-wire ke mesin bisnis nyata** (batas non-integrasi dokumentasikan eksplisit, D-080) — `NumberingScheme.nextNumberPreview` bukan counter nyata (ID generation tetap `nextSequentialId` hardcoded per fixture, LOCKED), `ReadinessGateConfig` tidak me-rewire derivasi departure-readiness (Section 12, LOCKED), `AssignmentRule` tidak me-rewire mutator lead-routing (Section 04, LOCKED). Ini adalah keputusan desain permanen (menghormati hard rule "jangan mengarang integrasi/mesin bisnis nyata"), BUKAN gap yang perlu ditutup section berikutnya.
- **Airport/Airline/Hotel — referensi admin murni, SENGAJA TIDAK ditautkan sebagai foreign key ke `FlightBooking`/`HotelBooking`** (Section 13-14, LOCKED) — kedua entitas tsb tetap memakai field bebas teks/bisnis apa adanya. Keputusan desain permanen yang sama dengan poin di atas.

## 19. Tooling — RESOLVED Section 24 (2026-08-01)

- **Q8 — Lint/typecheck/test** — ~~tetap `NEEDS_VALIDATION` sejak Section 06 lama~~ **RESOLVED Section 24.** `eslint@8.57.1` (peer-compatible dengan `@nuxtjs/eslint-config-typescript@^12.1.0` yang sudah ada sejak Foundation) dan `vue-tsc@2.2.12` terpasang via `pnpm add -D` (proyek ini secara aktual memakai **pnpm** — `pnpm-lock.yaml`+`node_modules/.pnpm` ada, tidak ada `package-lock.json`; `npm install` polos gagal dengan bug arborist di tree ini — nama script tetap netral `npm run lint`/`npm run typecheck` agar bekerja lewat package manager apa pun). `.eslintrc.cjs` baru (format legacy, dipaksa `.cjs` karena `package.json` `"type":"module"`) extending `@nuxtjs/eslint-config-typescript`, dua rule dilonggarkan eksplisit (`vue/multi-word-component-names: off`, `vue/no-v-html: warn`) untuk menghindari rewrite mekanis yang dilarang protokol. `package.json` +script `lint`/`typecheck`.
  - **Hasil lint:** ~9.428 temuan (3.752 error/5.676 warning, diverifikasi ulang independen) — hampir seluruhnya gaya kode murni (`vue/singleline-html-element-content-newline`, `vue/html-indent`, `comma-dangle`, `object-property-newline`, `curly`, `space-before-function-paren`, `quotes`, dst.), wajar untuk 24 section yang belum pernah di-lint. **SENGAJA tidak di-mass-reformat** (`eslint --fix` tidak dijalankan) — akan menghasilkan diff raksasa lintas seluruh codebase yang melanggar prinsip "perubahan lintas section minimal". Rule yang berpotensi menandakan bug nyata ditriase satu-per-satu (`@typescript-eslint/no-unused-vars` 40, `vue/no-unused-vars` 1, `vue/no-side-effects-in-computed-properties` 1, `require-await` 1, `vue/no-template-shadow` 2) — hasil triase melahirkan 2 bug fix nyata (lihat bagian 20 di bawah); sisanya genuinely inert (~35 unused-import, `import/no-duplicates` 9, `require-await` di `login.vue` pola mock-login yang memang tidak butuh `await`).
  - **Hasil typecheck:** `vue-tsc --noEmit` — **0 error, bersih.**
  - **Hasil build:** `nuxt build` — exit 0, sukses (hanya warning pre-existing tidak terkait: Tailwind "ambiguous class", Nitro plugin-timing info).
  - **Status:** `RESOLVED`. Lint 9.428 temuan style TETAP ada sebagai backlog non-blocking terkatalogkan (lihat bagian 20).
- **Q7 — Adopsi `vee-validate`+`zod` untuk form baru.** Sejak diusulkan Prompt 4 (`docs/mockup-open-questions.md`), TIDAK PERNAH menjadi blocker di 24 section berikutnya — seluruh form baru (Lead Qualification, Requirement Detail, Quotation, Cost Sheet, RFQ, Booking lifecycle, Change Request, Invoice, Master Data, dst.) dibangun konsisten dengan pola manual (`ref`+validasi inline) yang sudah dipakai sejak Foundation, tanpa insiden kualitas yang tercatat akibat pola ini. **Keputusan final Section 24: `DEFERRED_PERMANENTLY`** — bukan `RESOLVED` (tidak pernah benar-benar diadopsi) dan bukan `NEEDS_VALIDATION` (tidak lagi menunggu keputusan, karena roadmap sudah selesai) — migrasi ke `vee-validate`+`zod` tetap layak dipertimbangkan HANYA bila proyek dilanjutkan ke Fase 3 (lihat `docs/frontend-end-to-end-implementation-guide.md` bagian 8), bukan sebagai pekerjaan wajib menutup roadmap ini. `docs/mockup-open-questions.md` diperbarui selaras.

## 20. Section 24 — Full Regression dan Final Implementation Guide (2026-08-01) — Temuan Baru

Bagian ini merangkum temuan Phase 1 Section 24 (regresi/bug-fix/Project Closed) dan Phase 2 (dokumentasi) — laporan lengkap dengan sitasi file/fungsi: `docs/mockup-section-reports/section-24-full-regression-final-docs.md`.

**Bug nyata ditemukan dan DIPERBAIKI (RESOLVED, bukan lagi open):**
- ~~`app/pages/customer-journey/lead-sources/index.vue:41-45` — computed `sourceBreakdown` memanggil `recapRows.value.sort(...)` in-place, memutasi cache computed `recapRows` sebagai side effect dan diam-diam mengacak urutan tabel "Detail per Sumber" yang tidak disortir.~~ **FIXED** — sort dilakukan atas salinan array, bukan array asli.
- ~~`app/pages/accommodation/[id]/index.vue:423` dan `app/pages/transportation/[id]/index.vue:436` — variabel loop `v-for` bernama `traveler` membayangi (shadow) fungsi helper level-script `traveler(id)`.~~ **FIXED** — variabel loop di-rename jadi `t`. Sebelumnya inert (tidak menyebabkan bug perilaku nyata saat ditemukan), tetapi genuine footgun untuk perubahan berikutnya.

**Gap genuine yang TETAP ADA, sengaja TIDAK diperbaiki (KNOWN_GAP, dilaporkan jujur, bukan disembunyikan):**
- **Accommodation/`HotelBooking` tidak memiliki flag schedule-change** setara `hasScheduleChange` (Flight, Section 13)/`hasChange` (Transport, Section 15)/`hasChangeOrder` (MICE, Section 16) — satu-satunya dari 4 domain booking yang tidak punya padanan ini. Genuine parity gap, kecil, kosmetik (tidak memblokir alur Change Request manapun — `ChangeRequest`/Section 19 tetap berfungsi penuh untuk Accommodation lewat jalur generik). Sengaja TIDAK diperbaiki di Section 24 — menyentuh type `HotelBooking` (LOCKED sejak Section 14/D-071) untuk gap kosmetik ini dinilai tidak sepadan dengan risiko cross-section di tahap paling akhir roadmap. Rekomendasi bila dikerjakan: tambah field `hasScheduleChange?: boolean` aditif murni ke `HotelBooking` (`app/types/booking-orchestration.ts` atau `app/types/accommodation.ts`, sesuai lokasi type existing) — aman kapan pun karena aditif.
- **Project Closed "documents complete" check bersifat best-effort/expiry-only** — `evaluateProjectClosureGate` mengevaluasi `documentsComplete` hanya dengan mengecek tidak ada `Document` berstatus kedaluwarsa untuk project tsb; TIDAK ADA konsep "daftar dokumen wajib per tipe project" di data model manapun untuk dibandingkan kelengkapannya. Sama seperti keterbatasan usage-count master data (bagian 18) — jujur, bukan fabrikasi.
- **Verifikasi klik-interaktif jalur sukses Close Project tidak dapat dilakukan** — tidak ada fixture project berstatus `completed` saat ini (seluruh `PRJ-10x` masih di status operasional aktif), sehingga path "ready:true" tidak dapat divalidasi via smoke-test SSR konten (SSR hanya bisa mengonfirmasi blocker-list state yang genuine hari ini). Dimitigasi via code review ketat terhadap `evaluateProjectClosureGate`/`closeProject` (pola identik `evaluateFinanceClosureGate`/`closeProjectFinance` Section 20 yang sudah diverifikasi sebelumnya) — konsisten dengan keterbatasan tooling yang sama di SETIAP section sejak Section 06 (tidak ada headless browser).
- **9.428 temuan lint style** (lihat bagian 19) — dikatalogkan eksplisit, non-blocking, bukan bug.
- **`app/pages/projects/[id]/edit.vue`** — dikonfirmasi ULANG tetap LOCKED template stub sejak Section 05, tidak disentuh Section 24 (di luar scope regresi — bukan halaman yang perlu berfungsi penuh menurut Definition of Done manapun sejak awal).

**Hasil regresi Section 24 (Phase 1, independen diverifikasi ulang):**
- Route audit: **89/89 checks pass** (82 file page, 89 varian URL termasuk probe not-found). `app/pages/expenses.vue`/`tasks.vue` dikonfirmasi ULANG tetap sengaja tidak ditautkan ke nav (bukan regresi terlewat).
- Role audit: **16/16 PASS** — tidak ada bug access-control ditemukan; setiap role (termasuk `viewer` read-only) punya minimal satu aksi inti genuine.
- Full workflow chain: **23 langkah + 9 edge case seluruhnya terverifikasi bekerja** (lihat `docs/frontend-workflow-map.md` penutup dan laporan section lengkap untuk sitasi file/fungsi per langkah).
- Build: exit 0. Typecheck: 0 error. Lint: lihat bagian 19.

## 21. Penutup — Status Akhir Seluruh Known Issues (Section 24, 2026-08-01)

Roadmap 25-tahap (Section 00–24) selesai. Ringkasan status akhir kategori known issues di dokumen ini:
- **RESOLVED penuh (tidak ada sisa pekerjaan):** bagian 0–17, 19 (Q8), sebagian besar bagian 18.
- **PERMANENT_SCOPE_BOUNDARY (bukan gap, batas protokol permanen):** payment gateway nyata, integrasi kurs real-time, reconciliation engine otomatis (bagian 15), seluruh larangan D-006 (integrasi vendor/airline/hotel/WhatsApp nyata).
- **DEFERRED_PERMANENTLY (keputusan sadar, tidak diblokir, tidak wajib dikerjakan lagi):** Q7 vee-validate/zod (bagian 19), Unified Activity Timeline client/supplier-safe (bagian 16), sebagian drill-down Reports/Activity Center yang sengaja tidak ditautkan (bagian 17).
- **KNOWN_GAP kecil yang genuinely tersisa, dilaporkan jujur (tidak diperbaiki, bukan disembunyikan):** Accommodation schedule-change parity (bagian 20), 9.428 lint style backlog (bagian 19/20), Project Closed documents-complete best-effort (bagian 20), tiket/asuransi Traveler document tracking (bagian 9), sejumlah kecil batas non-integrasi Section 23 master data (bagian 18).
- **Tidak ada klaim backend/integrasi/persistence produksi di mana pun dalam dokumen ini** — seluruh isi konsisten dengan D-005/D-006 (LOCKED) sepanjang 24 section.

Tidak ada section berikutnya yang direkomendasikan — dokumen ini adalah versi FINAL `docs/frontend-known-issues.md`.
