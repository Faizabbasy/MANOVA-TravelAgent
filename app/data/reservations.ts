import { reactive } from 'vue'
import type { Reservation } from '~/types/reservation'

/** Reservation view (Client Experience — Repair Phase Section 1, Foundation). Array kosong — dibangun section "Core Project", derivasi dari `BookingOrchestrationRecord` (Section 18). */
export const RESERVATIONS: Reservation[] = reactive([])
